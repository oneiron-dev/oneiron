# W8-T07-split — Remote client error publication follow-up

- The implementation and acceptance tests are already committed as `8545c6fa` (`W8-T07`). At intake, that commit is both this branch HEAD and the local `origin/main` ref; there is no unmerged implementation diff to publish from this branch.
- Canon: `generated/oneiron/agents/oneiron-arch-0028-mcp-external-agent-surface-v1.md:209,212`. Typed remote output failures remain wire-visible as transport `INTERNAL_SERVER_ERROR`, keyed input shape failures retain their field in `BAD_REQUEST`, and generated `invoke` handlers decode once. The prior implementation note `impl-notes/W8-T07.md` explains these decisions and the `describe` exception.
- The docs registry omits ONE-2572; add its row there. The ticket says “16 MB cap”, but current code caps remote transport at 64 MiB and an entity payload separately at 64 KiB. The canon page should state those limits separately if it discusses a single cap. Do not edit the read-only docs mirror from this ticket.
- Publication is assigned to the factory merge stage by this writer contract, while rule 5 prohibits this seat from touching remotes. No PR or bot thread can be claimed from this seat. A PR from this branch now would carry only this process note because the implementation is already in `origin/main` locally.
- The split seat reran `python3 scripts/sdk/generate.py --check` and the 10 `test_agent_sdk.py` tests successfully. The context pack records the prior writer's green scoped Rust gates at the committed implementation. A later Rust rerun was interrupted on session restart and has no usable result; do not count it as a pass.
