# W7-C07 implementation notes

## Canon and decisions
- Canon: `generated/oneiron/identity.md`, `identity/oneiron-arch-0022-identity-world-model-v1.md`, `sync/oneiron-arch-0043-federation-membership-v1.md`, and `decisions/oneiron-dec-0005-policy-residence.md` in the read-only docs mirror.
- Scope: registry OF-453 amendments define worlds × facets × bands × audience × verbs × sensitivity. The ticket's ARCH-0067 link instead names the Context Board. Empty or malformed authority never means Top. Named worlds exclude base unless base is explicitly present.
- The ticket's flat deletion/error paths predate directory modules. Courtesy-request errors live in `error::RecordError`; stable `ErrorKind` names are appended. The protocol is one-way signed bytes, never a deletion acknowledgement or proof.
- Host posture wire values are exactly `managed`, `relay`, `self-host`. ReRoot replaces RecoveryReboot and revokes the old roster without changing genesis-derived identity or granting an instant widen. Delayed ordinary key rotation remains a separate operation.
- Canon clarification for ARCH-0022: PERSON holds the persona baseline, ordinary lifecycle-managed `persona.change` CLAIMs hold recorded changes and `made_by`, and only scenario overlays use FACET. The earlier context packet incorrectly assumed a baseline compiler already existed. The new compiler rejects pending, closed, stale, scoped-away and malformed changes; rebasing changes neither claim bytes nor mask count.
- Consumer persona content is caller-owned JSON. The engine includes no product prompt or personality. No docs repository files were edited.
- FED-S5 remains an owner key-custody policy question. Org-admin setup/enrollment never accepts member private keys. The backup kit's sealed-envelope VEK-rotation path is conditional on an unratified design and is not invented here.

## Per-step implementation and acceptance
All 39 steps are implemented. Each row names its production door and passing acceptance fixture(s). No implementation remains. `Narrow` means the completed sync/test-hooks full-profile suite. `Server` includes the full server run with its nine repaired cases validated by targeted reruns. `Featureless` combines the 6,587 unchanged passing cases with the two corrected persona cases. These evidence labels do not claim an unfiltered workspace scripted gate.

| Step | Implemented door | Passing acceptance test(s) | Evidence / implementation left |
|---|---|---|---|
| 1 | comm/projector.rs receipt append/projector | `comm::tests::send_and_reply_receipts_project_only_last_touch` | Narrow; none |
| 2 | channel_identity codec and registration | `channel_identity::tests::channel_auth_modes_register_without_credential_material` | Narrow; none |
| 3 | channel_identity/actors.rs scoped actor and slip issuance | `channel_identity::tests::actors::mail_and_enrichment_are_distinct_scoped_actors_with_own_keys_and_priors` | Narrow; none |
| 4 | deletion cooperative request codec/signing; terminal pact gate | `deletion::tests::no_remote_deletion_attestation_surface; cooperative_request_rejects_noncanonical_scope_and_wrong_domain` | Narrow; none |
| 5 | batch/export/export_companion.rs sensitivity ceiling | `batch::export::tests::companion_export_checks_channel_ceiling_and_lifecycle` | Narrow; none |
| 6 | authority tier-floor fold checks | `authority::tests::tier_floor::{hardware_floor_rejects_software_signer_under_every_arrival_order,floor_softening_is_delayed_vetoable_and_causal}` | Narrow; none |
| 7 | llm/step admitted generate and settlement | `llm::step::tests::admitted_step_finishes_and_settles_after_deadline` | Narrow; none |
| 8 | federation/org_admin.rs immutable policy; server org routes and pairing | `setup_is_fixed_closed_and_renders_only_granted_powers; org_admin_routes_render_only_setup_powers_and_refuse_excluded_operations; org_ticket_requires_fixed_policy_registered_admin_and_closed_subset` | Narrow + Server; none |
| 9 | config privacy posture validation | `config::tests::three_postures_have_exact_wire_values_and_key_pairings; server privacy merge tests` | Narrow + Server; none |
| 10 | task_verb/consult_fanout_admission.rs | `fanout_submit_estimate_gate_tick_dispatched_paused_threshold; fanout_tunable_threshold_auto_history_and_human_authentication` | Narrow; none |
| 11 | task_verb/consult_fanout_resume.rs same-txn ruling/ED escalation/TASKs | `fanout_approve_policy_deny_resume_exact_digest_and_gate_receipts` | Narrow; none |
| 12 | Memory::fan_out_agents_section | `fanout_pathologies_park_with_count_fan_out_board_rows_and_evidence` | Narrow; none |
| 13 | authority/checkpoint.rs quorum, horizon and checkpoint chain | `authority::tests::checkpoint::quorum_checkpoint_roundtrip_matches_replay_and_rejects_tampering` | Narrow; none |
| 14 | Vault RSI configure/reserve/settle/refund/reopen line | `llm::budget::rsi::tests::rsi_line_reserves_settles_refunds_and_survives_reopen` | Narrow; none |
| 15 | authority managed-host consent/genesis policy | `authority::tests::hosted_consent::managed_host_genesis_passes_consent_and_self_host_refuses_it` | Narrow; none |
| 16 | authority FederationConfirm fold and codec | `federation_confirm_consumes_ids_nonces_and_requires_live_roster; federation_confirm_sibling_replay_is_permutation_independent; federation_confirm_codec_all_kinds_and_zero_rejection` | Narrow; none |
| 17 | authority independent recovery/history APIs | `authority::tests::history_transfer::two_vaults_recover_with_independent_genesis_and_signatures` | Narrow; none |
| 18 | authority causal actor binding histories | `authority::tests::actor_binding::revoke_fork_beats_concurrent_higher_epoch_bind_in_every_order` | Narrow; none |
| 19 | authority final-txn causality and read/export checks | `authority::tests::causal_claim::concurrent_claim_quarantines_at_replay_and_later_read_while_regrant_descendant_lives` | Narrow; none |
| 20 | ClaimInput/ClaimCandidate and MCP relationship field; transactional validation | `claim::tests::relationship_candidate_stamps_and_rejects_unknown_or_wrong_kind` | Narrow + Server + bindings; none |
| 21 | dreamer_consolidation/conflict.rs bucket identity/hash | `dreamer_consolidation::tests::{nickname_relationships_form_distinct_buckets,bucket_hash_conformance}` | Narrow; none |
| 22 | claim/scope_stamp.rs strict v2 door and one-time sweep | `four_scope_keys_are_required_at_raw_and_replay_write_doors; legacy_claim_scope_sweep_stamps_explicit_base_once` | Narrow; none |
| 23 | batch/person_substrate.rs deterministic PERSON substrate | `person_mints_one_substrate_facet_with_sensitivity_and_replay_is_idempotent` | Narrow; none |
| 24 | Companion facade retired storage + companion/persona.rs PERSON baseline, CLAIM changes, scoped compiler and scenario FACET | `persona_rebase_replays_changes_on_person_without_minting_masks; persona_scenario_is_a_scoped_owned_mask_and_malformed_changes_fail_closed` | Narrow + all-features + Featureless; none |
| 25 | authority ReRoot lineage replaces RecoveryReboot | `authority::tests::recovery_ceremony::migration_preserves_genesis_and_does_not_restore_instant_widen_authority` | Narrow; none |
| 26 | authority Genesis recovery acknowledgement and fragile flag | `authority::tests::recovery_ceremony::genesis_secret_step_cannot_be_skipped_and_dismissal_is_visible` | Narrow; none |
| 27 | recovery/privacy_kit.rs independent 2-of-3 kit | `published_field_vector_restores_a_key; any_two_shares_restore_only_their_vault_key` | Narrow; none |
| 28 | managed authenticated-owner account recovery | `authority::tests::history_transfer::account_auth_migrates_managed_root_without_widening` | Narrow; none |
| 29 | authority revocations-first ready-entry scheduler and causal grant checks | `authority::tests::actor_binding::revoke_fork_beats_concurrent_higher_epoch_bind_in_every_order; binding_fold_is_permutation_invariant` | Narrow; none |
| 30 | authority/slip.rs MAC/caveat/TTL/holder verification | `v2_roundtrip_tamper_and_missing_binding_deny; offline_meet_order_and_ttl_expiry_never_widen; named_record_meets_and_empty_channel_meets_never_restore_generic_reads` | Narrow + Server; none |
| 31 | authority logged mint/revoke/consume and credential-door one-shot | `log_mint_requires_parent_narrowing_and_revoke_kills_subtree; log_single_use_burn_is_atomic_and_survives_fresh_decode; consuming_child_spends_single_use_ancestor_and_siblings` | Narrow + Server; none |
| 32 | authority pairing ticket + server pairing/liveness/binding | `pairing_link_mints_once_and_requires_connection_private_key; server auth/pairing tests` | Narrow + Server; none |
| 33 | retired device lease issuance and registration | `one_1191_root_rotation_residue_cannot_mint_or_authorize; server lease-registration refusal tests` | Narrow + Server; none |
| 34 | logged host-root slip + scoped-read fail-closed admission | `claim::scoped_read::slip_tests::host_root_reads_stamped_rows_but_plain_keys_and_revoked_proofs_do_not` | Narrow + Server; none |
| 35 | federation/scope.rs six-axis lattice and bumped world codec | `empty_scope_and_empty_axis_decode_to_bottom; six_axis_meet_and_narrowing_laws; disjoint_worlds_meet_at_bottom_not_base` | Narrow; none |
| 36 | four stored Scope grant adapters, schema sweep and RetiredCeiling history | `legacy_policy_grants_migrate_on_write_and_once_on_open_with_selectors_and_budget; current_selectors_and_budget_only_narrow_stored_scope; empty_and_partial_new_scopes_persist_bottom_and_deny_real_reads_and_effects` | Narrow; none |
| 37 | federation/record_scope.rs positive stamps and scoped operations | `unstamped_records_are_excluded_from_read_export_delete_and_debug_is_explicit` | Narrow; none |
| 38 | authority ReRoot fold, codec and live-slip signer checks | `signed_history_export_import_re_root_preserves_identity_and_bytes; migration_preserves_genesis_and_does_not_restore_instant_widen_authority` | Narrow; none |
| 39 | signed authority history export/import before ReRoot | `authority::tests::history_transfer::signed_history_export_import_re_root_preserves_identity_and_bytes` | Narrow; none |

## Integration details
- Local writes stamp a deterministic PERSON substrate or an explicit mask. CLAIM v2 requires all four scope keys. The one-time sweep handles pre-axis rows; raw and replay writes never silently upgrade incomplete new stamps.
- Stored grants carry six-axis Scope; selectors and budget counters only narrow it. Schema 1.1 migrates to 1.2 once. Current empty/partial Scope remains Bottom. Retired SetCeiling history decodes inertly; new append is refused.
- Common Scope and causal checks cover retrieval, hydration, sync, export and deletion. Unknown stamps stay hidden unless the explicit debug view is used. Deleted metadata requires full-position authority, any named-ID bound, and a live slip. No deleted body is disclosed.
- CLAIM envelopes carry the observed authority frontier. Replay never invents one. Concurrent revokes dominate grants; only causal descendants of regrant authorize writes. Existing rows can become quarantined without losing raw evidence.
- Logged slips use v2 MAC chains, caveat meet, expiry and holder binding. Revoking a parent kills its subtree. Empty named-record/channel intersections remain denied. Atomic one-shot redemption consumes applicable ancestors. Admission consumes a holder-proof nonce once, including HTTP, WebSocket and app-tier binding.
- Pairing preserves owner-approved principals and closed org powers. MCP registration is metadata plus a ceiling, not authority; unrelated host credentials cannot lend it root access. Managed custody derives its issuer from the verified vault DEK, not a per-spawn callback token.
- Fanout admission precedes TASK mint. Exact-digest authenticated rulings, Gate receipts, ED-06 remembered caps and resumed TASKs share the transactional door. Board rows expose total/per-peer counts and pause evidence.
- T2 fixture roots use canonical temporary paths on macOS. Production symlink rejection and all negative path checks remain unchanged. Selector fixtures now use binary CLAIM scope IDs. The edge-shipping fixture uses a real PERSON provenance actor outside its mirrored window and positively stamps all read controls.
- The pending-claim index fixture hashes the actual committed body, including its writer stamp, rather than the pre-write candidate. All replay/index preservation assertions remain.
- The root surface intentionally drops only `CompanionExportClassification`; new persona types are exported under `companion`, not the root. No lint allowance, process-global state or ratchet budget was added.

## Validation
Host: owner's MacBook through the factory Cargo wrapper, worktree-owned target, at most three build jobs per slot and two test threads. The ticket-local routing guard refuses Arch overflow. No shared host configuration was changed.

| Command / scope | Result |
|---|---|
| Core `nextest --features sync,test-hooks --profile full` | **7,837 passed, zero failed, 16 skipped**; all integration binaries and compile-fail tests included. |
| Core all-features full-profile run + targeted repair rerun | **7,832 passed** in the initial core/server selection; all **14** failures passed after repair. The two new persona cases also passed `cargo test --lib --all-features`. |
| Core `cargo test --lib --no-default-features` + corrected persona rerun | **6,587 passed** in the full run, plus **2** corrected-source passes; **4 ignored**. The two original failures were traced to the pre-fix snapshot lacking ClaimOf fixture edges. |
| Server `cargo test --all-features --no-fail-fast` + targeted repairs | Library **736 passes + 7 repaired passes**, integration **89 passes + 2 repaired passes**; managed-mode **36**, managed-privacy **4**, app-tier WebSocket **4** passed. Five library cases remain ignored. |
| `cargo test` for oneiron-napi, oneiron-remote, oneiron-uniffi, all features | **67 passed**, zero failed. |
| Workspace doctests, all features, excluding oneiron-bench | Passed. |
| Workspace strict rustdoc, all features, no dependencies | Passed with `RUSTDOCFLAGS="-D warnings"` and inherited encoded flags unset. |
| Server-production clippy, all features, no all-targets | Passed with warnings denied. |
| Workspace clippy, all targets/all features | Passed with warnings denied. |
| Core featureless clippy, all targets | Passed with warnings denied. |
| Formatting for all five touched crates | Passed. |
| Code-map, structural ratchet, root surface, whitespace | Passed: **2,559 mapped files**, **700 root names**, ratchets **2 / 98 / 87 / 33**. |

### Evidence reuse and platform boundary
- Production code has not changed since the persona implementation commit `87a25af6`. Later edits fix only fixture edges, lint style, formatting and these notes. Green runtime and production-build evidence is retained; unchanged broad suites were not rerun ceremonially.
- The corrected persona row and ClaimOf edge commit together. The new tests passed in narrow sync, all-features and featureless selections. No approval, Scope, causal or symlink gate was relaxed to fix a fixture.
- Current implementation decisions and per-step results above supersede the historical worker handoff notes in `W7-C07-slips.md` and `W7-C07-policy-scope.md`.
- The earlier `cargo test --workspace --no-fail-fast` ran the workspace. Its changed-scope failures are all repaired and covered above. The seven unchanged macOS benchmark preflight failures (`UnsupportedPlatform`) are not counted as passes; oneiron-bench was not changed. No Linux reference-host or unfiltered `scripts/verify.sh` / `VERIFY-OK` verdict is claimed.


## Acceptance-test follow-up

The factory's Linux test selection included `oneiron-napi` and stopped at its
known unresolved `napi_*` symbols before executing any selected test. This was
not runtime evidence for the five changed crates. The follow-up runs those
crates on the MacBook, including the N-API tests, rather than omitting them or
changing linker policy.

Additional acceptance assertions:
- ONE-2280: N-API conversion preserves an explicit relationship and its omission.
  The shared SDK rejects oversized relationship refs before either backend can
  dispatch. UniFFI's generated lower/lift codec round-trips the relationship.
- ONE-2280: MCP writes persist the nickname's relationship. Unknown references
  and non-RELATIONSHIP rows fail without changing claims or Gate receipts.
- ONE-2262: a checkpoint with a false roster summary is refused even when the
  quorum signs that false summary with valid signatures.

No production behavior, public API, dependency, or shared build configuration
changed. The canon needs no further amendment for these test-only additions.

Validation receipts are under the ticket's `fix-tests/` directory on the build
host. The MacBook uses `/Volumes/Cinema/w7-build/wt/W7-C07/target`, at most three
Cargo jobs, and two test threads. SDK test run: **69 passed**, zero failed,
covering all three of `oneiron-napi`, `oneiron-remote`, and `oneiron-uniffi` with
all features.

Final core/server command:

```sh
cargo nextest run -p oneiron -p oneiron-server --all-features --profile full --test-threads 2 --no-fail-fast
```

**8,718 passed**: 7,839 core and 879 server tests; zero terminal failures;
21 skipped by the existing configuration. Both added MCP and checkpoint
assertions passed on their first attempt. The unchanged booking test
`api::booking_anti_abuse::tests_guards::tests::book_limit_uses_combined_ip_email_key`
failed once and passed on its configured second attempt. Its guard takes a
fresh wall-clock time for each minute-bucket observation. This is retained as
one flaky result, not reported as a clean first-attempt pass. At that earlier run, neither that test
nor its guard differed from the branch's merge base; the later clock repair below
changes the guard's test-clock access without changing production quota policy.

SDK command:

```sh
cargo test -p oneiron-napi -p oneiron-remote -p oneiron-uniffi --all-features --no-fail-fast
```

The complete logs are `fix-tests/core-server-tests.log` and
`fix-tests/sdk-tests.log` under `/home/lexi/w7-build/tickets/W7-C07/`.
The core/server service completed with exit status 0. The SDK command also
completed with exit status 0. Formatting, whitespace and the code-map check
passed. This validates every touched crate; it is not a new full-workspace
`VERIFY-OK` claim. Prior unchanged production lint, rustdoc, featureless and
narrow-sync evidence remains as recorded above.


## C07_BOOKING_REPAIR_20260919 — ordinary-test quota recovery

The latest factory `tests:2` ordinary Cargo run is a separate, retained failure:
server library **741 passed, 3 failed, 5 ignored**. The failures were
`slot_list_ignores_default_form_timestamps_but_spends_its_ip_quota`,
`slot_list_limit_is_ip_scoped_and_cache_is_30_to_60_seconds`, and
`page_wide_rows_govern_event_typed_requests`. The earlier 7,839 core / 879 server /
69 SDK passes, including the recorded nextest retry, do not clear that failure.
The last-run failure blocks are preserved in the ticket's
`booking-repair/factory-tests-2-failures.txt`; no old appended failure was used.

Cause: quota counters are transactional and vault-local. The engine resets a
counter when `now_secs / 60` changes. Each HTTP guard previously sampled real
wall time, while these tests assumed all 121 calls stayed in one minute.
Parallel library execution can cross that boundary and correctly return
`Continue` in the new window. This is a test clock assumption, not evidence of
shared counters or an incorrect production quota.

Repair: a test-only `Option<u64>` belongs to each `SyncServer`, defaults to
`None`, and is pinned only by the booking guard fixture. All booking guard and
cache time reads use the same server-local clock method; production still reads
`SystemTime` and retains the same clock error behavior. There is no new process
global, production setting, engine counter change, quota increase, skipped test,
thread-count override, retry, or Cargo routing/profile change.

The new boundary regression asserts both vaults have independent 120-request
quotas, exact `RetryAfter { seconds: 1 }` before rollover, a fresh quota only on
the advanced server, an unchanged exhausted sibling, `RetryAfter { seconds: 60 }`
after spending the new window, no counter reset from a cache hit, and cache
presence at 44 seconds versus expiry at exactly the configured 45-second TTL.

The existing bounded independent review (`booking-repair-review.md` under the
ticket directory) confirmed this cause and repair and found no additional fix.
No PR exists for this branch (`gh pr list --state all --head w7/W7-C07` returned
`[]`), so PR feedback retrieval and posting are not applicable. A new broad
architecture review is not needed for an instance-local test clock with no
production policy change. Core/SDK reruns are skipped because those bytes are
unchanged and their retained green evidence remains valid.

Focused ordinary validation (native wrapper selected Arch):
`cargo test -p oneiron-server --lib api::booking_anti_abuse:: --no-fail-fast`:
**11 passed, 0 failed, 0 ignored, 739 filtered**, service exit status **0**.
This includes all three latest failures, the previously retried book-quota test,
and the new rollover regression. The complete receipt is
`booking-repair/focused.log` under the ticket directory.

Final repair validation, on implementation `b99a8a6b` plus formatting `a01c46d1`:

| Command | Native host | Terminal result |
|---|---|---|
| `cargo test -p oneiron-server --no-fail-fast` | MacBook | **880 passed, 0 failed, 5 ignored, 0 filtered**; exit 0 |
| `cargo clippy -p oneiron-server --all-targets --all-features -- -D warnings` | Arch | Passed; exit 0 |

The ordinary server totals are library **745 passed / 5 ignored**, integration
**91 passed**, managed-mode **36 passed**, managed-privacy **4 passed**, and
WebSocket app-tier **4 passed**. Both binary unit targets contain zero tests.
The five pre-existing ignored library tests remain ignored; no skip was added.
This is ordinary Cargo test execution with default parallelism and no test
retries, not the earlier nextest retry result. All three factory-failing tests,
the previously flaky book-quota test, and the new regression passed here.

The retained services `w7-c07-booking-server` and `w7-c07-booking-lint` each
reported `Result=success`, `ExecMainStatus=0`, and `ActiveState=inactive`.
Complete receipts are `booking-repair/server.log` and `booking-repair/lint.log`;
exact commands, source revision and terminal totals are in the adjacent JSON
receipts. Native routing, Cargo profiles, dependency selection and test-thread
environment were not changed. Existing vendored heed lifetime warnings were
not suppressed or edited; scoped project clippy completed successfully.

The assigned booking repair is complete. This is not an unfiltered workspace
`VERIFY-OK` or a fabricated factory pass: the historical standard `tests:2`
rejection stays preserved, and the mandatory factory rerun follows normal
submit under the manager. No unrelated PR was posted. Prior core, SDK and other
unchanged-scope evidence remains retained rather than rerun.

## Review-fix ledger (2026-09-19)

Review input head: `6ba83dd9`. Sources: internal Opus root
`01a0ba38-63e7-732e-a777-b3fac2034520` (three blocking findings),
internal Grok root `01a0ba38-63fe-7686-81b7-053042960b8e` (LANDABLE),
and retained booking repair review. Original roots, partial subreview transcripts,
logs, and jobs remain untouched. Opus's incomplete per-step acceptance coverage
is retained as a review caveat, not represented as completed.

Before edits, both `gh pr list --state all --head w7/W7-C07` and the
repository-wide `--search W7-C07` returned `[]`. There is no actual PR
whose Qodo/Codex/CodeRabbit top-level comments, reviews, or inline threads can
be paginated, and no GitHub disposition destination. No unrelated PR is used.

| Source ID | Disposition | Fix |
|---|---|---|
| Opus #1 (both generated map entries) | Valid | Regenerate the booking guard's `s` → `m` size bucket. |
| Opus #2 (export predicate and inverted test) | Valid | Restore fail-closed shared-vault destination scoping alongside the sensitivity ceiling. |
| Opus #3 (persona snapshot sensitivity threshold) | Valid | Only Public relationships enter the portable card; cover all four sensitivity rungs. |

No reported blocking finding is dismissed. These duplicate code/test locations
are tracked as three laws, not separate findings. No canon rewrite is needed
for #1 or #3; the implementation had diverged from the portable disclosure law.

### Disclosure repairs

- Portable `companion_export_layer` has no destination and excludes all
  `SharedVault` rows, even at the highest sensitivity ceiling.
- `companion_export_layer_for_channel` uses the existing `ChannelIdentity`
  `Vault { vault_id }` binding. The identity owns the scope predicate. Shared
  rows need an exact vault match; actor-bound identities do not imply one.
  Identity validation, active self-held sending state, record lifecycle, and
  the sensitivity ceiling remain independent requirements. This pure content
  filter does not authorize sending or replace the effector gate. No new scope
  axis or persisted identity format is needed.
- The export regression retains the portable exclusion and adds matching/wrong
  vault, actor-bound, inactive/invalid identity, and Public/Private/Bottom
  ceiling cases. Existing expression and lifecycle assertions stay in place.
- Persona snapshots now admit only Public relationships. The new four-rung
  regression checks both relationship and derived third-party claim rows, plus
  both exported artifact formats. No test is disabled or removed.

The old step-5 notes described only the sensitivity replacement. The missing
clause is now explicit: shared-vault destination scoping belongs to channel
identity, and portable export has no such destination. The read-only canon is
not edited.

The bounded context worker left no final report after session loss. Its retained
transcript exposed the same missing vault-match clause in sync selector export.
That path now uses the channel-identity binding predicate with the already
validated federation grant's vault destination. Its existing negative fixture
now has the same Private sensitivity as the admitted shared-vault row; only the
vault mismatch can exclude it. This extends Opus #2 to the second export door,
without changing the sync sensitivity ceiling or retirement propagation.
