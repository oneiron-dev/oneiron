# W7-C07 implementation notes

## Canon and decisions
- Canon: `generated/oneiron/identity.md`; `identity/oneiron-arch-0022-identity-world-model-v1.md`; `sync/oneiron-arch-0043-federation-membership-v1.md`; `decisions/oneiron-dec-0005-policy-residence.md`.
- The ticket's flat `deletion.rs` and `error.rs` paths predate directory modules. Courtesy request errors belong to `error::RecordError`, with stable `ErrorKind` values appended.
- Exact posture wire strings are `managed`, `relay`, `self-host`; source variants Hosted/SelfHostLocal remain to avoid unrelated API churn.
- ARCH-0067 in the Scope ticket points to the Context Board. The Scope formula is in registry OF-453: worlds × facets × bands × audience × verbs × sensitivity.
- No docs mirror files are edited.

## Validation environment
MacBook via the factory Cargo wrapper; worktree-owned `target`, wrapper slot budget at most 3 build jobs, test threads 2. No full gate claimed. The per-step implementation list below is historical. The latest validation section supersedes older pending/running statements; no final integrated pass is claimed.

## Per-step status
1. **Comm send/reply receipts** — Added InboundReply receipt/event to the existing projector. Sends/replies update only comm.last_touch. Receipt replay/no-semantic-twin fixture passed in scoped run 5.
2. **Channel auth mode** — Required closed ChannelAuthMode metadata and pinned claim. Codec versions 5/6 reject unknown modes and credential fields. Registration/codec fixtures passed across scoped runs 5/7.
3. **Channels as actors** — Added atomic channel-owned provider actor, identity, connector budget key, confidence prior and a real WORLD/FACET-scoped slip. Uses the existing provider PERSON carrier with system actor class, not a new entity kind. Identity now speaks as the channel actor; its prior route target is metadata. Mail/enrichment composition fixture added; runtime pending.
4. **Courtesy deletion requests** — Strict canonical signed request codec and terminal-pact signing door. Tamper, domain, local-world, terminal status/cutoff and no-attestation fixtures passed in scoped run 5. No transport or remote acknowledgement exists.
5. **Export sensitivity ceiling** — Removed the classification type/strings and made portable export use sensitivity versus channel ceiling. PERSON/FACET facade storage replaces type 78. Removed the obsolete root-surface pin name as required by this contract. Runtime fixtures pending.
6. **Signer tier floor** — Added ancestry and concurrent-floor signer checks. Hardware-vs-Software/permutation fixture passed. Causal floor-event tracking now permits only observed restrictions to be cleared by delayed, vetoable softenings. Both signer-tier and delayed/vetoable softening fixtures passed in the authority runtime run.
7. **Admitted LLM deadline** — Removed deadline race after admission. Injected-clock completion, absolute settlement, next-step refusal and existing retry tests passed in the initial core run. Committed as ce0e13f8.
8. **Org-admin powers** — Added immutable closed four-power setup, scoped org credentials, owner-only policy route, and filtered Console power-list route. Core, token and HTTP fixtures added; compilation/validation pending. Member private keys are not inputs. FED-S5 remains an owner custody-policy confirmation, not an enrollment implementation.
9. **Three hosting postures** — Added Relay and exact managed/relay/self-host wire values. Engine pairing/wire test passed. Server merge cases added; production library compiled in the later integration check, runtime pending.
10. **Fanout admission** — Source integrates estimate/admit before any TASK, inclusive configurable threshold, frozen durable pauses, cycle/rate/AUTO evidence. Worker handoff is frozen. Tests added; runtime pending.
11. **Fanout ruling/resume** — Exact frozen digest, authenticated human ruling, Gate receipt and ED-06 remembered cap are wired into resume/deny. Runtime pending.
12. **Fanout board rows** — Total/per-peer consult counts and pause evidence render as board rows. Runtime pending.
13. **Authority checkpoints** — Added canonical checkpoint encoding, selected quorum signer transcript, horizon replay, roster/floor summary and parent-checkpoint chain verification, with atomic persisted head update. Raw signed history is retained. Tamper/quorum/round-trip fixture passed in the authority runtime run.
14. **RSI budget line** — Durable vault-local reserve/settle/refund with terminal accounting, suspension, soft/pinned shares and protected exploration. Reopen/reserve-before-spend fixture passed. Research-loop caller adoption remains to review.
15. **Managed host root** — Codec accepts host owner roles; vault folds select managed versus self-host/relay consent. Managed genesis, human binding and critical confirm test passed, while self-host still refuses cloud root. Later authority changes need regression validation.
16. **Federation confirmations** — Fold consumes IDs/nonces, rejects zeros and non-roster signers, and deterministically rejects concurrent replays and descendants. All four kinds/replay fixtures passed.
17. **Independent multi-vault recovery** — Batch recovery invokes each vault’s own signed in-chain ReRoot, without common key material or cross-vault atomicity. Two-vault fixture passed in the authority runtime run.
18. **Causal revoke floors** — Fold-only revoke hash provenance controls binding liveness. Higher epochs cannot clear unseen revokes. 120-permutation and causal regrant fixtures passed.
19. **Revoke-window quarantine** — Write envelopes carry an observed authority frontier. Causal binding histories preserve affected actor/class pairs across rebind-away. The shared batch door checks final-transaction claim causality; scoped reads and exports recheck it so a later revoke can quarantine an existing row. A typed audit disposition exposes quarantine without deleting raw evidence. Replay never invents a frontier. End-to-end concurrent/regrant-descendant fixture added; runtime pending.
20. **Relationship claim writer** — ClaimInput/binding DTOs, MCP edit and ClaimCandidate carry relationship. Transactional RELATIONSHIP validation, overlay fence and scoped upsert added. Core claim/upsert tests passed; server/binding builds still pending.
21. **Relationship bucket key** — Bucket identity/hash include relationship. Two-relationship bucket test and updated golden passed.
22. **Required claim scope keys** — Codec v2 requires worldId, scopeFacetId, scopeRelationshipId, scopeProjectId plus scopeVersion. All write/replay paths validate; one-time open sweep stamps legacy rows. Reserved base/default-project IDs remain explicit. Runtime and fixture migration pending.
23. **Substrate facets** — PERSON materialization creates one deterministic substrate FACET with its own sensitivity. Removed facet world/relationship fields. Runtime pending.
24. **Companion retirement** — Dedicated type-78 writes are refused; old rows sweep to PERSON/FACET. CompanionRecord is a transient facade rather than an independent stored kind. Persona/rebase fixtures still need runtime validation.
25. **Recovery via RotateKey lineage** — Replaced RecoveryReboot with in-chain ReRoot. Vault ID/genesis stay fixed, old roster is revoked and migrated root has no instant widen privilege. No recovery epoch/floor override remains. Runtime pending.
26. **Fragile genesis** — Required recovery-secret acknowledgement/dismissal in Genesis; dismissal exposes GENESIS_FRAGILE until the second device actually enrolls. Fixtures added; runtime pending.
27. **2-of-3 backup** — Vault-scoped privacy-mode Shamir GF(256) shares for device, physical media and guardian. Sensitive buffers scrub on drop; reconstruction checks expected vault/kit and commitment. Pair/single/mixed/corrupt/golden fixtures added, not yet compiled. Restores key custody, not authority widening. Unratified sealed-envelope VEK rotation is not invented.
28. **Account recovery** — Authenticated-owner recovery is managed-only and signs ReRoot to the new host, retaining vault identity and no instant widen. Runtime pending.
29. **Revocations-first fold** — Scheduler reselects after each transition and prioritizes ready revocations, using hash order within each class. Backdated concurrent-bind permutation fixture passed.
30. **Slip verifier/delegation** — Serializable v2 MAC chain, canonical token, offline caveat meet, expiry/TTL and holder binding. Final-fold verification includes every ancestor’s live signer and unresolved fork quarantine. Tamper/narrowing fixtures added; runtime pending.
31. **Slip log mint/revoke** — Mint/revoke/consume are authority entries; subtree tombstones and atomic one-shot consumption are real. Withdrawals bypass a pending-widen freeze without applying a skipped grant. Runtime pending.
32. **Pairing and binding** — One-time ticket redemption mints a logged slip, with throwaway-key proof and unauthenticated liveness descriptor. Core and HTTP fixtures added; old token-based server fixtures require migration.
33. **Retire device lease** — Removed live client lease mint/request path and rejects server lease registration. Receipt-origin attestation remains forensic evidence, not a credential substitute. Residue regression added; runtime pending.
34. **Fail-closed reads** — Root slip bootstrap lands with missing-grant and named-world/base fail-closed checks. Proof-backed reads recheck live authority; policy is read from the current transaction, not a stale cache. Claim/nonclaim retrieval and hydration use scoped gates. Root-read/revocation regression added; runtime pending.
35. **Six-axis Scope** — Six-axis order/meet/Bottom, kind sets, sensitivity and project audience are wired into grants/slips/record readers. World selector codecs bumped; named worlds no longer imply base. Original lattice fixtures passed on an earlier snapshot; integrated suite pending.
36. **Stored Scope grants** — Grant families now carry stored Scope with separate budget constraints and a one-time sweep. Old set_ceiling wire records decode as inert RetiredCeiling; new append rejects them. Historical signed import preserves their bytes. Runtime pending.
37. **Record stamps** — Claims carry intrinsic Scope; other records use positive, digest-bound local stamps. Normal read/export suppress unknown stamps; explicit debug reveals suppressed rows. Delete-by-scope remains transactional. Runtime pending.
38. **ReRoot** — Single signed in-chain entry replaces the roster while retaining genesis-derived identity. Old-host slip verification fails and the new host can mint. Fixtures added; runtime pending.
39. **Signed history migration** — Full signed history exports parent-first and imports atomically with identical bytes/genesis. Host migration then applies ReRoot; mixed roots/bad parents/signatures reject. Round-trip fixture added; runtime pending.

## Validation evidence and open work
- Initial `cargo test -p oneiron --lib`: 6510 passed, 11 macOS temporary-path failures, 4 ignored. This predates most cluster work and is not evidence for the final source.
- MacBook `cargo test -p oneiron --lib authority:: -- --test-threads=2`: **195 passed, 4 failed**. The passing set includes all ten real-slip tests, checkpoint verification, account-auth recovery, independent recovery/history transfer, tier floors, and pending-widen veto tests. The four fixtures now use a fresh `BindActor` after withdrawal, actual hash-based fork order, and the mandatory Genesis recovery step. Their rerun remains open.
- Combined core/server compilation first found the private `encode_claim_body` fixture call. The fixture now uses public `Vault::put_claim`; production visibility did not widen.
- `.w7/core-server-lib.log` compiled current core lib tests and stopped on one server fixture namespace error (`lease_registry_key`). The module path is corrected. No runtime tests ran in that attempt.
- Output-free queued reruns were replaced by `cargo test --workspace --no-fail-fast -- --test-threads=2`, with a worktree-local real-path `TMPDIR` configured through Cargo. That run is pending. macOS has known unchanged benchmark preflight failures; none will be counted as a pass.
- Formatting passed on the integrated core/server/binding snapshot before the final external HTTP/WS fixture edits. Code-map regeneration, final formatting, all current-source runtime results, featureless/production build lanes, strict lint/docs, remain open. Implementation commits are recorded; validation fixes will be separate commits.

## Integration decisions
- Policy schema 1.2 stores six-axis Scope separately from selectors and budget counters. Explicit 1.1 input migrates once; malformed/current empty Scope never becomes a permissive legacy preset. Positive raw policy fixtures now write the current schema and canonical Scopes. Migration/negative fixtures retain their old inputs.
- Canonical authority Scope participates in the consent-frontier hash. Both live-slip checks use the vault clock. The last audit's claims that these were still missing were stale; the implementation and focused frontier test already exist.
- Pairing preserves owner-approved principal/class/org fields and validates the closed org allowlist. General Scope Top does not grant org powers. Owner grade requires an uncaveated Top instrument. HTTP/WS root credentials carry actual logged slip IDs.
- In-crate HTTP, Git/LFS, facade, idempotency, livequery, and app-tier fixtures use real logged slips with holder proofs. External campaign and WebSocket fixtures now use the same public issuance/verification doors. The WebSocket boundary test proves the scoped slip works over HTTP before checking the owner-only upgrade refusal.
- App-tier negative fixtures isolate forged MACs from holder signatures, wrong holder keys, expired/missing binding proofs, unknown fields, and revocation. Invalid actor classes are rejected at mint, not by a retired-token parser.
- Device-lease enrollment fixtures now assert unconditional refusal without mutation. Historical lease withdrawal, tenant isolation, expiry, and durable root/mirror rollback remain covered. No old lease or residual key can mint a new credential.
- The CLI's 64-hex revocation test checks signed parent/subtree withdrawal and live siblings. The old 32-hex tombstone helper remains explicitly separate for historical/dev revocation; it cannot substitute for a signed slip withdrawal.
- Privacy backup-kit tests were not selected by the authority filter. No full crate/workspace pass or 39-step acceptance is claimed yet.

## Build routing boundary
- The installed factory wrapper can select an Arch overflow slot while the MacBook is busy. This ticket's local guard refused that selection before compiling. That exit is a routing result, not a compiler/test failure.
- The queued workspace run now uses `.w7/cargo-remote`, a ticket-local copy of the installed wrapper with only the Arch overflow entry removed. It retains the native per-ticket lock, three MacBook slots, and three Cargo jobs per slot. Shared launchers/configuration were not changed. The mini was rechecked at 100% disk capacity and is not used.
- The finite validation process records its exit in `.w7/workspace-current.status` and output in `.w7/workspace-current.log`; it is independent of a seat-turn cleanup. These private routing files are not part of the product change.

## Follow-up validation and binding correction
- The remote-only workspace run reached the MacBook and compiled the workspace dependencies. It stopped on an external WebSocket fixture's wrong `ENTITY_TYPE_PERSON` namespace. Both new fixtures now use `oneiron::registry::ENTITY_TYPE_PERSON`; the org pairing fixture also passes `PairingLink.ticket` to the binding/redeem functions. These corrections await rerun.
- Transport admission now consumes each holder proof nonce once, including HTTP requests, WebSocket upgrades, and app-tier binding. Repeated downstream checks remain read-only. A captured proof cannot open another request or socket within its validity window. Regression requests use the same proof twice and then a fresh proof of the same live slip.

## Workspace runtime repair pass (not final acceptance)
- The completed MacBook `cargo test --workspace --no-fail-fast -- --test-threads=2` run compiled the workspace and failed ten test targets. Core library: 6,877 passed, 208 failed, four ignored. There were 322 failing cases across all targets, including the seven known unchanged macOS benchmark preflight failures. All authority tests passed on that snapshot, as did the four external `ws_app_tier` cases. This is not a green workspace result.
- Steps 30–34: named-record caveat intersections no longer confuse an empty meet with an unbounded generic read. Channel-bound credentials cannot regain generic reads by removing their channel set. Logged child slips cannot erase a parent's named bound. The new regression checks offline ordering, explicit empty sets, and attempted child widens.
- Steps 35–37: legacy/current grant shapes are separated strictly. A schema-1 federation grant carrying `authority_scope` is refused instead of silently replacing it with a broader preset. Malformed stored Scope values retain each grant family's typed error. Migration and mixed-version tests cover all three typed adapters.
- Steps 23–24: workspace onboarding now attaches persona FACETs to the companion PERSON, not its AGENT_DEF. Replay/preflight use FACET rows, and profile grants reference that PERSON. Identity receipt projection and serialization now follow FACET storage. Structural tests distinguish the mandatory substrate edge from the edge under test.
- Steps 22, 34–37: positive read fixtures receive explicit grants; no-grant and missing-base authority stays denied. Deleted/dangling short-id metadata requires a live grant covering every possible record position, because the missing row can no longer prove its old Scope. Narrow or absent grants do not learn deletion metadata.
- Sync export now uses one read transaction for Scope, causal, facet and coreference checks. This removes nested LMDB reader-slot failures without skipping checks. In-scope tombstones can propagate without re-exporting deleted bodies or expanding from deleted seeds. Federation staging rejects retired type-78 rows before promising materialization.
- The repairs above and the remaining fixture changes have not yet been compiled or run. Formatting, maps, focused regression reruns and current-source touched-crate tests remain open. No DONE or full-gate verdict is claimed.

- The first focused all-features core rerun stopped at a test-only `EntityId` Display formatting error in the sync stamp fixture. It now uses debug formatting. No runtime tests ran in that attempt; the identical filter is being rerun. Calendar oracle read authority is now a separate test-only policy row, not a default policy or source-permit mutation. Timeline deletion shells use the same full-position, live-authority check as deleted hydration and do not expose old bodies.

## Authenticated adapter integration (validation pending)
- MCP registration is metadata plus a narrowing ceiling, not authority. The exact registered token must verify as a logged, holder-bound slip. An unrelated host Authorization header cannot lend it root power. Pairing registers representable MCP credentials without minting a second instrument. Unprojectable bounds fail closed; /v1 keeps the full six-axis instrument.
- Managed contract-v1 custody derives a private, stable issuer from the verified vault DEK, not the per-spawn callback token. Protected-socket requests use its logged root proof. Forwarded client credential headers do not replace the managed principal; existing root mismatch and canary gates remain closed.
- Reconnect and idempotency compare the exact signed instrument and projected principal, not a derived remaining TTL that changes with the clock. Holder-signature and nonce checks remain in force.
- Focused all-features core run: 214 executed, 168 passed, 46 failed. It is not the full crate gate. Named-record/channel attenuation and grant migration cases passed except one new delegate migration expectation, now corrected to the read preset. Remaining failures include unfinished consumer fixtures and eleven sync selector cases under repair.
- Deleted timeline admission exposed one additional nested LMDB read: a CLAIM shell called `is_deleted_shell` while already holding a read transaction. It now uses `archive_tombstone_in_txn` in the existing transaction. The proof/deletion regression is not yet rerun.
- Ordinary delayed key rotation remains distinct from ReRoot migration. Its sidecar-repair test retains the original delayed/vetoable expectation. ReRoot recovery removes instant-widen authority and keeps the vault identity; this does not make a legacy RotateKey delay disappear.

- Server all-features validation stopped before runtime on two fixture integration errors: the depth-spend helper migration was incomplete, and the external MCP oracle constructed a now-private proof-bearing actor field. Depth fixtures now use the existing authenticated server helpers. The trusted injected-host oracle resolves its actor through the public registry without forging a read proof. The same server test command is running again.
- Sync follow-up withholds malformed CLAIM rows instead of aborting the whole export, while local storage faults still fail the export. Opaque positive controls are explicitly verified locally before export. Two forged-edge negative fixtures also stamp their known fixture bodies, independently of the hostile edge, so the missing-stamp door cannot make the facet-boundary regression vacuous. These edits still need runtime verification.

## Latest regression results (current integration)
- The preserved full server run completed: library 736 passed / 7 failed / 5 ignored; integration 89 passed / 2 failed; managed-mode 36 passed; managed-privacy 4 passed; app-tier WebSocket 4 passed. MCP, nonce replay, managed credentials, reconnect and contract snapshots passed. The nine failures were repaired; their current-source rerun remains pending.
- The focused core rerun passed all 59 cases. This covers the preceding 46 failures plus new or renamed grant, Scope, slip and inbox regressions. It is not a full-crate verdict.
- Claim deletion lookup now uses the canonical deleted-shell/deletion-presence checks inside the existing read transaction. The older archive-only check missed pending and published deletion metadata. Full-position authority and named-ID constraints still apply to deleted timeline metadata.
- FACET projection rejects trailing MessagePack bytes before interpreting or projecting a body. It does not expose malformed persona bytes through the generic projection fallback.
- Positive context-pack fixtures use owner credentials only where the assertion concerns owner-visible disclosure. Delegated-client disclosure clamps remain unchanged. Selector fixtures use current CLAIM scope keys, explicit nonempty bands and positively stamped controls, including denied controls so the negative boundary remains meaningful.
- Formatting of the nine repairs completed successfully. Full-profile core tests plus the nine server regressions are compiling; workspace clippy, featureless clippy, server-production clippy and strict rustdoc remain queued on the same fixed Rust snapshot. No result is claimed from a queue or a running process. Featureless runtime tests, doctests, final maps and the final structural checks remain open.
