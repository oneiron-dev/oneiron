# W8-T43 implementation notes

- ARCH-0004 (`/home/lexi/code/oneiron-docs/generated/oneiron/retrieval/oneiron-arch-0004-retrieval-graph.md`, lines 18–20) says the recency, salience, confidence, and gravity blend has the same shape at every effort. The canon is current; no correction needed. The mirror does not yet contain the ONE-2598 registry row; add a link to this behavior and its Light tests when that registry is updated.
- Apply all three requested boost flags on both memory recall paths at Light. Make `retrieval_effort` set salience and confidence independently of its non-Light PPR stage. Keep Light's seed exclusion, graph/rerank stages, hydration, edge, and field-profile settings unchanged.
- Update the explicit stage-plan test to include the Light blend flags, preserving its fork-hash and channel-trace comparison. Pin Light recency using the existing Medium fixture and Light salience using two separate-subject claims, avoiding supersession. The claim fixture explicitly indexes the same body text for each claim, since `claim_upsert` by itself does not index this value for lexical retrieval.
- Review fix: shared `retrieval_effort` now enables the default recency blend for raw pipeline and context-pack callers, including Light. The existing `recency_blend_applies` host-window check still suppresses recency for a caller-supplied temporal window. The explicit five-tier stage-plan fixture and a raw Light ranking regression cover this door.
