# W7-C06 implementation notes

Status: engine implementation is committed and its acceptance regressions are validated.
The nonblocking step-25 live model study is not run; its harness is implemented and tested.
This records scoped and reused evidence, not a `scripts/verify.sh` full-gate verdict.

## Canon and decisions

The read-only docs mirror and `.w7/CONTEXT.md` supply the contract. Main pages:
ARCH-0068 repo custody (`/oneiron/code-in-oneiron/oneiron-arch-0068-repo-custody-v1/`),
ARCH-0055 entity identities, ARCH-0024 resolution waterfall, ARCH-0023b federation,
ARCH-0043 shared-vault authority, identity §§2.6/5.4, Dreamer v2 contradiction/write
closure, ARCH-0006a/ARCH-0044 access, ARCH-0040 rendered provenance and foreign agents,
ARCH-0067 memory board, DEC-0005 manifest/read ceilings, ARCH-0038 write wall,
ARCH-0032 fingerprints, ARCH-0047 policy checks, and ARCH-0052 anonymous sessions.

## Step ledger

1. Docs export/registry source registered. Thin-star ASSET/ASSET_TEXT ingestion consumes one exact owner approval atomically. Optional injection classification is a separate derived annotation. Runtime evidence: docs registry, bulk-consent and derived annotation fixtures also passed in the f0034488 featureless run.
2. Pipeline semantic segments produce derived SUMMARY nodes with source envelopes. Scoped summary-first retrieval returns references; explicit expansion carries a read receipt. Rank fusion and consent fixtures are included. Runtime evidence: summary-first scoped expansion passed in the docs bulk-consent fixture, including the f0034488 featureless run.
3. Per-kind name/alias/phonetic/contact hints are indexed at the common body and deletion doors. Same-key lookup returns all candidates and never merges. Unit fixture added; validation retained below. Evidence: The copied-source full run passed `same_key_lookup_returns_every_candidate_and_never_links`.
4. Mention intake looks up hints before scoring. Prepared candidates are revalidated under the same writer transaction as waterfall scoring, provisional mint and imported claim admission. Re-mention, distinct-pair and rollback fixtures are included. Evidence: The copied-source full run passed `evidence_remention_reuses_ids_without_merging_a_distinct_pair` and `refused_import_rolls_back_provisional_person_too`.
5. New ids use monotonic thread-local ULID entropy and Crockford text. Existing raw/hex UUIDv7 ids stay opaque. Reopen/hydrate legacy-row fixture added after the older full-test build; validation retained below. Evidence: The copied-source full run passed ULID ordering and `legacy_uuidv7_row_hydrates_and_resolves_after_reopen`.
6. Authenticated principal/grant/vault-bound observations persist normalized peer rate and structural streak. Vault size changes the relative decision. Quiet/busy/reopen fixtures are included. Runtime evidence: all six federation burst tests passed, including relative baseline/vault-size comparison.
7. Selector and inbound updates use durable defer with actual bytes, authorization context and automatic replay. Pending bytes stay outside live entities. A coalesced advisory review projection groups them per peer; no human pause. Review projection and replay fixtures are present. Runtime evidence: deferred replay and coalesced review-bundle tests passed without a human pause.
8. One-shot shared-membership initialization stores preset choice, explicit grant rows and default policy. No preset adds no implicit roles. Customized policy cannot be overwritten. Five preset/reopen fixtures added; validation retained below. Evidence: The copied-source full run passed `every_preset_persists_explicit_grants_and_policy_once`.
9. Authenticated active Admin/Owner grants append retained rulings with previous-holder/value receipts. A persisted logical ledger order, then opaque id, selects the live ruling; live pact state is checked. Fold/permutation fixtures cover the logical ledger order. Evidence: The copied-source full run passed `equal_admins_keep_both_rulings_and_newest_wins_in_any_fold_order`.
10. Durable per-signer maxima and immutable entry-observation receipts protect replay across reopen without invalidating all historical entries. Reopen/rollback fixtures supplied; validation retained below. Evidence: The copied-source full run passed `signer_hwm_survives_reopen_without_rejecting_its_original_history`.
11. Named duration-v1 observation policy controls a clock-free local stale-roster window. The explicit local setter changes subsequent folds; signed entry timestamps do not supply freshness. Window boundary fixtures supplied; validation retained below. Evidence: The copied-source full run passed duration-v1 and first-seen stale-approval boundary tests.
12. The authority replay door persists per-peer counts and raises one typed advisory check above its configured threshold. It never refuses for rate. Former authority quota debits were removed from both materialization paths. Evidence: The 150fe8cd focused run passed admit-all, typed-check, idempotent replay and local-only observation integration tests.
13. RS256 relay read/propose scopes remain non-owner and cannot confer write/auth. Added /v1/core/propose: server-minted Proposed tool-output claims only, no client ids/approval/authority rows. Signed-token HTTP tests are included. Evidence: The copied-source server run passed both signed RS256 relay propose/grant and real HTTP Proposed-only admission tests. The generated OpenAPI fixture was committed and passed the 30c741db no-update contract run.
14. Production contradiction detection receives stored prior heads. Merge/Accumulate/Escalate and terminal judge failure route to the existing judge or durable conflict.open candidates. Focused fixtures supplied; validation retained below. Evidence: The copied-source full run passed `prior_head_judge_routes_merge_accumulate_escalate_and_down`.
15. Memory upsert and Dreamer promotion share transactional deferred closure. Proposed replacement does not close its prior; bound approval/Auto settles the operation. A mechanical raw supersede primitive remains distinct. Featureless f0034488 passed the upsert single-cardinality, multi-cardinality, checked promotion and bound critical-closure tests. Proposed state precedes settlement in those fixtures.
16. Trusted manifests must agree on strict single_valued_predicates. Fast-path requires one prior and non-weaker incoming source. The list participates in the frontier hash. Evidence: The copied-source full run passed manifest trust fast-path, unanimity and frontier-change tests.
17. Attributed/critical destructive proposals retain Proposed status and coaching/conflict evidence. Critical closure needs the named confirmation, not ordinary inbox approval. Featureless f0034488 passed `checked_promotion_cannot_override_attributed_proposal_hold` and `critical_supersession_closes_only_after_bound_clear_with_companion`.
18. Messages, summaries and relationshipClaims have distinct scope/capability pairs and fail-closed codecs. A reserved test fixture id was corrected after the current run reported it. Evidence: The copied-source codec tests and 150fe8cd witness-backed message/summary grant test passed.
19. expires_at and Expired status deny inactive grants across consumers. Expiry/revocation fixtures are included. Evidence: Record expiry tests and 150fe8cd witness-backed retrieval expiry passed.
20. Durable PENDING/APPROVED/DENIED requests and authenticated responses create active grants atomically. Owner proof is revalidated in the writer transaction. Evidence: The copied-source full run passed `request_approve_and_deny_are_durable_terminal_and_atomic`.
21. Non-owner ScopedRead actors use stored membership plus live active grants. Query/context-pack project grantedData/accessLimited without withheld ids. The shared final retrieval predicate applies relationship checks to nonclaim rows as well. Evidence: The 150fe8cd run passed witness grant filtering and the delegated summary board fixture with an explicit summaries grant.
22. Critical cheap-lever demotions park action-bound operations until human Clear; expiry/decline leave original truth intact. Normal demotion mechanics remain. Evidence: The copied-source full run passed critical demotion hold/clear and expiry/decline invariants.
23. Runner supersession writes a companion citing both ids and mirroring source taint. Local and replicated admission share structural validation. `tainted_head_clean_candidate_folds_taint` passed at 820e1703: both refs, real replicated round-trip, and forged-mirror rejection before persistence.
24. Owner-authenticated foreign introductions cap confirmed scope by the introducer chain. Claim/effect/witness gates consume the independent cap; widening needs a normal Allow. Removed a patch reference to the retired/nonexistent CONNECTION type; foreign status is explicit, never inferred from product-band bytes. Evidence: The copied-source run passed `foreign_door_clamps_live_claim_gate_and_widen_requires_allow`.
25. Added scripts/evals/memory_tags.py: 72 paired prompts, six wrappers, two/three-model runner, hash-only request receipts and Wilson intervals. Fixture dry-run passed. Live observations remain unavailable: no raw model endpoint is configured. Agent-session wrappers are not a substitute for clean independent model requests. No statistical result is claimed.
26. Memory rows carry stored ClaimSource and render distinct trust labels beside engine-issued refs. Featureless f0034488 passed the engine-owned label/fence and authorized-snapshot render fixtures.
27. Foreign worlds render last, index-only, in attributed non-consolidatable evidence fences outside first-party memory framing. Featureless f0034488 passed `labels_and_foreign_fence_are_engine_owned` and the `no_claim_value_changes_board_structure` property test.
28. The server renders shared cross-world pinned/snippet/index tiers. Pins derive from stored trusted manifest criticality and explicit refs, independent of query selection; actor and audience limits still apply. Pinned floors are never silently shed (ARCH-0067 explicitly exempts them from budget shedding). Board tests passed at 820e1703, including snapshot-consistent rendering and denied audience pins. The expanded actor-ceiling/suppression test passed at 30c741db.
29. Mandatory typed receipts cover scoped search/filter/hydrate and graph/timeline reads. Receipt construction shares the filtering snapshot. Narrowed and un-narrowed doors, nonclaim graph ceilings, missing-versus-withheld counts, and automatic pin suppression passed in the 150fe8cd, 6245a2c6, and 30c741db focused runs.
30. Core and HTTP query/hydrate/batch/context-pack responses carry narrowing receipts and re-plan axes; query/context-pack also carry access projections. Generated OpenAPI/success fixtures are committed. All 12 final contract/fixture checks passed without update mode at 30c741db; the bench non-widening re-plan test passed in the earlier copied-source crate run.
31. Write wall detects BIP39-shaped mnemonic runs, sensitive env/password assignments and generic/provider keys recursively before batch staging. Exact mnemonic run lengths avoid false positives from repetitive safe text. Runtime evidence: batch secret-shape and rejection tests passed in the 168-test batch run.
32. Shipped secret source blocklist, compiled list and CI superset check. Python superset and Rust deny fixtures passed. Runtime evidence: batch blocklist fixtures and the policy-superset script passed.
33. Shared recursive serve-time redaction covers core memory/read, server projections and code-run values. Nested safe/secret fixtures added; validation retained below. Evidence: Native MessagePack/JSON batch tests and the copied-source server credential redaction tests passed.
34. Whole-vault entity enumeration uses recursive serialization nulling and secrets_nulled manifest. Custody and tainted exhaust are nulled. This is not a full edge/index restore format. Nested taint reads reuse the transaction. Runtime evidence: legacy export nulling and its secrets_nulled manifest test passed.
35. Local POLICY_MANIFEST content receives a durable origin stamp; replay can only narrow existing source-trust slots. Missing stamps are untrusted. Built-in seed is explicitly stamped; no implicit missing-tag exemption. Two manifest-authenticity tests passed on manifest-test5. Runtime evidence: restrict-only source-row tests passed. Featureless f0034488 also passed all trust-boundary and manifest-authenticity fixtures, including their repaired receipt expectations.
36. Legacy product-band permits are inert until explicit owner re-authoring on POLICY_MANIFEST. Restrictive replay is visible and content-bound quarantine can remove it from the fold. Migration and scan-order tests also passed in featureless f0034488. Runtime evidence: legacy product-band reauthoring and quarantine fixtures passed.
37. Entity-local asset/text-root/section/block fingerprints use the docs pipeline segments and only BOM/CRLF normalization. Unchanged sections/blocks skip lower hashing and derivation. Direct text writes invalidate the tree; purge removes it. Re-ingest, case-change, transport-change and purge fixtures are included. Runtime evidence: docs fingerprint changed-block, stable-id and case-edit fixtures passed again in featureless f0034488.
38. Human-named owner policy rows produce Hold, a local hash-only moderation queue, a system-voiced notice to user_and_model and a queue-linked receipt. No wait on the human and no plaintext in the queue. Later authenticated resolution records a ruling but never auto-sends content. Evidence: The copied-source full run passed `human_named_match_holds_queues_and_notifies_both_readers_without_plaintext`.
39. Configured GitWire checkouts use lease-bound door remotes; server GET/upload/receive-pack recheck stored leases and authenticated principals. Inherited secret-bearing Git configuration is refused. Host must select the actual served endpoint. Stock-Git core/server fixtures supplied; validation retained below. Evidence: The copied-source full run passed both actual stock-Git checkout/push tests, including the real HTTP lease route.
40. Signed authority operations mint, spend and revoke door slips. One-shot consumption is durable, authority-device-bound and fails closed; no door-local spend ledger. Actual quorum rules remain. Codec/fold tests passed in the completed core runs. Evidence: The copied-source full run passed authority-backed single-use mint/redeem, TTL and log-unavailable refusals.
41. Closed verb-class registry replaces expanded lists. Missing verb produces ASK bound to normal consent; scope/floor/identity failures cannot ask around denial. Pact scope is re-read and intersected. Runtime evidence: verb-class and consent/pact unit fixtures passed.
42. Removed the fixed burst breaker, trip rows/receipts/dial/pending projection and retired federation pause configuration. Native outcomes are allow/defer/proposed, not a human rate stop. Runtime evidence: repeated allowed writes and unknown retired-dial tests passed; the repaired repeated-id receipt fixture also passed in featureless f0034488.
43. Native automatic-check candidates carry normalized rate and structural streak; federation observations train only completed baseline buckets. Featureless f0034488 passed `native_checker_observes_structural_streak_outage_neutrality_and_success_reset` and both normalized burst-input tests. The real checker sees the streak, neutral outages do not inflate it, and success resets it.
44. Anonymous sessions are immutable Discard routes with no durable entities/claims/telemetry/receipts. Audit-required effects and witness writes refuse rather than report fake persistence. External provider/host logs are outside the engine contract. Evidence: The copied-source full run passed anonymous entity/witness/telemetry/close and audited-effect refusal tests.

## Final acceptance summary

- **Runtime:** the core/server/bench all-feature Cargo run recorded 8,960 passes,
  50 failures and 21 ignored tests at c9f95561. Every one of those 50 failures is
  accounted for by a later passing case, including contract-driven fixture renames.
  The original failed result is preserved; it is not relabeled as a passing command.
- **Core featureless:** 6,564 passes plus one stale PPR fixture failure at f0034488;
  the corrected fixture passed at ef69fb77. Four ignored cases remain ignored.
  This covers 6,565 nonignored library cases without repeating unchanged green work.
- **Benchmark:** 285 main-binary cases passed on Arch at f3b6a725, including all
  eval cases that cannot run on macOS. The isolated provenance fixture passed at
  27800b0f. All 10 unchanged materializer integration cases passed in the earlier
  broad run. No remaining observed benchmark failure is being waived.
- **HTTP/contracts:** the final no-update run at 30c741db passed all 12 selected
  read, manifest-pin, peer-confirmation and schema/snapshot cases. The generated
  OpenAPI/success fixtures are committed. Earlier 108/109 board/provenance passes
  were retained; the sole failed peer clock fixture is one of the corrected 12.
- **Compile/lint:** workspace all-target/all-feature clippy and core featureless
  all-target clippy passed at f0034488. Production-only server clippy passed at
  27800b0f after gating a fixture reexport to its actual test/test-support consumers.
  Later test-only edits have successful native compile/runtime evidence.
- **Documentation:** workspace all-feature rustdoc with warnings denied passed on Arch
  at `1e74dfcc6` (2m29s). Commits `1fb0167c` and `1e74dfcc6` corrected a moved
  type link and private-helper links without changing visibility or behavior.
- **Structure:** code map, 701-name root surface and structural ratchet passed.
  All 334 changed Rust files pass rustfmt check. Secret-blocklist superset and
  memory-tag evaluation fixture tests passed. The final retired-breaker source audit
  found no production matches; only the unknown-retired-dial regression names it.
  No new process-global state or
  root-surface expansion was introduced.
- **Remaining:** only the nonblocking raw-model observations for step 25. There
  is no configured raw endpoint in this seat. No live results, model comparison,
  confidence conclusion or significance claim has been fabricated. The fixture
  runner is ready for a host with two or three raw model endpoints.

Source hashes, commands, exit-state receipts and raw output are retained under
`/home/lexi/w7-build/tickets/W7-C06/validation/`. The reconciliation index is
`prior-50-failures-reconciliation.json`. This ticket did not rerun the coordinator's
entire nine-stage `scripts/verify.sh` after every fixture edit. Its existing green
results are reused only where the relevant source/behavior did not change.

## Historical validation evidence and boundaries

The entries below preserve the repair sequence. Pending statements describe that
earlier point in time; the final acceptance summary above is the current status.

- `.w7/secret-native-tests.log`: **168 batch tests passed** after native MessagePack
  credential filtering. This is scoped evidence, not an integrated verdict.
- `.w7/focused-runtime2.log`: **491 tests ran, 479 passed and 12 failed**. Exact failures
  are retained in `.w7/runtime-failures.md`. Repairs are committed in `c1ae3555`:
  witness-backed message grants; booking confirmation time; contradiction indexes and
  manifest fixture; Proposed-first attributed holds; foreign widen input; deterministic
  receipt order; current frontier hash and typed denial fixtures. Their broad rerun is recorded below; remaining failures are being repaired.
- `.w7/recovered-clippy.log` and its receipt retain the first recovered final-source
  Clippy failure: 11 core lints. `e4fcad95` fixes those without changing admission behavior.
- Code-map, structural ratchet, root-surface pin, whitespace and secret-blocklist superset
  checks passed. Tag-eval fixture tests passed (two tests); no live model result is claimed.
- All-target/all-feature Clippy for core, server and bench passed on MacBook against
  `cc809308` (5m44s). `.w7/clippy-final-source.log` and its receipt retain the result;
  every recorded input SHA-256 still matched after host sync-back.
- Full `cargo test` for core/server/bench (`--all-features --no-fail-fast`) completed on
  the copied `c9f95561` MacBook source. The durable receipt and complete output are
  `.w7/crate-tests-c9f95561-receipt.json` and `.w7/crate-tests-c9f95561.log`.
  Core library: 7067 passed, seven failed, four ignored. Eight targets failed overall,
  including the seven documented macOS-only bench failures. This is not a pass.
- `fb6adc6d` honors explicit stale grants in final reads/traversal. `8e47a3ee` repairs
  the post-confirmation reference, native streak fixture and off-record refusal code.
  `6f602c65` adds graph/timeline receipts and closes type/numeric traversal bypasses.
  `e413d214` replaces obsolete authority rate-block fixtures with admit-all/typed-check
  assertions and separates bound federation traffic from own-device replay.
- Server publication fixtures now confirm deferred revisions and preserve stable ids.
  Peer-answer policy fixtures use the stamped maintenance path, not a raw database edit.
  The legacy edge endpoint retains its array body and carries the mandatory structured
  receipt in `x-oneiron-read-receipt`, matching the existing raw-entity transport.
- Workspace all-target/all-feature check found two remaining graph-response ownership
  errors in server callers; both are repaired. Its remote input hashes are retained
  under `validation/20260919T075012Z-workspace-check-remote-sources.json`; source changed
  during queue wait, so the admission-time commit alone is not its tested snapshot.
- Final core/server/bench acceptance, featureless/server-production lanes,
  strict rustdoc and response/OpenAPI snapshots remain to be accepted on final source.
  Earlier read-only all-target checks and manifest-only passes predate this integration
  and are not substituted for those gates.

## Canon/code corrections

- Historical high-water marks cannot be compared against every prior entry during each
  fold. Immutable admission observations preserve historical validity while refusing new rollback.
- Selector-fetch requests are not CRDT row-rematerialization markers. Deferral stores real
  request bytes and has a retry/completion protocol; automatic replay is not a renamed Pause.
- Missing manifest stamps are never trusted. The built-in seed and local raw test fixtures
  explicitly record local authorship. Exact replay echoes retain authority only while the
  same locally authored row still occupies its slot.
- A human's actor ceiling is not a human-confirmed foreign grant. Introduction authority
  is explicit and separate. A legacy product CONNECTION kind is not a current engine type.
- The merge judge already existed; the production gap was empty prior-head input and
  missing durable conflict markers. No second judge was invented.
- Default shared membership is stored data, not runtime preset policy. Personal/community
  default ordinary additions to Viewer; family/team/org to Member. Owner is explicit.
- Equal-holder newest-wins uses logical ledger order, not a caller's wall-clock timestamp.
- The base export format had no whole-vault restorable serializer. Entity enumeration and
  nulling are implemented; the result does not claim to preserve edge/index restoration.
- Anonymous means no bound engine persistence. It cannot promise deletion of provider logs.
- The separate ONE-1548 dependency was not a usable moderation primitive in this checkout.
  Human-row parsing, hash-only queue and Hold enforcement were added on the existing policy plane.
- Live tag-name research is nonblocking in its own contract, but remains an explicitly unrun
  acceptance item here. No invented model scores or significance conclusion is included.

- Witness MESSAGE relationship scope comes from typed fields in its authenticated metadata,
  not from prose or a fabricated raw message write. Ambiguous duplicate axes fail closed.
- Credential filtering runs on native MessagePack before lossy JSON conversion. Raw serving
  retains safe binary/ext types; export nulls secrets and records that fact in its manifest.
- Booking confirmation uses commit time; the publication's historic validity interval is a
  separate read-time condition. Attributed revisions still wait for owner confirmation.
- Structural failure streaks follow durable receipt append order, including same-clock-tick
  receipts. A random UUID ordering must not move a later success before an earlier refusal.

## Validation custody

All implementation is in this worktree. The owner reconciled the old child and queued build
handles on 2026-09-19; no old admission owns source or validation. All diagnostic workers have been retired; none owns source or builds.
The integrating seat owns the final source and the retained validation evidence.

Active validation uses a named transient user job that invokes the **unchanged normal Cargo
wrapper** from this worktree. Logs, source SHA-256 manifests and exit-state receipts live in
`/home/lexi/w7-build/tickets/W7-C06/validation` outside host sync while commands run. Completed
evidence is copied into `.w7/`. The authoritative active job is recorded in `validation/active.json` outside host sync;
a stale PID or a missing process is never a pass. No installations, permanent service units,
shared host configuration, host-routing overrides, duplicate builds or build timeouts were added.

## Historical runtime repair decisions

- The full copied-source run recorded 8960 passes, 50 failures and 21 ignored tests.
  Failure names and exact diagnostics remain in `validation/runtime-failure-details.txt`.
- Current app/selector socket fixtures negotiate protocol 9, not the feature-introduction
  threshold 8. Version 7's hello remains distinct from the authenticated v9 selector lane;
  no unauthenticated selector compatibility bypass was restored.
- Deleted non-claim timeline anchors retain their typed deletion metadata under the type
  ceiling. They do not become content-search hits. Erased claims and relationship-scoped
  message/summary shells still cannot prove read authority and remain withheld.
- A missing relationship on MESSAGE/SUMMARY is not proof of public content. Keep this
  fail-closed rule; the board-slot fixture now declares its summary's space and grants
  `summaries` to its principal. Disclosure alone is not an access grant.
- Adapter parity pins scoring time through the established maximum learned-at fixture,
  not a relaxed float comparison. Both adapters retain NOT_FOUND for hidden/missing refs;
  mandatory receipts correctly distinguish withheld count 1 from unresolved count 0.
- The first scoped repair run selected Arch through the normal wrapper (no override).
  Input hashes matched `921c0f88`; compilation found two remaining claim-test consumers
  of the old bare edge result. No tests ran. Those consumers and a test-only import are
  now repaired. Remaining acceptance still needs actual runtime and lint results.

- `runtime-repairs2` on MacBook at `150fe8cd`: **176 run, 173 passed, three failed**.
  The three failures were a missing typed checker-reason mapping, a reserved test id,
  and the old peer-supersession assumption. `9847f45c` repairs them without bypassing
  confirmation. The active rerun and schema generation are not yet a pass.
- `d70584ff` removes an unguarded board claim reread after authorization and applies
  audience disclosure to pinned references. New tests pin snapshot-consistent refs and
  snippets, and prove that pins bypass relevance but not disclosure. Runtime pending.

- ARCH-0067's safety-pin obligation is now wired to trusted manifest criticality,
  not only caller-supplied references. The scoped pin door reads policy, claim
  admission and engine refs in one snapshot, with a receipt. Query scope cannot
  hide an authorized critical claim. The board still applies audience disclosure
  and never silently drops pinned floors to meet a render cap. New runtime proof
  is pending; this is not covered by the older renderer tests.

- The four-test MacBook rerun matched all tracked final-source input hashes at
  `6245a2c6`: checker streak and graph receipts passed. The peer correction test
  still counted coaching as if every queue row were a proposed fact. It now
  checks the exact correction's approval and closed consent, while keeping
  coaching/provenance annotations distinct. The provenance regression also
  now replays the companion through replicated batch admission, checks both
  cited ids, and refuses a forged taint mirror before persistence.
- The snapshot-generation flag was passed to Cargo before the external nextest
  subcommand and was not applied to the test environment. Move the config to
  `nextest run` arguments. The failed comparison is not a generated fixture or
  a successful contract check; regenerate and then rerun without update mode.

- MacBook focused runtime at `820e1703`: **109 run, 108 passed, one failed**.
  Board fencing/projection/snapshot consistency, scope-independent safety pins,
  audience disclosure, and real replicated companion taint all passed. The sole
  failure was the peer fixture using wall-clock approval before its fixed future
  claim timestamp; it now uses the existing explicit-time approval door.
  All source hashes matched admission except the two generated contract fixtures.
  The generated OpenAPI and success snapshots are collected and reviewed; error
  fixtures and security schemes are unchanged. A non-update contract run is next.
- `e02deb6d` extends the safety-pin test with a denied actor ceiling and mandatory
  suppressed-count receipt. The preceding run did not include that assertion.

- Final non-update contract run at `30c741db`: **12 passed, zero failed**.
  MacBook source hashes match every admitted tracked input. This closes the last
  peer fixture failure and validates the expanded automatic-pin ceiling assertion.
  Workspace lint and featureless/production gates remain outstanding.

- Workspace all-target/all-feature clippy and featureless core all-target clippy
  passed at `f0034488`, with source-matched receipts. The full featureless library
  run completed: **6,564 passed, one failed, four ignored**. The sole failure was
  an old PPR fixture relying on ungranted nonclaim reads. Its restricted reader
  now has an explicit NOTE/CODE_SYMBOL grant while CLAIM bridges remain denied.
  No production boundary was relaxed. Only that changed fixture needs a rerun;
  the 6,564 unchanged successful cases remain valid evidence.

## Historical validation continuation

- The f0034488 full featureless library command finished with 6,564 passes, one
  stale-fixture failure and four ignored tests. The fixture depended on the removed
  ungranted nonclaim bypass. Commit ef69fb77 gives its restricted reader explicit
  NOTE/CODE_SYMBOL access, still excluding CLAIM. Its exact featureless rerun
  passed (1/1); both runs have matching source hashes. This is combined evidence,
  not a claim that the earlier failing command exited successfully.
- Workspace all-target/all-feature clippy and core featureless all-target clippy
  passed at f0034488. Production-only server clippy later found a fixture-only
  reexport visible without test-support. Commit f3b6a725 gates that import to its
  actual consumers, without disabling tests. Its production recheck remains due.
- A fresh whole `oneiron-bench` crate run is active through the normal wrapper,
  which selected Arch. This can cover the seven macOS UnsupportedPlatform cases.
  No pass is claimed until that run completes. Strict workspace rustdoc and final
  validation closure remain pending. Step 25 still has no live model observations.

- The new benchmark command completed on Arch: **285 passed, one failed**
  (694.52s runtime). All eval cases passed, including the seven unsupported on
  macOS, and both long beam sweeps passed. The lone failure was an isolated
  provenance fixture inheriting the enclosing real checkout through TMPDIR. Its
  fixture now installs an empty `.git` boundary at its temporary root, so the
  missing-build case can test the executable fallback without ambient checkout
  authority. Production provenance resolution is unchanged. Exact rerun follows.

## Test repair: macOS secret-file fixtures

- The factory crate run failed 11 secret lease/snapshot cases because macOS's default
  temporary directory traverses the `/var` symlink. T2 correctly refused it.
- Create these fixtures under the canonical temporary root. Keep production's
  no-symlink policy unchanged, and assert that the deliberate symlink-parent case
  reports the planted parent rather than an unrelated host ancestor.
- No canon correction: this is test isolation from host path aliases.

## Test repair: descriptor-bound benchmark opens

- Eval's production existing-only open is Linux-only and deliberately returns
  `VaultRootPreflight::UnsupportedPlatform` on macOS. Do not substitute a
  create-capable open or enable unsupported filesystem behavior to pass tests.
- Keep all seven cases active. Success scenarios now require the typed platform
  refusal and unchanged outcomes/weights on macOS, while retaining the Linux
  mutation assertions. The command parser, open path, and exit-code mapper run
  normally on both hosts. No ignore attributes or feature gates were added.
- The empty-index wrong-dictionary test captures the initial doctor identity
  and compares the durable data file before/after the refused command. It no
  longer uses an unsupported strict reopen just to inspect the fixture, nor a
  create-capable reopen that could conceal an incorrect manifest rewrite.
- No canon change. Linux-only descriptor binding remains the storage contract;
  these tests now assert its existing macOS refusal instead of assuming support.

## Test repair validation (2026-09-19)

- Commits `c0b85039` and `05161b59` fix the host-dependent test assumptions.
  Production code and security boundaries are unchanged.
- MacBook, per-ticket `target/`, at most three compiler jobs and three test
  processes; final test-source SHA-256 hashes match the committed local inputs.
- `cargo nextest run -p oneiron -p oneiron-bench --all-features
  -E 'test(secret_) | test(eval::tests)' --test-threads 3`: **172 passed,
  zero failed** (145 core cases, 27 benchmark cases). Both crates compiled.
  All 18 failures from the factory run have explicit passing cases in this log,
  including all seven eval cases. The stronger symlinked-parent refusal also passed.
- Default profile, no retries. The filter skipped 7,962 unrelated cases. Earlier
  green evidence for unaffected tests is reused; this is not a new full-gate pass.
- Changed-file formatting, `git diff --check`, and the code-map pin passed.
- Durable log: `validation/20260919T122759Z-fix-tests.log`; exit status 0 and
  source hashes: `validation/fix-tests-active.json` under the W7-C06 ticket directory.
  The initial detached command is not used as pass evidence.

## Review repair: live foreign-world MEMORIES (G1)

- Valid finding: CLAIM hydration replaced its validated binary `world` with JSON
  null. The HTTP board projects from that snapshot with edges off by default,
  so its foreign-world fence and index-only tier never saw the world identity.
- Project the validated world as a canonical hex string in the hydrated fields.
  The final scoped snapshot check uses the same projection. Do not re-read a
  claim during board projection or make provenance depend on optional edges.
- The live regression creates stored claims, retrieves them with default
  edge-free hydration, and checks home-before-guest rows, guest index-only/no
  snippet, and the separate non-consolidatable guest evidence fence. Both
  result and neighbor projection lanes are exercised.
- Canon correction: none. This closes an implementation gap in the existing
  foreign-world contract, not a new wire compatibility obligation.
- Validation is recorded below after the focused native run completes.

## Review repair: peer-relative burst acceptance (G2, G3)

- Valid coverage gap: comparing rate ratios did not prove the verdict boundary.
  The existing same-burst test now asserts Defer for the quiet peer and Allow
  for both the busy peer and the grown vault. The growth fixture adds 100 rows
  so the same ten-write burst crosses the size-relative equality boundary.
- Added `scripts/check-federation-burst.py` and its three fixture tests. The audit
  rejects const/static data declarations throughout the federation burst module
  and its shared normalizer, including new child files and renamed limits.
  It ignores comments/strings, permits const functions, and fails on missing
  required sources. Transport payload bounds remain separate from rate policy.
- The explicit const-audit acceptance is a tooling source-policy test, separate
  from Rust behavioral tests. Run it with:
  `python3 -m unittest discover -s scripts/tests -p test_federation_burst_audit.py -v`.
  All three cases passed, including rejection fixtures for absolute counts,
  fixed pauses, renamed statics, and associated constants.
- Canon correction: none; no production burst threshold changed.

## Review collection and validation custody (2026-09-19)

- Reviewed the complete existing internal verdicts at
  `fb02cda5e9b36ad8078e138157812c3e710d0455`: the recovered Opus review was
  LANDABLE; Grok's three blocking findings are G1-G3 above. All three were
  accepted and fixed; no internal blocking finding was skipped.
- GitHub REST lookups before and after fixes returned no PR for `w7/W7-C06`
  (including closed PRs). There are therefore no own-PR Qodo, Codex, or
  CodeRabbit comments/reviews/threads to triage or a PR on which to post the
  explanation. The supplied PR933 packet was consumed read-only; its head is
  `w7/W7-C08`, so its findings and posting target are inapplicable here.
- Raw PR lookups, internal verdicts, deduplicated ledger, source hashes, logs,
  and native exit receipts are retained in
  `/home/lexi/w7-build/tickets/W7-C06/validation/review-fixes-20260919T151350Z/`.
- Passed so far: changed-file pinned rustfmt, code-map pin (2554 files, zero
  generated changes), structural ratchet, whitespace check, and all three
  Python const-audit tests. Production code changed only in CLAIM hydration.
- Native validation is queued through the unchanged normal Cargo wrapper,
  using its per-ticket serialization lock and bounded host slots:
  - `w7-c06-review-focused-tests-152542`: core all-feature context-pack,
    MEMORIES, and federation-burst nextest cases, three test threads.
  - `w7-c06-review-core-clippy-152813`: core all-target/all-feature clippy with
    warnings denied.
  - `w7-c06-review-featureless-world-152813`: the new live-world regression
    as a featureless library test.
- At this checkpoint the MacBook and Arch slots are occupied by other tickets
  and the mini is below its 30 GiB admission reserve. C06 has not reached Cargo
  compilation. No new Rust pass or full-gate pass is claimed. The durable
  jobs remain queued; inspect their receipts before completing this repair.
  The earlier factory 9014-test pass is unchanged-source historical evidence,
  not validation of these new bytes. The interrupted formatter attempt is
  retained as interrupted, not substituted for a pass.

## Review repair validation complete (2026-09-19)

The productive-wait checkpoint above is historical. Both remaining native jobs
completed successfully; the root consumed their real exit receipts and logs.

- `cargo nextest run -p oneiron --all-features -E
  'test(context_pack::) | test(context_board::memories_) | test(sync::federation_burst::)'
  --test-threads 3`: **96 passed, zero failed**, 7768 skipped. This includes
  the live edge-free world fence and quiet/busy/grown-vault verdict assertions.
- `cargo test -p oneiron --lib --no-default-features
  live_memories_keep_foreign_world_fences_without_edges -- --test-threads=3`:
  **1 passed, zero failed**, 6587 filtered out. The new base-mode test runs
  featureless; it was not feature-gated away.
- `cargo clippy -p oneiron --all-targets --all-features -- -D warnings`:
  **exit 0**. The unchanged vendored heed warnings remain dependency warnings;
  no lint allowance or vendor edit was added.
- All three commands ran on **Arch selected by the normal wrapper**, using
  the worktree's `target/`, at most three compiler jobs and three test threads.
  Every changed Rust source SHA-256 in all three admission receipts still
  matches the final source. Log hashes and systemd exit status 0 are retained
  in the receipt directory above.
- The three const-audit fixture tests, audit CLI, changed-file formatting,
  code-map pin, structural ratchet, and whitespace check also passed.
- Prior green factory evidence is preserved, not fabricated or cleared. These
  scoped checks validate the review repair; they are not a new full nine-stage
  `scripts/verify.sh` verdict. Remaining factory review/full-gate/publication
  stages retain their existing obligations.

Review disposition: G1-G3 are fixed. The original internal non-defects were
rechecked against current code: I1 widening requires matching frontier plus
normal-gate Allow under revalidated owner authority; I2 imported-mention
admission is a public consumer library door; I3 ScopedReadResult already has
its must_use receipt obligation, so the extra compile-fail fixture remains an
informational test-shape request. No blocking finding was silently dismissed.
The final GitHub REST lookup again found **no PR for `w7/W7-C06`**. Therefore
no public comment was posted; posting on PR933 would be cross-ticket noise.
The complete explanation is ready in `github-explanation.md` beside the raw
receipts, and this committed summary rides the eventual C06 PR.
