# W7-C06 implementation notes

Status: implementation committed in concern-sized changes; final validation is pending. This is not a shipping receipt.

## Canon and decisions

The read-only docs mirror and `.w7/CONTEXT.md` supply the contract. Main pages:
ARCH-0068 repo custody (`/oneiron/code-in-oneiron/oneiron-arch-0068-repo-custody-v1/`),
ARCH-0055 entity identities, ARCH-0024 resolution waterfall, ARCH-0023b federation,
ARCH-0043 shared-vault authority, identity §§2.6/5.4, Dreamer v2 contradiction/write
closure, ARCH-0006a/ARCH-0044 access, ARCH-0040 rendered provenance and foreign agents,
ARCH-0067 memory board, DEC-0005 manifest/read ceilings, ARCH-0038 write wall,
ARCH-0032 fingerprints, ARCH-0047 policy checks, and ARCH-0052 anonymous sessions.

## Step ledger

1. Docs export/registry source registered. Thin-star ASSET/ASSET_TEXT ingestion consumes one exact owner approval atomically. Optional injection classification is a separate derived annotation. Tests added; runtime pending.
2. Pipeline semantic segments produce derived SUMMARY nodes with source envelopes. Scoped summary-first retrieval returns references; explicit expansion carries a read receipt. Rank fusion and consent fixtures added; runtime pending.
3. Per-kind name/alias/phonetic/contact hints are indexed at the common body and deletion doors. Same-key lookup returns all candidates and never merges. Unit fixture added; runtime pending.
4. Mention intake looks up hints before scoring. Prepared candidates are revalidated under the same writer transaction as waterfall scoring, provisional mint and imported claim admission. Re-mention, distinct-pair and rollback fixtures added after the current full-test build; they need a new run.
5. New ids use monotonic thread-local ULID entropy and Crockford text. Existing raw/hex UUIDv7 ids stay opaque. Reopen/hydrate legacy-row fixture added after the current full-test build; runtime pending.
6. Authenticated principal/grant/vault-bound observations persist normalized peer rate and structural streak. Vault size changes the relative decision. Quiet/busy/reopen fixtures supplied; runtime pending.
7. Selector and inbound updates use durable defer with actual bytes, authorization context and automatic replay. Pending bytes stay outside live entities. A coalesced advisory review projection groups them per peer; no human pause. New review projection needs its focused assertion; replay fixtures pending.
8. One-shot shared-membership initialization stores preset choice, explicit grant rows and default policy. No preset adds no implicit roles. Customized policy cannot be overwritten. Five preset/reopen fixtures added; runtime pending.
9. Authenticated active Admin/Owner grants append retained rulings with previous-holder/value receipts. A persisted logical ledger order, then opaque id, selects the live ruling; live pact state is checked. Fold/permutation fixtures added; latest logical-order change needs runtime validation.
10. Durable per-signer maxima and immutable entry-observation receipts protect replay across reopen without invalidating all historical entries. Reopen/rollback fixtures supplied; runtime pending.
11. Named duration-v1 observation policy controls a clock-free local stale-roster window. The explicit local setter changes subsequent folds; signed entry timestamps do not supply freshness. Window boundary fixtures supplied; runtime pending.
12. The authority replay door persists per-peer counts and raises one typed advisory check above its configured threshold. It never refuses for rate. Former authority quota debits were removed from both materialization paths. Runtime pending.
13. RS256 relay read/propose scopes remain non-owner and cannot confer write/auth. Added /v1/core/propose: server-minted Proposed tool-output claims only, no client ids/approval/authority rows. Signed-token HTTP tests added; server compile/runtime and OpenAPI snapshot pending.
14. Production contradiction detection receives stored prior heads. Merge/Accumulate/Escalate and terminal judge failure route to the existing judge or durable conflict.open candidates. Focused fixtures supplied; runtime pending.
15. Memory upsert and Dreamer promotion share transactional deferred closure. Proposed replacement does not close its prior; bound approval/Auto settles the operation. A mechanical raw supersede primitive remains distinct. Runtime pending.
16. Trusted manifests must agree on strict single_valued_predicates. Fast-path requires one prior and non-weaker incoming source. The list participates in the frontier hash. Runtime pending.
17. Attributed/critical destructive proposals retain Proposed status and coaching/conflict evidence. Critical closure needs the named confirmation, not ordinary inbox approval. Runtime pending.
18. Messages, summaries and relationshipClaims have distinct scope/capability pairs and fail-closed codecs. A reserved test fixture id was corrected after the current run reported it. Runtime pending.
19. expires_at and Expired status deny inactive grants across consumers. Expiry/revocation fixtures added; full-suite failures are being inspected, not accepted.
20. Durable PENDING/APPROVED/DENIED requests and authenticated responses create active grants atomically. Owner proof is revalidated in the writer transaction. Runtime pending.
21. Non-owner ScopedRead actors use stored membership plus live active grants. Query/context-pack project grantedData/accessLimited without withheld ids. Current suite exposed a non-claim search-filter bypass; the shared final retrieval predicate now applies the relationship check. That repair needs rerun.
22. Critical cheap-lever demotions park action-bound operations until human Clear; expiry/decline leave original truth intact. Normal demotion mechanics remain. Runtime pending.
23. Runner supersession writes a companion citing both ids and mirroring source taint. The common structural validator checks its mirror on local and replicated rows. Runtime pending.
24. Owner-authenticated foreign introductions cap confirmed scope by the introducer chain. Claim/effect/witness gates consume the independent cap; widening needs a normal Allow. Removed a patch reference to the retired/nonexistent CONNECTION type; foreign status is explicit, never inferred from product-band bytes. Runtime pending.
25. Added scripts/evals/memory_tags.py: 72 paired prompts, six wrappers, two/three-model runner, hash-only request receipts and Wilson intervals. Fixture dry-run passed. Live observations remain unavailable: no raw model endpoint is configured and nested prime-agent CLI fails with an owned-session channel error. No statistical result is claimed.
26. Memory rows carry stored ClaimSource and render distinct trust labels beside engine-issued refs. Label fixtures added; runtime pending.
27. Foreign worlds render last, index-only, in attributed non-consolidatable evidence fences outside first-party memory framing. Structure-escaping property fixture added; runtime pending.
28. Shared cross-world rendering sheds snippets/index rows before pinned rows. The server projects the frame. Stored pin selection and the all-pins-over-budget case still need final acceptance review.
29. Mandatory typed receipts cover scoped search/filter/hydrate. Filter and context-pack receipt construction now shares the filtering snapshot. Final suppression-count accuracy and missing-versus-withheld hydrate cases remain under review.
30. Core and HTTP query/hydrate/batch/context-pack responses carry narrowing receipts and re-plan axes; query/context-pack also carry access projections. OpenAPI/success snapshots and the bench re-plan case need final validation.
31. Write wall detects BIP39-shaped mnemonic runs, sensitive env/password assignments and generic/provider keys recursively before batch staging. Exact mnemonic run lengths avoid false positives from repetitive safe text. Runtime pending.
32. Shipped secret source blocklist, compiled list and CI superset check. Python check passed; Rust deny fixture pending.
33. Shared recursive serve-time redaction covers core memory/read, server projections and code-run values. Nested safe/secret fixtures added; runtime pending.
34. Whole-vault entity enumeration uses recursive serialization nulling and secrets_nulled manifest. Custody and tainted exhaust are nulled. This is not a full edge/index restore format. Nested taint reads reuse the transaction. Runtime pending.
35. Local POLICY_MANIFEST content receives a durable origin stamp; replay can only narrow existing source-trust slots. Missing stamps are untrusted. Built-in seed is explicitly stamped; no implicit missing-tag exemption. Two manifest-authenticity tests passed on manifest-test5.
36. Legacy product-band permits are inert until explicit owner re-authoring on POLICY_MANIFEST. Restrictive replay is visible and content-bound quarantine can remove it from the fold. Migration and scan-order tests passed on manifest-test5; later changes need final integrated compile.
37. Entity-local asset/text-root/section/block fingerprints use the docs pipeline segments and only BOM/CRLF normalization. Unchanged sections/blocks skip lower hashing and derivation. Direct text writes invalidate the tree; purge removes it. Re-ingest, case-change, transport-change and purge fixture added; runtime pending.
38. Human-named owner policy rows produce Hold, a local hash-only moderation queue, a system-voiced notice to user_and_model and a queue-linked receipt. No wait on the human and no plaintext in the queue. Later authenticated resolution records a ruling but never auto-sends content. Runtime pending.
39. Configured GitWire checkouts use lease-bound door remotes; server GET/upload/receive-pack recheck stored leases and authenticated principals. Inherited secret-bearing Git configuration is refused. Host must select the actual served endpoint. Stock-Git core/server fixtures supplied; runtime pending.
40. Signed authority operations mint, spend and revoke door slips. One-shot consumption is durable, authority-device-bound and fails closed; no door-local spend ledger. Actual quorum rules remain. Codec/fold tests observed passing in the in-progress core run; complete suite pending.
41. Closed verb-class registry replaces expanded lists. Missing verb produces ASK bound to normal consent; scope/floor/identity failures cannot ask around denial. Pact scope is re-read and intersected. Runtime pending.
42. Removed the fixed burst breaker, trip rows/receipts/dial/pending projection and retired federation pause configuration. Native outcomes are allow/defer/proposed, not a human rate stop. Runtime pending.
43. Native automatic-check candidates carry normalized rate and structural streak; federation observations train only completed baseline buckets. Upstream failure-streak propagation still needs final end-to-end acceptance review.
44. Anonymous sessions are immutable Discard routes with no durable entities/claims/telemetry/receipts. Audit-required effects and witness writes refuse rather than report fake persistence. External provider/host logs are outside the engine contract. Runtime pending.

## Validation evidence and boundaries

- Cargo uses the installed factory host/slot wrapper. No local override, global installation,
  shared Cargo configuration, or other worktree was used. Test parallelism is two threads.
- `.w7/read-check.log`: core/server all-target sync,test-hooks check passed on an earlier
  read-authority snapshot. It does not validate later docs, manifests, membership or patches.
- `.w7/manifest-test5.log`: core library sync,test-hooks build succeeded; both manifest
  authenticity tests passed. Later edits include atomic mention admission, fingerprint
  membership cleanup, human-hold/fingerprint tests, proposal route and read-filter repair.
- `.w7/core-integrated-test.log`: full core `cargo test -p oneiron --features sync,test-hooks`
  ended without a terminal summary. `.w7/core-integrated-source.json` records source hashes at launch; local source
  changed while the host wrapper was queueing, so final changed scopes must be revalidated.
  The first two observed failures were grant cases. One used reserved id 0xA2 (fixed to
  0x52). The other exposed missing relationship admission in final non-claim filtering
  (fixed at the common retrieval predicate). Neither repair has a runtime pass yet.
- The wrapper retried a failing Mac mini rsync and later acquired an Arch slot itself.
  No host configuration was edited or build routing overridden.
- `scripts/ratchet/check.sh` and `scripts/ratchet/root-surface-check.sh` passed before final
  formatting. Code-map regeneration, final fmt, featureless/server checks and server tests
  remain. No full-gate verdict is claimed.
- `python3 scripts/check-secret-blocklist.py` passed. Tag-eval fixture generation passed;
  live model evaluation has not run and the artifact explicitly says fixtures-only.

## Canon/code corrections

- Historical high-water marks cannot be compared against every prior entry during each
  fold. Immutable admission observations preserve historical validity while refusing new rollback.
- Selector-fetch requests are not CRDT row-rematerialization markers. Deferral stores real
  request bytes and has a retry/completion protocol; automatic replay is not a renamed Pause.
- Missing manifest stamps are never trusted. The built-in seed and local raw test fixtures
  explicitly record local authorship. Exact replay echoes retain authority only while the
  same locally authored row still occupies its slot.
- A human's actor ceiling is not a human-confirmed foreign grant. Introduction authority
  is explicit and separate. A legacy product CONNECTION kind is not a current engine type.
- The merge judge already existed; the production gap was empty prior-head input and
  missing durable conflict markers. No second judge was invented.
- Default shared membership is stored data, not runtime preset policy. Personal/community
  default ordinary additions to Viewer; family/team/org to Member. Owner is explicit.
- Equal-holder newest-wins uses logical ledger order, not a caller's wall-clock timestamp.
- The base export format had no whole-vault restorable serializer. Entity enumeration and
  nulling are implemented; the result does not claim to preserve edge/index restoration.
- Anonymous means no bound engine persistence. It cannot promise deletion of provider logs.
- The separate ONE-1548 dependency was not a usable moderation primitive in this checkout.
  Human-row parsing, hash-only queue and Hold enforcement were added on the existing policy plane.
- Live tag-name research is nonblocking in its own contract, but remains an explicitly unrun
  acceptance item here. No invented model scores or significance conclusion is included.

## Integration custody

All burst, authority, anonymous, federation, contradiction and credential patch artifacts
were applied in this worktree. Completed synthesis workers were collected and retired.
The integrating seat owns all remaining source fixes, tests, maps, snapshots and commits.

## Latest integration (validation pending)

- Raw operator-authored manifest fixtures now stamp the local write origin; intentionally
  unauthenticated/replay fixtures remain untrusted.
- Booking revisions remain Proposed and invisible until the live Human owner confirms
  through the shared deferred closure. Both publication slots get transaction-local proof;
  the proof is removed before commit. Agent confirmation is refused.
- Companion HTTP grant creation accepts expiry and responses derive expired status. The
  companion revoke route refuses other grant kinds instead of panicking after mutation.
- Binary credential-shaped byte arrays are also redacted/nullified recursively.
- Blob-artifact births now use the same four-rung cache as docs ingest. Exact binary bytes
  deduplicate; no text is invented for a binary payload. UTF-8 uses the pipeline segmenter.
  Fingerprint persistence passes the write-wall, and artifact purge drops the tree.
- Explicit memory pin references are hydrated through ScopedRead. Pins bypass query relevance
  and render shedding, not read authority. Their receipts are exposed beside board responses.
  Non-claim body fields cannot manufacture a ClaimSource or a foreign-world provenance label.
- The final read-receipt integration retains prefilter suppression counts, refreshes authority
  without widening, hydrates whole pages in one snapshot, returns receipts on 404 hydration,
  and narrows the old byte-only point helper to internal use. Code-run, server, benchmark and
  contract snapshots were updated. The point methods live in `scoped_read/point_reads.rs`.
- These changes postdate the running core test binary. They are not covered by that older run.
  Formatting and code-map generation passed on the preceding integration, followed by a
  green ratchet check. New compile repairs still need formatting and compile validation.
  The old full-test process ended without a summary; failed-case reruns and server tests remain.

### Compile repair follow-up

The first final-source check found one calendar consumer that needed the explicit read
receipt value. Clippy then found that embedding a whole narrowing receipt in a failure
inflated the root error type. The receipt is now boxed at the new error variants instead
of weakening size lints across the engine. Other repairs remove unfulfilled lint
expectations and keep the common put function under its line limit. The final test
results are still pending; the 48 failures reported by the old partial run are recorded
in `.w7/failed-pre-final.json` for focused reruns.

The Mac mini had ENOSPC. A package-scoped clean ran through the normal wrapper; that
wrapper selected this ticket's Arch target and removed its oneiron artifacts. The next
normal compile ran on the MacBook. No host-selection override or shared-cache purge ran.

## Validation lifecycle issue

This client-owned seat did not retain its asynchronous Cargo work across continuations.
Queued commands 1029471 and 1055481 disappeared without terminal results; the earlier
full core run also stopped before its summary. No result is inferred from an empty log.
The attempted independent validation session failed with the exact runtime refusal:
`rlm.create_session requires a daemon-backed depth-0 session`.

Required handoff: resume this existing branch in a daemon-backed writer session with a
stable Cargo completion loop. Do not reset the worktree or reapply `.w7` patches. Start
with final-source core/server/bench compilation; fix remaining diagnostics, then rerun the
48 earlier failing cases, the new acceptance fixtures, touched-crate tests, featureless
and production-server checks, and the required final gate. Source patches are integrated,
but their final acceptance is NOT established. Live tag-name model evaluation is also
unrun and needs an explicit endpoint; it is a separate nonblocking research item in canon.

`cargo clean -p oneiron` was the only cleanup. It used normal wrapper routing and removed
this ticket's own Arch package artifacts, not another ticket's target or a runner cache.
No build host override, factory mutation, host restart or dependency installation occurred.

Latest validation retry: the requested three-crate `cargo test` has now been admitted on
MacBook through the normal wrapper and is compiling. Output is `.w7/validation-tests.log`.
This admission is progress, not a completed test result. Final formatting, code-map check,
diff whitespace check and ratchet check passed on the committed implementation snapshot.

Final-source test compile attempts reached all three touched crates on MacBook. The first
found a private schema import and a legacy raw read still expecting a bare Option. Those
are fixed, with same-snapshot raw projection, credential filtering and a receipt header on
both success and not-found. The second found a benchmark fixture using private `gate` rather
than the already-public `RetrievalFilter` export; that import is fixed. The next test attempt
is `.w7/validation-tests3.log`. There is still no final runtime or Clippy pass.

Correction after the completion event: `.w7/validation-tests3.log` did return a terminal
result, so the earlier claim that this seat cannot retain any asynchronous validation was
too broad. The concrete failure was a read-parity test match that constrained
`VaultReadError::Engine` to `narrowing: None`, leaving the new receipted case uncovered.
The parity taxonomy now matches both while keeping receipt assertions at their own tests.
Continue normal validation; a daemon handoff is not established as a necessary blocker.
