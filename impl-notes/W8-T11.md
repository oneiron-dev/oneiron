# W8-T11 / ONE-2576

- Extracted the live, unrevoked `ROLE_OWNER` roster predicate for both `require_host` and the fold’s slip arm. The fold rejects a non-owner primary before `SlipAuthorityState::apply`; the write-side conflicted-root and quarantined-fork checks remain unchanged.
- Kept cosigner processing and non-slip arms unchanged. Reused the actor-class constants at the two named sites.
- Canon: read-only `generated/oneiron/identity.md` lines 97, 104, 110, 143. The canon is not stale for this behavior; no update is needed. Its owner-through-host rule now holds when the fold handles replicated slip ops. The docs mirror has no ONE-2576 registry row yet; add that row in the docs workflow, not in this engine PR.
