# W7-C14 implementation notes

## Status

In progress. No whole-cluster completion or verification claim.

## Per-step ledger

### Step 1

Implemented typed table-driven search/execute/ask, actor-scoped query/contextPack plans, and lazy one-call builders. Thin-client tests passed (20); Rust parity awaits integration. Execute is a bounded typed read program, not an arbitrary-code sandbox.

### Step 2

Span handles now bind exact body-version hashes and Unicode-scalar ranges to engine-issued Loro cursors through the single resolver. Eight tests written, native compile repair in progress. The current cursor document is deterministically reconstructed from the pinned body; no stored historical Loro document is claimed.

### Step 3

Pointer-only quote triples bind message, revision and scalar range; strict decoding rejects text copies and missing fields. Tests written, awaiting Rust validation.

### Step 4

Implemented pending tests: actor-scoped vault-lens projection emits ledger, dossier, scrubber, bounded graph and witnessed thread sections using the closed atom kit. Limits are checked before querying; nodes are deduplicated and hidden graph targets do not render. Existing admission remains conjunctive.

### Step 5

Implemented pending tests: on-this-day action subtracts one calendar year in UTC (Feb 29 clamps to Feb 28), reruns the actor-scoped bitemporal ledger read, and changes the scrubber only after a successful projection. Historical browsing does not reactivate closed claims or bypass current consent.

### Step 6

Implemented pending tests: LensMountRegistry always mounts Vault/Admin and re-reads a pack SKILL lifecycle for every render. Pack removal and non-Active states hide the lens.

### Step 7

Implemented pending tests: lens intents live in replicated ASSET_TEXT records. Contract mismatch reruns from that stored prompt through regenerate_lens; changed reads become a pending candidate; failures keep the last-good mount.

### Step 8

Implemented pending tests: production request_artifact_publish dispatches artifact.publish through OF-327. Exact per-artifact grants replace caller-asserted booleans. Share-style receipts project from the durable outbound receipt. Direct pointer writer is test/test-hooks only.

### Step 9

Implemented five-axis ScopePosition/ScopeCeiling, strict codecs, bottom for unknown/missing/revoked clearances, and owner-signed exact-value widening. No unsigned grant fallback. Rust tests written; compile validation pending.

### Step 10

One roster resolver branches to TOP before the empty fold and treats unknown present parties as bottom. Public disclosure is a separate union branch; a product-lattice join would incorrectly erase world/facet restrictions. Tests cover owner, disjoint and mixed rosters; pending execution.

### Step 11

Scope conjunct applies to assembled memories and transcripts. Vault-bound generation snapshots gate every released chunk and reject roster/clearance changes. Signed roster confirmation is required to remove parties; silence stays restrictive. Host model loops must use this release door. Tests written, pending execution.

### Step 12

Implemented reaction kind107, immutable body plus retained soft tombstone, atomic toggles and outbound enqueue, mirrored external-ID dedupe, batched pills, actor inbox, context signals, and native routes. Explicit ParticipatesIn membership-time visibility fails closed. Replay/rematerialization rebuilds the derived inbox. T1–T8 and surface-event tests written; native compilation is being repaired.

### Step 13

Implemented: explicit reaction-vocabulary discovery; bridge tapbacks and in-app Unicode react verbs. LINE Messaging API and Messages for Business stay typed unsupported because their configured providers have no reaction-send door. Focused reaction-capability test passed.

### Step 14

Implemented, tests in progress: connect_request OutboundExecutionSink, optional note, fresh before/after connection-state observations, durable consume-before-send guard, provider receipt fields. Seat caps/cadence now cover connect requests as well as DMs.

### Step 15

Implemented, tests in progress: production container-command runtime, digest-pinned/no-pull images, vault-scoped refs, durable one-seat reservation, member 2FA handoff, fail-closed destroy/catalog revoke. Runtime is host-injected; no host install or container launched during this task.

### Step 16

Implemented, tests in progress: MailSend scope/codec/OAuth map, custody scope check, immutable GmailSendMessage, GmailSendWire, human/owner-authorized per-message approval, consume-once send sink behind email.send and per-message receipt. Inbox parser remains unchanged; send-only scopes do not authorize inbox access.

### Step 17

One typed verb table generates engine SDK, napi method census, HTTP routes, WS dispatch and thin-client catalog. remember/forget execute engine-side. Root added five reaction verbs, bringing the current table to61. Rust binding compile/census tests pending; earlier56-row TypeScript tests passed.

### Step 18

Every wire-projectable Memory verb gets a generated HTTP POST route and engine DTO dispatch. Session/callback-only methods cannot be serialized and remain host-only. Route census and embedded/HTTP parity tests written; execution pending.

### Step 19

Excel16.112.4 minted834 cases (833 cached results, one malformed LET formula rejection). The82 cases with text setup were rerun after finding automatic number/boolean coercion; final merge validates stored setup types. Hash-linked privacy-normalized workbook fixtures and cached-value CI are present. Full LibreOffice baseline completed and is being refreshed for typed text. Unchanged formualizer ran, but the first comparison is provisional pending numeric date/time serialization correction. Word/PowerPoint proof still pending.

### Step 20

Added pinned formualizer0.9.3 crate behind the shared docedit recalc trait. Initial13 unit tests passed; corpus census found only the stamp-format mismatch, now repaired. Formula package adapter/property tests in progress. No in-process default enabled while same-corpus parity remains unproved.

### Step 21

Implemented pending Rust tests: OPC, addresses, operations, manifests, inspection, session validation and four-stage round-trip orchestration moved into `oneiron-docedit`, with their pure tests. A `DocumentStore` trait reads verified version snapshots; the engine adapter owns Vault, authorization, provenance and transaction settlement. Dependency-direction fitness test passes. Storage-free locator/reanchor extraction remains to review against the ARTL-2 scope.

### Step 22

Retained Package open/limits/no-op identity and append-new-part path implemented. First-comment DOCX now uses insert without rebuilding untouched ZIP records. Synthetic insertion/identity tests pass. All100 pinned PPTArena inputs downloaded and hash/ZIP verified into ignored scratch only; native corpus identity run pending.

### Step 23

Implemented pending tests: prepare commits to base hash/version, run, output bytes, manifest writes and validation report using domain-separated length-framed hashing. Settlement verifies the commitment inside the write transaction, then appends bytes before the consume-once receipt. Base version is checked in addition to hash, closing an ABA stale-base case. Added substitution, extra/dropped-write, report-change and stale-version tests. Added a vault-owned one-shot seam and subprocess crash test after bytes are staged but before transaction commit. The parent reopens the vault, proves no partial version/receipt, retries, and proves consume-once durability. This new test awaits validation.

### Step 24

Actual reduced Apache-2.0 Stemma source fork now provides typed revision state and the production post-serialization checker. Native tracked insert/delete/replace/comment/paragraph-join writer lowers into shared prepared settlement and anchor replay. Test run reached59 passes plus5 failures caused by fixtures missing final sectPr; fixtures repaired, rerun pending. No Word or POI/docx4j corpus pass claimed.

### Step 25

External-link routing and _xlfn helpers implemented; parser must retain leading = to distinguish formulas from literal text. Actual package adapter remains in progress. Shared preparation and every blob version now bind/store producer engine+version, with explicit unattested-import/session values rather than guessed calculator versions.

### Step 26

Implemented pending tests: registry ArtifactFamilyKind maps code/blob storage bytes into one artifact family. Hosting uses the registry classifier. Unknown bytes have no family. Anchor/provenance projections still need this classifier.

### Step 27

Implemented pending tests: shared published/preview pointer values pin code forks or blob version numbers. The local /a route serves either; direct blob version URLs use an entity id plus ?version=. Repoint/unpublish remain pointer operations; public-tier enabling was not added.

### Step 28

Added the shared birth envelope and both projection directions over `artifact.born_from` claim ledger rows for Code and Blob family kinds. An atomic creation door stores the artifact plus its immutable birth, binds the referenced prompt bytes to the input hash, validates task/skill/ask record kinds, and keeps generated skill reports/candidates Proposed. Tests cover task/run reverse lookup across both kinds, replay/immutability, proposal status, and rollback for missing/substituted references. Not validated yet. Existing producer doors still need migration so every birth, not just the new atomic door, records its envelope; task-facing projection wiring also remains.

## Decisions and canon corrections

- OF-372: provider capability is not inferred from ordinary messaging. LINE and Messages for Business cannot advertise a made-up reaction-send API. Both expose an explicit unsupported vocabulary and the existing typed unsupported-capability error. The local iMessage bridge advertises the six classic tapback glyphs. In-app accepts non-empty strings of at most 64 Unicode scalars.
- OF-373: a successful connect tool return is not proof of delivery. Fresh pending/connected state is the proof. Ambiguous calls retain a durable local guard and cannot resend after restart.
- OF-374: Gmail is a provider for the existing `email` ChannelIdentity, not a second mailbox channel. Its send sink uses OF-327 `email.send`; Google `gmail.send` OAuth maps to `MailSend`. An OAuth token read binding alone cannot grant MailSend: the custody binding must explicitly carry `mail.send` too. Approval is specific to the identity and serialized message hash, never a reusable boolean.
- ARCH-0078 lists stale type bytes (105/110). This tree uses CODE_ARTIFACT=103 and BLOB_ARTIFACT=105. Do not renumber the registry from prose.
- ARCH-0075 oracle fixtures and upstream source pins need resolution before formula-engine measurement/default selection. No full-corpus benchmark score or default-engine decision is claimed. Excel 16.112.4 passed the six-case smoke with hashed cached values, a live/stored recalc canary, and preserved workbook count. The malformed LET fixture is classified formula-rejected, not a cached error golden.
- The Scope and reaction prerequisites described by the cluster are absent on this base: no unified Scope, no CONV-01 records listing/speaker door, and no CONV-05 record visibility function. See local context packets; these are not grounds to invent permissive substitutes.

## Evidence

- Host: MacBook through the provided cargo shim. Target: worktree `target/`. `CARGO_BUILD_JOBS=2`, `RUST_TEST_THREADS=2`.
- Passed `cargo test -p oneiron --lib outbound::tests::reaction_capabilities`: 1 test. Log `.w7/evidence/reaction-capabilities.log`.
- `cargo fmt -p oneiron` applied. Subsequent edits require another format pass.
- LinkedIn compile found a missing Cow borrow, then a missing test BTreeMap import. Both repaired. Gmail approval record initially used non-serde EntityId fields; changed to checked hex refs. Focused outbound test currently running.

- Replacement formatter passed. Current compile found and fixed ASSET_TEXT registry naming, then two old artifact pointer assertions in secret_rotation tests. The generic pinned version replaces the old code-only field. These repairs still need a successful compile.
- The factory wrapper selected Arch for the first replacement compile and MacBook for the second; host claims follow each log, not an assumed route. No direct local Cargo invocation or routing change was made.

- The queued document test reached MacBook but stopped on a duplicate Cargo manifest key introduced while adding dependencies. The duplicate is removed. No Rust pass is claimed; a new formatter is running before the replacement test.

## Current integration evidence (supersedes running/compile status above)

- `cargo test -p oneiron-docedit`: 42 tests passed plus doctests (`.w7/evidence/docedit-test-3.log`). This predates the new ZIP-header hardening and worker edits.
- Current core library compile succeeded on MacBook. Full run: 6488 passed, 14 failed, 4 ignored (`core-current-full.log`). Lens and prepared-commit mutation/ABA tests passed. Eleven failures were the known `/var` symlink TMPDIR refusal, not disclosure or lease-policy changes.
- Artifact tests exposed two independent issues: the capability's unconditional policy-risk hold, and the legacy test vault's absent policy. The hold is removed. The fixture now installs a policy with an Auto actor ceiling but **no** effect grant, so the exact per-artifact grant still supplies authority. The old secret-rotation byte-count assertion is replaced by decoded pointer/version/taint assertions. These final repairs await rerun.
- The factory wrapper does not forward local TMPDIR. Scoped tests use `cargo --config 'env.TMPDIR.value=".w7/tmp"' --config env.TMPDIR.relative=true --config env.TMPDIR.force=true ...` with the worktree-owned `.w7/tmp` directory. A single inline-table `--config` argument is not accepted by Cargo; the three dotted keys are.
- Excel save-as invalidates JXA workbook specifiers and sometimes blocks the JXA save call. Native AppleScript save/close, with name/path checks, fixes this. Timed-out test workbooks were individually closed; no app was killed and no pre-existing workbook was closed. The successful smoke-6 restored the initial workbook count for every case and released the shared lock.
- The full 834-case Excel oracle is running under the shared Office lock; `.w7/office-active.json` pins its command, output location, and input hash. No full result claimed yet. The upstream archive contains 834 cases, not the canon's older 604.
- `test_office_corpus.py`: 2 tests passed on the current Python harness. Stored-value golden CI and unchanged formualizer/LibreOffice same-corpus scores remain pending.
- Separate implementations are in progress for SDK/facade, span/quote handles, Scope admission, reactions, formula engine, and native DOCX. Their files are not validated evidence until integration tests pass.
- No full gate, commit, PR, or completed cluster is claimed at this point. Step 28 now has the ledger primitive and tests, but existing producers and task-facing views still need wiring; the remaining per-step acceptance stays binding.

- Follow-up narrow compilation on Mac mini stopped at two `unreachable_pub` constants in the partial Scope draft before executing tests. The finishing worker owns that repair; this is not an artifact/crash test result.
- LibreOffice 25.8.2.2 smoke: five usable cases recalculate to the Excel values, including FILTER/SEQUENCE/XLOOKUP; malformed LET has no Excel golden and is unscored. The baseline runner removes cached formula/output cells before conversion, checks the 3333 recalc canary, and uses a private LibreOffice profile. The byte-preserving cache scrub has a passing Python regression test; it leaves unknown XML/namespaces untouched.

## Latest integration boundary

- Mac mini link failed with ENOSPC (116 MiB free). No shared cache deleted. This session excludes the mini from candidate build hosts; the factory still selects MacBook slots then Arch overflow.
- Network index timeouts resolved far enough to fetch xml/xmltree with a locked build; dependencies are now cached.
- Current core compile reached reaction/SDK/span integration errors; fixes are in progress. No whole-cluster green or terminal is claimed.

- Current document-organ validation: `cargo test --offline -p oneiron-docedit` passed64 tests plus doctests (`docedit-integrated-5.log`), after final sectPr fixture repairs. This includes real Stemma checks, shared commitment, paragraph joins and retained insertion. Corrected Excel cached-value/type validation and LO cache-scrub tests pass.
