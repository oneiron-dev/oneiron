# W7-C14 implementation notes

## Status

In progress. No whole-cluster completion or verification claim.

## Per-step ledger

### Step 1

Implemented typed table-driven search/execute/ask, actor-scoped query/contextPack plans, and lazy one-call builders. Thin-client tests passed (20). Embedded/remote parity tests passed; the generated HTTP route census passed in the server suite. Execute is a bounded typed read program, not an arbitrary-code sandbox.

### Step 2

Span handles now bind exact body-version hashes and Unicode-scalar ranges to engine-issued Loro cursors through the single resolver. Span tests passed in the repaired featureless core suite. The current cursor document is deterministically reconstructed from the pinned body; no stored historical Loro document is claimed.

### Step 3

Pointer-only quote triples bind message, revision and scalar range; strict decoding rejects text copies and missing fields. Pointer, version and scalar-offset tests passed in the featureless core suite.

### Step 4

Implemented and core-tested: actor-scoped vault-lens projection emits ledger, dossier, scrubber, bounded graph and witnessed thread sections using the closed atom kit. Limits are checked before querying; nodes are deduplicated and hidden graph targets do not render. Existing admission remains conjunctive.

### Step 5

Implemented and core-tested: on-this-day action subtracts one calendar year in UTC (Feb 29 clamps to Feb 28), reruns the actor-scoped bitemporal ledger read, and changes the scrubber only after a successful projection. Historical browsing does not reactivate closed claims or bypass current consent.

### Step 6

Implemented and core-tested: LensMountRegistry always mounts Vault/Admin and re-reads a pack SKILL lifecycle for every render. Pack removal and non-Active states hide the lens.

### Step 7

Implemented and core-tested: lens intents live in replicated ASSET_TEXT records. Contract mismatch reruns from that stored prompt through regenerate_lens; changed reads become a pending candidate; failures keep the last-good mount.

### Step 8

Production request_artifact_publish dispatches artifact.publish through OF-327 with exact per-artifact grants. Integration found that a replayed dispatcher result omits new sink fields. Publication now co-commits a stable share receipt with its pointer; replay reads that receipt without restoring an obsolete pointer. Restart/unpublish/replay regression passed. Direct pointer writer remains test/test-hooks only.

### Step 9

Implemented five-axis ScopePosition/ScopeCeiling, strict codecs, bottom for unknown/missing/revoked clearances, and owner-signed exact-value widening. No unsigned grant fallback. Scope and strict codec tests passed.

### Step 10

One roster resolver branches to TOP before the empty fold and treats unknown present parties as bottom. Public disclosure is a separate union branch; a product-lattice join would incorrectly erase world/facet restrictions. Tests passed for owner, disjoint and mixed rosters. A server-only missing-principal path incorrectly treated an unknown reader as an empty roster; the repair explicitly inserts that unknown party. The 23-test server regression run passed, including the missing-principal, absence-clamp and roster-merging cases.

### Step 11

Scope conjunct applies to assembled memories and transcripts. Vault-bound generation snapshots gate every released chunk and reject roster/clearance changes. Signed roster confirmation is required to remove parties; silence stays restrictive. Host model loops must use this release door. Generation-barrier and context redaction tests passed.

### Step 12

Implemented reaction kind107, immutable body plus retained soft tombstone, atomic toggles and outbound enqueue, mirrored external-ID dedupe, batched pills, actor inbox, context signals, and native routes. Explicit ParticipatesIn membership-time visibility fails closed. Replay/rematerialization rebuilds the derived inbox. T1–T8 and surface-event tests passed.

### Step 13

Implemented: explicit reaction-vocabulary discovery; bridge tapbacks and in-app Unicode react verbs. LINE Messaging API and Messages for Business stay typed unsupported because their configured providers have no reaction-send door. Focused reaction-capability test passed.

### Step 14

Implemented and core-tested: connect_request OutboundExecutionSink, optional note, fresh before/after connection-state observations, durable consume-before-send guard, provider receipt fields. Seat caps/cadence now cover connect requests as well as DMs.

### Step 15

Implemented and core-tested: production container-command runtime, digest-pinned/no-pull images, vault-scoped refs, durable one-seat reservation, member 2FA handoff, fail-closed destroy/catalog revoke. Runtime is host-injected; no host install or container launched during this task.

### Step 16

Implemented and core-tested: MailSend scope/codec/OAuth map, custody scope check, immutable GmailSendMessage, GmailSendWire, human/owner-authorized per-message approval, consume-once send sink behind email.send and per-message receipt. Inbox parser remains unchanged; send-only scopes do not authorize inbox access.

### Step 17

One 63-row typed verb table generates the engine SDK, napi method census, HTTP routes, WS dispatch and thin-client catalog. remember/forget execute engine-side. Current generated-catalog check and thin-client tests pass (20 tests, 92 assertions). Current 63-row TypeScript check passes with the exact 5.7.3 compiler and pinned Bun/Node declarations already in Bun's cache; no dependencies were installed. HTTP/remote test targets compile. Remote tests and the HTTP census/parity tests passed. The WS receipts null/default regression was repaired at the engine DTO boundary; the production HTTP/receipts/defaults regression passed in the 23-test server run.

### Step 18

Every wire-projectable Memory verb gets a generated HTTP POST route and engine DTO dispatch. Session/callback-only methods cannot be serialized and remain host-only. Route census and embedded/HTTP parity tests passed.

### Step 19

Excel 16.112.4 minted 834 cases: 833 cached workbooks and one malformed LET formula rejection. The 82 text-setup cases were rerun to prevent numeric/boolean coercion. Privacy-normalized cached XLSX goldens and stored-value/type CI pass. Corrected LibreOffice baseline is complete. Final unchanged-engine score: formualizer **754/811** vs LibreOffice **753/811**, excluding 22 volatile cases and one rejected formula. Date/time values now use Excel numeric cache representation. The committed comparison is recomputed by offline CI. Native Word insert/delete/comment and native one-slide PowerPoint samples passed; these are not full Office-corpus acceptance.

### Step 20

The measured formualizer source is now an owned, Apache-2.0-selected fork (five checksum-verified package archives, unchanged evaluator bytes). The compatibility-subset result justified an owned fork rather than an evaluator rewrite; the full real-workbook comparison is still pending. Pinned formualizer 0.9.3 now has an explicit opt-in, actual XLSX EditSession adapter: multi-sheet evaluation, typed retained cache patches, date systems, package/property tests, and producer stamps. The compatibility subset clears its LibreOffice threshold, but the required full real-workbook Excel and same-corpus LibreOffice runs are not yet complete. Native default selection therefore remains disabled. The host-owned wrapper remains explicit; unsupported workbook features and external links still use the supplied precision fallback. The path-fork targets passed 17 library, 17 real-XLSX/session/property and two corpus tests. The latest post-lint run again passes all 17 session tests, including prepared settlement and adapter stamping. Workspace lint, featureless/production lint and strict rustdoc also pass. Full-corpus threshold evidence, not crate compilation, remains outstanding.

### Step 21

OPC, addresses, operations, manifests, inspection, session validation and round-trip orchestration live in storage-independent oneiron-docedit. DocumentStore supplies verified base snapshots; the engine adapter owns Vault, authorization, provenance and settlement. Dependency direction test passes. The current organ unit suite passes **70/70**, including retained-header, protected-document and unlocked-settings checks. The unlocked fixture now includes its required section-properties element.

### Step 22

Retained Package open/limits, archive-exact no-op, part insertion and unknown-XML splicing are implemented. All 100 hash-pinned PPTArena archives passed native no-op and edit identity checks. Editing DEFLATE entries now clears compression-specific flags when switching a changed entry to stored mode. Receipt and source manifest are checked in; corpus bytes remain ignored because the dataset has no explicit redistributed license text. The new DEFLATE-header regression passed the latest organ run.

### Step 23

Prepare binds base hash/version, run, output, writes, report and engine stamp with one domain-separated commitment. Settlement checks it in the write transaction and stages bytes before the consume-once receipt. Substitution, extra/dropped-write, report and ABA tests exist. All focused settlement tests passed after the common Scope codec repair, including the subprocess crash test.

### Step 24

Pinned reduced Apache-2.0 Stemma fork and retained Rust tracked insert/delete/replace/comment/join writer are integrated with prepared settlement and anchor replay. Word 16.112.4 opened the native sample with two revisions, one comment and three paragraphs, saved DOCX/PDF, and restored zero documents. A pinned 40-DOCX POI/docx4j/Stemma corpus, native measurement CLI, paragraph-join expectations and strict protection/settings tests are added. Native corpus classification: 40/40 no-op archive identities, 15 edit proposals and 25 explicit refusals/rejections. Word accepted/rejected the native insert/delete sample and the paragraph-join sample with exactly the authored text and paragraph boundaries. Hash-bound receipts and stored-value CI are committed. The same-edit preservation comparison completed: **15/15 native outputs** retain their revision semantics in Word, versus **14/15 LibreOffice roundtrips** of the same edited inputs. The other 25 source fixtures remain explicit refusals/rejections, not false passes; this is not a broad capability-rate claim. The earlier delayed save was safely closed by its verified output path; zero documents and released custody were confirmed.

### Step 25

Actual XLSX adapter preserves unknown parts/XML and routes external links to the existing fallback; it refuses fallback loss of link parts/formulas. _xlfn rewriting is Unicode-preserving and idempotent, with real-XLSX XLOOKUP tests. Engine/version identity crosses preparation and every blob version; unattested imports are explicit. All 17 real-XLSX/session/property tests pass, including XLOOKUP, cross-sheet recalc, typed caches, date systems, external-link preservation, contextual-formula fallback and version stamps.

### Step 26

Implemented and core-tested: registry ArtifactFamilyKind maps code/blob storage bytes into one artifact family. Hosting uses the registry classifier. Unknown bytes have no family. Provenance projections use this classifier. Document-anchor admission now matches the family kind too and refuses code forks or non-artifacts as blob-version locators; the guard passed in `open_thread_rejects_bad_anchor_version`, including code and non-artifact fixtures, as part of the 404-test changed-core run.

### Step 27

Implemented and core-tested: shared published/preview pointer values pin code forks or blob version numbers. The local /a route serves either; direct blob version URLs use an entity id plus ?version=. Repoint/unpublish remain pointer operations; public-tier enabling was not added.

### Step 28

All migrated producer doors now write immutable artifact.born_from ledger envelopes: typed code/blob imports, repository ingest, Memory, calendar, booking, BYOA, skill reports and improver candidates. Inputs are hashed records rather than invented tasks; generated reports/candidates stay Proposed. Raw/replay puts require the birth ledger, with retryable missing dependencies. Both task/run/skill directions are exposed through two generated Memory verbs. Birth-specific tests passed the latest core run. Legacy aggregate fixture counts now include the extra input/claim; the full core library regression run passed.

## Decisions and canon corrections

- OF-372: provider capability is not inferred from ordinary messaging. LINE and Messages for Business cannot advertise a made-up reaction-send API. Both expose an explicit unsupported vocabulary and the existing typed unsupported-capability error. The local iMessage bridge advertises the six classic tapback glyphs. In-app accepts non-empty strings of at most 64 Unicode scalars.
- OF-373: a successful connect tool return is not proof of delivery. Fresh pending/connected state is the proof. Ambiguous calls retain a durable local guard and cannot resend after restart.
- OF-374: Gmail is a provider for the existing `email` ChannelIdentity, not a second mailbox channel. Its send sink uses OF-327 `email.send`; Google `gmail.send` OAuth maps to `MailSend`. An OAuth token read binding alone cannot grant MailSend: the custody binding must explicitly carry `mail.send` too. Approval is specific to the identity and serialized message hash, never a reusable boolean.
- ARCH-0078 lists stale type bytes (105/110). This tree uses CODE_ARTIFACT=103 and BLOB_ARTIFACT=105. Do not renumber the registry from prose.
- ARCH-0075 names an older 604-case spreadsheet corpus; the pinned upstream release has 834. Its compatibility-only unchanged-engine comparison uses native date/time serial values and meets that subset's LibreOffice threshold. This does not satisfy the separately required 15,970-workbook corpus gate. The malformed LET case is a formula rejection, never a fabricated cached error. Native defaults cannot be selected from a smoke test.
- The Scope and reaction prerequisites described by the cluster are absent on this base: no unified Scope, no CONV-01 records listing/speaker door, and no CONV-05 record visibility function. See local context packets; these are not grounds to invent permissive substitutes.

## Current validation evidence

- Featureless core library: **6540 passed, 0 failed, 5 ignored** (`.w7/evidence/core-full-repaired`). Scope codec, reaction admission, publication replay, span/quote and provenance repairs are included. The later anchor-family guard and null/default DTO repair need their targeted rerun.
- Document organ: **70/70** current unit tests passed. Native Office/corpus evidence is recorded above and committed beside the fixtures.
- Remote binding: all eight test targets passed (33 tests total). Server: 743 library tests passed and seven failed; 82 integration tests passed and nine failed; managed/WS suites added 43 passes. Repairs target the unidentified reader, scoped fixtures, reaction kind allocation, additive OpenAPI arm, artifact actor posture, and receipts null/default behavior. The source-count thin-client passthrough assertion was removed: the client runtime tests prove response identity instead.
- Current thin-client tests: **20 tests, 92 assertions passed**. The 63-row generated catalog check and cached TypeScript 5.7.3 typecheck pass. No dependency installation was needed.
- Formula path-fork/napi build reached Linux through the factory's bounded host overflow. Napi could not link off a Node host (`napi_*` symbols), the documented Linux limitation. This is not a source-compile failure or a formula test pass. The next scoped compile excludes napi; native Mac napi validation remains to do.
- Cargo commands are serialized. Runtime TMPDIR is `target/w7-tmp`, not a source-synced scratch directory. The factory's approved wrapper owns host and job admission; no direct local Cargo override is used.
- Full integration compilation, scoped failing-test reruns and final lint/doc checks remain in progress. No whole-cluster gate is claimed.

## Latest integration validation

- Current core/server/formula/Stemma test targets compile together (`acceptance-compile-4.log`). The retained Stemma annotation suite passes 47 tests.
- Artifact publication now has a typed owner approve-once door bound to artifact, channel, pinned version, actor and intent. An exact committed Share receipt can reconcile a Pending outbound row after a crash without resending, re-spending consent, or restoring an obsolete pointer. Runtime regressions are running.
- `DocumentSession` bridges an organ session into the Vault prepare/settle API. The new real-XLSX settlement test checks the engine stamp and consume-once settlement. Current tests are running.
- The native XLSX writer restored an intentionally removed XLOOKUP prefix and poisoned cache. Excel 16.112.4 read the expected 2 and saved it. Receipt, worksheet parts, and the explicit driver-parser/custody recovery are pinned under `spreadsheet-compat/measurements/native-xlookup`.
- The second-corpus acquisition yielded 5,455 unique SpreadsheetBench workbooks. The unchanged engine's saved-cache diagnostic completed: 49,073 of 1,225,018 formula cells match. This is **not fresh Excel truth**. Refused workbook cells remain in the denominator. FUSE initially returned HTTP 504 on both hosts; the API range-download recovery has now verified the complete archive (receipt below). The upstream harness does not publish its regenerated truth files. The full two-corpus release gate remains unmet; no default-engine promotion is claimed.

- Current changed core areas: **404 passed, 1 ignored**, including publish approve-once, crash recovery, anchor family/parser admission, gate policy and outbound replay. Lens regeneration (17) and vault read parity (9) also pass.
- Current formula fork: **17 library, 17 session/property and 2 corpus tests pass**. The native settlement bridge, actual adapter stamp, context-dependent fallback, and Mac-absent function behavior are included.
- Current server integration target: **91/91 pass**. Library: **747 pass, 4 fail, 5 ignored**. Three remaining cases used scoped credentials while claiming owner-grade fixtures; they now use actual owner credentials. The existing unbound-reader red-team test also checks that a supplied cleared contact cannot identify the bearer. The fourth failure is the shared malformed-body error envelope. Engine DTO error text and HTTP parse-before-actor ordering are repaired; a scoped rerun is running.
- Native macOS napi: **23/23 pass** through the factory wrapper. Its earlier Linux-only link failure is superseded by this native result.
- Ratchet counters remain 2 / 98 / 91 / 33; root surface remains 701 names. Current code-map generation and SDK generation checks pass.

- Server repair rerun passes **23/23**, including all four previous failures, generated facade parity, and the new cleared-contact spoof check.
- Core code-memory integration passes **36/36**. The related integration selection passed 102 cases and exposed two stale expectations: the pre-CONV-09 vendored byte census lacked REACTION107, and a saved-query prefix-collision test reused its newly shifted byte109. The census now applies the explicit W7-C14 P23 additive row to the strict comparison while leaving the old docs snapshot and its hash untouched; the collision fixture uses unused byte110. Both fixes await the next scoped run.
- The first workspace clippy failed, but the factory's clippy source-copyback replaced the redirected source-tree log. The replacement run writes under target/w7-checks and uses a config-prefixed, non-fixing invocation. No pass is claimed from the lost log.

## Corpus recovery and lint completion work

The Zenodo **API content URL** works even though the public download URL returned 504:
`https://zenodo.org/api/records/581678/files/fuse.zip/content`.
The published archive size is 9,392,830,118 bytes and MD5 is
`13e955c44f0b77d1c36088c0bbb3366d`. Exact-range resumable acquisition is running;
its final hash is not yet verified. Bulk range files live under the worktree's
`target/`, not in the Cargo source-sync payload.

The fresh real-workbook Excel driver passed three smoke cases, then stopped on a
pre-open foreign-workbook guard. An independently updated host helper staged files
inside Excel's own sandbox to avoid per-file access prompts. The hash-bound
resume refused that changed helper. A separately named, reviewed driver starts a
new measurement identity and preserves staged files on custody failure. The first
90 files of that new run passed; this is progress, not corpus acceptance. The
whole 5,455-workbook SpreadsheetBench run and an isolated-profile LibreOffice
baseline are active. Global Excel settings and unrelated open documents are not
changed. The driver refuses active content before asking Office to open it.

Workspace lint found six document-organ issues, seventeen core issues, three XLSX
adapter issues, and one server closure. They are repaired without new lint
suppressions: boxed proposal variants, moved the publication precheck and artifact
manifest into small functions, and mechanical expression/item-order changes.
Current library checking reached core, organ, formula, remote and server; the
remaining lint/typecheck lanes and post-boxing tests are running. No full gate or
native-default promotion is claimed.

## Owner Office rules v2 (2026-09-19)

Consumed the MacBook `SANDBOX-FIX.md` and canonical
`docx-comparison-3/{receipt.json,RESULTS.md}` without repeating the corpus. The
tracked comparison now matches that canonical receipt: 40/40 no-op archive exact;
15/15 native proposals accepted by Word; 25 refused/gate-rejected unscored;
LibreOffice 14/15 on the same edits. The equation revision loss is in LibreOffice,
not native acceptance.

All committed Office runners stage input, output, PDF and resolved-text paths in
the app-specific container. `Powerpoint` uses the owner-pinned lowercase `p`.
Per-case scripts no longer quit. Timeout/cleanup failures retain only the owned
staging directory and custody lock. Excel separators are bound outside `tell`;
workbook inventories use indexed lookup and include unsaved identities. Only a
verified untouched blank Book1 can be closed; nonblank unsaved/recovered work is
preserved or causes a fail-closed refusal. Batch custody compares identities, not
counts. These revisions are not substituted under any running measurement hash.
The active real-workbook driver already uses sandbox staging, verified-blank
startup handling and exclusive-empty fail-closed custody. Its old `sb-truth-1`
identity is unchanged; the reviewed run is `sb-truth-2` against the verified
`/Volumes/Cinema/w7-build/wt/W7-C14/.w7/corpus-spreadsheetbench/all_data_912_v0.1`.

Validation: 10 Office-tool/stored-measurement tests pass; four real-workbook
measurement tests pass. Fake-app tests exercise all four file drivers, proving
that paths stay staged and that timeout retains the stage and lock. Three updated
AppleScripts compile on the MacBook without running their application commands.
Old revision receipts remain bound to their exact observed script bytes in the
non-executable `docx/word-semantics/observed-script.txt`, not to newer drivers.
Workspace all-targets/all-features clippy now passes. Featureless/server-production
lint, rustdoc, and remaining post-lint runtime checks are still running.

Post-owner-rule validation: workspace clippy (all targets/all features), core
featureless clippy, server-production clippy, and strict workspace rustdoc all
pass. Exact commands are in `impl-notes/W7-C14-validation.json`. Runtime checks
following the last lint repairs are still pending; prior green scoped/runtime
receipts remain evidence for unchanged behavior.

Local free space fell to approximately 12 GB during concurrent wave builds. The
FUSE range worker was stopped without deleting partial ranges. Those owned ranges
are moving to the MacBook worktree's `target/w7-checks/`; every transferred range
must hash-match before the local copy is retired and remote acquisition resumes.
No shared cache was cleared, no Office run was stopped, and no source download was
restarted from zero. Source-archive verification is still outstanding.

The post-lint runtime batch has now passed: 3 conformance tests, all 70 document
organ tests, and all 17 XLSX session tests. The FUSE transfer finished with every
range hash verified; the MacBook acquisition worker resumed those bytes and the
verified local copies were retired. A separate extraction job waits for the full
published-archive hash before it can read corpus members. Fresh SpreadsheetBench
comparison waits for complete Excel and LibreOffice receipts and restored Office
custody; partial counts do not feed the acceptance number.

A further offline custody regression supplies equal before/after workbook counts
but different nonblank-unsaved identities. The file driver refuses and retains
its staged input and lock, as required. This exposed an inaccurate `running`
receipt on terminal validation failure; the driver now records `validation-error`
without changing timeout/provider-error statuses. All four staging/custody tests
pass. No live corpus driver or measurement identity was replaced.

Final static check completed successfully after waiting for a host slot:
`cargo fmt --check`, code-map regeneration/check, structural ratchet, and the
701-name root-surface pin all pass. No source format changes were needed.
Remaining acceptance work is the already-running SpreadsheetBench and FUSE Office
measurement chain, not a failed code or static check. Full-corpus completion is
still not claimed.

## Shipped-adapter real-workbook measurement

The original real-workbook chain measures unchanged upstream formualizer. That
alone does not measure the shipped retained XLSX adapter. Added a separate
`recalc_native` CLI over `FormualizerEngine::recalculate_xlsx` with no precision
fallback, a stamped JSON result, and create-new-only output custody. Its focused
integration test checks actual recalculated bytes, input preservation, refusal to
overwrite results, and refusal rather than fallback for contextual formulas.
Validation is queued through the normal Cargo wrapper. After it passes, the
native adapter will be measured against the same manifest and fresh Excel truth,
separately from the unchanged-upstream baseline. No native default promotion or
full-corpus pass is claimed from the compatibility subset.

The shipped-adapter CLI now passes its focused integration test, owning-crate
all-targets/all-features clippy, and format check. One formatting-only failure was
repaired without repeating the green tests. Its Mac executable is hash-frozen;
separate native-adapter SpreadsheetBench and FUSE measurements are admitted.
Their fresh comparisons wait for complete matching Excel truth. An orchestration
quoting error stopped the first launcher before any measurement began; it was
repaired without changing any Office driver or starting duplicate corpus jobs.

FUSE acquisition is now complete. The 9,392,830,118-byte archive matches the
published MD5 `13e955c44f0b77d1c36088c0bbb3366d`; its SHA-256 is
`4f9126bd83aa27118366b2b16b3b378d3bdb27b27728337b392e48c0ef3d93bb`.
The receipt is pinned under `real-workbooks/fuse-download-receipt.json`. The
already-admitted extraction job is active. This closes source acquisition only,
not formula accuracy, Office acceptance, or default-engine eligibility.

FUSE extraction also completed. Its 249,376 members classify as 10,702 unique
XLSX inputs, 238,673 non-ZIP files, and one ZIP that is not XLSX. The extraction
receipt and compressed exact manifest are pinned with decompressed SHA-256
`f211b1a811d493afe0e82b5faadc6093ec5fefaff7579aa021e2de8cef438404`.
Both formula measurements and the isolated LibreOffice lane have started; the
FUSE Excel lane correctly remains behind the current SpreadsheetBench Office
session. Input classification is complete, but accuracy acceptance is not.

SpreadsheetBench's LibreOffice and first retained-native lanes completed the full
5,455-entry cohort. LibreOffice recalculated 5,449 and refused six. The native
candidate recalculated 2,478 and refused 2,977. Complete rows and identities are
pinned in `real-workbooks/`; the native saved-cache diagnostic is explicitly not
a fresh-Excel score. Fresh comparisons remain waiting for Excel.

The native refusal inventory exposed 53 workbooks with empty inline-string
cells (`t="inlineStr"` without `<is>`). A source workbook confirms styled empty
cells with no payload. The reader now treats the missing payload as blank,
keeps explicit empty string content as text, and still rejects a contradictory
non-empty `<v>` payload. A focused fixture distinguishes those cases through
`ISBLANK` and checks input-part preservation. The owning session suite, clippy,
and formatting check are running. Existing candidate measurements remain
immutable; this repaired candidate needs separate measurement. There is still
no C14 PR at the latest GitHub check; no external review finding was applied.

The unchanged formualizer FUSE lane also completed all 10,702 manifest entries:
327 recalculated and 10375 refused. Its pinned saved-cache diagnostic
matches 3,222 of 5,666,313 formula cells and two of 3,493 scored workbooks. These
numbers are not fresh-Excel accuracy. The complete row set matches the manifest
and its recorded row hash; source refusals have not been removed from the
cell denominator. Fresh comparisons still wait for the existing Excel jobs.

The blank-inline-cell repair passed all 19 session tests, owning-crate clippy,
and formatting. Tests ran through the wrapper's Linux fallback; a stale Mac
CLI was rejected by the new-candidate probe before any measurement admission.
A narrow normal-wrapper CLI build is queued, with the same probe required.

SpreadsheetBench Excel timed out on input
`1aff968ec2eefca4363a38a622ad90e463a0ec98176ffdba71a47f29a86d3901`.
The failed run retained custody. Indexed inventory found only its hash-verified
staged input. It was closed without saving, and a second inventory confirmed
zero workbooks. No foreign workbook was touched and Excel was not restarted.
The original driver, script, and manifest hashes remain unchanged. All 1,364
completed output hashes were checked before resuming the same output folder;
the 20 other prior classifications and the timeout row remain unchanged. The
recovery receipt is pinned. This is not reuse of the old, invalid `sb-truth-1`.
The resumed Excel job and replacement dependent jobs are admitted. No partial
cohort is claimed as fresh-truth acceptance.

A second input timeout occurred after another 29 completed workbooks. A separate
identity-preserving supervisor now wraps the unchanged SpreadsheetBench driver.
It recovers only a recorded timeout whose staged input hash, lock owner, and
indexed workbook identity match. It never kills or restarts Excel, never closes
foreign/recovered work, and stops on changed identity or lack of progress. Raw
rows and driver identity stay unchanged; each recovery archives its custody
receipt. Six dependency-free tests cover owned recovery, foreign/unsaved/recovered
refusal, substitution, late foreign work, output corruption, and a subsequent
owner's lock. The supervised corpus is running; timeouts remain unscored.

The supervisor also supports a new, empty corpus output while refusing an existing
Office owner before starting a driver. Eight focused tests pass. FUSE Excel is
queued behind the supervised SpreadsheetBench run, using a separately frozen
copy of the current indexed-custody scripts and a new output folder. The running
SpreadsheetBench script is not replaced. Fresh-truth comparison controllers now
depend on the supervised jobs, not the failed one-shot attempts.
