# W7-C02 implementation and acceptance ledger

This is implementation evidence, not a full-cluster acceptance claim. The core
APIs and focused fixtures are implemented. Full source-pack restoration and the
native audio acceptance still have the explicit boundaries below.

## Canon

Paths below are relative to the read-only docs mirror's `generated/oneiron/`:

- `agents/oneiron-arch-0027-import-pipeline-v1.md`: export/import and adapter skills.
- `agents/oneiron-arch-0053-skills-agent-self-knowledge-v1.md`: skills, hubs,
  held-out admission, pack manifests and actor attribution.
- `agents/oneiron-arch-0067-context-board-v1.md`: residents, asks, delegation,
  board widening, code-mode composition and the amended project-leader scope.
- `identity/oneiron-arch-0022-identity-world-model-v1.md` and
  `sync/oneiron-arch-0043-federation-membership-v1.md`: identity and authority.
- `gate/oneiron-arch-0072-foreign-actor-admission-grants-v1.md`: foreign publishers.
- `agents/oneiron-arch-0061-voice-loop-v1.md`: transcription and global diarization.
- `agents/oneiron-arch-0062-booking-loop-v1.md`: CC intake and propose/confirm.
- Registry rows OF-335, OF-352, OF-379 and OF-414 define their remaining row-level intent.

## Per-step work and remaining acceptance

| Step | Ticket | Implementation and evidence / remaining work |
|---|---|---|
| 1 | ONE-2058 | Six-section snapshot, five-format dispatch, versioned manifest and atomic JSON import. Stored skill source trees and derived agent folders carry hashes. Existing document and source-bundle tests pass. New conversions now retain exact files, and forks copy/reidentify/hash them atomically; the new native-source round-trip fixture is under validation. Predicate/connector source catalogs and several domain restore adapters do not exist in this base; see export limits below. |
| 2 | ONE-2059 | Serializer-owned recursive credential nulling applies to all five formats, with no opt-out. Opaque binary and credential-bearing source files are nulled, not encoded around inspection. Serializer, document and source-file redaction cases pass. |
| 3 | ONE-2060 | WORKFLOW byte 18 (`wf`), strict codec, ordered AGENT_DEF refs, save/get/CAS update/fork and replay validation. Existing and new agent-definition tests pass. |
| 4 | ONE-2061 | Durable inert wrapper and ordered real leaves, frozen definitions, callback runner, retry-tip tracking and write-once provenance report. Six headless workflow tests pass. Hosts invoke the typed runner/recovery pump; no workflow DSL or fake actor was introduced. |
| 5 | ONE-2062 | Memory authoring verifies local author receipts for edits/retractions, supports own skill save/fork and refuses foreign-head erasure. Daemons need exact owner-delegated actions. Explicit admin overrides carry receipts. Eight authority fixtures pass. |
| 6 | ONE-2063 | Typed conflict-of-interest question creates one private, digest-bound Gate review bundle. Authenticated admin ruling authors a new claim and supersedes, retaining original/proposal rows and evidence. Stale/revoked rulings roll back; exact replay returns one receipt. Authority fixtures pass. |
| 7 | ONE-2064 | Provenance-selected VAD trajectory, goal velocity and engagement produce a stored scalar with idempotent supersession. No disease/severity fields. Scoped fixture passes. |
| 8 | ONE-2065 | Vault/Memory read returns index and numeric mode inputs; missing subject returns explicitly absent neutral 0.5. Scoped fixture passes. |
| 9 | ONE-2069 | Deduplicated severity-ranked findings, Beta confidence and threshold auto-resolution. Triage/tournament fixtures pass. |
| 10 | ONE-2070 | General document/design/CLAIM review fan-out with private critic artifacts and persisted parent verdict. Scoped fixtures pass. |
| 11 | ONE-2071 | Anchored human accept/dismiss outcomes update reliability claims and recurring-finding queries. Automatic agreement never trains its own posterior. Scoped fixtures pass. |
| 12 | ONE-2072 | Owner-bound untouched approvals mine Proposed affirmations with receipt evidence. Healthy skills emit explicit keep/no-op outcomes citing real DEV wins, not invented winning-skill receipts. Scoped MacBook fixtures pass. |
| 13 | ONE-2073 | Authenticated deltas select explicit ban/style/charter/brief proposal families. Unknown deltas retain lexical/content defaults. Style heads are not silently installed. Target fixtures pass. |
| 14 | ONE-2074 | Principal-bound claims and independently authenticated decision receipts; miner/optimizer filter principal and age. Decayed evidence remains auditable but stops influencing reads. Scoped fixtures pass. |
| 15 | ONE-2075 | Child actor assignee on wire, consult admission, evidence/abstention result checks and dangling-ref refusal. A child address names an existing executor, not a new spawn. Scoped fixtures pass. |
| 16 | ONE-2076 | Dreamer consult uses the same typed payload, deadline and landing machinery. Scoped answer/expiry fixtures pass. |
| 17 | ONE-2077 | Live ACTION-scope authority holders resolve under the admission snapshot, delegates first, one consult each. Revoked/unrelated grants do not count. Authority ask fixtures pass. This does not claim resolution for every unrelated authority family. |
| 18 | ONE-2078 | Protected group/member facts and causal first-answer projection record a common winner. Backdated later answers do not win; abstention does not settle; replicated rows converge in either delivery order. Fixtures pass. |
| 19 | ONE-2079 | Ask returns a durable handle without parking. Unknown routes queue with an explanation. Explicit idle wait uses C9 traps, multi-waiter wakeup and restart recovery. Fixtures pass. |
| 20 | ONE-1689 | Persistent resident goal-as-data/inbox/room pointer and callable inbox dispatch; anchored threads link existing DAG messages without changing HEAD or copying the conversation. Proposed comments are not admitted agent replies. Resident/anchor fixtures pass. Automatic application ingress scheduling is not part of the core callback API. |
| 21 | ONE-2080 | Typed code-mode agents.spawn/ask/tasks.wait and replay codec; actor/parent/run come from the bound attempt. The bounded worker and full blind-panel oracle pass with an explicit live owner policy; an Auto-capable definition alone is not authority. |
| 22 | ONE-2081 | Single-agent and whole-workflow widening persist the exact frozen intent. Board approval rechecks all definitions, ancestry and authority; receipt spend, slice override and dispatch co-commit. Wrapper never becomes an authority parent. Widening and rollback fixtures pass. |
| 23 | ONE-2082 | Real bounded pinned Git subtree fetch and closed same-origin HTTP index/file fetch; strict file/frontmatter/hash checks and persisted exact packages. Local Git/static HTTP drift/refusal fixtures pass. No marketplace-specific protocol. |
| 24 | ONE-2083 | Verified/community/untrusted ask surfaces differ, but all require authenticated human consent and reject unknown tiers. All-tier gate fixtures pass. |
| 25 | ONE-2084 | Owner-admitted offer-only foreign publisher; imports stay Candidate until consent plus held-out replay. Same bytes via two hubs produce one entity and two source rows. Activation is protected at materialization/replay. Native owner forks are not falsely treated as imports; imported ancestry stays governed. Fixtures pass. |
| 26 | ONE-2085 | Submitted fork bytes enter shared-base useful-upstream and held-out gates, then atomically activate/supersede with lineage receipts. Two-vault fixtures pass. Membership selection and shared-base routing remain the host's existing responsibility. |
| 27 | ONE-2086 | Company PR path accepts submitted bytes, never a personal Vault handle. Useful-upstream and held-out checks precede merge and lineage. Two-base fixtures pass; the company cannot read personal storage through this API. |
| 28 | ONE-2087 | Dispatch stamps tier-1 dependency index. Attempt-bound skill/actor loaders append ordered revisions. New pack loader returns actual persisted files and stamps exactly once; Candidate refusal leaves no row. Terminal receipt preserves order. Fixtures pass. |
| 29 | ONE-2088 | Bounded terminal task-lane sweep, captured routing facts, rule-default/injectable judge, defect/lapse/win projection and separate applied cursor. Rerun is idempotent. Scoped fixture passes. |
| 30 | ONE-2089 | Bounded CHAT host pump, rotating cursor and per-sitting isolation; claims and job consumption co-commit. Healthy/empty/poison-sitting fixtures pass. |
| 31 | ONE-2090 | Core speech-boundary packing, static glossary, ASR role route, conservative cleanup checks and imported bulk-consent normalization pass synthetic fixtures. Native ffmpeg/Silero/Qwen text ASR ran on MP4. Full artifact, normalizer parity and measured E1 remain blocked by missing aligner/runtime qualification; no success is fabricated. |
| 32 | ONE-2091 | One global exclusive diarization result and source-clock/IoU word-to-speaker alignment pass algorithm fixtures. Installed host lacks community-1/pyannote and forced-alignment weights. No real two-speaker or E3 acceptance claim. |
| 33 | ONE-1828 | Identity-filtered lens composes stamped SurfaceEvents, passport threads, comm.thread_member, send receipts and approval drafts; stable paging follows dedup. Fixtures pass. |
| 34 | ONE-1830 | Atomic named-email CC route/passport/thread join/ProposeConfirm handoff before ACK. Correlation retargeting rolls back; inbound content remains evidence, not instructions. End-to-end fixture passes. |
| 35 | ONE-1833 | Owner-receipted per-space presets and platform capability/risk data preserve room scope. The normal outbound gate now checks exact extra consent; ledger payload and sink carry the resolved presentation. Pending recovery rechecks consent and actor binding. The preset fixtures and full end-to-end send/revoke/recovery fixture pass. |

## Trust and implementation decisions

- SELF-GRANT is local author proof, not a copied actor/source field. Imported or
  raw native rows do not become a caller's authored memory by assertion. Native
  Vault/projector doors remain trusted; app-facing edits use the Memory wrappers.
- Admin authority requires authenticated root ownership or exact named delegation.
  PERSON type, job title, review participation and an Auto descriptor grant none.
  Conflict packets keep refs/digests, not an undeletable second plaintext archive.
- A workflow is inert saved data, not a code-mode language. Only leaf AGENT_DEF
  actors execute. Host external effects still need their normal idempotency key;
  callback recovery is not exactly-once external I/O. Cancellation uses existing
  descendant controls rather than cancelling only the paused wrapper.
- Ask answers are evidence, never consent. No-live-route is a queued status, not a
  blocked run. Authority facts bind actual group membership and answer lineage.
- Hub scoring runs outside the LMDB writer lock, then rechecks exact bytes,
  baseline, policy and publisher authority before commit. No source file grants
  activation. Changed Active hub revisions now return a deduplicated held-out
  proposal; the synchronous unscored sync door cannot overwrite admitted bytes.
  Candidate narrowing and the owner's governance tier still survive sync.
- Shared merge accepts only submitted bytes. It does not open the sender's vault,
  install a process-global scorer or invent federation membership.
- OF-335 does not specify weights/defaults. Equal normalized weights and an
  explicitly missing neutral 0.5 are implementation choices, not clinical labels.
- All new public types stay under owning modules. The 701-name crate-root pin is
  unchanged. Unpublished codecs change in place; no compatibility-only decoder.

## Export limits and canon corrections

The source-folder path is real for persisted packages, not a claim that a
metadata row contains missing files. SKILL.md/scripts retain exact UTF-8 bytes,
file SHA-256 and canonical tree hash. Agent folders derive PACK.md, actual
instructions, inert policy, stored skill hash refs and selected ordinary claims.
AGENT_DEF remains in the evidence ledger; no fictitious AGENT_PACK type is added.

A local birth/fork captures the real parent tree hash in the same transaction.
Export-time lookup of a subsequently edited parent is not historical proof.
Older/sync-born rows without this binding have an explicit omission. Imported
bindings remain untrusted lineage and cannot enable a definition.

Remaining source/restore boundaries:

1. The base has no predicate-pack source catalog/PackByteMap storage. Predicate
   names cannot reconstruct source folders.
2. Compiled ingest adapter registrations are descriptors, not stored connector
   packs. OF-519's connector-pack source/admission foundation is not present.
3. Older/authored/distilled skills without persisted files get
   `skill_source_unavailable`; descriptions are not invented SKILL.md files.
4. Historical/sync-born fork hashes cannot be recovered honestly. Replication of
   the new local birth binding is not implemented.
5. Other maintenance families, reserved claims, witness/owned rows and provenanced
   edges need domain-owned restore adapters. No generic reserved-write/replay/Auto
   bypass is supplied. Closed archive-only hub provenance/configuration/scan rows
   are not restored as authority; local scanning runs over the imported bytes.
6. Redacted/binary source files have no asserted clear-file hash. Partial trees
   cannot import as complete packages. Agent knowledge folders are private owner
   archives, not a permissionless hub publication surface.

ARCH-0027 describes intended pack foundations, not the base's former metadata-only
export. Its statement that TOON calls `toon_format::encode_default` is also stale:
the base already uses its own native tabular encoder. JSON import is ordinary
foreign admission, not an unrestricted byte-faithful owner restore.

## Native audio evidence and exact limits

Tracked proof: `crates/oneiron/tests/fixtures/ingest/native_audio/`. The MP4 hash is
`0401d5626fa0a5e6401da721f62479033358d9b3dc2db6a19c889913d4c0d089`.
The real MacBook run decoded 11,889 ms, produced four full-file Silero spans and
real Qwen text. `transcribe_pack` ran inference, then returned
`ForcedAlignmentUnavailable`. Community-1 returned `Community1Unavailable`.
No timestamped words, speaker identities or successful artifact were invented.
The trace records the exact script/model/environment revision used; subsequent
process-timeout hardening is not retroactively claimed by that trace.

The native script invokes the existing interpreter and absolute cached model
snapshot directly. It never calls the UV script launcher or downloads models.
The Qwen API uses `system_prompt` for immutable glossary data, not `context` or
previous transcript. Process output/lifetime and ffmpeg decoding are bounded.
The manual proof is `scripts/proofs/meeting_audio_native.py`; the Rust producer
probe is `examples/meeting_audio_native_probe.rs`.

Missing: forced-aligner weights, pyannote/community-1 runtime, cleanup backend,
real labelled two-speaker/E3 cohort, E1 comparison and authenticated OF-133 default
selection receipt. Chunk timestamps are not word alignment. Synthetic orchestration
and native text-only inference are not full transcription acceptance.

An earlier `transcribe --help` probe unexpectedly let its inline UV launcher add
53 cached packages despite `UV_NO_SYNC=1 UV_OFFLINE=1`. This was reported. No
further launcher/install invocation occurred; later probes used the existing
interpreter directly. No unauthorized uninstall or shared configuration change
was used to hide that incident.

## Validation ledger

- Older featureless scoped core run: **268 passed, 1 ignored** (state, critics,
  tournament, workflow storage, consults, serializer, optimizer).
- MacBook workflow/preference/inbox scope: **187 passed**.
- MacBook posting/audio algorithms: **18 passed**.
- Pre-final-integration full all-feature core library: **7,078 passed, 1 failed,
  4 ignored**. Only failure was the type-byte fixture; repaired and now passing.
  Cargo stopped before integration tests/doctests. This was not a full crate pass.
- First integrated scope: **140 passed, 5 failed**. Fixed invalid fixture version,
  pinned ids and ask authority; repaired imported-vs-native fork admission and
  changed Active sync into a held-out proposal rather than an overwrite.
- Expanded integrated/export/agent-definition scope: **226 passed, 0 failed**.
  This includes the repaired bundle provenance and explicit lead-panel owner policy.
- Full core all-feature library: **7,067 passed, 59 failed, 4 ignored**.
  Most failures were old direct-import activation fixtures. The others exposed
  changed author/consult refusal ordering, stale byte-75 expectations, the Git
  process boundary, and unresolved replica fork ancestry. Cargo stopped before
  integration tests/doctests. Repairs below are not yet accepted.
- Native protocol self-test: **11 passed**. Native host and proof committed as
  `9cf9d259`; text-only inference is still not full transcription acceptance.
- Follow-up clippy and scoped regression builds stopped with `No space left on
  device` in the mini's ticket-owned target. No shared cleanup or router change
  was made. The router subsequently selected Arch for an all-target type check.
- Rustfmt 1.96, code-map generation (**2,605 files**), structural counters
  **2 / 98 / 91 / 33**, and the **701-name** root pin pass on this repair snapshot.
  Compilation, runtime regression validation and strict linting remain pending.
- No full-crate or full-gate pass is claimed. Logs in `.w7/` are local evidence,
  not tracked release artifacts. Cargo host selection belongs to the factory
  wrapper; source-changing commands do not overlap editing.

## Regression-boundary decisions after the full core run

- Metadata-only imported tier/scan classifications need no Active status.
  Active imported fixtures that exercise runtime or lifecycle behavior use actual
  package import, authenticated human consent and host-scored held-out evidence.
  The native scanner reports Unknown, so such packages keep the unvetted prior.
- Native attribution/miner/reservoir fixtures use owner-authored skills. Their law
  is evidence projection, not marketplace birth. No admission ticket is forged.
- Scan-consult tests use owner-authored carriers with real content-global verdicts;
  hub activation has an additional independent human/held-out gate.
- A replica can rematerialize a native Active fork only after its native ancestry
  resolves. Unknown ancestry cannot prove the absence of an imported ancestor.
  The previous test expected a dangling Active fork to execute. That expectation
  now fails closed; a resolved native ancestor still permits replica birth.
  Imported Active replay does not receive local execution authority from remote
  bytes. A replica-hydration/quarantine policy is a remaining shared-boundary
  concern, not permission to skip the admission guard.
- The same-id retraction race uses an explicitly delegated daemon edit. A foreign
  resident cannot create that race through an unauthorized overwrite any more.
- Code-mode attempts to overwrite an owned claim now encounter the authorship
  refusal first; other generic doors still report ReservedPredicate. Human
  consults with no native route report INVALID_STATE, without any task residue.
- Hub Git uses the single pinned GitWire subprocess constructor. A private scratch
  profile permits only file/HTTP/HTTPS reads, disables hooks/credentials/redirects,
  and preserves time, output, file-size and total-disk limits. It does not widen
  the default GitWire policy or add a public arbitrary-argv entry point.

## W7-C02 source-preserving new skill births (draft integration)

- Conversation conversion now persists the exact `RefinedSkill.files` tree in the same
  transaction as its Candidate record. The canonical hash and `convert-<first16hash>`
  version are still computed from those unmodified files. An exact duplicate still
  returns the existing holder before interpreting a new package.
- `SkillPackageFormat::{Folder, Native}` records source syntax, not provenance or trust.
  Folder requires scalar name/description/version. Native permits absent or partial
  frontmatter and keeps native identity/description/version separate. Capability
  declarations are still parsed from actual source. Native storage rejects envelope
  capabilities that differ from those declarations. No string version is inserted into
  the bytes that generated it.
- A source-bearing fork re-reads the parent and files under its write transaction. It
  copies all companion files, edits SKILL.md name/version, re-declares the stored
  capability sets, recomputes the canonical hash and persists the new package with its
  record and DerivedFrom edge. An already-resident resulting hash is refused before
  writing a second holder. The original body after frontmatter remains exact.
  Existing non-identity frontmatter lines survive. A historical parent without stored
  source still produces an explicitly source-unavailable metadata fork. No description
  is passed off as recovered source.
- Whole-vault bundles carry source_format paired with source_tree. Validators enforce
  pairing, tree hash, syntax and native-field agreement where the syntax promises it.
  Archive reimport parses actual source, forces Imported/Proposed/Candidate via the
  existing importer, runs the native scanner and keeps the activation guard unchanged.
  All serializer credential nulling, reserved-row policy and replica gates are unchanged.
- The unshipped v1 local package codec now stores a one-byte format tag. No compatibility
  decoder is added for development-only bytes. The root export pin is unchanged.

### Optimizer audit boundary

`SkillEditDraft::Edit` (`skill_optimize/brief.rs`) accepts only `desc` and `rationale`.
`run_skill_optimize_bound` (`skill_optimize/job.rs`) stores that exact authored text in
`SkillRecord.desc`. It receives no file tree and no replacement instruction-file payload.
The held-out replay path also scores `desc`. Thus there is no discarded file argument
analogous to conversion. Materializing `SKILL.md` from this metadata would invent a
source representation; copying a parent's instruction files would advertise the old
instructions as the new draft. This draft does neither. A real optimizer source-edit
contract needs a source-aware author brief, edit payload and scorer binding together.
Its metadata-only proposals remain `SkillSourceUnavailable`, as do historical missing
packages. This is an explicit substrate boundary, not synthesized recovery.

### Canon correction

`skill_convert/types.rs` formerly said the host retains source and the engine stores only
record+hash. That comment is now wrong for Step 1 and is corrected here. The canon should
say accepted converted sources are locally persisted atomically, while native metadata
is an envelope over exact bytes, not normalized frontmatter. Fork source identity is a
new hash of copied-and-reidentified bytes, never a copy of the parent hash. Archive source
format is untrusted syntax metadata and carries no consent, approval, scanner verdict or
activation authority.

## Latest validation and remaining checks

- Arch `cargo check -p oneiron --all-targets --all-features` passed. Its one
  unused integration-test import was removed. This check preceded the final
  Native-package source-format integration.
- Arch regression run: **470 passed, 2 failed**. Posting execution/revocation,
  imported admission/reliability, claim-author race and the old full-run failures
  passed. The failures were a second stale byte-75 list and Git falling through
  to an enclosing repository after its own `.git` disappeared.
- The registry list is fixed. GitWire now sets a per-call discovery ceiling;
  the fixture explicitly nests repositories so this law is host-independent.
- MacBook source/fork/export/Git run: **182 passed, 2 failed**. Registry and nested
  Git refusal passed. Exact conversion persistence, rollback and Native-format
  consent admission passed. The new source-only JSON fixture and the hub Git
  error boundary needed repairs; their focused rerun is pending.
- Source-only JSON no longer deletes a witness to remove it from the fixture:
  deletion correctly creates a local redaction-audit row. Actual conversion is
  tested separately. No audit or witness restore gate was weakened.
- Hub Git keeps a separately bounded diagnostic allowance when stdout has a tiny
  limit. It maps internal Git failures back to the hub's typed InvalidSkillBody
  boundary and never exposes child diagnostics.
- Full integration tests, doctests, featureless compilation and strict clippy are
  still outstanding. No full-crate or full-cluster acceptance is claimed.

## Fresh-vault policy import boundary

The exact failing row was the production-seeded POLICY_MANIFEST, not skill
lineage. Fresh `Vault::open` installs it; the older export test helper does not.
Policy bodies now stay visible in the export with the closed
`policy_authority_not_restored` omission. Import never replaces the destination's
live policy. The source/fork fixture checks that its target policy bytes stay
unchanged. This does not authorize replay of other reserved entity families.
The full requested `cargo test -p oneiron --all-features` run is now in progress,
including this repair and the integration tests/doctests that the earlier failed
library run never reached.

Commits recorded from Git: `cc4d33e5` persists portable skill sources behind hub
admission; `08afdc5b` aligns native projection fixtures. The preceding posting
execution commit is `4fbc47d4`.

### Resumed validation custody

The previous full-core command had no surviving process or saved terminal output
when the seat resumed. It is not counted as a pass. The replacement writes a
persistent worktree-local log at `target/w7-validation/core-all-features.log`
(outside source sync). The narrow source/authority review was restarted too.

### Stored source revision boundary and durable validation

The final source review found an accepted-write/failed-load mismatch: a generic
owner metadata revision could change version/description while leaving the
stored package unchanged. The common SKILL materialization preflight now refuses
that mismatch before any write, including the raw and replicated doors. The
existing typed hub-sync transaction remains allowed to replace both halves.
The source/fork fixture asserts typed refusal and unchanged stored rows through
all three ordinary doors. A general owner/optimizer source-edit producer is still
not supplied by the metadata-only draft API; no file edit is fabricated from it.

The factory writer is client-owned: `rlm.create_session` explicitly refused it,
and its child workers and background commands did not survive continuations.
An existing systemd user manager now owns one transient validation job. No service
was installed/restarted and no shared configuration changed. The first transient
invocation omitted routing variables, began local compilation, and was stopped;
that invocation is not validation evidence. The replacement forwards only the
factory Cargo routing/SSH/build knobs and uses the normal wrapper. Its durable
log is `target/w7-validation/core-durable-routed.log`; exact unit/status are saved
in `.w7/session-checkpoint.json`. Full-crate acceptance remains unclaimed while
that job is running.

### Generated archive records

The source review also found that changing a generated SKILL or AGENT_DEF to
Imported without changing its coupled authorship flags makes its codec reject
re-import. The importer now sets the ordinary Imported flag pair along with its
existing Proposed/Candidate or disabled-definition floor. These flags do not
create a local author receipt. The source-only fixture now starts with a
Generated skill, and the five-format agent fixture starts with a Generated
agent; both assert the imported flags and inert state. Their fresh targeted
rerun is required after the current full run, which may have captured the prior
source revision while compiling.

## Final validation queue

The durable core run survived the next writer continuation and finished compiling
on the router-selected Arch slot (three Cargo jobs, two test threads). Its tests
are running; compilation alone is not a runtime pass. Remaining independent
checks are queued through the same factory per-ticket lock, so only one Cargo
build runs for this worktree: changed source/export tests, strict all-feature and
featureless clippy, featureless library tests, core rustdoc, and server-production
clippy. Logs and exact unit names live in `target/w7-validation/queue.json`.
No queued or still-running check counts as green evidence.
