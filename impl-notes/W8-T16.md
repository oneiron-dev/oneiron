# W8-T16 — bootstrap skill seeder

- Content-hash dedup can return an existing skill entity instead of the preferred stable bootstrap id. In that case the seeder retains that entity without promoting or rewriting its record, continues seeding the other embedded skills, and writes `SEED_KEY` after the loop.
- Keep malformed-manifest deferral and stable ids. A newly created embedded Candidate still becomes Active; an earlier import retains its existing lifecycle.
- Canon: `generated/oneiron/agents/oneiron-arch-0053-skills-agent-self-knowledge-v1.md` §6–7 establishes a single skill lifecycle, hub content dedup, and the four shipped skills. `generated/oneiron/decisions/oneiron-dec-0005-policy-residence.md` requires fail-closed malformed policy and reconciliation that does not overwrite present rows. Both pages are in the read-only docs mirror.
- The canon is silent about bootstrap order and the earlier-import id collision on a deferred open. It should state explicitly that a content-identical pre-existing imported entity satisfies that bootstrap item without reactivating it or blocking `Vault::open`; the remaining items seed and the pass marks itself complete.
- Tests import judge under a fresh id while the policy is healthy, install a malformed trusted manifest before the first seeded open, confirm seeding defers, remove the malformed manifest, then reopen and assert success plus byte/lifecycle preservation. The ticket described an import during malformed-policy deferral, but the public hub import is rightly denied by the fail-closed gate (`SourceNotTrustedForAuto { claim_source: "observed" }`). The canon should clarify this ordering constraint rather than suggest bypassing the policy.
