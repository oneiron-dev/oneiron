# W8-T02 — missing-source words and counted decisions

- Decision: retain `MissingSource` and `unmet_sources` as evidence/receipt, but let the existing `Counted` filter decide without a receipt-wide veto. A reached decision with met coverage and no fallback may bind its outcome despite an excluded word.
- Canon: `generated/oneiron/agents/oneiron-arch-0067-context-board-v1.md:230` (read-only docs mirror) describes a typed reducer, separate coverage and decision, and validation of obligations, but is silent on the single-word missing-source case. The ticket cites unavailable `int/packets/P5.task.md:24` as the specific ruling: count qualifying words.
- Suggested canon clarification: a word missing a class-required source remains visible as `MissingSource` and contributes missing sources to the receipt, but is uncounted; it does not veto a decision reached by counted words or an otherwise eligible outcome binding. No stored-field/ABI change.
