# W8-T44 implementation notes

- The effort preset installs a now-anchored temporal channel. Only a host-supplied temporal search or explicit `occurred_range` suppresses query parsing; when the effort anchor is present, a parsed query range replaces its temporal scan bounds and removes the Max-tier learned-time anchor. The filter also uses the parsed range.
- The effort flag remains set after substitution so the independent recency blend keeps the 4187129 rule; a host window still turns recency blending off. Unsupported query temporal phrases fail closed unless a host window has priority. Maintenance-kind exclusion is unchanged.
- Canon `generated/oneiron/retrieval/oneiron-arch-0004-retrieval-graph.md` names temporal as one of five signals and describes effort presets and the constant blend; it is silent on query-parsed ranges. It should state that query temporal hints replace only the effort default, while host windows and explicit occurred ranges win.
