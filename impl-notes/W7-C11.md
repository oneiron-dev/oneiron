# W7-C11 implementation notes

The original 24 implementation steps have behavior evidence recorded below at
source `d1f85d14`. The latest guarded-host recovery is validated at `cf9a9a46`:
the original six-package command passed **9,154 tests on Linux**, with zero
failures, zero filters and 21 unchanged pre-existing ignores. The earlier Mac
9,129-test pass, focused fixture checks and Android native proof remain valid
and are preserved separately. No implementation step remains. This is not a full
`scripts/verify.sh` workspace VERDICT. The Android API36.1 proof is not a claim of
running the CI API35 job. Historical entries below remain chronological.

## Canon and decisions

- NOTE contract: `oneiron/core/oneiron-arch-0032-agentic-notes-v1.md`.
- NOTE remains byte 106 (current v3 registry). The mirror's byte 111 and old code comment's 86 are stale.
- The six v1 kinds plus existing `opinion/take` ship as descriptor data, not enum variants. Extra kinds are namespaced by their registering pack.
- Loro becomes a base dependency: editable documents must work in a featureless embedded vault, not only with transport sync enabled.
- On first edit a birth-text core becomes a document-head core. No stale duplicate markdown remains in the active entity row.
- Local document/fork/head/receipt rows reuse `sync_state` and `vault_meta`; no named database is added.

Other defining canon pages:

- Calendar and streaming: `oneiron/core/oneiron-arch-0002-structural-entities.md`.
- Criticality/conflicts/ranking: `oneiron/core/oneiron-arch-0003-semantic-memory.md`; policy residence: `oneiron/decisions/oneiron-dec-0005-policy-residence.md`.
- Canonical recovery: `oneiron/core/oneiron-arch-0038-deletion-redaction-canonical-snapshot-v1.md`.
- Conversation DAG/summaries: `oneiron/backend/oneiron-arch-0006a-conversations-api-core.md`.
- Embedded deployment: `oneiron/core/oneiron-arch-0019-oneiron-db-v1.md`.
- Ports: `oneiron/backend/oneiron-arch-0005a-backend-ports-integrity.md`.

## Steps

1. **NOTE descriptors** — Six shipped descriptor rows plus namespaced pack kinds; unknown kinds fail at local and replay doors. Round-trip and agent-facade tests passed in the full-profile workspace and narrow-sync runs.
2. **NOTE documents** — Immutable core/head binding, stamped Loro birth on first edit and no document for untouched bridged text. Document-birth tests passed in both full-profile runs.
3. **Anchored edits** — Stable-cursor verbs, base-frontier whole-text diff and isolated declared rewrites. Concurrent/stale edits and wrong-document rollback tests passed in both full-profile runs.
4. **Proposal review** — Actor-bound grant routing, per-fork receipts, merge/head-switch and the five-fork two-land/three-review fixture passed in both full-profile runs.
5. **Source bridge** — Latest ASSET_TEXT selection, DerivedFrom provenance, source preservation and lazy divergence passed in both full-profile runs.
6. **Calendar admission** — Live-origin requirement, legacy dreamer read default, projection and generic/replay guards implemented. Origin tests passed in both full-profile runs; repaired importer integration fixtures passed in the narrow-sync run.
7. **Native EVENTs** — Atomic projector-recorded native origin and rejection of evidence fields. Field-matrix and admission tests passed in both full-profile runs.
8. **Dreamer EVENTs** — Atomic dreamer origin, evidence/frontier requirements and recurrence refusal. Field-matrix and admission tests passed in both full-profile runs.
9. **Origin behavior** — Indexed dreamer invalidation, native survival, imported origin/provenance and drift preservation implemented. Origin tests and repaired ICS/import integration fixtures passed.
10. **Canonical payload** — Carry-list codec, per-document snapshots, head bindings/receipts, recovery envelope and blake3 implemented. Canonical tests passed; the new non-mutating snapshot-read regression passed in the full narrow-sync run.
11. **Canonical rebuild** — Fresh Loro reconstruction, standard forward materialization and strict span exclusion passed in the full narrow-sync run. The API does not claim an unrelated historical sweep.
12. **Recovery ladder** — Healthy/targeted/full tiers, rename quarantine and bounded OverlayLimit passed in the full narrow-sync run.
13. **Conversation DAG** — Reserved structural edges, local HEAD/canonical state, bounded scope/walk/migration and HTTP routes implemented. Core interleaving/fork/migration tests, server routes and edge-byte conformance passed in the workspace run; core tests passed again in the narrow-sync run.
14. **Sub-sessions/summaries** — SpawnedBy, reverse membership, full covers with a 256-edge cap, gated header, drill and engine-authored late reply implemented. Core 300-record/nested-session tests and server routes passed in the workspace run; core tests passed again in the narrow-sync run.
15. **Pack criticality** — Manifest resolution, critical-first ordering and narrowing-only override preserve status and world/facet gates. Tight-budget tests passed in both full-profile runs.
16. **Critical overflow** — Warning/count with hard item/group/whole budgets passed in both full-profile runs. Server DTO contract snapshots, including the new stats fields, passed in the 96-test consumer repair run.
17. **Android** — JNI/Kotlin adapter and arm64 NDK workflow implemented; the native target build and Android-emulator open/put/get/reopen instrumentation passed. The initial target build exposed the missing Android plugin filename cfg in pinned Sudachi. Commit `ca4d011d` retains the exact dependency version and adds that branch in an audited local snapshot; the native target retry passed. Rust1.96.0 `aarch64-linux-android`, NDK27.2.12479018 and the API26 linker built the minSdk26 library. Kotlin `connectedCheck` then passed `org.oneiron.EmbeddedVaultTest.openPutGetReopen` on the owner-authorized API36.1 ARM64 emulator (Android16/API36), using the identical library. This is Android JNI runtime evidence, not host-JVM smoke and not execution of the CI-pinned API35 job. The owned emulator exited successfully after artifact collection.
18. **Persistent conflicts** — Stable conflict.open and explicit conflict.resolved through the promotion writer; rerun idempotence passed in the full-profile workspace and narrow-sync runs.
19. **Prior-head/topic conflicts** — Prior heads enter conflict detection; extractor-supplied per-predicate topic keys partition conflict sets. Prior-head clashes, grouping and stable marker tests passed in both full-profile runs.
20. **Source ranking** — Pack multipliers default to no observed-over-stated ranking across predicate families. Default ordering and explicit override tests passed in both full-profile runs.
21. **Entity/claim/edge/place ports** — Transaction-composable traits, LMDB/memory adapters and caller migration implemented. Conformance and typed-read regressions passed in both full-profile runs. The production caller census finds no data-access bypass outside declared adapters.
22. **Retrieval/deletion ports** — Retrieval/short-id/tombstone/dependency contracts, indexed source invalidation, lazy session-view reads and generation-fenced regeneration implemented. Adapter conformance and delete/dependency tests passed in both full-profile runs.
23. **Queue/blob/changelog/clock ports** — Adapters, injected per-store clock and migrated queue/id/audit callers implemented. Conformance and clock tests passed. The 96-test consumer repair run validated all previously failing driver/server clock fixtures through the public clock API.
24. **MESSAGE streaming** — Memory-only begin/append/flush/finalize/cancel, actor-bound mode overrides, duplicate-stream refusal and transactional finality receipts implemented. Stream tests, including injected-clock receipt timestamps, passed in both full-profile runs. Ephemeral frames are returned to the host; no transport broadcast or crash recovery of memory-only buffers is claimed.

## Current validation receipts

- `continued-workspace-runtime-1.log`, source `02bcc459`: 9,387 tests ran; 9,356 passed, 31 failed, 21 skipped. This was not a full pass. Seven failures are the documented macOS bench platform cases.
- `continued-narrow-sync-runtime-1.log`, source `be666297`: 7,856 ran; 7,855 passed, one failed, 16 skipped. All eight earlier core/calendar failures passed. The remaining failure found Git ancestor-repository discovery after a nested repository disappeared.
- `consumer-git-repairs-2.log`, source `2b30fabf`: 96/96 passed, 7,846 skipped. This validates the Git boundary repair and all 16 earlier driver/server failures, plus the surrounding Git/queue scopes.
- These are separate receipts. Their overlapping test counts are not summed. Prior passing cases are retained; the seven macOS bench cases are not represented as Linux passes.
- Final compiler sequence at `d1f85d14`: workspace all-target/all-feature Clippy, featureless all-target Clippy, production-server Clippy, production-NAPI Clippy and strict workspace rustdoc are **all green**. Production checks do not use `--all-targets`; rustdoc clears inherited encoded flags and sets `RUSTDOCFLAGS=-D warnings`. Logs are `final-{workspace,featureless,server-production,napi-production,strict-rustdoc}-d1f85d14.log`.
- `final-featureless-libtest-1.log`, source `d1f85d14`: actual `cargo test -p oneiron --lib --no-default-features` passed **6,580**, failed zero, ignored four, filtered zero. The pthread-key-limit and HNSW churn tests passed.
- `final-napi-runtime-1.log`, source `d1f85d14`, MacBook: actual `cargo test -p oneiron-napi` passed **22/22**, including calendar bridge DTO checks.
- `final-workspace-doctests-1.log`, source `d1f85d14`: workspace all-feature doctests, excluding `oneiron-bench` as the gate specifies, passed **7/7**, including compile-fail examples.
- The only changed benchmark file, `beam/tests_judge_cost.rs`, adds false/zero criticality fields to two fixtures. Its source blob is unchanged since `02bcc459`; all **14** tests in that affected scope passed in the workspace run. That receipt also contains 288 benchmark passes and the seven documented macOS `eval::tests` UnsupportedPlatform failures. These facts are not a claim that the complete benchmark crate passed on macOS or that a Linux reference gate ran.
- `final-scoped-rustfmt.log`: pinned Rust1.96 Rustfmt checked all 500 changed non-vendored Rust files successfully, with no source edits. The redundant queued `cargo fmt --check` was cancelled before host admission; it is not represented as a completed Cargo command.
- Code-map, structural ratchets and the 701-name root-surface pin passed at `ca4d011d`. Their inputs have not changed since that check. Logs and terminal receipts live under `target/w7-logs`; original early scoped logs remain under `.w7`.
- `android-native-target-3.log`: the initial actual NDK compilation failed E0425 in Sudachi. `android-native-target-4.log`, source `ca4d011d`, is the successful retry: `cargo +1.96.0 build --locked -p oneiron-android --target aarch64-linux-android --release -j2`, exit zero. The library SHA-256 is `1e79c0f68b4e84d28e3c6f8bc66def3e9fd57059c3fcfa0bbd67ae273b8bca8c`.
- `android-instrumentation-1/receipt.json` and its fresh JUnit XML record `org.oneiron.EmbeddedVaultTest.openPutGetReopen`: **one passed, zero failures/errors/skips**. Gradle `connectedCheck` and artifact copying both exited zero. The copied JNI library hash matches the native build above; Cargo.lock hash is `d41fe0a42e9115d2794c785c96b0105f2b104a34051ed07ab2a79da14742199b`. Runtime was the authorized API36.1 ARM64 image, Android16/API36, 4096-byte pages. The owned emulator exited zero. XML SHA-256: `306ac01e66942485cf0871592f84278a3724e194578283b9173efef8bf6ebf7e`. The ELF, Gradle/device logs, XML and native artifact are preserved; no host-JVM or unexecuted workflow is substituted for this proof.

## Historical validation log

The entries below preserve the implementation sequence. Statements such as “running” or “pending” describe that historical checkpoint; the per-step list and current receipts above are the current status.

- MacBook via the host cargo shim. Worktree-local target, `CARGO_BUILD_JOBS=2` and `-j 2`. No global configuration changed.
- First scoped `cargo test -p oneiron --lib note:: -j 2` failed compilation: private Loro cursor root imports and missing EntityId serde. Corrected imports and added local validated-id wire adapters; second compile running.
- Full touched-crate tests and final required gates remain pending.

### NOTE first runtime run

`cargo test -p oneiron --lib note:: -j 2` reached 9 tests on MacBook: 5 passed, 4 failed. Cursor concurrency, stale whole-text diff, rewrite isolation, and wrong-document anchor rollback passed. Three failures exposed a nested read transaction in `read_note` (LMDB `BadRslot`); resolve descriptors before opening the entity read txn. The fourth was a fixture using the public raw policy-manifest door; it now uses `batch_in` in one write txn. Rerun pending.

### Criticality decisions

- `ContextEntity.critical` is a manifest-derived predicate-class projection, not stored per-claim guessing. Hydration resolves one policy snapshot after D19. Serialization never expands the visible set or the world/facet partition.
- `criticality(bool)` on query/pack builders selects a tier only. It cannot promote a normal predicate.
- The old serializer exempted a hardcoded predicate list from item and section budgets. That contradicts ARCH-0003 and is removed. Tests asserting that exemption are replaced by hard-limit tests.
- A positive serialized budget smaller than the format's empty scaffolding emits `{}` for JSON or empty bytes for text formats. Exact token stats and the warning remain available on the typed stats API; no unlimited envelope exception remains.
- The factory cargo dispatcher selected Arch slot 1/2 for the latest run. It used this worktree's target and the existing job limit; no machine or tool was installed or changed.

### NOTE runtime evidence

`cargo test -p oneiron --lib note:: -j 2` passed 9/9 on Arch via the factory cargo dispatcher (`.w7/note-build-7.log`). This covers all six built-in kinds plus registered plugin, lazy bridge, latest asset text, stable cursor concurrency, stale whole-text diff, rewrite isolation and explicit switch, five-fork grant routing (two land / three bundle), concurrent merge, and rollback on wrong-document anchors. Not a whole-cluster verdict.

### Calendar origin implementation

Steps 6–9 now have `calendar::origin` typed native/extracted/imported admission, field matrix, live-origin generic EVENT overwrite validation, read-default `dreamer`, and an indexed source-deletion invalidation hook in both hard and soft erase. New typed admissions stage the EVENT and projector-recorded origin in one transaction. Existing imported calendar adapter migration and all-door provenance audit are still pending. The new calendar tests are not yet green.

### Scoped integration receipt

`cargo nextest run -p oneiron --lib -j 2 --no-fail-fast -E 'test(note::) | test(calendar::origin::) | test(context_pack::tests::criticality) | test(serialize::) | test(memory::tests::takes_notes)'` passed **80/80** (`.w7/storage-scoped-5.log`). Added a facade-level fixture that creates the six registered NOTE kinds, edits them, and observes current text through entity reads, lexical search, and context packs. The NOTE text index now updates in the same transaction as document state. Calendar typed admission and projection tests pass. The NAPI calendar DTO now carries origin; NAPI compilation remains to be checked. This is not a full24-step acceptance or full gate.

### Conflict persistence and source ranking draft (steps18–20)

Consolidation now gathers prior heads through `claims_for_subject` and `claim_consolidatable`, includes their values in the merge request, and partitions by `(subject,predicate,world,facet,topic_key)`. Multiple prior heads participate in detection, but only a unique head can be superseded. Escalation retains the gap obligation and also emits `core.conflict.open` through the existing promotion sink at `conflict_open_marker_id`. `close_persistent_conflict` writes a stable `core.conflict.resolved` audit through the same gated writer. Source taint from priors is retained. Tests are pending.

ARCH-0003 §G.1 explicitly says per-predicate topic-key derivation is not pinned, and an older paragraph still says the engine never detects conflicts. The later Wave contract overrides that older baseline. This implementation records the extractor-supplied `scope.topic_key` question as canonical data; it does not guess a question from natural-language answer strings. Absence retains conservative full-predicate grouping, so a missing key cannot hide a changed answer in its own answer-key bucket. Canon should document this extractor-data seam.

Source ranking uses shipped `context_pack/source_ranking.json` PACK data and an explicit builder override. Default equal multipliers clamp observed claims below their user-stated mates across predicate families. Mates include subject/predicate/world/relationship/facet/topic; world sections remain separate. This is read-time ranking only and cannot supersede a stated claim. Tests are pending.

### Conflict runtime receipt

`cargo test -p oneiron --lib dreamer_consolidation -j 2` passed **52/52** (`.w7/conflicts-1.log`). It covers captured escalation to persistent marker, prior-head clash, topic partition, stable marker rerun through the real promotion writer, and stable resolved audit. Source-ranking tests were added after this run and still need execution.

### Android implementation and host gap (step17)

Added `oneiron-android` JNI crate and Kotlin `EmbeddedVault` ownership surface, a native round-trip test, Android instrumentation open/put/get/reopen, and a self-hosted arm64 CI leg that builds with NDK then exercises the Kotlin API on an emulator. No software was installed on the working host or MacBook. Capability discovery found Java and later located Kotlin 2.1.10 inside Android Studio plus an Android SDK outside PATH. No NDK was found in that SDK; installed Rust targets are `aarch64-apple-darwin` and `aarch64-unknown-linux-gnu`, not Android. Native JNI tests are running. The Android target/emulator acceptance has no green runtime evidence yet.

### Current integration receipts

- `.w7/android-native-3.log`: `cargo test -p oneiron-android -j 2`, 1 native round-trip passed plus doctests. This is not Android/Kotlin evidence.
- `.w7/source-ranking-2.log`: `cargo test -p oneiron --lib source_ranking -j 2`, 1 passed.
- `.w7/streams-1.log`: four MESSAGE stream tests passed.
- `.w7/dag-streams-1.log`: nextest scope `conversation_dag:: | scope_summary:: | message_stream::`, 18/18 passed. This includes the post-adoption legacy-write rejection regression, exact 300-record covers, nested sub-session isolation and randomized thread/HEAD behavior.
- `.w7/ports-recovery-calendar-1.log`: initial compile failures were fixed; `.w7/ports-recovery-calendar-3.log` passed 122/122 featureless calendar/ports/recovery tests. Sync recovery and HTTP DAG scope is now running.

### Canon decisions for the newly integrated modules

DAG canon: `oneiron/backend/oneiron-arch-0006a-conversations-api-core.md` and the CONV-01/02 SOW. HEAD/canonical/session sidecars stay local. Reply revision hashes the encoded TURN body; late headers append through the engine door. Legacy writes cannot add invisible bare records to an adopted DAG.

Recovery canon: `oneiron/core/oneiron-arch-0038-deletion-redaction-canonical-snapshot-v1.md`. A NOTE can have multiple heads/forks, so canonical state carries every document key, current binding and immutable landing receipts. Unknown container schemas reject rather than disappear. The recovery driver returns fresh state and restores standard materialization; the caller owns stopped-writer coordination and live-window publication. No historical redaction-sweep completion is asserted.

Ports canon: `oneiron/backend/oneiron-arch-0005a-backend-ports-integrity.md`. Transactions stay caller-owned. Adapter methods must not start or commit nested writers. Explicit conformance adapters are not proof of complete production migration; remaining direct callers are being moved separately. Existing overlay/session reads must retain their view semantics.

Streaming canon: `oneiron/core/oneiron-arch-0002-structural-entities.md`, Streaming MESSAGE write path. Receipts are written inside the existing witness transaction, after MESSAGE validation and before final actor revalidation. A refused finalize retains its buffer and commits neither MESSAGE nor receipt. Buffers never become partial CRDT ops.

The 122-test integrated scope includes LMDB and memory-adapter conformance, exact dependency deletion, clock-floor reopen, canonical carry list/blake3, fresh document reconstruction, redaction byte preservation, quarantine and typed repair pressure. It precedes the full caller-migration patches and the new originless-field/conflicting-origin guard regression.

### Kotlin/JNI receipt

The installed Android Studio Kotlin 2.1.10 compiler built the shipped `EmbeddedVault.kt` with `kotlin/tools/JniSmoke.kt`. Java 26 loaded the existing green native JNI dylib and returned `JNI-KOTLIN-OK open/put/get/reopen/refusal/close` (`.w7/kotlin-native-proof.log`). No dependencies were installed. Kotlin source and native dylib hashes are in `.w7/kotlin-native-proof.sha256`. The next Cargo sync removed the transient JAR before its hash was captured; no JAR hash is claimed. This is a native-host JVM proof, not Android-target evidence. The Mac SDK contains API36/build-tools36.1/emulator images but no NDK; Rust has no Android target. The NDK/emulator workflow is supplied but has not run here.

### Sync and HTTP receipt

`.w7/sync-server-scope-1.log` passed 10/10 across core and server with `sync,test-hooks`. This covers fresh forward recovery into a separate vault, retained soft headers/graph, hard-delete refusal, NOTE documents and indexes, span-exclusion materialization, all HTTP DAG/scope/migration operations, auth separation, 300-record covers/drill/late-reply projection and OpenAPI registration. The new calendar-only-field/no-origin and conflicting-live-origin regression also passed. Full touched-crate tests and complete caller-migration integration still remain.

### NOTE registry door audit

Syntax-only plugin kinds were accepted by the raw materialization door even though the facade rejected unknown kinds. The shared body-staging path now resolves NOTE kinds against shipped/installed descriptor records for local, replay and overlay writes. A raw-local/replay refusal regression is running; no result is claimed yet. The shared opt-in entity-id hex serializer was moved to `entity_id/serde_hex.rs` so NOTE and changelog records can reuse it without adding a global serialization ABI to EntityId.

### Production clock migration integration (not validated yet)

The injected per-store clock and ID source now reach 300 former direct clock/ID callsites. Caller-supplied IDs and world timestamps remain caller data. A persisted ID high-water mark prevents repeated custom sources from reusing IDs; a per-store floor never moves backward. Queue verbs, including task/actor-scoped in-transaction enqueue, use the ports and retain dedupe and lease fences. Mutation chokepoints append Create/Update/Delete/Redact/Forget/Merge/Supersede/TierTransition audit rows in the same transaction. They store hashes, not copied entity bodies. A missing authenticated actor is labeled as the storage-service principal, never guessed from the target or old author. ManualClock is exposed through `store::ports` for deterministic callers/tests.

The first two migration compile attempts found source-conversion and transaction-generic integration errors. These were fixed locally; the third scoped compile is pending. Existing queue tests that supplied timestamps must inject ManualClock instead of treating request timestamps as clock authority. Remaining standalone prompt/serializer timestamps and caller-owned proposal IDs are not persisted engine recording operations; their explicit caller contracts stay unchanged.

The NOTE materialization registry guard passed all 11 NOTE tests (`.w7/note-registry-guard-1.log`) after the mini transfer retries recovered without host changes. It is committed as `c1418973`.

Updated query conformance count assertions to measure three added rows over the initial installed-row count. LMDB fixtures carry the default policy; the in-memory adapter starts empty. No storage was cleared to make them look identical.


## Integrated caller and clock migration (validation in progress)

The final caller patch is integrated, including typed lazy entity/type/timeline/edge streams,
short-id/tombstone queries, composed `SessionStoreView` reads, ranked/scoped text retrieval,
operational body/edge rewrites, and dependency regeneration completion. The corresponding
clock patch routes generated persisted IDs/timestamps through the injected store clock and
adds same-transaction mutation audit rows. Earlier port test passes predate these migrations.

The first integrated compile exposed missing trait/import bindings, which were repaired.
The next reached code generation but failed with `ENOSPC` on the Mac mini. Only this ticket's
`oneiron` target artifacts were cleaned (Cargo reported 8.1 GiB). No shared cache or other
ticket was changed. The next scoped conformance build is on the MacBook, whose Cinema
volume had 1.5 TiB available. No runtime result from this newest source is claimed yet.

Consumed worker stage trees were archived in `.w7/consumed-worker-stages.tar.gz` and removed
from the source-scanning scratch directory. The real production ratchet passes at
2 giant files, 96 allow attributes, 91 print macros and 33 process globals. These values
do not use a baseline bump. The obsolete raw-key reader functions were removed after
checking all consumers; test coverage was not gated away.


### Caller migration integration checkpoint

The integrated featureless library and test targets now compile. The full library test
binary did not run: the mini failed at link with `errno=28`. Cargo clean then also failed
while rewriting its mirrored lockfile. Only this ticket's disposable incremental cache
was removed, and a package-only `profile.test.package.oneiron.debug=0` override is now
used for validation to reduce link output. This changes neither dependencies nor source
behavior. The router and host slot limits are unchanged. The compact retry runs on the
MacBook. The mirror is resynced from this worktree, including its authoritative lockfile.

The final raw-caller census found two remaining domain edge readers (DAG traversal and
cleanup presence probes); both now use `EdgeStoreRead` with the same row/work bounds.
Low-level batch/index/deletion/overlay code remains the adapter; doctor/open guards and
fault-injection fixtures are not domain callers. Full runtime and sync/server gates
remain pending; no acceptance status is upgraded by the compile or census alone.


### First integrated runtime results and repairs

The compact featureless run compiled and linked under the unchanged factory router's
Arch overflow slot. It recorded 4183 passing cases and 60 failed cases before two
executor loops stopped making progress. The known test process was terminated after
tracing the loop to retries scheduled from a caller's fixed tick but claimed at the
new injected-clock horizon. No full pass is claimed.

Repairs now under validation:
- `ClaimAttempt.now` narrows readiness only; the injected clock caps it and owns lease
  timestamps. LMDB and memory adapters use the same rule. One executor tick cannot
  consume the fresh retry it just scheduled.
- Clock-dependent fixtures inject and advance per-vault manual clocks. No production
  clock fallback, cfg-disabled tests, or process-wide test override was added.
- Booking births record a live native origin in the same transaction; later EVENT
  rewrites retain the origin field. Native EVENTs keep authored state despite source
  provenance links.
- Counterparty cache eviction preserves the durable claim graph, unlike semantic
  entity deletion. Typed port reads stay separate from forensic raw-byte reads.
- The NOTE source bridge now refuses retained stale ASSET_TEXT and skips stale
  candidates when choosing an asset's current text.
- Hydration preserves its corrupt-vector/header error classification and hex short-id
  fallback. Deleted payloads are filtered before pack assembly, never returned.

Next scoped validation: `.w7/ports-regressions-2.log`. Earlier green evidence does not
validate these newest repairs. Sync/server, final touched-crate gates, and real Android
NDK/emulator execution still remain.

### Post-migration regression scope

`.w7/ports-regressions-3.log` ran 658 selected featureless tests: 653 passed, five failed. All new LMDB/memory port conformance, booking-origin, native-dependency deletion, stale NOTE-source bridge, contact-cache and forensic-read cases passed. The outbound retry loop completed normally. Five remaining fixtures advanced caller timestamps without advancing their injected clock, or reused a second system-clock vault. Those fixtures now control the source clock explicitly; the rerun is pending. This is not a full touched-crate pass.

Android capability checks also found no SDK/NDK in the mini or local standard locations. The mini has only its native Apple Rust target. The supplied Android workflow still needs a provisioned NDK/Android Rust target; no installation was attempted.

`.w7/ports-regressions-4.log` passed 37/38 cases on MacBook, including all DAG, summary, streaming and featureless recovery cases plus four repaired clock fixtures. The remaining outbound check compared all `vault_meta` rows as if they were delivery evidence; the only changed row was the persisted clock floor. It now checks the actual immutable audit receipts, winner, idempotency lookup and queue rows. Its repeated no-op still requires an identical storage snapshot. Maintenance and inbox fixtures now use injected recorded time too. The unfinished featureless tail and four previously uncovered regressions are running in `.w7/featureless-tail-1.log`.

### Final integration audit follow-ups

The topic key was used for conflict partitioning but not for write-once claim IDs. Two questions with the same predicate and answer could collide in one extraction. IDs now include the canonical topic key, for extraction and merged candidates. A production-executor replay fixture requires distinct IDs for coffee/tea with the same answer and stable IDs on replay. Topic-key data is exposed in the response schema. No new hardcoded agent instruction is added to the existing extraction prompt.

The Android Gradle module now enables AndroidX for its instrumentation dependencies. Its CI path trigger covers core/toolchain/LMDB inputs and honors the runner cache budget. These are configuration fixes, not Android runtime evidence.

`.w7/featureless-tail-3.log` ran 2,188 tests: 2,158 passed, 30 failed. This includes the new topic-ID replay test and all consolidation tests. Eleven failures were the documented macOS `/var` temporary-path symlink refusal. The next command uses host-relative Cargo TMPDIR settings. Other failures exposed typed-read error handling, preserved empty-shell semantics, strict temporal-key bounds, three run-tree/four surface-event clock fixtures, no-op transactions writing clock-floor rows, and one incorrect newly expanded edge-prior expectation. Repairs are under scoped validation; passing rows are retained rather than rerun wholesale.

The final static port census is `.w7/ports-final-audit.md`: no production entity/claim/edge/place/retrieval/deletion/queue database bypasses remain outside the declared adapters. It found one persisted clock/ID bypass in streaming; stream IDs now use the vault source and the finality timestamp is sampled in the witness transaction. Dependency regeneration completion is a public transaction-composable `DependencyIndex` operation on the Vault/adapter, used by host projectors after writing replacement content; this change does not invent an autonomous content-generation worker.

CONV-01 reserves `Parent`, `SpawnedBy` and `RepliesTo`, not `AddressedTo`. The registry and test now follow that explicit list. The expanded edge census preserves the pre-existing `Blocks` stored prior of 1.0; its PPR traversal weight remains absent.

### Final featureless regression receipt

`.w7/final-regressions-3.log` passed **141/141** on the Mac mini. This includes all thirty failures from the unfinished tail, adjacent port conformance, stream finality clock stamps, malformed-manifest refusal, soft-shell visibility, strict temporal bounds, share denial and no-op onboarding replay. Cargo consumed `.w7/test-env.toml`; a host-relative, non-symlink TMPDIR fixed the eleven macOS environment failures. Formatting completed afterward. Cross-feature/workspace compilation and runtime gates remain pending.

### Cross-feature lint integration

The all-features core compiler reached Clippy after the sync-only `Error` import repair (`aa787a2f`). The terminal JSON receipt is `target/w7-logs/core-clippy-diagnostics-1.jsonl`: 137 findings, of which 134 have machine-applicable substitutions (127 redundant borrows, three method closures, two Option expressions, one comparison borrow, one nested condition). Applied those substitutions against exact compiler source-line snapshots. Moved the unchanged SKILL content/source index writes to the existing staging module to keep `apply_put` under its 500-line limit; moved the timeline bound-view helper out of a statement block; added the macOS unsafe-call safety comment. No warning was suppressed. These lint repairs still need compiler validation.

Remote source copy-back replaces logs stored in the source tree. Validation logs now live under the sync-excluded `target/w7-logs/`. The mini filled again (117 MiB free) after the ticket-only cleanup had freed 6.6 GiB; no other ticket or shared cache was cleaned. The queued formatter was stopped before admission, and the already installed Rust 1.96 formatter formatted only changed core files locally (no local Cargo build). The ticket's next compiler command disables incremental caching, which the native wrapper forwards, to reduce its own footprint. Android-target and full touched-crate validation remain outstanding; these are not green by inference from the featureless tests.

### Android API26 static repair

The final compatibility audit found a concrete new Android build defect. In pinned libc 0.2.189, Android's `RENAME_NOREPLACE` is `c_int` but `renameat2` expects `c_uint`. More importantly, Bionic's `stdio.h` marks `renameat2` `__INTRODUCED_IN(30)`; the adapter's NDK linker targets API26. The Linux/Android recovery branch now calls `libc::syscall(SYS_renameat2, ...)` instead, keeping atomic no-clobber semantics without requiring the API30 Bionic symbol. Unsupported kernels return an error; no overwrite/copy-unlink fallback is added. Existing `quarantine_uses_next_invalid_suffix_for_distinct_existing_bytes` and `quarantine_skips_symlink_candidate_to_preserve_bytes` are the behavior falsifiers. The changed syscall branch has not been compiled or rerun because of the build-host block below.

Source evidence: matching cached libc `src/unix/linux_like/android/mod.rs` (constant and declaration) and Android's authoritative `https://android.googlesource.com/platform/bionic/+/refs/heads/main/libc/include/stdio.h?format=TEXT`. This static evidence is not Android-target execution. The JNI private-instance ownership path and Kotlin/Gradle configuration were also checked; native Kotlin/JNI evidence remains a host-JVM check, not emulator acceptance.

## Previous validation-blocked handoff (superseded by the continuation below)

- All 24 implementation steps have code and named fixtures. The per-step list above distinguishes scoped evidence from remaining cross-feature/cross-crate validation.
- Latest post-migration featureless regression receipt: **141/141 passed**, including all 30 prior tail failures and adjacent port/stream laws. The wider tail passed 2,158/2,188 before those repairs; the earlier port/booking/context scope passed 653/658, and the five failures were repaired and rerun. These receipts overlap and must not be summed into a fictitious full-suite pass.
- Initial NOTE/calendar/recovery/DAG/server/native JNI scopes are recorded above. No full touched-crate, final workspace, final server-production, NAPI, strict-rustdoc or Android-target pass is claimed.
- Core all-feature lint findings were repaired, including the sync import, two compiler-suggested private paths and final function-length split. The last workspace command (`target/w7-logs/workspace-clippy-after-fixes-3.jsonl`) terminated with `couldn't create a temp dir: No space left on device` in the mini's W7-C11 target. It did not produce a successful build receipt.
- Host receipts: the mini was at 100% with 117 MiB free. Cleaning only this ticket previously freed 6.6 GiB, which did not hold; later `cargo clean -p oneiron` itself failed writing its remote lockfile. Removing only this ticket's core incremental directories recovered about 800 MiB, and `CARGO_INCREMENTAL=0` was forwarded for subsequent commands, but the next metadata allocation still failed. No shared cache, other ticket, global configuration or host budget was changed. The next authorized sync must restore the remote lockfile from the committed local copy before building.
- The required Android NDK and Rust Android target are absent on inspected hosts. Installation is prohibited in this task. The workflow provisions them for its Android target/emulator leg, but that leg has not run here.
- Final local source formatting uses the already installed pinned Rust1.96 formatter. Code-map, structural counters (2/96/91/33) and root-surface pin (701) pass. No local Cargo build was forced.
- Remaining work is validation and any defects it exposes: restore build-host headroom through the host owner; run final workspace/touched-crate tests plus sync/featureless/production/rustdoc lanes; run the provisioned Android target and Kotlin instrumentation. W7-C11 must not be marked accepted until those receipts exist.

## Owner-authorized validation continuation

The repaired normal wrapper skips the mini below its 30GiB reserve and admitted the continued all-feature build on Arch without any forced local run. The previous ENOSPC receipt remains historical evidence, not a current reason to stop independent host validation. Source and receipts through `e3ddefad` are preserved.

A single ticket-owned transient user command retains each gate across factory writer continuations. It runs the existing normal wrapper with the existing routing environment and job limits. No daemon restart, install, shared-cache edit, host-budget override or direct-SSH build is performed. Gate logs and terminal statuses stay in sync-excluded `target/w7-logs/`; a completed command is retired before its successor starts.

The continued all-target build compiled core production and NAPI targets, then exposed two JNI lints (fixed in `c5cec7e6`) and four test-target lints. Shared DAG fixtures now load once behind cfg(test); no production visibility is widened. The in-memory changelog helper returns its infallible Vec directly and the trait boundary still returns Result. Phonetic query deduplication is retained explicitly: simply following the collection-removal suggestion would have inflated duplicate-code scores. Existing cross-backend conformance already pins the duplicate-code score at2.4. The outbound clock fixture uses a struct initializer. No lint was suppressed.

The same inspection found the in-memory scoped text adapter returned a row for limit0. It now returns an empty result; the existing both-backend scoped-query fixture checks budgets0 and1. NAPI's existing calendar DTO fixture now explicitly asserts both native and dreamer origin values. These changes await the next compiler/runtime receipts.

### Separate Android provisioning requirement

A fresh read-only MacBook probe still finds no NDK manifest, no Rust1.96 `aarch64-linux-android` standard library, and no installed Gradle CLI/wrapper distribution. It finds only SDK platform36, build-tools36.1.0 and an ARM64 API36.1 Play Store image. Earlier mini/local absence probes were retained, not retried on an unsafe mini.

Required workflow: `.github/workflows/android-embedded.yml`, **Android embedded**, job **android**, self-hosted macOS ARM64. It requires Rust target **aarch64-linux-android**, NDK **27.2.12479018** under `$ANDROID_HOME/ndk/27.2.12479018`, Java17, Gradle8.11.1, AGP8.9.2, Kotlin2.1.20, SDK platform35/build-tools35.0.0 and the API35 google_apis arm64-v8a emulator image. The expected current MacBook NDK location is `/Users/olety/Library/Android/sdk/ndk/27.2.12479018`.

Within the provisioned, scheduled macOS runner job, set `ANDROID_NDK_HOME` and `NDK_BIN=$ANDROID_NDK_HOME/toolchains/llvm/prebuilt/darwin-x86_64/bin`; set `CARGO_TARGET_AARCH64_LINUX_ANDROID_LINKER` and `CC_aarch64_linux_android` to `$NDK_BIN/aarch64-linux-android26-clang`, and `AR_aarch64_linux_android` to `$NDK_BIN/llvm-ar`. Then run `cargo build --locked -p oneiron-android --target aarch64-linux-android --release`, copy the resulting `liboneiron_android.so` to `crates/oneiron-android/kotlin/src/main/jniLibs/arm64-v8a/`, and run `gradle -p crates/oneiron-android/kotlin connectedCheck` with the scheduled emulator connected. The existing emulator workflow step supplies API35/google_apis/arm64-v8a. API26 is the link/minSdk floor; this does not claim runtime execution on an API26 device. That extra run would require an actual provisioned ARM64 API26 device/emulator, checking `adb shell getprop ro.build.version.sdk`, then the same connectedCheck command.

A host/runner owner must authorize and provision those dependencies and the scheduled workflow under the existing CI policy (`CI_PAUSED` currently gates the job). No install, workflow dispatch or CI-variable change was made. Host-JVM JNI proof and shipping this workflow are not Android acceptance. Exact commands, capability results and authority limits are also in `target/w7-logs/android-provisioning-requirements.md`.

## Current compiler and static receipts at d9e47226

All four required compiler selections passed through the normal wrapper on Arch with `-D warnings`:

- Workspace all targets/all features: `continued-workspace-clippy-durable-2.jsonl`; core production and test targets include all declared features.
- Core all targets/no default features: `continued-featureless-clippy-1.jsonl`; Cargo enables the existing `test-support` fixture feature, with no sync/test-hooks. Both library and test targets passed.
- Production server (no all-targets): `continued-server-production-clippy-1.jsonl`; core features are exactly `sync`, test=false.
- Production NAPI (no all-targets): `continued-napi-production-clippy-1.jsonl`; core features are empty, test=false. Keeping this separate from server prevents sync feature unification from concealing featureless failures.

Every log has a terminal `.status` receipt under `target/w7-logs`. `validated-core-feature-selections.json` records the actual compiler artifact selections, not an inference from command flags. These are compiler checks, not runtime test passes.

Pinned Rustfmt1.96 `--check` passed all490 changed Rust files; the file list and receipt are `fmt-d9e47226-files.json`, `fmt-d9e47226.log` and `.status`. Code-map verification passed (2584 files,17 current artifacts); ratchets are2/96/91/33 and the root surface remains701 names. `git diff --check` passed. These read-only results are in `static-d9e47226.log` and `.status`.

Strict workspace rustdoc is the current retained command, with `RUSTDOCFLAGS=-D warnings` and inherited `CARGO_ENCODED_RUSTDOCFLAGS` removed. It has no terminal result yet. Full workspace/featureless/narrow-sync runtime lanes, doctests and host-appropriate NAPI runtime evidence remain. The new zero-limit and NAPI origin assertions have compiled but have not yet run. The Android provisioning limitation above remains separate and unwaived.

Strict rustdoc's first terminal run found one stale link in the NOTE take writer: `EntityId::now` had been replaced by the injected vault ID source. The comment now describes the actual writer; no executable code changed and no warning was suppressed. The failure receipt is `continued-strict-rustdoc-1.jsonl.status`; rustdoc will be rerun while the four compiler selections and runtime receipts remain retained.

## Full workspace runtime and first corrections

The full-profile unfiltered workspace nextest run at02bcc459 completed on MacBook: **9356 passed,31 failed,21 skipped** across42 binaries (`continued-workspace-runtime-1.log`, terminal status100). Retries are not counted as separate tests. Exactly7 failures are the documented unchanged macOS bench eval UnsupportedPlatform cases. The other24 are real integration failures to resolve; this is not a green workspace verdict.

Four core sync and three server sync tests exposed a production defect: reading an absent Loro root with `get_map(name)` creates that root without recording an operation, so the receiver gained three empty carrier maps and failed deep convergence. Canonical binary-carrier reads now inspect existing shallow ContainerIDs before accessing a map. They return no rows for an absent root and reject an existing non-map carrier. Existing deep-equality tests remain unchanged. A new public canonical-capture regression pins both read-only behavior and wrong-kind refusal.

Three calendar fixtures are updated for the origin contract: semantic imported candidates still require imported/proposed/archive provenance; the separate origin fact is explicitly pinned as one imported-value, Auto/Active, source-less projector-recorded claim. The prep reschedule fixture now stages its live native-origin fixture claim before later EVENT overwrites. The quarantine retry fixture checks retained raw bytes separately from safe reads: a durable delete hides the payload even while a failed purge leaves its row; successful drain must remove the raw row too.

Remaining full-run failures:10 driver clock/deadline fixtures and3 server API contract/run-tree fixtures, plus the7 server/core convergence cases targeted by the production fix. Read-only context workers are tracing driver/server deltas without source/build custody. Full narrow-sync and featureless runtime lanes remain required.

### Android provisioning supersedes the earlier external blocker

The owner supplied **ANDROID_PROVISIONED_20260919**: MacBook now has the1.96.0 `aarch64-linux-android` target, NDK27.2.12479018, SDK35/build-tools35, Java17 and Gradle8.11.1. No source/build was performed by the provisioning agent. The owner authorizes C11 to run the native target build and Kotlin instrumentation under its existing MacBook build custody/capacity. Use explicit toolchain1.96.0, not a default toolchain change. Java is at `/Volumes/Cinema/w7-build/toolchains/android-20260919/java17/Contents/Home`; Gradle at `/Volumes/Cinema/w7-build/toolchains/android-20260919/gradle-8.11.1/bin/gradle`; isolated Gradle home is the sibling `gradle-home`.

Local instrumentation may reuse **Medium_Phone_API_36.1** ARM64. That proves the embedded JNI/Kotlin minSdk26 path on API36.1, not execution of the checked-in CI API35 runtime pin. Keep source/.so hashes and Android instrumentation XML/logs. The old no-NDK/no-target limitation is resolved; Android execution is pending, not blocked. Copied provisioning evidence and exact environment are under `target/w7-logs/ANDROID-PROVISIONED.md` and `android-provisioned-{report.md,verification.txt}`.

Driver/server failure investigation was recovered from the context workers' partial transcripts after their legacy parent continuation ended them. No worker source edits or builds were performed. The writer prepared and applied fixture-only corrections while the independent core-only test lane ran; `crates/oneiron` and Cargo inputs stayed unchanged.

Driver queue/deadline fixtures now explicitly inject their intended recording times. The four session-order tests share the same clock with the vault (milliseconds converted to seconds), preserving every deadline/hint ordering assertion. Tick fixtures advance a per-vault manual clock before enqueue and set the commitment producer's time to3 seconds. The supervisor's due-work fixture records10/11-second queue times; its bounded progress assertions are unchanged.

Server run-tree fixture injects10/20-second timestamps and keeps exact wire timestamp/event assertions. A structural JSON diff of the success snapshot shows only four additions: `critical_count:0` and `critical_over_budget:false` under context-pack and context-board pack stats. The OpenAPI fixture now declares those two required fields and their integer/boolean types. No unrelated field, ordering, permission, status or normalization rule was changed. This pins steps15–16 rather than broadly blessing snapshot output. Consumer runtime and lint checks for these changes are still pending.

## Exact-source acceptance evidence recovered from the full workspace run

The completed `continued-workspace-runtime-1.log` at02bcc459 contains passing NOTE descriptor/document/anchor/proposal/source-bridge tests; all four origin admission/field/deletion tests; canonical carry-list/rebuild/redaction/recovery-ladder tests; all core DAG/scope-summary tests and all three server DAG API tests; manifest criticality, source ranking, persistent conflict and all four streaming tests; and all fifteen LMDB/memory port conformance tests. The edge registry census `byte_space_v3_conformance::conversation_dag_edge_byte_census` and structural door-only test also passed. Exact names are indexed in the retained `workspace-acceptance-evidence-map.json`. These facts come from one completed receipt, not the sum of overlapping scopes.

This does not turn the full workspace run green: it finished9356passed/31failed/21skipped. Seven failures are the documented unchanged macOS bench platform cases. The core/replay root-map observation repair and fixtures in be666297 are under current narrow-sync validation. Consumer clock and critical-stats snapshot fixture repairs in363b5d60 still need runtime/lint validation. No previous pass is claimed for the new canonical observation regression, Android-target build/instrumentation, or the new NAPI assertion. Historical paragraphs above are retained as the chronological investigation, not current blockers.

## Narrow sync/test-hooks terminal and Git discovery correction

`continued-narrow-sync-runtime-1.log` (Arch through the normal wrapper, core tree from be666297 unchanged by the consumer fixture commits) finished **7856 run:7855 passed,1 failed,16 skipped**, fullprofile, status100. Type-boundary compile-fail fixtures passed. All five earlier core sync/quarantine failures and all three earlier calendar integration failures passed. The new canonical observation regression and the entire canonical recovery group passed. This is not a full green run: the sole failure was `git_wire_reads_absence_positively_and_keeps_fatal_failures_typed`.

That failure exposed real ancestor-repository discovery: removing a nested repository's `.git` made Git use the surrounding checkout and report absence instead of a repository error. It appeared under the worktree-local real TMPDIR on Arch. Native Git2.54.0 confirms a ceiling at the current root does not prevent upward discovery, while a ceiling at its parent does. The process boundary now canonicalizes the working root and sets that parent as its per-invocation discovery ceiling, independent of ambient environment. An unencodable ceiling refuses through the existing typed boundary error. The existing regression always nests its disposable repository, keeps present/absent distinctions, asserts typed failures after destruction, and proves the parent remains unchanged. No assertion was removed or relaxed.

The staged Git repair and acceptance-notes refresh are now applied. Android native-02 had zero output and was still blocked on the ticket lock when withdrawn, so no Android build work was lost or duplicated. The final Git/consumer repair runtime and lint checks remain pending; Android target/device execution, full featureless library tests, native NAPI runtime and doctests remain unproven. The terminal narrow-sync green cases are retained rather than summed with previous overlapping runs.

The focused Git/consumer build exposed fixture-only public API mistakes before runtime: cross-crate fixtures must use `oneiron::store::ports`, not the private root implementation module, and construct the non-exhaustive config with `VaultConfig::device()` before assigning its public `store_clock`. Those four fixture corrections are applied without widening engine visibility or changing test outcomes. The failed compile is retained as `consumer-git-repairs-1.log`/status101; the same focused runtime scope is rerun, not counted as prior green evidence.

## Android target portability correction

The first provisioned aarch64-linux-android build reached Sudachi 0.6.11 and
failed E0425: its plugin loader covered Linux/FreeBSD/Windows/Apple but not
Android. Keep the exact existing upstream commit and vendor its library,
resources, and test plugins with one Android ELF filename cfg addition.
`crates/oneiron/vendor/SUDACHI.md` records provenance and the packaging-only
workspace trim. No tokenizer is disabled, no version changes, and no host
cache is modified. The locked Android target build must validate this repair;
the prior attempt is a compiler failure, not Android acceptance.


## Test repair follow-up — completed

The rejected factory run included `oneiron-napi` in an off-Node Linux test binary.
It failed at linking (`napi_reference_unref`, `napi_delete_reference`, and
`napi_call_threadsafe_function`), before runtime tests. That run is not test evidence.
The fresh executed results below cover every touched crate. No implementation
step remains. This is a scoped acceptance result, not an unfiltered workspace or
Linux full-gate VERDICT.

### Added acceptance assertions

- Step 3: `note::document_tests::append_to_section_uses_its_unicode_anchor_after_another_edit`
  covers the third anchored verb with a stale section cursor and non-ASCII text.
- Step 13 T4: `conversation_dag::tests::branch_scope_with_forks_includes_siblings_but_not_retained_sub_sessions`
  compares exact sets with/without forks and preserves HEAD. The existing server
  DAG round trip also checks `include_forks: true` on a branch.
- Step 22: `ports::tests::step22_conformance::vault_delete_invalidates_and_enqueues_only_indexed_dependents`
  drives `Vault::delete_entity`, then checks visible text, retrieval results and
  exactly two regeneration jobs. Unrelated source/dependent rows remain visible.
- Step 24: `message_stream::tests::agent_chat_override_survives_reopen_is_isolated_and_only_changes_future_streams`
  uses an actual Agent actor through the memory facade. It checks persisted
  overrides/removal, actor isolation, unchanged active-stream behavior, and no
  stored partial or finality receipt after dropping an unfinished stream.

The two driver warnings in the rejected run were voice-only imports. Their
imports now use the same `unix` + `voice` condition as their consumers. No test
was disabled. The first HTTP test compile caught a shadowed route-path variable;
`e4634cdf` fixes it. Final Rust source is `e4634cdf`; the closing change is notes only.

### Fresh executed results

All five final commands exited zero on the MacBook. They used the dedicated
`/Volumes/Cinema/w7-build/wt/W7-C11/target`, Rust 1.96, two compiler jobs and four
runtime threads. Nextest used `--profile full`, `--no-fail-fast`,
`--build-jobs 2`, and `--test-threads 4`. Cargo test used `-j 2` and libtest
`--test-threads=4`. Tests used `--config profile.test.package.oneiron.debug=0`
and `.w7/test-env.toml`, which sets TMPDIR to the worktree's real `.w7/tmp` path.
No shared Cargo configuration changed.

| Lane | Result | Log under `target/w7-logs/` |
|---|---|---|
| Named core storage acceptance scopes + complete server suite, all features | **1,116 passed, zero failed**: 232 core + 884 server; 7,626 out-of-scope tests filtered | `fix-tests-core-server-5.log` |
| Driver, N-API, benchmark and Android Rust consumers, all features | **388 passed, zero failed**; 26 filtered for the follow-up below | `fix-tests-consumers-macos-5.log` |
| Previously filtered benchmark evaluation siblings | **19 passed, zero failed** | `fix-tests-bench-eval-1.log` |
| Four newly added core tests, `cargo test -p oneiron --lib --no-default-features` | **4 passed, zero failed**; 6,584 filtered | `fix-tests-featureless-2.log` |
| `cargo clippy -p oneiron-driver --all-targets --no-default-features -- -D warnings` | **Passed** | `fix-tests-driver-lint-2.log` |

The two consumer selections are disjoint. Together they passed **407 tests**:
288 benchmark, 96 driver, 22 N-API and one Android Rust test. The initial substring
filter excluded all 25 `eval::tests` tests plus one matching `retrieval::tests`
test. The follow-up ran the 18 supported evaluation tests and that retrieval
test. Only the seven exact macOS `UnsupportedPlatform` exceptions in
`.github/workflows/ci.yml` remain excluded; they are not represented as passes.

The core filter covers `note::`, `memory::tests::takes_notes`,
`calendar::origin::`, `calendar_ics_ingest_adapter::`, `calendar_prep::`,
`recovery::`, `conversation_dag::`, `scope_summary::`,
`context_pack::tests::criticality`, `context_pack::tests::source_ranking`,
`serialize::`, `dreamer_consolidation::`, `ports::`, `message_stream::` and
`byte_space_v3_conformance::`. Every server test remains selected. All four new
core tests and the expanded server DAG round trip passed in this run. The earlier
broad core receipts above are retained: since `d1f85d14`, the core diff contains
only the four new tests, with no production-source change. The four featureless
results overlap the all-feature results and are not added to a unique-test total.

The Android NDK build and Kotlin `openPutGetReopen` instrumentation receipt above
are also retained for unchanged bytes. The saved JUnit file was rechecked: one
test, zero failures/errors/skips, SHA-256
`306ac01e66942485cf0871592f84278a3724e194578283b9173efef8bf6ebf7e`.
This remains the owner-authorized API36.1 runtime proof, not execution of CI API35.

Pinned formatting of all seven changed Rust files, `scripts/codemap/check.sh`
(2,584 files / 17 artifacts), and `git diff --check` passed after the final Rust
edit. Exact command arrays, service exit records and per-binary counts are saved
in `target/w7-logs/w7-c11-fix-tests-*.json` and `fix-tests-*-success.json`.

### Runner corrections and custody

Queued/interrupted calls and the initial nextest CLI rejection ran no tests.
Nextest's `-j` means test threads, so the corrected commands use `--build-jobs`
for compiler concurrency. The ticket-local Mac lane omits Arch fallback for N-API
and waits in the kernel FIFO for existing MacBook slot 3. It retains the factory
host/ticket locks and CPU/disk limits. It neither adds capacity nor changes the
factory launcher. All final validation services are terminal with exit zero;
no C11 test process or build slot remains held by this follow-up.


## Owner fixture repair — C11_FIXTURE_REPAIR_20260919

The latest factory `tests:2` actually ran the six touched packages on macOS and
failed two targets: core library (7,078 passed / 11 failed / four pre-existing
ignored) and benchmark binary (278 passed / seven failed). These are the latest
blocks in `tickets/W7-C11/logs/cargo-test.log`, not the old off-Node linker failure.
The earlier scoped receipts above remain true, but their seven benchmark exclusions
are not an acceptable final replacement for the mandatory unfiltered target run.

`415632c6` canonicalizes the trusted temporary root before the secret fixtures
create their vault and declared paths. Production secure-path checks still refuse
symlink ancestors. The negative parent-symlink test now asserts the exact intended
parent is refused, so an ambient `/var` refusal cannot falsely satisfy it.

`da7c04c5` keeps all seven benchmark eval tests executable. Linux retains successful
existing-only open, ingest and tune assertions. On macOS the real command must
return the typed `UnsupportedPlatform(Data)` refusal, with no outcomes or weight
changes. The wrong-dictionary test compares durable data bytes before/after refusal
without reopening through a creation-capable door that could repair the manifest.
Equivalent C06/C13 fixtures were compared read-only; no other worktree was edited.
No production source, feature gate, test-ignore annotation or CI skip list changed.

Review census: paginated PR head lookup/search found no C11 PR and no remote C11
branch. Therefore no C11 bot comment/review/thread exists to gather or answer; no
quota failure was involved. PR933 is C08, not C11, and received no post. Original
internal review roots remain preserved. `target/w7-logs/c11-fixture-repair-review-`
`{ledger,disposition}.md` records source IDs and individual findings. Old compile,
clock, Android and snapshot findings were already repaired; informational API
wording/extra HTTP coverage suggestions do not change this fixture repair. The
public regeneration-completion port is for host projectors, not a promise of a
new autonomous generation worker.


Final internal finding IDs: `C11-FR-01/02` are fixed and runtime-validated above.
For `.w7/dag-server-review.md` (`C11-OO-01`), F1 is a stale compile allegation,
F5 was fixed in `01ddd11f`/`e4634cdf`, F2/F3/F4 are informational documentation and
naming requests, F6 is additional HTTP negative-case coverage rather than a
broken guard, and F7 expressly allows the behavioral pin. For
`.w7/ports-final-audit.md` (`C11-OO-02`), the clock residual was repaired and the
host-projector completion-port decision above resolves the worker-caller claim.
`C11-OO-03/04/05/06` are already closed/superseded storage-read, historical runtime,
Android and server-snapshot findings. Original roots and the pre-repair census
remain intact; no replacement review fanout was started.

### Terminal validation — source da7c04c529525bef4c03e9659bf458cea8a32ccf

All commands below exited zero on the MacBook with native macOS TMPDIR, two
compiler jobs and four test threads. No `.w7/test-env.toml` override was used.
The two focused receipts were consumed once, not rerun:

| Run | Passed | Failed | Existing ignored | Filtered |
|---|---:|---:|---:|---:|
| Featureless core `secret_` focus | 132 | 0 | 0 | 6,456 |
| Benchmark `eval::tests::` focus | 26 | 0 | 0 | 259 |
| Original nonfiltered six-package command | **9,129** | **0** | **21** | **0** |

The benchmark focus includes 25 eval tests and one `perf::retrieval::tests` name
that also matches the substring. Its typed `UnsupportedPlatform` stderr is an
asserted refusal, not a test failure. Focused counts overlap the full run and are
not added to it.

Both changed-crate lint commands passed with warnings denied:

```text
cargo clippy --locked -p oneiron --all-targets --no-default-features -j 2 -- -D warnings
cargo clippy --locked -p oneiron-bench --all-targets --all-features -j 2 -- -D warnings
```

The mandatory package set and test selection were unchanged:

```text
cargo test --locked --no-fail-fast -p oneiron -p oneiron-android -p oneiron-bench -p oneiron-driver -p oneiron-napi -p oneiron-server -j 2 -- --test-threads=4
```

| Package (all selected targets, including doctests) | Passed | Existing ignored |
|---|---:|---:|
| oneiron | 7,842 | 16 |
| oneiron-android (host Rust test) | 1 | 0 |
| oneiron-bench | 295 | 0 |
| oneiron-driver | 85 | 0 |
| oneiron-napi | 22 | 0 |
| oneiron-server | 884 | 5 |

Core library alone passed 7,089 tests; the benchmark binary passed all 285.
All 11 repaired secret tests and all seven repaired eval tests ran and passed.
The 21 ignored names exactly match the pre-repair factory run: four core library,
12 core integration and five checkpoint-dependent server tests. They are not
passes. No new ignore, skip, filter, `--include-ignored` override or retry was used.

The native oneshot chain `w7-c11-fixture-validation-chain-01.service` serialized
both lints and the full command under the existing host/ticket locks. It ended
inactive with `Result=success`, `ExecMainStatus=0` and all three stage-success
markers. No build or test job remains active for this repair. Exact commands,
source binding and per-package totals ride this change in
`impl-notes/W7-C11-fixture-validation.json`. Raw logs, split stage logs, all ignored
names and hashes remain in `target/w7-logs/c11-fixture-repair-terminal.json` and
`fixture-validation-chain-1.log`. Formatting and the code-map check passed after
the last Rust edit; the closing commit changes only notes and this receipt.

Prior 1,116 storage/server, four added featureless-test, consumer, Android NDK and
Kotlin API36.1 receipts remain unchanged and are not added to these totals. No
Android toolchain or device run was repeated. No factory state, launcher or gate
result was changed; the manager's normal retry remains the next factory action.
No PR exists for C11, so no GitHub explanation was posted to an unrelated PR.

## Guarded-host recovery — completed at cf9a9a46

The later MacBook 16-target failure cascade was host-wide POSIX semaphore
exhaustion, not a regression of the repaired fixtures. The owner's held-pair
probe could create only one semaphore; LMDB needs two. The installed dispatcher
now honors action-scoped `W7_CARGO_EXCLUDE_MACBOOK=1` before claiming that host.
This recovery used that guard, not force-local. Mini admission retained its
30 GiB reserve; all five commands ran through the existing Arch two-slot,
three-compiler-job maximum. Outer Cargo commands requested `-j 2`; runtime tests
used four threads and the ticket's existing `/home/lexi/w7-build/target/W7-C11`.
No capacity limit, shared configuration or factory result was changed.

The first guarded factory attempt exposed a different, pre-existing test-host
problem: `oneiron-napi` could not link its standalone Linux test executable without
Node-provided `napi_reference_unref`, `napi_delete_reference` and
`napi_call_threadsafe_function`. It ran zero tests. `cf9a9a46` adds the existing
upstream `napi` dynamic-symbol feature only under Linux **dev-dependencies**.
The normal dependency declaration, Rust sources, Cargo.lock and production
link contract are unchanged. Resolver-2 keeps this test feature out of normal
builds, verified by the actual production dependency graph and production-only
Clippy run. No custom symbol stub, disabled test or production fallback was added.
The Rust DTO/boundary tests are not a claim of executing a JavaScript Node host.
The old guidance that Linux cannot link these Rust tests is now stale for this
crate's dev configuration; existing workspace/CI exclusions were not changed.

The sole native oneshot chain `w7-c11-guarded-host-recovery-01.service` completed
all five stages at source `cf9a9a46b7e0c1658366914005a44c52a797d05b`:

| Stage | Actual result |
|---|---|
| Normal/build dependency graph, inverted at `napi-sys` | Exit 0; no `dyn-symbols` in production |
| `oneiron-napi --lib` Rust tests | 22 passed; zero failed, ignored or filtered |
| `oneiron-napi --all-targets --all-features` Clippy | Exit 0, `-D warnings` |
| `oneiron-napi --lib` production-only Clippy | Exit 0, `-D warnings` |
| Original nonfiltered six-package test command | **9,154 passed; zero failed; 21 pre-existing ignored; zero filtered** |

The last command retained the mandatory package set and selection:

```text
cargo test --locked --no-fail-fast -p oneiron -p oneiron-android -p oneiron-bench -p oneiron-driver -p oneiron-napi -p oneiron-server -j 2 -- --test-threads=4
```

| Package (all selected targets, including doctests) | Passed | Existing ignored |
|---|---:|---:|
| oneiron | 7,867 | 16 |
| oneiron-android (host Rust test) | 1 | 0 |
| oneiron-bench | 295 | 0 |
| oneiron-driver | 85 | 0 |
| oneiron-napi | 22 | 0 |
| oneiron-server | 884 | 5 |

The Linux core library passed 7,107 tests and its `it` target passed 481;
these are 18 and seven more than the earlier Mac targets respectively. This
accounts for the 25-test difference between the two host totals. All 18 former
secret/benchmark failures passed here too, so the eval fixtures exercised their
supported Linux success branches as well as the earlier macOS refusal branches.
The 21 ignored names exactly match the prior receipt; they are not passes.
No new ignore, skip, filter or retry was used. The separate 22-test focus overlaps
the full run and is not added to its total.

The unit is inactive with `Result=success`, `ExecMainStatus=0` and all five EXIT-0
markers. Raw logs, per-stage receipts, initial link-failure evidence and terminal
source binding remain under `target/w7-logs/guarded-host-*`. The compact receipt
rides this change in `impl-notes/W7-C11-guarded-host-validation.json`. No validation
job remains active. The closing commit changes documentation/receipts only.

Review ledger addendum: `C11-HR-01` is the owner's host semaphore finding, handled
by the authorized action guard, not by weakening storage checks. `C11-HR-02` is
the actual guarded Linux N-API linker failure, fixed and validated above. Original
`C11-FR-*` and `C11-OO-*` roots and dispositions remain intact. A fresh paginated
lookup of all PR states for `oneiron-dev:w7/W7-C11` returned no PR; there are no
C11 Qodo/Codex/CodeRabbit surfaces to answer. No unrelated GitHub post was made.

The Mac 9,129-test receipt, 132 featureless and 26 benchmark focused passes, prior
storage/server/consumer proofs, and Android NDK/API36.1 instrumentation remain
unchanged. In particular, the one host Rust Android test above does not replace
or rerun the native proof. The manager still owns the normal mandatory factory
retry; this seat did not write a synthetic pass or alter gate selection.

## Current mandatory-gate recovery — completed at c71d87f8

The latest factory run on the Mini reached runtime and failed two test targets.
This is separate from the earlier idle-125 capacity wait and from the retained
successful Mac/Linux runs. No old green receipt is used to close these failures.

- C11-CG-01: `type_boundaries_compilefail` failed before its ten compiler cases.
  trybuild forwarded both the Git-tag dependency and its local patch into a fresh
  project. The copied lock did not yet contain that new root. Cargo consequently
  had to resolve the tag offline before applying the patch. The Mini cached the
  audited commit, but not `refs/remotes/origin/tags/v0.6.11`. Commit c71d87f8 makes
  Sudachi a direct path dependency at the same version and removes the redundant
  outer Git patch. The upstream snapshot and Cargo.lock are unchanged. No cache
  warming, revision change, custom compiler harness or disabled case was used.
- C11-CG-02: `vault_read_parity::in_process_is_not_privileged` compared consecutive
  reads whose access-decay scores sampled different wall-clock seconds. The
  observed scores were 0.4159326 and 0.41593257. Commit d583201f reuses the existing
  `with_claim_learned_at(u64::MAX)` fixture so saturating age is always zero. Full
  serialized equality is retained; new assertions require one result and score
  1.0. Existing one-ULP sensitivity coverage remains. Scores are not rounded or
  normalized away, and production scoring/authorization code is unchanged.

The original review roots and dispositions remain intact. A fresh all-state,
paginated GitHub lookup for `oneiron-dev:w7/W7-C11` returned `[[]]`: no actual C11
PR exists. There are no C11 Qodo/Codex/CodeRabbit comments, reviews or inline threads
to gather or a valid PR posting target. No provider quota failure occurred and no
unrelated PR was posted to. Current failure evidence and the read-only diagnosis
remain under `target/w7-logs/current-*`.

### Fresh terminal validation

The sole chain `w7-c11-current-gate-repair-01.service` finished inactive/dead,
`Result=success`, `ExecMainStatus=0`, with all three EXIT-0 markers. All three
commands ran on Arch through normal dispatcher admission: slot 2/2, the existing
three-compiler-job ceiling, explicit `-j 2`, and four runtime test threads.
Action-scoped `W7_CARGO_EXCLUDE_MACBOOK=1` remained set. Real capacity-wait messages
were retained; no guard, shared configuration or factory result was changed.

| Fresh stage | Actual result |
|---|---|
| Both failing integration targets, featureless and offline | **10 passed**, zero failed/ignored/filtered |
| Core all-target featureless Clippy, `-D warnings` | Exit 0 |
| Original nonfiltered six-package command | **9,154 passed**, zero failed/filtered, **21 pre-existing ignored** |

The focused command ran all nine `vault_read_parity` tests and the single
`type_boundaries_compilefail` driver. All ten nested compile-fail cases passed;
they are not ten additional Rust tests. The regenerated trybuild manifest names
Sudachi 0.6.11 by its audited local path, with no Git/tag/rev edge. No Git cache was
warmed. The same two targets and all ten compiler cases also passed in the full
run. Focused counts overlap the full run and are not added to it.

```text
cargo test --locked --offline --no-fail-fast -p oneiron --no-default-features --test type_boundaries_compilefail --test vault_read_parity -j 2 -- --test-threads=4
cargo clippy --locked -p oneiron --all-targets --no-default-features -j 2 -- -D warnings
cargo test --locked --no-fail-fast -p oneiron -p oneiron-android -p oneiron-bench -p oneiron-driver -p oneiron-napi -p oneiron-server -j 2 -- --test-threads=4
```

| Package (all selected targets, including doctests) | Passed | Existing ignored |
|---|---:|---:|
| oneiron | 7,867 | 16 |
| oneiron-android (host Rust test) | 1 | 0 |
| oneiron-bench | 295 | 0 |
| oneiron-driver | 85 | 0 |
| oneiron-napi | 22 | 0 |
| oneiron-server | 884 | 5 |

The core library passed 7,107 tests. All 18 formerly repaired secret/benchmark
fixtures passed again. The 21 ignored identities match the retained prior run
exactly and are not counted as passes. No filter, new ignore or retry substituted
for the original command. The three existing heed dependency lifetime warnings
remain dependency diagnostics; the owning-crate lint succeeded without vendor
edits. This is the requested six-package validation, not a claim that the entire
workspace `scripts/verify.sh` ran.

The compact receipt is `impl-notes/W7-C11-current-gate-validation.json`, bound to
`c71d87f8e0caba6d8445d52c10f3fb6e17d4f5ae`. Raw stage logs, exact target counts,
ignored names, generated manifest and terminal evidence remain under
`target/w7-logs/current-gate-*`. The audited Sudachi code snapshot and Cargo.lock
are byte-unchanged from 135aeb90. Rustfmt was applied to the changed test file and
the code-map check passed after that edit. The closing commit changes only these
notes and the compact receipt; no validation job remains active.

Final ledger dispositions: `C11-CG-01` is CLOSED by c71d87f8 and `C11-CG-02` by
d583201f, with fresh focused and full runtime proof above. The original
`C11-FR-*`, `C11-OO-*` and `C11-HR-*` roots and dispositions are preserved. The
closeout paginated PR lookup again returned no C11 PR, so no external review
surface or lawful PR explanation target exists. No unrelated GitHub post was
made. The prior 132/26 focuses, storage/server/consumer and native Android
NDK/API36.1 receipts remain unchanged. The host Android test does not replace
that native proof, and the N-API Rust tests do not claim Node-host execution.

The manager still owns the normal mandatory factory retry. This repair addresses
the current two real Mini failures; it does not relabel the earlier idle-125 wait,
reuse an old pass as the new result, or write a synthetic factory pass.
