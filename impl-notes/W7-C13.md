# W7-C13 implementation notes

## Result and boundaries

This change implements the engine-local surface work below. It is **not full
cluster acceptance**. The real QuickJS guest/runtime/provider sources,
authenticated NOTE transport, and exact-keyed LangGraph binding are applied.
Core, server, world-scope, board and real QuickJS native tests now pass in the
scoped runs recorded below. The deleted-cursor/shallow-history NOTE regression now passes.
Final featureless, featureful and production lint checks pass. Installed SDK acceptance passed. The real QuickJS
artifacts build reproducibly with owner-provisioned tools. The
SDK dependencies were provisioned only in this ticket after owner authorization. Downstream V2 deployment
is outside this worktree. The docs mirror was read only. No product-specific
worker, migration, billing logic, unchecked NOTE importer or recall-based KV
emulation was added. Historical results below are evidence for their stated
source revisions only; the current ledger and continuation sections supersede
older scope/host-blocker descriptions.

## Per-step ledger

| Step / ticket | Implementation and evidence | Remaining work or boundary |
|---|---|---|
| 1 / ONE-2460 | Plugin tags retain the three-key NOTE ABI. A verified person installs the one blessed brief descriptor. Version/default decoding fails closed; unknown Plugin namespaces still round-trip. | Canon does not name concrete defaults; the pack pins disabled extraction, relationship-scoped context and durable retention without granting authority. |
| 2 / ONE-2461 | Loro documents carry markdown and two-pin citation/frontier/cursor/quote/hash data. Existing round-trip, drift and pinned-purge fixtures passed; authenticated canonical state now carries pins. | Exact source/claim dependency erasure and receipt-carrier fixtures pass. The featureless beyond-use/reopen/sweep fence also passes; no remaining named pin/purge fixture. |
| 3 / ONE-2462 | Briefs render fresh through Instrument with current scoped claim/source metadata. The final metadata repair passed all seven sync NOTE fixtures in owner-recovery-sync-note. | Transport/erasure integration and live metadata/redaction fixtures pass; no stored HTML was introduced. |
| 4 / ONE-2463 | Pinned Wasmtime 38.0.4 boundary, WIT world, generated host types/stubs/declarations, exact tier imports, fresh Store, deterministic host inputs, resource limits and no WASI. Component fixtures and byte-drift checks pass. | Boundary acceptance is complete; steps 5–6 qualify the real interpreter and host. |
| 5 / ONE-2464 | Real pinned QuickJS C interpreter guest, two-tier canonical component build, typed host bridge, deterministic clock/random, isolated Engine handles and real-JS fixtures are applied. | Both real component binaries and license are present with SHA-256/source/toolchain pins. Two clean output builds are byte-identical. Real language, typed writes, deterministic replay, foreign proposals and escape refusal pass; no WAT or native-JS substitute. |
| 6 / ONE-2465 | Per-vault McpQuickJsProvider binds the hash-verified runtime to durable EngineNativeExecutor; readiness-gated listings, resolved actor, same-run resume and single-flight are applied with a real-HTTP fixture. | Real HTTP execute/resume/terminal retry and concurrent isolation pass against the pinned artifact. Production hosts supply a backend and issued budget lease through McpQuickJsProvider; unconfigured admission now returns code_host_unbound, not the retired execute_code_unavailable. The updated refusal and real QuickJS resume pass their final wire tests. |
| 7 / ONE-258 | Respected the amended hold: no migrator, cutover, Convex teardown or ingest source. | Engine-host adoption remains held on billing design in the downstream product repository. |
| 8 / ONE-259 | No product-specific worker code was added to the engine. | V2 production wiring requires packages/protocol and workers/agent in the downstream repository, outside this worktree. |
| 9 / ONE-2466 | One escaped render_instrument entry takes bounded, validated closed atoms and resolves backing values at view time. Hostile atoms and golden rendering pass. | Full client widget rendering is not introduced; non-value atoms have safe enum-driven representations. |
| 10 / ONE-2467 | Closed atom-language interpreter accepts exactly ScopedRead, ResolveBackingRef and EmitAtom. A write import fails construction. | This follows ARCH-0048's typed atom language, not arbitrary JavaScript lens execution. |
| 11 / ONE-2468 | WorldScope::WorldSet now carries actual WorldAuthoritySet ids plus explicit base membership. CodebaseSet retains repository semantics. Lens scoped bytes, backing refs, filter and trace paths use the corrected distinction. | Ordinary-world and codebase distinction tests passed in recovery-integrated-tests3; no CodebaseScopeKey-only substitute. |
| 12 / ONE-2469 | Proptest byte fuzzing plus checked-in golden fixtures exercise atom decoding and rendering in the normal CI library suite. Unknown kinds reject and rendering does not panic. | No separate cargo-fuzz executable or long fuzz campaign. |
| 13 / ONE-2470 | Native Web SurfaceEvents validate identity and backing refs before a mediated write plan. Forged/foreign frames refuse; a positive dispatch fixture passes. | None in the named dispatch fixture. |
| 14 / ONE-2471 | Stamped semantic edits enter through MAC-bound NOTE_OPS and same-writer author/grant/policy checks. Canonical document rows, durable receipts/outbox and raw NOTE-update refusal are applied; immutable birth ABI remains three keys. | Authenticated cross-peer/socket fixtures pass. Replicas queue semantic edits while offline; canonical read-your-write waits for the single admission authority. |
| 15 / ONE-2472 | Canonical NOTE state carries stable cursor pins and persistent authorship across the C15-compatible registry/state transport. Two-vault and reopen fixtures are applied. | Native sync/socket and reopen fixtures pass; deleted cursor resolution after shallow compaction now passes its native regressions. No unchecked stamp import or local-only pin sidecar. |
| 16 / ONE-2473 | Local and remote own/reverse cited spans use the same reviewed candidate/gate path in the committing writer. Free prose applies as document ops. Receipt and erase replay regressions are applied. | Reviewed gate, receipt selector and erased queued-receipt fixtures pass in the two integrated/selective runs. |
| 17 / ONE-2474 | Existing L2 producer/cache/delta fixtures and featureless checks passed. Owner recovery completed seven sync NOTE and fourteen proper benchmark-binary fixtures. | Producer/cache/delta acceptance passes. Final featureless, touched-crate featureful and server-production Clippy all pass; the old mini ENOSPC is historical. |
| 18 / ONE-1442 | Vercel three-verb tools are present. Real actor/class-bound gated CLAIM key-value APIs now project through shared remote/native SDKs; oneiron-langgraph implements actual BaseStore batch/abatch. Generic leak/overwrite guards and native StateGraph fixture are applied in 0d21ac7d. | Engine/HTTP KV security fixtures and 15 real BaseStore interface tests pass. The real StateGraph and clean-install artifact qualification both pass. Owner-authorized ticket-local Python/Node provisioning now provides LangGraph, pytest and maturin. Native wheel/module construction, clean installs, both constructor quickstarts, and framework tests pass. The real AI SDK generateText/tool loop passes under Node with deterministic provider replies and real native memory. A live external provider call still needs its own credential, not an engine code change. |
| 19 / ONE-1543 | llms.txt indexes a generated full-fidelity Memory Wire mirror, stable typed errors and complete JS/Python snippets for both constructors. Four snippet identities and generator checks pass. | Installed-native execution of all four README snippets passes against the actual npm archive and wheel. |
| 20 / ONE-2475 | Memory Wire naming, complete taught wedge and generated full-facade census now cover nine actual exported verbs including keyed methods. CI retains measured clean-install quickstarts. | Both clean installs and embedded quickstarts took 13.35 seconds. Native artifacts, full TypeScript build and the nine-verb JS/Python export census pass. |
| 21 / ONE-2476 | ACTIVE/OFF world producer, base-world handling, owner exclusion before projection/counts, additive off overflow and count shedding. Fixtures pass. | None in the producer acceptance. |
| 22 / ONE-2477 | Core hydrate/query/context-pack and MCP memory doors stage typed served/body observations in server-owned actor/session state. Status-only scoped lifecycle resolution feeds existing RESIDENT/keyframe/delta riders without unsolicited push. | All five host router fixtures and integration compile pass. No source text parsing or process-global observer store. |
| 23 / ONE-2478 | Host get/body reads feed session-loaded skills; typed capability hits feed turn-found rows. Existing-frame riders and shedding fixtures are applied. | Category-aware memory/SKILL/AGENT collection now filters before each channel limit under one read transaction and one telemetry record. Starvation/reverse-pressure, caller type restriction and host fixtures pass. |
| 24 / ONE-2479 | Typed AGENT_DEF hits feed turn-local Cand beside child/peer. Candidate rows shed before memory snippets and are not saved in the read set. | Pre-topk and native board acceptance pass with step 23. |
| 25 / ONE-2480 | Stateless ROOM reads current channel membership, presence, ordinary world-authority meet, composed posture and peer-readable claims. rooms.list/messages/speak/claim use real verbs. Membership rechecks in both committing write transactions; deterministic revocation tests pass. | Presence remains explicit host input; no independent room authority type or cached claim snapshot. |
| 26 / ONE-2481 | Postcommit per-vault queue broadcast drives run-scoped, core:read SSE. Subscribe-before-snapshot, lag refresh and unchanged-tree suppression. Actual subscribed mutation and existing snapshot fixtures pass. | None in the named observe acceptance. |
| 27 / ONE-2482 | Typed redirect placement preserves payload, validates parent/run/cycle, fences/requeues worker moves and records one idempotent operator event. Retry and successor placement preserve predecessor lineage. API/queue/retry fixtures pass. | None in the named redirect acceptance. |
| 28 / ONE-2483 | Entity-document dependencies replace wildcard window membership. Materialization preserves per-key diffs; canonical edge endpoints invalidate both entities. Membership probes cover empty/new/deleted rows. Production disjoint-subscription and bridge fixtures pass. | Governance invalidations are deliberately conservative. This does not replace the underlying sync transport. |
| 29 / ONE-2486 | Engine-generated eight-row read/nav/ask census, native schemas and 1:1 tool bindings. Actor-scoped execution, narrow-connector refusal, typed runtime errors, stable sorted listings and retired-name rejection all pass. | Deferred engine methods retain their real typed RuntimeUnavailable result, rather than pretending to execute. |

## Decisions and canon corrections

- NOTE/brief/editor: `oneiron/core/oneiron-arch-0032-agentic-notes-v1.md`.
  The contract omits concrete brief defaults; the conservative pack choices
  above are policy seeds, not grants. Stored HTML is not a NOTE body.
- Sandbox/tool projection:
  `oneiron/agents/oneiron-arch-0028-mcp-external-agent-surface-v1.md`.
  Checked-in `oneiron:code-run/guest@1.0.0` is the source for imports and JS
  declarations. Wasmtime is optional behind `code-sandbox-wasmtime`; generated
  whole-world linkers are not used to accidentally grant foreign writes.
- Lens/ROOM/observe: `oneiron/surfaces.md` and
  `oneiron/surfaces/oneiron-arch-0048-genui-protocol.md`. The prior repository's
  CodebaseScopeKey-only WorldSet was narrower than the general world-set prose;
  the implementation now distinguishes real world ids from codebase ids.
- L2: `oneiron/agents/oneiron-arch-0026-dreamer-v2.md`. The digest covers
  canonical evidence bytes, not just rendered text. Confidence/evidence
  changes can change the hash, but neither ranking scores nor confidence
  values enter the rendered base. Cache locks cover only lookup/publication;
  fresh hydration revalidates authority and evidence under its read snapshot.
  Erasure fences prevent an older reader from repopulating erased evidence.
  Unstamped Tier-A evidence is correctly withheld despite an allowlist; the
  positive HTTP fixture labels evidence public and also tests a private rewrite.
- Wire: `oneiron/surfaces/oneiron-arch-0044-public-surface-ws.md`. “Full facade”
  means the actual nine-row FACADE_VERB_CATALOG in oneiron-remote
  (the original four plus five keyed-memory verbs), not every internal engine method.
  npm reported 22,696,952 `ai` downloads for 2026-09-10–2026-09-16; PyPIStats
  reported 47,362,821 `langgraph` downloads in its last-month window at pickup.
- Board: `oneiron/agents/oneiron-arch-0067-context-board-v1.md`. Tracking is
  session-owned, never process-global. Capability discoveries use the turn
  clock; loaded bodies use the session clock. Riders are not new push traffic.
- Reactivity: RD-23 v86 and CROSS-ARCH-0020. Read sets and membership probes
  are distinct; the old dropped-diff queue metadata had to be repaired for
  entity-granular invalidation to reach actual subscriptions.
- Downstream: `cross/infrastructure/cross-arch-0021-eiri-live-wire.md`. The
  held migration prose must not be interpreted as authority for a cutover.

## Historical NOTE transport prerequisite (superseded by applied transport)

This branch's sync schema carries entities, edges and tombstones, not Loro
entity-document operations. ONE-2540, ONE-2541 and ONE-2542 own the missing
registry and transport. A Loro commit's actor text is not peer authentication.
No unchecked snapshot importer was added merely to pass a two-peer fixture.
Integration must co-commit document ops, reverse pins and outbound intent;
apply selectors to cited sources; preserve admitted provenance; and reject
forged stamps, unauthorized edits, citation bypass and erasure replay before
publication. Raw immutable NOTE birth bytes remain the identity; live body
reads project the admitted document under their current transaction.

## Validation evidence

These are scoped results, not a full workspace VERDICT. Earlier failing fixtures
were repaired and rerun. Latest-change validation is stated separately below;
no incomplete or disk-full run is counted as green.

- `.w7/note-tests7.log`: five NOTE tests passed with sync,test-hooks.
- `.w7/surface-tests.log`: 324 passed in the broad surface run. The two
  failures were repaired, not waived; see corrective runs below.
- `.w7/surface-tests3.log`: 18 passed, including production disjoint live
  queries, canonical edge dependencies, cross-document cited-edit mediation
  and hidden-citation redaction. Its outdated OpenAPI snapshot was fixed.
- `.w7/wasmtime-tests2.log`: 31 passed; OpenAPI, SurfaceEvent and source-erasure
  views passed. The one invalid WAT type fixture was corrected.
- `.w7/wasmtime-tests3.log`: five passed, including the corrected component
  fixture and atomic changed-rider replacement.
- `.w7/l2-memory-tests2.log`: all six selected MCP tests and the actor-clamp
  fixture passed. Reserved test seed and public-evidence fixture failures were
  fixed without loosening production admission.
- `.w7/l2-memory-tests3.log`: all six L2 producer fixtures plus OpenAPI passed.
- `.w7/integrated-surface-tests2.log`: all 11 passed: NOTE live reads/reopen,
  concurrent ops, pins/proposals, ROOM scope and commit-time membership
  revocation, base-world exclusion, redirected retry lineage and HTTP L2.
  The preceding integrated run lost its handle without a terminal log; it is
  not counted as evidence. This successful rerun used the factory wrapper's
  automatic Arch fallback, not a forced local Cargo invocation.
- WIT drift check, three dependency-free generator fixtures and the Node guest
  stub fixture passed. Memory Wire mirror/snippet checks and two AI-SDK adapter
  tests passed. Installed-native quickstarts were not run.
- `target/w7-validation/cargo-test-l2.log`: native Cargo passed seven core
  L2/clamp tests and two server tests (including HTTP context-board L2).
  Correction to the initial report: the combined `--lib` command did **not**
  test the binary-only `oneiron-bench` package. Its proper binary command is
  listed below and remains blocked.
- `target/w7-validation/durable-retry-featureless-clippy.exit`: **0**.
  `cargo clippy -p oneiron --all-targets --no-default-features -- -D warnings`
  passed on the MacBook, with only the existing vendored heed lifetime warnings.
- `target/w7-validation/durable-retry-featureless-note.exit`: **0**.
  `cargo test -p oneiron --lib --no-default-features note:: -- --test-threads=2`
  passed all three tests. The new test proves NOTE birth-ABI validation runs
  without sync, rejects malformed NOTE bytes, and leaves non-NOTE bytes alone.
- The NOTE reader validates the birth ABI in both modes. No test was cfg-disabled
  to make the featureless lane pass. No lint suppression or ratchet bump was added.
- The final brief repair derives confidence and lifecycle from the exact claim
  bytes admitted by ScopedRead, rather than reading the raw ledger a second
  time. This avoids disclosing metadata from a later, no-longer-readable row.
  Stored reserved predicates use the same decoder flag as ScopedRead. The
  missing decoder argument found by the first compile was fixed in `04528ea3`.
- Known style findings were fixed: redundant L2 clones, a map/unwrap expression,
  two ROOM statements, and the Wasmtime supersede macro-arm semicolon.
  Featureless Clippy covers its applicable fixes. No complete featureful
  all-target Clippy pass is claimed.
- Pinned rustfmt checks passed on the final five changed Rust files. Code-map
  checks passed after the final source change (2535 files, 16 current artifacts).
  Whitespace checks passed. Structural ratchets remain at 2 giant files /
  98 allow attributes / 91 print macros / 33 process-global statics; the root
  surface remains 701 names. Final policy rechecks are recorded with the commit.

## Exact validation blocker and resume commands

The last three native commands all terminated with exit **101** on the Mac
mini at `/Users/olety/w7-build/wt/W7-C13`, with **No space left on device
(os error 28)** while creating fingerprints, metadata or the engine archive.
They did not produce test results. `/System/Volumes/Data` was 100% full at
inspection; the MacBook’s `/Volumes/Cinema` had 1.4 TiB available. The unchanged
factory wrapper selects an available mini slot when MacBook slots are occupied
and returns its build failure; it does not retry that failure on another host.
No routing override, target relocation, cache deletion, installation or factory
change was made. The shared-host owner must restore space on the mini before
these commands can be reliably completed through the prescribed wrapper:

```sh
cargo test -p oneiron --lib --features sync,test-hooks note:: -- --test-threads=2
cargo test -p oneiron-bench --bin oneiron-bench beam::tests_judge_cost -- --test-threads=2
cargo clippy -p oneiron -p oneiron-server -p oneiron-bench --all-targets --features oneiron/sync,oneiron/test-hooks,oneiron/code-sandbox-wasmtime -- -D warnings
```

Final receipts are `target/w7-validation/durable-sync-note-fixed.{log,exit}`,
`durable-bench-fixed.{log,exit}`, and `durable-touched-clippy-fixed.{log,exit}`.
All detached validation jobs reached terminal exit files; no validation worker
remains queued. Detaching the ordinary wrapper commands was necessary because
this client-owned writer’s round changes interrupted native handles. It did not
bypass host order, slot limits or the per-ticket lock. Logs live under `target/`
because the wrapper excludes it from remote sync-back; `.w7` is not excluded.
The temporary final review child did not return a report and is not claimed as
review evidence. The scoped-metadata finding above came from direct source review.

## Remaining contract scope — not a DONE claim

- QuickJS WASM component and production resumable `execute_code` (steps 5–6).
- Downstream V2 worker/protocol wiring (step 8); step 7 remains expressly held.
- General world-ID lens scope, distinct from CodebaseScopeKey (step 11).
- Authenticated production NOTE text-plane integration (steps 14–16); the
  local operation/cursor primitive is implemented, not the missing transport.
- LangGraph BaseStore over a real exact-key/delete/namespace binding contract
  (step 18), plus live provider and installed-native quickstart evidence (18–20).
- Complete host get/serve/capability event plumbing for session board producers
  (steps 22–24). The core producers and their named fixtures are implemented.
- Latest sync/benchmark/runtime-lint validation after host storage is restored.

The per-step ledger above records the narrower delivered behavior and remaining
limits, including physical quote-copy erasure and the renderer’s enum-driven
widget representations. None of these gaps is hidden behind a successful
scoped test or a factory-wide acceptance marker.


## Owner-directed continuation from `1477420a`

The owner rejected the proposed split as completed scope and ordered the in-repo
remainder implemented. The historical mini ENOSPC section above is retained as
an incident record, not a current ticket blocker. New commands use the installed
wrapper without routing overrides, installs, storage changes, or duplicate jobs.
`target/w7-validation/owner-recovery.json` records current command/result custody.

- The normal wrapper completed the previously blocked sync NOTE command: **7
  tests passed**, including the final brief scoped-metadata repair. Evidence:
  `owner-recovery-sync-note.{log,exit}` (exit 0).
- The proper benchmark **binary** command completed: **14 tests passed**.
  Evidence: `owner-recovery-bench.{log,exit}` (exit 0).
- Featureful touched-crate Clippy reached source checks and reported five
  server-test style errors (four hexadecimal groupings and a local import after
  statements), not ENOSPC. The literals keep the same numeric values; the import
  is now module-scoped. No suppression was added. Its initial recovery receipt
  is exit 101; a green rerun is not yet claimed.
- Step 11 now uses `WorldScope::WorldSet(WorldAuthoritySet)` for ordinary world
  IDs and explicit base membership. Repository membership is separately named
  `CodebaseSet(CodebaseScopeKey)`. This deliberately corrects the pre-release
  public enum rather than disguising world IDs as a codebase key. Existing
  repository callers were updated. Filter configurations borrow the scope;
  canonical trace hashes include the ordered ordinary world set. Lens frames
  evaluate the exact body admitted by `ScopedRead`, and backing refs use that
  same restriction. New tests cover two disjoint ordinary worlds, base and empty
  membership, forbidden backing refs, and the shared retrieval filter. These
  source changes are applied; `general-world-set-tests` is the pending focused
  native validation, not a pass claim.
- Current C15 document registry/transport and C04 runtime boundaries were
  inspected read-only as the owner directed. Implementation proposals for NOTE
  authenticated text admission, the real QuickJS component/runtime, and exact
  namespaced long-term-memory bindings are being developed independently under
  `target/w7-validation`. They are not counted as applied or accepted code.
- Step 7 remains explicitly held. No external migration, deployment, credentials,
  or downstream repository edit has been authorized or attempted.


### Board host integration in progress (not yet validated)

- Replaced the process-global MEMORIES cursor singleton with a mutex on each
  `SyncServer`. Cursor/read-set keys encode the principal/session tuple, not a
  vault pointer or ambiguous colon concatenation. Existing bounded eviction also
  drops the matching session read set; epoch changes do not clear it.
- Added scoped lifecycle observations over the exact served body. A unique
  readable immediate `Supersedes` successor supplies `changed.to`; an unreadable
  or ambiguous successor is omitted. Skill versions come from the typed body,
  and metadata-only rows do not mark a skill loaded.
- Core single/batch hydrate now accepts optional `session_id` and records real
  body observations. Batch changes stage until the whole response succeeds.
  Context-pack rows co-update their read set with the cursor. The HTTP board
  response now resolves `changed` and the session `loaded` line from this state.
- Shared bearer identities never acquire a read set. The production observer
  refuses a new id after 4096 session rows instead of silently dropping reads.
- Still pending: capability-channel partition/admission and real turn hits,
  query and MCP memory event plumbing, RESIDENT/STREAM riders, endpoint tests,
  and compilation of these new board edits. A new real-ledger unit fixture is
  present but has not run. No host-integration acceptance is claimed yet.


## Step 18 recovery: actor-owned keyed long-term memory

The proposed `memory/key_value/` surface stores canonical CLAIM revisions,
not record type 107 or a raw KV side database. Identity includes the bound
actor ID, actor class, literal namespace segments and exact key. It is
explicitly worldless. There is no loose `world_ref: null => all` selector and
no new WorldScope declaration. The five `key_value_*` verbs are projected
through the shared backend, authenticated HTTP facade, both native clients,
and both SDKs. The root surface pin stays unchanged.

The recovery context packet was not an approved design. Its raw globally
actor-unscoped IDs, unguarded overwrite, structural payload storage, arbitrary
owner erasure and unchecked type-byte proposals were rejected. There is no
new registry type, raw storage endpoint, or MCP projection here.

The existing public `claim_upsert` is NOT composed naively: it supersedes an
existing active claim even when the replacement must park as proposed.
BaseStore cannot return success for that result. Keyed put therefore composes
the SAME gated ClaimCandidate/apply machinery and canonical supersession
primitive in one actor-verified transaction, with no proposed fallback. A
policy refusal aborts the whole transaction, including candidate, pending
consent, supersession and receipts. This is the same strict synchronous
choice already used for typed expression preferences. Source defaults to
`generated`, not `observed`; source trust and supersession protections stay
with the existing engine gate. Generated output cannot impersonate user
statements merely because a framework called put.

Delete composes the canonical retraction primitive after actor/class,
subject, envelope and exact scope are checked in the SAME writer snapshot.
It is not `safe_delete`, does not grant owner authority, and preserves
historical records. Reads use the existing ClaimOf index and fail loudly on
its work ceiling. Conflicting synced heads fail `INVALID_STATE` rather than
silently choosing a winner. Canonical demotion metadata does not turn exact
key access into ranked retrieval or expiry. No sync format changed.

Request IDs are domain-separated and actor/class/address bound. Full stored
identity, source and payload are checked before an identical replay returns.
A stale replay after replacement/retraction cannot resurrect a prior key.
The generic facade claim commit/upsert door rejects this typed predicate and
checks the STORED predicate inside its writer transaction, preventing a
caller from overwriting a keyed revision by disguising its incoming predicate.
This is an app-tier binding boundary, not a new ACL over the privileged raw
Vault/BatchBuilder systems surface. Those surfaces retain their existing
host-trust contract; this proposal does not widen them.

`oneiron-langgraph` subclasses the REAL `BaseStore` and implements
`batch`/`abatch`. It does not override the public convenience methods, import
checkpoint savers, open/rebind a handle, send HTTP or emulate exact get using
recall. The whole batch is preflight-validated, then dispatched in order.
Batches are not atomic; engine errors pass through and earlier successful
operations remain committed. Unsupported TTL, vector index fields, semantic
queries, wildcard namespace patterns and operator filters fail explicitly.
Search supports exact top-level field equality and exact namespace prefixes.
Namespace listing supports prefix/suffix, depth truncation, deduplication and
pagination. The README contains a runnable StateGraph sample.

The live SDK export censuses derive method names from the Rust catalog and
therefore cover the additional five methods automatically. The explicit
Rust expected catalog was updated. Added behavior tests cover engine gates,
actor/class isolation, disguised generic overwrite, replay/deletion,
namespace paging, demotion, authenticated HTTP scopes, native JS/Python,
installed remote parity and actual BaseStore operations. Wire CI includes
the new satellite and installed parity test, without publishing any artifact.

Validation for the guarded proposal: Python syntax parsed and every original
file hash checked. No cargo, build, install, test runtime, formatter, codemap
writer, toolchain or git mutation was executed by the proposal author.
The integrating root owns serialized native compilation and runtime evidence.
After integration regenerate the code map and SDK agent mirror, then run
focused all-features/featureless engine tests, server tests, remote tests,
SDK typecheck/native tests, satellite tests and installed remote parity before
the required full gate. External application cutover remains HELD.


Root integration status: the guarded keyed-memory proposal is now applied. Native compilation and behavior tests have not yet run.


Validation continuation: the first general-world native build reached the
compiler and found one test using a private backing-ref field. The test now
uses the existing `target()` accessor; no visibility was widened. The focused
combined world/observation/keyed-memory test command is now pending under
`target/w7-validation/recovery-focused-tests.*`. The five original server
Clippy style repairs are committed in `fb2db678`. Keyed-memory Rust formatting,
Python syntax compilation, agent-mirror generation, code-map generation and
whitespace checks pass. These tooling checks are not native behavior evidence.


## Authenticated NOTE text plane continuation (not yet validated)

## Implementation

- Ports the actual C15 canonical `sync/documents` registry, storage, v9 document/batch codecs, manager API, socket handlers, client/connection plumbing, sweep and Doc reactive notices. Carries the necessary `BroadcastPayload` enum hunk in `server/core.rs`; preserves C13's memories-cursor field. C13's entity-level window invalidations stay intact.
- Does **not** port unrelated C15 custody-dial, federation confidence, ImportTier or replay changes. These are not dependencies of the document seam. Does not introduce a second protocol or retain `e:note:`. No compatibility migration is added.
- NOTE birth comes from the immutable ledger row and seeds deterministic Loro IDs. All editable NOTE state uses `d:e:` plus the canonical registry's key family. Snapshots include validated `pins` and persistent `authorship`, not a local-only pin sidecar. Reverse indexes are derived atomically on every admitted import/write.
- Adds `NOTE_OPS=4` and `NOTE_RECEIPT=5` inside C15 document framing. Commands contain an idempotency id and typed Edit/Cite operation, never a caller-authoritative actor field or raw Loro bytes. Upstream raw NOTE UPDATE/STATE and generic `EntityDocument::edit_text/import` refuse. A replicated ledger overwrite cannot change NOTE birth author or markdown at the batch put chokepoint.
- The real `/ws` upgrade authenticates an owner-grade transport credential, **not an actor**. Therefore NOTE REQUEST/NOTE_OPS additionally require existing MAC-verified `auth.bind`: core:read, core:write for mutation, registered principal, actor class and revocable jti. Selector member must equal that principal. Each stored subscription binds that selector/grant; every mutation rechecks its live grant, role and principal in the committing writer. A selector cannot name another writer.
- Core `Memory::admit_note_operation` receives the already verified host identity through the existing Memory binding. Its host contract is documented. No credential is recovered from a Loro message. The same LMDB writer rechecks actor binding, note author/owner, grant/pact/selector, token revocation, policy, erasure, pin sources and citation guards. Remote cross-author writes require an active human authority binding plus an Owner grant; the embedded unrooted-owner fallback is not accepted as remote authority.
- A cited-span edit uses the existing gated `ClaimCandidate`/`WriteEnvelope` path with Proposed approval, recording a real gate receipt **inside that same writer**. It does not call a wrapper that opens a nested writer. Free prose merges at the supplied base frontier and checks the resulting merged spans before persistence. The proposal leaves the NOTE unchanged.
- Local canonical writers publish invalidations only after commit. Retained NOTE registry handles refresh from canonical storage. They cannot return stale erased content or overwrite a newer Memory edit. Sweep delegates live NOTE history retention to the citation-aware purge door. Erase deletes canonical snapshots/VVs, update/admission/subscription rows, semantic outboxes, receipts and both pin indexes.

## Explicit authority interface and limits

A subscribed NOTE has **one admission authority**. This is the safety boundary needed because raw C15 updates contain no authenticated actor.

- On a replica, `DocumentRegistry::submit_note` queues a semantic command in `qn:e:` and sends it on the existing socket. It does not speculatively mutate canonical text. Reconnect replays commands after the state exchange. The queue clears only after the received canonical frontier covers the Applied receipt (or a reviewed Proposed receipt arrives). Results persist in `nc:e:`.
- `SyncClientConfig.note_session = Some(NoteSyncSession::new(actor_slip))` explicitly opts into the configured server as NOTE authority. The client uses the current C13 MessagePack app envelope for auth.bind, not a parallel JSON RPC dialect. It accepts NOTE state only after the bind reply and for a persisted subscription. It requires WSS, or numeric loopback WS for same-host use. A remote cleartext endpoint is rejected. The existing owner upgrade credential is still separate.
- The client-only, crate-private canonical importer accepts admitted authority state, keeps pins and provenance, preserves existing local operations, and refuses citation drift. It is not callable by the upstream server handler. A subscribed replica refuses direct local Memory edits and cannot itself admit NOTE_OPS as a second authority. This prevents a replica that has not received every citing document from silently accepting a cross-citation edit.
- Facet-narrowed NOTE selectors currently fail closed. The canonical ledger admits NOTE as un-faceted, and transactional NOTE admission does not guess a one-hop facet closure from an older window snapshot. Empty-axis pact semantics remain fail-closed. World/band/grant checks and citation-source/claim disclosure checks run transactionally. Export withholds the entire NOTE if any retained citation would disclose a source or claim outside its selector. Coreference citations require their separate consent export door and are refused here.
- Existing v9 document clients keep their non-NOTE behavior. NOTE clients must enable the new authenticated semantic-operation lane; raw legacy NOTE deltas are deliberately not accepted. Deploying NOTE_OPS-aware peers together and configuring actor slips/TLS is deployment work, not an in-repo engineering blocker.
- Birth ABI carries author_ref but no authenticated actor class. Deterministic birth provenance says `ledger` for that class; actual subsequent operations carry the credential's verified class. Local brief creation records its actual writer separately. Loro actor strings never grant permission.


The supplied core/socket fixtures are now applied but have not run. No source hash or proposal review is counted as acceptance.

## QuickJS production continuation (not yet validated)

The real interpreter C guest, pinned source build, typed bridge, two-tier artifact path, Wasmtime adapter, verified runtime factory and per-server production provider are now applied. See `impl-notes/W7-C13-quickjs.md` and `components/code-run-quickjs/README.md` for exact ABI, artifact pins and build instructions. No compiled QuickJS artifact or native runtime pass is yet claimed. The authenticated host provider requires an actual LLM backend and issued budget lease; startup does not invent either.


## Capability and validation continuation

- Capability candidates now partition from the unified retrieval results before
  the memory cut. SKILL and AGENT_DEF each have an independent five-row cap;
  unused capability capacity is not donated to or taken from memory. SKILL
  requires approved/auto plus active; typed AGENT_DEF candidates do not require
  approval/enabled status. Current scoped bytes revalidate labels before serving.
  `ContextPack.capabilities` and the HTTP carrier preserve typed ids; MEMORIES
  and its edge-neighbor budget exclude these discovery records.
- The HTTP board projects found/cand from this turn's hits and loaded/changed
  from its actor/session read set. Plain context-pack requests can name that
  session. The changed rider uses a status-only scoped history door: ordinary
  body reads still suppress superseded/retracted claims. No lifecycle metadata
  is emitted for erased, hidden, ambiguous-successor or keyed-private rows.
- The preceding integrated build found three source errors, all repaired:
  SkillLifecycle naming, explicit hexadecimal capability-id serialization, and
  owned erasure keys before mutating the writer. The replacement selected run
  is `target/w7-validation/recovery-integrated-tests.*`; no pass yet.
- A read-only keyed-memory review found generic-read disclosure and an ingest
  overwrite path, plus duplicate-stamp and deleted-shell edge cases. These are
  being repaired before the new store is accepted; isolated KV tests alone do
  not prove confinement across generic doors.
- Read-only MacBook and local probes found clang but no configured WASI SDK,
  wit-bindgen or wasm-tools. The pinned QuickJS build tools have not been
  installed. Source/runtime integration is separate from artifact acceptance.


## Applied review repairs (native validation pending)

The keyed-memory confinement repair is applied. One shared predicate excludes
keyed claims from generic app-tier views for every actor; the exact keyed API
retains actor/class ownership. The typed ingest path checks incoming and stored
predicates inside the committing writer, not in a separate precheck. Adversarial
fixtures force indexed/graph reachability and disguised overwrites. See
`impl-notes/W7-C13-key-value-security.md` for source/erasure/replay decisions.

Core query and MCP memory reads now stage served/body observations through the
server-owned session store. MCP session keys include actor, class, credential
fingerprint and registered scope. Loaded/changed riders decorate existing frames
only. Found/cand remain turn-local typed results. The five host router fixtures
are applied, not yet run. A remaining pre-topk starvation defect in the initial
capability partition is being repaired; separate final quotas alone were not
sufficient when the search channel itself returned only one skill.

QuickJS runtime handles now own separate Engine epoch counters. Memory refusal
traps, C allocator bounds, closure isolation, prototype-independent file proposal
validation and typed inert-proposal validation are applied. The actual interpreter
concurrency fixture and foreign hostile-path fixture await a real built component.
The canonical WIT remains unchanged. No WAT/native-JS substitution is used.

The LangGraph suite now includes a real StateGraph using the native Oneiron
handle for get/put/search/namespaces/delete, in addition to interface adaptation
fixtures. The inspected project Python environments lack the installed SDK,
LangGraph, pytest and maturin. No dependency or tool was installed. One workflow
fixture command failed at pytest import, executing no tests. Bun transpilation,
WIT drift, README snippet identity, source formatting and static policy checks
pass; none proves installed-native runtime or the five-minute clean-install gate.


## NOTE transport review repair

Malformed or divergent replicated NOTE births now use the quarantine-and-continue
path, with a valid-sibling/replay fixture. Non-NOTE socket UPDATE imports now use
`import_from_peer`: role, grant, selector, scope, expiry, pact and facet authority
are checked in the same writer that appends the document update. Privileged local
embedding imports remain separate. NOTE operation delivery queues one lightweight
state notice before its receipt; the existing delivery door reauthorizes the
actual state. Covering idempotent receipts remain valid, uncovered receipts retain
the outbox, and desired remote subscriptions are not pruned based on a missing
local grant. The review's contrary suggestions would weaken those invariants.
The guarded repair and regression fixtures are applied; native checks are pending.

## Category collection correction (owner continuation)

The first capability projection partitioned only after channel top-k, so a
`search_text(..., 1)` could return only SKILL and starve the memory lane.
`pipeline/execution/channels/categories.rs` now collects memory, SKILL and
AGENT_DEF independently on the same read snapshot. Caller admission, world,
corpus and type predicates still apply. Each capability kind has five slots;
capabilities do not seed or spend memory PPR expansion. Predicate-filtered
BM25 selection and indexed-population scope filtering for vector/temporal
happen before the per-lane cuts. This is not a fixed overfetch multiplier.
The public request still emits one retrieval record. Tests now use a channel
limit of one, both directions of category pressure, an explicit type filter,
one-record accounting and a later skill lifecycle change.

The first integrated native attempt reached the MacBook and failed on two
new compile errors (missing `RerankOptions: Clone`, and a retry function
pointer specialized to a different closure type). Both are repaired.
`recovery-integrated-tests2` owns the single replacement build. Its durable
log/exit/pid are under `target/w7-validation`; a waiting wrapper is not a test
failure or ticket blocker. The NOTE derived-erasure proposal is independent
and has not yet been applied.

## Exact copied-citation erasure

`note/{pin_index,citation_erase,citation_scrub}.rs` closes the derived-carrier
gap. Source **and claim** indexes locate only their citing NOTE documents and
pending citation commands. Hard erasure removes those pin entries in the same
writer and rebuilds StateOnly text state, retaining unrelated prose, surviving
pin cursors and authorship. Old full-view receipts and document history cannot
restore removed quotes. A persistent dependency fence rejects poisoned
upstream state, and a bounded authority-VV rebase allows safe synchronization
after local erasure. There is no whole-vault substring scan and no deletion
of a citing document just because one pin was erased.

Featureless reads fence the exact affected document without throwing away
its edited carrier. The ordinary hard-erase sweep remains incomplete until a
sync-capable sweep replays every committed update and performs the precise
scrub. Its final receipt cannot claim completion while that obligation remains.
ARCH-0032 ordinary drift still retains quote text. ARCH-0038 hard erasure
supersedes that retention only for exactly tracked dependencies. The old gap
was in this implementation, not in the canon. New tests cover source and claim
erasure, two pins, reopen, stale/new poisoned authority state, surviving edit
and purge behavior, featureless fencing, and ordered-update recovery.

The guarded 16-file proposal was applied only after every original SHA-256
matched. Parser formatting passed in the proposal, but these new tests are
not yet reported as compiled or green. `target/w7-validation/brief-erasure-applied.json`
records the exact proposal digest and paths.

## Memory Wire JSON boundary check

The MacBook's installed TypeScript compiler ran the real package's
`tsconfig.json` with `--noEmit`. All six input hashes matched the local source.
The command returned 2 solely because the project lacks `node:module` type
declarations and `ImportMeta.url` (the absent development type packages); it
is not a passing package typecheck. Receipt: `memory-wire-tsc.log`, with input
hashes in `memory-wire-tsc-source.json` under `target/w7-validation`.

Inspection exposed a separate value-loss bug: `JSON.stringify` silently
converted Map/Set values or called `toJSON` before the existing replacer could
validate them. The private keyed DTO adapter now copies finite JSON primitives,
plain objects and dense arrays once before encoding. It rejects non-JSON
prototypes, custom serializers, symbols, cycles, sparse arrays and undefined
values except the explicitly optional top-level request fields. Repeated
references without cycles and null-prototype JSON objects remain valid. No
storage, policy or transport logic moved into the SDK. The adapter-only test
was moved out of the native-importing file so its three small tests can execute
without pretending that the native package or real graph is installed.

The final JSON adapter regression run passes: 3 tests and 20 assertions
(`key-value-json-tests2.log`). The actual TypeScript 5.9.3 compiler then checked
the three changed/required DTO modules with the package compiler options and
no diagnostics (`memory-wire-scoped-tsc.log`). The compiler host received exact
source bytes in memory; hashes are in the receipt. It used real standard
library declarations and supplied no stub for the missing native/Node surface.
This scoped pass does not replace the still-blocked full package typecheck or
installed-native quickstart.

`recovery-integrated-tests2` returned 101 after the normal installed wrapper
selected its Arch fallback. It compiled production core source, then found
`EntityId` lacking Serialize in the MCP read-set scope key and one fixture
missing the additive `capabilities` field. The key now explicitly encodes
optional world/facet IDs as hex, preserving the tagged actor/session/scope
namespace. The fixture is complete. `recovery-integrated-tests3` is the sole
replacement job and also selects the actual foreign-proposal validator,
OpenAPI/success fixtures and canonical document tests. No real QuickJS artifact
pass is claimed by these host-boundary tests.

## NOTE receipt disclosure and in-flight erasure

The authenticated semantic admission now checks the existing citation closure
inside its committing writer, for edits and saved-receipt replay as well as
new citations. Socket delivery already repeated selector admission, so this
also prevents a mutation from committing under a narrow selector only to have
its full-view response withheld later. A focused fixture proves an uncited
NOTE is editable under a base selector, then refuses that selector once a
world-specific citation is present; the full selector still admits and replays.

A separate in-flight carrier gap existed after hard erasure: a queued receipt
held the old pin bytes, while the newly scrubbed NOTE was exportable again.
Delivery could therefore pass the current document's scope check and forward
the stale receipt payload. Delivery now also asks the engine whether the exact
receipt remains in its authoritative durable row. Citation erasure removes
that row atomically. The ordered-delivery fixture now checks a receipt before
erasure, a still-exportable clean NOTE afterward, and withheld stale receipt
bytes. No test uses log output or source-text scanning as its oracle.

These source changes are formatted but their native regressions are not yet
green. Confirm the new test name and this source snapshot in the outstanding
`recovery-integrated-tests3` receipt before counting it as coverage.

## Provisioned real QuickJS build

Owner receipt `QUICKJS_PROVISIONED_20260919` supplied wasi-sdk 27,
wit-bindgen 0.46.0 and wasm-tools 1.239.0 on the MacBook. The build now accepts
the official version token followed by commit/date, while refusing a changed
version or executable name. The pinned dtoa source included `setjmp.h` without
using that API; the builder removes only that unused include after checking
there is no setjmp/longjmp dependency. No experimental exception support,
ambient WASI adapter or security bypass was added. Both patched source hashes
are in the manifest.

`quickjs-native-build3.exit` is 0. A second build into a different output
directory returned 0 and produced byte-identical component hashes:
- first-party: `3ddb36f6ca801c9b19dc59f17960ad139539a6a05a3f74d05de4220dc295f1f6`
- foreign: `4ae3c57ba74758d5669b6568c2508b76914ce036fde9389788b3704381366a07`

`components/code-run-quickjs/artifacts/` contains both components,
`manifest.json` and upstream LICENSE. A dependency-free CI test checks component
headers, all file sizes/hashes, source/WIT pins and licensing; two tests pass.
Native tests are running against these real binaries. The C04 handoff must
label build proof separately from native qualification until that run passes.

The integrated source run completed 87 tests: 77 passed, 10 failed. Green
receipts include the complete authenticated socket path, narrow receipt refusal,
queued-erase receipt protection, ordinary world scope, canonical document crash
recovery, generic keyed-data leak guards, and HTTP keyed access. Fixture repairs
use proper skill candidate activation, a real 32-hex proposal id, a separately
replayed ClaimOf edge for competing keyed heads, and before/after pending-write
comparison rather than assuming policy seeding creates no pending rows. The
three Loro compaction/cursor failures are under repair, not reported green.

With owner provisioning authority, the ticket's `target/w7-validation/sdk-env`
holds Python 3.12 and native-packaging/LangGraph/test dependencies. Node project
dependencies are local to `packages/oneiron/node_modules`. The full Memory Wire
TypeScript project now passes (`memory-wire-provisioned-tsc.log`), replacing the
older missing-types blocker. This is still not installed-native acceptance.

## Loro shallow-state and quote endpoint repair

The erasure failures were a Loro API mismatch: `fork_at` rejects shallow state
even at its current frontier. An exact-current request now uses Loro's independent
live `fork()`. Older requests still require actual retained history and refuse
when unavailable; no old offsets are applied to a new body. Citation admission
uses the same rule instead of accepting an unavailable frontier because today's
text happens to match.

The ordinary free-prose failure exposed Loro's end-of-document cursor sentinel:
`get_cursor(len, ...)` follows appended text. New pins instead bind to the first
and last quoted characters with explicit left/right sides. A deleted character
never remaps to identical neighboring text; drift retains the original quote.
This is a pre-release endpoint convention correction with the same public
fields, not a migration for deployed data. New tests include Unicode scalar
boundaries, one-character/end-of-body spans, StateOnly/reopen, deleted anchors,
and discarded historical bases. Existing erasure/sync tests remain enabled.
The three guarded source replacements were applied only after SHA-256 matches.
Native validation of this repair remains pending.

The first real-artifact native qualification returned 100 with 9/14 passing.
The actual HTTP `execute_code` run/resume/terminal-retry test passed, as did
pin/instruction limits and independent concurrent runtime handles. The two
remaining interpreter failures were invalid 34-character entity IDs in the
JS fixture inputs; their typed validation refused them. Fixture IDs are now
exactly 32 hex characters, without changing component bytes. Skill queries
needed their text index refreshed after activation rewrote the entity. Three
board/category fixtures are rerunning after that setup correction.

`quickjs-note-qualification2` owns the selective remaining run: those two real
interpreter cases, the three repaired board/category cases, and NOTE tests for
the applied Loro repair. The previous 77 green integrated tests and native
QuickJS HTTP/limit/concurrency proofs remain recorded, not blanket-rerun.
The installed real LangGraph 4.2.0 BaseStore interface passed all 15 adapter
fixtures (`langgraph-real-interface-tests.log`); those use a fixture memory
client and are explicitly not the native StateGraph acceptance.


## Real artifact qualification and current native work

All five real QuickJS qualification cases now pass across
`quickjs-native-qualification` and `quickjs-note-qualification2`. The latter
ran 27 tests: 26 passed, with one NOTE shallow deleted-cursor test aborting
inside Loro's historical cursor fallback. This is a real failure under repair,
not a waived test. All selected NOTE socket/provenance/receipt/erasure cases,
category isolation and host board fixtures passed. C04's exact qualified
artifact directory, ABI and hashes are in `impl-notes/W7-C13-quickjs.md`.

`installed-native-sdk-build` is the only active Cargo job. It uses the normal
queue-aware wrapper to build real napi/PyO3 libraries plus the server and
pre-server actor provisioner. Installed SDK packaging/import and both
constructor quickstarts follow that result; the 15 BaseStore interface tests
are not substituted for a real native StateGraph run.


### Deleted-cursor repair and native build receipt

`installed-native-sdk-build.exit` is **0**, built on the MacBook via the normal
wrapper: napi/PyO3 libraries, server and pre-server provisioner compiled. The
wrapper rejected the mini's reserve once before Mac admission; subsequent calls
explicitly preserve the owner's mini exclusion in this seat's environment.
No mini build or manual storage cleanup ran.

The remaining NOTE regression is repaired with Loro's public `with_state`
read-only live ID lookup, using Unicode indices explicitly. This is the same
live path as `get_cursor_pos` without its deleted-anchor historical replay,
which could panic after history compaction. Missing live characters remain
`Drifted`; no catch-unwind, quote remapping, test removal or erased-history
restoration was introduced. `note-live-cursor-repair` owns the three targeted
native tests; their result is not yet known.

The real AI SDK native integration fixture now runs `generateText` with the
actual tool-pack and actual native vault. Only provider replies are deterministic,
using the official AI SDK test model. CI invokes this in its own process after
building native SDKs, separate from the adapter unit test's module mock. The
resolved ai 6.0.286 development dependency is pinned. Native execution remains
pending; JavaScript syntax and all five changed workflow fixtures pass.

Host-affine SDK packaging is prepared as a ticket-local dependency-free Cargo
runner. A normal `cargo run` wrapper admission holds the host/ticket slot while
sequential napi/maturin packaging and installed-artifact tests run on that same
host. This avoids copying a Mac dylib into a Linux import test or bypassing the
normal wrapper. Recipe and each result are preserved under
`target/w7-validation/native-sdk-runner-source` and `native-sdk/` respectively.


The final source policy checks passed on the continuation tree: giant files
2/2, allow attributes 98/98, print macros 91/91 and process-global statics 32/33.
The root surface remains exactly 701 names, and all 16 code-map artifacts are
current (2577 Rust files). No ratchet baseline or root export pin was raised.
The NOTE live-cursor guard is expressed as one checked conditional, preserving
its mapped/drifted result without a nested-if lint. Native validation continues
under the existing `note-live-cursor-repair` job; no duplicate was queued.


### Native graph proof without another build (2026-09-19)

While the existing NOTE test waited for a host slot, the already-built MacBook
cdylibs were exercised directly without starting Cargo. The Python wrapper was
copied unchanged beside the real `_native.abi3.so`; only the normal compiled
extension filename was supplied. Dependencies and the actual LangGraph adapter
were installed into a ticket-local environment. The source package dependency
on unpublished `oneiron>=1` made the first dependency solve fail; the retry
installed framework dependencies first and the adapter with `--no-deps`, because
the actual Oneiron module was supplied from the built artifact rather than PyPI.
This workaround applies only to this explicitly direct-artifact smoke test.

`native-sdk-smoke-run2.exit` is **0**: two Python key-value fixtures passed;
one real `StateGraph` fixture passed its two runs, exact get/update, filtered
search, namespace listing and delete; Node loaded the real N-API module and
reported its native exports. Binary SHA-256s and stage receipts are retained in
`target/w7-validation/native-sdk-smoke-receipt2.json`. No memory client,
BaseStore or graph runtime was mocked. This is runtime proof for step 18, **not**
a wheel/npm clean-install or five-minute quickstart result. The latter still
belongs to the prepared native packaging workflow after the queued NOTE job.


### One admitted host for the remaining checks

The note-only normal-wrapper call never obtained a host slot. Inspection showed
only its admission sleep child and no host-slot descriptor. It was replaced,
not duplicated: `note-live-cursor-repair.exit=143` records the intentional
pre-build cancellation. `queued-validation-replacement.json` preserves the
check. No compiler or other ticket was stopped.

`native-final-workflow` now owns one normal-wrapper admission for the sequential
remaining checks: three cursor regressions, featureless NOTE, the real
oneiron-remote keyed contract binary, featureful all-target Clippy over the six
touched crates, native SDK package assembly, clean installs and both constructor
quickstarts, wire keyed parity, real StateGraph and real AI SDK tool execution.
Each inner stage retains its own `.exit`, `.log` and receipt under
`target/w7-validation/native-sdk/` on the admitted host. The independent earlier
QuickJS, board, scope, L2 and benchmark greens are not blanket-rerun.

The wrapper holds the host/ticket slot while the helper runs. Nested package
builders use `rustup which cargo` from that already-admitted host, with its
three-job budget. This prevents recursive acquisition of the non-reentrant
ticket lock on Arch; it does not bypass admission or force a local build.
No outcome is claimed for this workflow until its stage receipts are collected.


### Real AI SDK agent proof (2026-09-19)

`native-ai-smoke-run3.exit` is **0**. The unchanged TypeScript Memory Wire wrapper
was compiled with tsc and loaded the already-built MacBook N-API library. The
real AI SDK 6.0.286 `generateText` loop invoked witness, recall and receipts;
assertions checked the actual returned write receipt, recalled memory and gate
receipts. Only provider replies were deterministic. Neither memory nor AI SDK
tool dispatch was mocked. No additional Rust build or shared build slot was used.
This proves the sample agent/tool-pack integration, not clean npm installation.
Artifact, wrapper and test hashes are in `native-ai-smoke-node-receipt.json`.

The native fixture now uses `node:test`, matching the SDK's supported Node
runtime and README. Bun 1.2.22 failed inside AI SDK `structuredClone`: an isolated
probe showed it rejects each raw N-API DTO, while Node clones those identical
DTOs correctly. No adapter normalization, DTO copy or native behavior was added
to conceal this runtime limitation. The adapter-only Bun mock test stays separate.
The existing Mac Bun executable lives in `~/.bun/bin`; the ticket-local packaging
helper now includes that directory in its child PATH, without installing or
updating Bun or changing a host profile. The clean installed-artifact workflow
will run the same native fixture through its `test:native` Node command.


### Native workflow partial results and repairs

- `note-live-cursors`: **0**, three tests pass, including deleted anchors and
  Unicode boundary cursors after StateOnly reopen. The retained local log's
  SHA-256 matches the remote stage receipt.
- `remote-keyed-contract`: **0**, both actual `oneiron-remote` integration-binary
  cases pass (embedded exact-key/replay and maximal framed filter parity).
- `featureless-note`: **101**, the new test incorrectly used sync-only
  `Vault::sync_state_get`. The fixture now reads the same stored document row
  directly through the feature-independent store. The test is not cfg-disabled,
  and no production visibility/feature gate was widened. This repair awaits its
  corrective native run.
- Featureful Clippy identified four source findings: the NOTE birth guard grew
  `apply_put` to 505 lines, two nested conditionals, and one redundant closure.
  The unchanged birth guard is now a private helper in its existing 771-line
  owning file; the conditionals and predicate call were simplified. No lint
  allowance or baseline was raised. These repairs also await validation.

The active remote workflow keeps its original source snapshot and continues
SDK packaging/acceptance independently. Local fixes are not copied over a
running compiler. On completion, only failed/repaired stages are rerun. The
new featureless test and newly added base-mode code also require the applicable
featureless Clippy pass; older unchanged green evidence remains preserved.


### Installed Memory Wire and satellites: acceptance passed

The completed `native-final-workflow` ran all SDK stages successfully. The
workflow's aggregate exit was 1 only for the two earlier Rust test/lint stages;
it does not invalidate the separately receipted SDK results:

- Actual napi-rs native package, full TypeScript build and maturin wheel built.
- Clean npm archive and wheel installs plus both embedded README snippets:
  **13.353921 seconds**, including constructor/runtime work, under 300 seconds.
- Embedded quickstart structural assertions passed.
- Both remote README snippets, real-server DTO/error parity and exact-key parity
  passed against the installed packages and temporary authenticated server.
- Installed native StateGraph, Python and Node keyed-memory tests passed.
- Real AI SDK generateText tool loop passed under Node with native memory.
- The actual SDK export census passed: JS four tests / 16 assertions, Python
  five tests, pinned to the nine-row Rust facade catalog.

Archive hashes:
- `oneiron-1.0.0-cp310-abi3-macosx_11_0_arm64.whl`:
  `e9893b470549c1ede25373a859d2916cfbbe2e82614e609b7a661d85f9d84d0e`.
- `oneiron.tgz`:
  `991a956b823c805fe2aaa004810d7303505b5999dba55c0ad34f03c5247549fa`.

The receipts are `target/w7-validation/native-final-receipt.json` and the
`native-final-*.log` / `sdk-export-census-*.log` files. Collected stage logs
match their receipt hashes. `native-corrections-1` now owns only the repaired
featureless NOTE and Clippy selections, plus the applicable featureless and
server-production lint lanes. No successful runtime/package test is repeated.


### Corrective native results and execution-refusal retirement

- Corrective `featureless-note`: **0**, all four tests pass, including the new
  beyond-use erasure fence and retained opaque document after reopen/sweep.
- Corrective server-production Clippy: **0**, without test-hook unification.
- The two all-target Clippy lanes exposed only additional fixture lint issues:
  redundant final-use clones, helper unwraps, a borrowed-slice conversion,
  artifact path option chaining, hash-string allocation, and hex grouping.
  These are repaired without disabling tests or adding allowances.
- Final step-6 review found the historical `execute_code_unavailable` response
  still named at unconfigured admission. It is removed from server source and
  replaced with the existing `code_host_unbound` configuration refusal, sharing
  one constant with the native host error and one recovery-suggestions arm.
  Configured QuickJS registration/resumption remains unchanged. An unconfigured
  server still creates no run and does not mint a budget or invent a backend.
  Stale host-free-release comments now describe the actual conditional binding.

A second narrow corrective workflow will check the fixture repairs and the
updated refusal at the wire. Green featureless NOTE, installed SDK, remote,
graph, AI SDK, and unrelated surface/runtime evidence is reused, not rerun.


## Final engine-local validation and exact remaining boundary

At source `92c678331dd2c02d2656fdde1ad74567f1aa7115`, the complete
`native-corrections-2` workflow returned **0** on the MacBook. Every stage log
was collected and verified against its receipt hash:

1. `cargo clippy -p oneiron --all-targets --no-default-features -- -D warnings`: **0**.
2. `cargo clippy -p oneiron -p oneiron-server -p oneiron-bench -p oneiron-remote -p oneiron-napi -p oneiron-py --all-targets --features oneiron/sync,oneiron/test-hooks,oneiron-server/code-sandbox-wasmtime -- -D warnings`: **0**.
3. Native server nextest with sync/test-hooks/Wasmtime: **3 passed**, including
   unverified-host refusal before a run, recovery suggestions and real QuickJS
   actor-bound execute/resume/terminal retry with no repeated effects.
4. `cargo clippy -p oneiron-server --features code-sandbox-wasmtime -- -D warnings`: **0**.

The preceding corrective workflow's featureless NOTE selection passed all four
tests. The final native SDK receipt proves actual executed cases, not all-skipped
runs: remote installed snippets/parity **3 passed**, native StateGraph **1 passed**,
Python keyed binding **2 passed**, Node keyed binding **1 passed / 11 assertions**,
AI SDK agent **1 passed**, and export census **JS 4 / Python 5 passed**. Earlier
scoped sync NOTE, world, board, lens, room, SSE, redirect, L2, live-query, MCP and
proper benchmark-binary passes remain the evidence for unchanged behavior.

Final structural checks are green: code map 2577 files / 16 artifacts, giant
files 2, allowances 98, prints 91, process globals 32 (baseline 33), crate-root
surface 701 names, and whitespace check. No counter was raised. These are scoped
ticket results, **not** a full-workspace `scripts/verify.sh` VERDICT.

### C04 handoff

Canonical shipped artifact directory:
`components/code-run-quickjs/artifacts/`, also available on the MacBook at
`/Volumes/Cinema/w7-build/wt/W7-C13/components/code-run-quickjs/artifacts/`.
It contains `quickjs-first-party.wasm`, `quickjs-foreign.wasm`, `manifest.json`
and upstream `LICENSE`. World, build recipe, hashes, independent reproducibility
proof and C04 foreign-proposal boundary are in `impl-notes/W7-C13-quickjs.md`.
The real interpreter is qualified; C04's separate VM integration is not claimed.

### Downstream V2 handoff outside this engine repair

Step 8 cannot be implemented in this engine worktree. Its required consumer
files are absent: `packages/protocol/src/index.ts` (DispatchPayloadV2 and its
ActiveEntityStack / SessionRagState / ActivatedMemory / PromptSegments types)
and `workers/agent/src/worker.ts` (production CF dispatch and executor wiring).
The task permits changes only in this worktree. Creating a second consumer
protocol or worker here would violate the engine/product boundary. The remaining
action is to update and test that downstream worker against its actual V2 schema,
using the shipped engine retrieval/L2 and SDK surfaces, in an authorized consumer
worktree. No external deployment or provider call is claimed.

Step 7 remains explicitly **HELD on billing design**, as ordered. No migrator,
cutover, Convex teardown, fake upstream endpoint, or alternate dispatch protocol
was introduced. All executable engine-local implementation and named scoped
acceptance work is committed. The external V2 consumer work remains a separate
handoff; it is not claimed complete and does not block the assigned engine
fix-tests repair.


## Acceptance-test repair (current seat)

### Changes and retained native evidence

- `a4b1c47e` moves the blessed-brief contract test into the feature-independent
  NOTE suite. It pins all three defaults and rejects each missing/malformed
  contract field with `InvalidNoteBody`. Five featureless NOTE tests passed.
- `388611fb` pins authored markdown, typed documents, and exact persisted bytes
  across two-pin brief rendering and supersession. HTML is not persisted.
- The real native Python/LangGraph suites passed **158 tests**. The real native
  Node SDK suite passed **186 tests / 766 assertions**. Five WIT/QuickJS tooling
  tests and the shipped generated-JS stub checks passed. These unchanged scopes
  are retained, not replaced by mocks or repeatedly rebuilt.
- Linux standalone NAPI Rust libtests originally failed to link three `napi_*`
  references. `97215be0` enables napi's supported `dyn-symbols` only through a
  Linux dev dependency. Cargo resolver 2, napi 3.12.0 and the existing lockfile
  were checked; C03 `104ba7b4` was inspected read-only, not cherry-picked.
  The ordinary addon dependency stays unchanged (`napi8`, `serde-json`, no
  default features). Linux production/test graph receipts prove `dyn-symbols`
  absent without dev edges and present with them. All **22 NAPI Rust tests
  passed on Linux** in the retained unfiltered run 13.
- `12f2648f` adds `crates/oneiron-napi/tests/node_host.cjs`. The fresh ordinary
  Mac addon loaded under real Node and passed keyed put/get/replay/delete and
  typed invalid-dimension refusal. This is additional production-addon evidence,
  never a replacement for Rust libtests or a claim of Linux addon execution.

### Concrete repairs from executed Rust failures

- Category collection no longer turns ordinary memory retrieval into an
  authority-filtered query. Its own bounded top-k allocation keeps capability
  discovery independent while preserving post-fusion filtering, claim decode
  reuse and memory suppression accounting. Explicit authority remains enforced.
- `e18c5ad5` fixes a second accounting issue in pack assembly. Capability hits
  keep the whole pack nonempty, but cannot replace the memory scope count with
  the zero surfaced count. Non-owner disclosure reports only admitted candidates,
  not the pre-clamp population. The regression compares the stored retrieval
  record and returned count against one, keeps five capability hits, and tests
  lifecycle revalidation. Its original query limit of 20 remains. A prior limit
  increase did not solve the failure and was reverted. All **91 context-pack
  diagnostic tests passed**, including disclosure and serialization cases.
- High-degree neighbor fixtures now contain real readable destinations. Private
  and dangling-entity refusals remain. Secret-file fixtures use a canonical temp
  parent; production symlink refusal is unchanged.
- Selector fixtures use non-system IDs and a genuinely distinct extra facet in
  the negative ceiling test. Current clients use v9 hello; v8 rejection, expiry
  clocks and transaction-bound grant checks remain unchanged.
- Structured native request DTOs and generated schemas are closed together,
  including nested depth/budget objects. Unknown-field regressions first prove
  the control request decodes. Golden wire tests retain valid aliases/defaults.
- MCP catalog and paging fixtures include all eight memory verbs. One-time
  cursors and scope/producer bindings remain asserted. Frozen listings check an
  exact registered credential, not the ordinary schema word `connector`.
  `53ea2e68` fixes that fixture's actor decoding via `EntityId::from_hex`.
  The packaged server skills artifact matches the engine's root copy.
- The existing-only bench vault door deliberately refuses non-Linux hosts.
  Tests execute the CLI and assert typed `UnsupportedPlatform`, no added outcomes
  and unchanged tuning there. Linux keeps all positive success assertions.
  No production pathname fallback, cfg-disabled test or gate filter was added.
- `ba330eb7` changes exactly four snapshot fields after disclosure leaves zero
  admitted candidates: two `no_data` reasons and their matching hints. No response
  shape/status/count or snapshot comparator was loosened. The focused snapshot
  case passed. There was no booking-clock failure to repair or copy from C07.

### Exact mandatory validation at source head ba330eb7

The sequential `chain18` service ran one native command at a time, unchanged
thread settings, stopping on failure. Its first three stages **passed**:

```sh
cargo test --no-fail-fast -p oneiron -p oneiron-bench -p oneiron-napi -p oneiron-py -p oneiron-remote -p oneiron-server
cargo clippy -p oneiron -p oneiron-bench -p oneiron-napi -p oneiron-py -p oneiron-remote -p oneiron-server --all-targets -- -D warnings
cargo clippy -p oneiron --all-targets --no-default-features -- -D warnings
```

These are actual terminal zero exits, with per-stage timestamps and source head
in `target/w7-validation/fix-tests/chain18-*.ok.json`. The unfiltered command
completed with **9,118 passed, zero failed, 21 existing ignored, zero filtered**.
It has no package/test exclusions or retries. Its full per-target results remain
in `chain18-unfiltered.log`; lint logs are adjacent. Built-in ignored tests are
reported by Cargo as-is. `oneiron-py` has no Rust harness under its existing PyO3
manifest contract; its real native Python suite is recorded above.

The initial last stage failed on the shared MacBook host (subsequently recovered
through the owner-authorized guarded route below):

```sh
cargo test -p oneiron --lib --no-default-features
```

On the MacBook it finished with 2,173 passed, 4,386 failed, 4 ignored and zero
filtered out. **4,384 failure blocks report StorageFull / OS error 28** at vault
open, including one interleaved stderr line. Two other failures are a post-LMDB
hook readiness timeout and an expected `InvalidConfig` assertion; both need
rechecking after the host is healthy. They are not claimed independently fixed.
The overall chain exit is 101, despite its three green stages.

The owner then confirmed Mac POSIX semaphore-pair exhaustion: a first fresh
held semaphore succeeded and a second returned ENOSPC; LMDB requires two.
There was no production-code change or host-global cleanup for that failure.
The owner explicitly authorized the installed wrapper's action-scoped
`W7_CARGO_EXCLUDE_MACBOOK=1` route. Mini reserve and Arch slot/job guards remained
unchanged; this was not force-local or a global host-order override.

One retry (`featureless-guarded20`) was stopped after startup exposed the saved
helper's detached old environment omitting the guard. The ticket-specific remote
worker check found no remaining test process. That stopped attempt is not a pass.
The successor service passed the guard explicitly and selected **Arch slot 2/2,
at most three Cargo jobs**, after the Mini reserve check declined that host.

**`featureless-guarded21` completed exit 0: 6,577 passed, zero failed, four existing
ignored, zero filtered.** This includes the two indirect failures above; neither
needed a code change. The exact command was the same featureless library test
command shown above. Only the failed runtime stage was repeated; the three
successful stages were retained. This portable result does not substitute for
native Mac/Office acceptance, and the separate real Mac Node-host proof remains.

The receipt and log are `featureless-guarded21.{exit,json,log}`, with route/source
bindings in `featureless-guarded21-manifest.json`. It ran at `bf9121b9`, whose only
difference from the fully tested source head `ba330eb7` is this notes file. No
implementation bytes changed between the green unfiltered/lint stages and the
successful featureless run. Raw host-failure evidence remains in
`featureless-storage-incident18.json`. `chain18-manifest.json` preserves the stage
manifest; `chain18.json` is its initial terminal service diagnostic, not a claim
that the original chain exit changed from 101 to zero.

Earlier failed/interrupted jobs are retained as failure evidence, not greens.
In particular, touched-crates3/4 rejected Cargo CLI configuration before tests;
unfiltered11/12 caught compile errors; unfiltered13/16 and focused14 exposed
real fixture/accounting failures repaired above. Focused skipped counts are not
full-gate coverage. All four required repair checks are now complete and green
across their actual receipts. No additional test was disabled, no successful
check was duplicated, and no factory state/pass was synthesized. This records
the requested six-crate and featureless checks, not an unrun workspace-wide
`scripts/verify.sh` verdict. The assigned engine test/link repair is complete.

### Review and external handoffs

There is no PR for `w7/W7-C13` at the recorded recovery check (`gh pr view` and
`gh pr list --state all --head w7/W7-C13`). The complete PR933 consolidation
packet concerns C08 and was read-only here. No PR933 disposition or fix is
claimed. C13's internal audit findings and test repairs are retained. On later
publication, gather all current bot comments/reviews/threads before review fixes,
then post the exact changes, skips/reasons and validation on that PR.
ONE-259 V2 consumer integration and owner-held billing remain separate handoffs;
they do not block this engine test/link repair. No external deployment is claimed.

## Post-acceptance audit repairs

A self-audit of the recorded head (`05291f16`) found four defects where source or
documentation still asserted a retired contract, or where a test observed through
a door that could mask the thing it asserted. All four are repaired below at
their root, not worked around. The validation commands below are reconciled
against the original executed receipts.

### 1. Orphaned sweep doc comment (`crates/oneiron/src/vault/open.rs`)

`notify_note_document` was inserted directly beneath the `live_window_for_sweep`
doc comment, so that comment documented the wrong function and
`compact_document_for_sweep` and `live_window_for_sweep` were left undocumented.
The sweep-compaction comment is restored onto `live_window_for_sweep`, and the
two document functions now carry their own contracts: the notify path is a
content-free best-effort invalidation whose recipients re-read committed rows,
and the compaction path reports whether it ran so an attached-but-dropped manager
defers instead of rewriting a row a live document may persist over.

### 2. Retired refusal code in the skills pack (both copies)

The `code-mode-repl` lane still told agents `execute_code` is registered on no
endpoint and refused with `execute_code_unavailable`. Source `92c67833` retired
that code, and step 6 binds registration to a verified host. The lane now states
the conditional binding and the actual `code_host_unbound` refusal. The root and
`crates/oneiron-server/` copies remain byte-identical, which
`skills_pack::crate_local_pack_matches_root_artifact` enforces.

### 3. Bench observation door reopened the create-capable vault

`stored_analyzer_manifest_hash` had been switched from `Vault::open_existing` to
the create-capable `open_vault_with`. That door rewrites the analyzer manifest on
an empty text index, which is exactly the state
`eval_outcome_ingest_refuses_a_wrong_dict_root_on_an_empty_text_index` sets up,
so both the before and after observation would restamp the correct identity and
the equality assertion would hold no matter what the command under test did. The
helper is back on the existing-only door. Its only caller is already excluded
from the macOS lane in `MACOS_SKIP` on both this branch and `origin/main`, so the
platform-refusal path the switch was meant to accommodate is not reachable here.

### 4. Stale oracle comment (`crates/oneiron-server/tests/it/mcp_oracle.rs`)

The ONE-1704 M2 header still described a host-free release and the retired code.
It now describes the fixture provider entering the seam directly while the wire
registration follows the server's verified host binding.

### Validation of these repairs

The original review session executed the repaired bytes. Its recorded commands
and outputs are retained in `target/w7-validation/review-fix/retained-review-*.json`.
Bench all-target/all-feature Clippy finished successfully (that command did not
set `-D warnings`). Core library Clippy passed with `-D warnings` in both
`sync,test-hooks` and featureless modes; these were library, not all-target,
checks. Core all-feature rustdoc passed with warnings denied.

26 `oneiron-bench` eval tests pass, including the repaired wrong-dict-root case
(receipt `b247ee2a`). Nine `oneiron-server` skills-pack/discover integration tests
pass, including cross-copy identity (`b14220fc`), along with the four
`mcp_oracle` arms (`44505ea9`). The 19 NOTE/document/erasure/live-body tests pass
(`8b3cd431`). Their implementation bytes are unchanged by this repair seat,
so this green evidence is reused rather than repeating these suites. These are
scoped results, not an unrun workspace gate.
No test was disabled or weakened, and no production behavior changed: repairs 1
and 4 are comments, 2 is agent-facing documentation, 3 restores a test's
observation door.

## Review-fix disposition (base `05291f16`)

Collected the original internal review roots, not replacement reviews:
`review-opus/01a0bac1-cdfa-719b-89d4-ae9936903c50.jsonl` (terminal message
`1d37a731`) and `review-grok/01a0bac1-cdf1-71a6-95a0-c23125a03025.jsonl`
(terminal `LANDABLE`), plus `.w7/review-livequery.md`,
`.w7/review-note-lens.md`, and `.w7/l2-review.md`. No child or validation job
was outstanding in this session; the obsolete `.w7/active-work.json` PID is
not a current receipt.

The pre-fix GitHub checks returned `[]` from `gh pr list --state all --head
w7/W7-C13` and `[[]]` from the paginated REST pull list filtered to
`oneiron-dev:w7/W7-C13`. `git ls-remote origin refs/heads/w7/W7-C13` returned
no ref. Qodo, Codex, and CodeRabbit top-level comments, reviews, and inline
threads are therefore **inapplicable: no C13 PR exists**, not pending or
quota-unavailable. No disposition was posted to PR933, PR938, or any other
ticket. When the factory publishes C13, collect its then-current paginated
surfaces and post this ledger with the published head and actual validation.

### Deduplicated findings

| Source / ID | Disposition |
| --- | --- |
| Opus D1 (`c41ee0bd`) | Fixed: both served skills artifacts describe conditional verified-host registration and `code_host_unbound`. |
| Opus D2 (`08fc0a66`) | Fixed: the bench identity observer uses `Vault::open_existing`, so it cannot repair the manifest it is meant to inspect. The existing macOS carve-out is unchanged. |
| Opus D3 (`c41ee0bd`) | Fixed: sweep safety documents `live_window_for_sweep`; notify and document compaction each have their own contract. |
| Opus D4 (`c41ee0bd`) | Fixed: the oracle header describes the unconfigured server, not a host-free release. |
| Opus Node-host observation (`6c31cfe1`) | Fixed: macOS CI now installs Node 22, builds `oneiron-napi` with the ordinary production graph, and executes `node_host.cjs`. The existing Rust libtest lanes and production/dev dependency split are unchanged. |
| Livequery precision, edge key, delete publication | Already fixed before this repair: bounded queued containers, canonical `parse_edge_key`, and tee normalization of deletion entity IDs. No duplicate edit. |
| NOTE/lens D1–D6 | Already fixed before this repair: reverse citation mediation, source-document admission, hidden-state suppression, host-only document doors with scoped render admission, WorldSet admission, and gate policy before proposal. Exact derived citation erasure also supersedes the earlier follow-up gap. |
| L2 D2–D5 | Already fixed before this repair: snapshot/scan outside the cache lock, hydration-snapshot revalidation, additive profile subjects, and typed invariant refusal. |
| L2 D1 | Not applied: canonical claim-byte hashing intentionally detects changes at hydration revalidation (D3). Hashing rendered text alone would remove that fence. |
| Livequery default membership suggestion | Not applied: the production `BoundSource` overrides the default and handles membership probes. The report identifies a hypothetical future implementation, not a current missed invalidation. |
| Livequery unreviewed run-tree | Closed by original Opus follow-up: subscribe-before-read and unchanged-snapshot suppression reviewed; no new defect. |
| NOTE/lens G1–G6 design questions | No contract change: author or verified owner-Human authoring authority, propose-on-drift, explicit frame scoping, persisted proposal text, trusted host surface identity, and bounded text refusal are existing deliberate contracts, not correctness defects demonstrated by this review. |

The original reviewer artifacts, source history, failed jobs, and green receipts
remain intact. External V2 consumer integration and owner-held billing remain
separate handoffs; this repair makes no external completion claim.


### CI follow-through and final validation

The new macOS CI step uses the same successful production build and real Node
invocation from the retained fix-tests session, rather than a test-only addon:
`cargo build -p oneiron-napi --lib`, then
`node crates/oneiron-napi/tests/node_host.cjs "$CARGO_TARGET_DIR/debug/liboneiron_napi.dylib"`
(with the workflow's normal `target` fallback when the variable is unset).
The normal Rust libtests remain independent. Node 22 setup and the addon step
use the existing Rust-relevant/dispatch condition, and both honor the job's
pause/draft guard.

The exact prior Mac Node terminal result is in fix-tests session
`01a0b997-5ffc-7752-83f2-3dd095b2df25`, command `cfe6fdc1`, result `84736ea4`:
exit 0 and `NODE-HOST-OK` for addon loading, keyed put/get/replay/delete, and typed
invalid-dimension refusal. Its production build was also exit 0; the original
`fix-tests/fresh-node-host.log` and production/dev graph receipts are retained.
The addon and Node test are unchanged by this review repair. This is retained
Mac evidence, not a new Linux addon run or a claim that GitHub CI has executed.

An extra attempted addon build in this seat never acquired an eligible build
slot and ended before compilation. Its `review-fix/node-build.log` contains only
capacity-wait messages; it is not a pass. No replacement build was launched:
the existing green Node evidence covers the unchanged code and exact command.
Mini reserve and Arch slot limits were not bypassed, no other job was stopped,
and no validation remains running for this repair.

The final workflow parses as YAML, its exact shell step passes `bash -n`, the
Node test passes `node --check`, and `git diff --check` and `CODEMAP-OK` pass.
The retained Rust test results above cover every changed Rust crate and repaired
source byte. No test was disabled or weakened. The dependency-policy network
limitation reported by the reviewer remains unverified locally, not passed;
the existing CI dependency-policy check remains required. This scoped repair
is not a workspace-wide `scripts/verify.sh` verdict.

## PR #942 bot consolidation (reviewed head `4248014b`)

Before source edits, fetched every REST page of PR reviews, issue comments and
inline comments, plus every GraphQL review-thread page and nested comment
`pageInfo`. The initial snapshot contained only running statuses; the later
fetch contained Qodo's full review. CodeRabbit's file-limit refusal is
unavailable, not a clean review. Codex was still running at the first edit;
its status is not used as evidence of completion. Original internal roots and
subreview artifacts were read, not replaced by new reviews. The final collection
and validation below supersede this in-flight status.

### Current distinct findings

| Sources | Assessment and disposition |
| --- | --- |
| Qodo review `5257617184`, summary `5745211801` item 1, inline `4054676457` | Not applied. ARCH-0067 §10 explicitly requires `SKILL approved+active; AGENT_DEF always`. AGENTS `cand` is discovery, not dispatch permission. The existing `capability_channel_keeps_memory_budget_and_revalidates_lifecycle` fixture deliberately admits a proposed agent. Dispatch approval/lifecycle/enabled checks remain unchanged. |
| Qodo `5257617184` / `5745211801` item 2 / `4054676460`; earlier L2 D3 | Fixed. Producer and hydration revalidation share full pipeline evidence admission, scoped visibility, disclosure and reference checks, plus one canonical evidence digest. Changed world, authority, candidate, temporal or evidence-set admission drops the optional prefix. The original pipeline is retained across retrieval. Regression cases change candidate admission without changing a claim and cross a claim's validity boundary. |
| Qodo `5257617184` / `5745211801` item 3 / `4054676463` | Clarified the actual deadline contract; no unsafe preemption added. `wall_time` is a guest-execution/event-admission ceiling, not a hard timeout on arbitrary trusted synchronous host code. Borrowed non-Send hosts cannot be detached. A started gated write must settle and be recorded; `WakeCancellation` explicitly forbids cancellation between write start and settlement. A host that blocks can exceed the deadline; host implementations own that bound. Abandoning a worker would allow an unrecorded write after a reported timeout. |
| Opus recheck `7a4ad3d8`, discovery observation | Fixed. `/api/core/discover` and `/api/health` now derive MCP capabilities from `server.mcp_surface`, exactly like listing/admission. The real QuickJS wire fixture checks both HTTP projections advertise execute_code and both endpoint tokens; the unbound-server census remains negative. |
| Opus recheck `7a4ad3d8`, production provider observation | No change. The public binding door requires a real backend and budget lease. Inventing either at server startup would violate spend authority; unconfigured hosts must refuse. |
| Opus recheck `7a4ad3d8`, test-feature observation | Evidence correction, not code defect. The 9,118-test factory lane has no `--all-features`; its result does not prove Wasmtime. Retained real-component feature-scoped tests cover that lane. No unrun full-workspace gate is claimed. |

### Original internal findings, deduplicated without discarding IDs

`review-opus/01a0bac1-cdfa-719b-89d4-ae9936903c50`, terminal
`1d37a731`; `review-opus-2/01a0bb47-6443-746a-8d2f-f1dbc13c77f5`,
terminal `7a4ad3d8`; both original Grok roots and their existing children were
collected. The earlier review-fix ledger above still records their exact fixes.

| Source findings | Disposition / reason |
| --- | --- |
| Opus D1 and D4; Grok cross-cutting `737585a1` item 7 | Already fixed in `c41ee0bd`: served skills copies and oracle header name conditional registration and `code_host_unbound`. |
| Opus D2 | Already fixed in `08fc0a66`: bench observer uses existing-only open, not a manifest-repairing door. |
| Opus D3 | Already fixed in `c41ee0bd`: sweep, notify and compaction comments are attached to their own functions. |
| Opus Node-host observation | Already fixed in `6c31cfe1`: macOS CI builds the ordinary production addon and runs the real Node-host test; Rust libtests remain enabled. |
| `.w7/review-livequery.md`: precision, edge-key, delete-producer findings | Already fixed: bounded entity containers, canonical `parse_edge_key`, and tee deletion normalization. |
| Livequery default membership suggestion | Not applied: production `BoundSource` overrides it. A hypothetical future implementation is not a current invalidation defect. |
| Livequery run-tree/PPR questions | No unreviewed blocker remains: original root verified subscribe-before-read, unchanged-snapshot suppression and production membership tests. PPR concern was a question, not a demonstrated missed invalidation. |
| `.w7/review-note-lens.md` D1 | Already fixed: reverse pins participate in cited-source edit mediation. |
| NOTE/lens D2 | Already fixed: source-document visibility gates quotes; exact copied-citation erasure also removes derived carriers. |
| NOTE/lens D3–D4 | Already fixed: hidden citation state is withheld; host-only raw document doors are separate from scoped render admission. |
| NOTE/lens D5–D6 | Already fixed: ordinary WorldSet membership and gate-before-proposal admission. |
| NOTE/lens G1 | No change: author or verified owner-Human authority, not arbitrary Human authority. |
| NOTE/lens G2 | No change: drift deliberately requires review rather than guessing a cursor span. |
| NOTE/lens G3 | No change: host chooses frame scope; executable lenses require explicit WorldSet/CodebaseSet. |
| NOTE/lens G4–G6 | No change: proposal text is retained claim data; surface emitter identity is trusted host input; oversized text is a typed refusal, not silent chunking. |
| `.w7/l2-review.md` D1 | Not applied: canonical claim-byte hashing is the evidence-change fence, not a hash of rendered text alone. |
| L2 D2, D4, D5 | Already fixed: scans outside cache mutex, additive subjects, typed invariant error instead of panic. |
| L2 D3 | Earlier body/disclosure fence was incomplete for retrieval admission; completed by Qodo item 2 above. |
| `note-transport-review.md` D1, D4, D5 | Already fixed: remote NOTE quarantine/continue; lightweight state notice then authorized delivery; non-NOTE peer role/grant checks in the committing transaction. |
| NOTE transport D2 | Not applied as proposed: covering idempotent receipts are valid; uncovered receipts retain the outbox. Deferring without retaining the received receipt would lose it, not add correctness. Ordered STATE-before-receipt delivery remains. |
| NOTE transport D3 | Not applied: the report itself traces `storage::load` replaying the update rows before snapshotting. Checkpointing that loaded document does not discard unapplied updates. No old deployed format needs a compatibility path. |
| NOTE transport D6 | Not applied: desired remote subscriptions must not be pruned because a local grant replica is missing. Server export still reauthorizes. Local absence is not proof of remote revocation. |
| `key-value-review.md` H1, H1b, H2 | Already fixed: generic reads exclude keyed content for all callers; imported incoming and stored keyed predicates are guarded in the writer transaction. |
| Key-value M1–M3 | Already fixed: exact-one actor/class stamps; malformed/erased rows cannot wedge unrelated keys; source-trust replacement refuses with typed advice and preserves history. |
| Key-value L1–L3 | Already addressed: engine owns payload caps; only known optional JS envelope fields omit undefined; numeric equality deliberately retains serde_json representation rather than lossy f64 normalization. |
| Key-value L4–L6 | No defect: engine enforces caps under the existing transport limit; fallback receipts make no false gate-link claim; adapter batches are explicitly ordered/non-atomic and upstream convenience namespace restrictions are not engine restrictions. |
| `quickjs-runtime-review.md` B1 | Stale proposal not applied: current BroadcastPayload enum was retained. |
| QuickJS H1–H6 | Already fixed: per-handle Engine epochs, trap-on-grow, C and host inert-proposal validation, million-tick backstop, guarded allocator subtraction, no duplicate abort override. C04 foreign integration stays a separate handoff. |
| QuickJS M1–M3 | Already fixed: readiness checks all typed globals, non-byte list cap, SDK/bootstrap closure. Pinned binaries and native receipts retained. |
| `acceptance-audit-core.md` stored-HTML test suggestion | Not added: the requested source proof shows markdown/pins storage and view-time Instrument output. A byte-marker scan is not a sound storage invariant because authored markdown may contain those same strings; no stored-render defect was found. |
| Core audit other minor coverage notes | No new defect: authority binding, pin validation and concurrency already have targeted tests; extra unchanged-byte tests would repeat evidence. |
| `acceptance-audit-host.md`, `note-source-review-during-queue.md` | No additional defects; these are source-coverage observations, not runtime receipts. |
| Grok `9e5d8700` SDK/census residuals | No change: nine runtime verbs with three-verb teaching wedge is intentional; PyPI README brevity and exact guidance/order wording are informational. Native catalogs and wire tests remain authoritative. |
| Grok `9d2fb3ca` board residuals | No change: step 21's named acceptance is the WORLDS producer; room verbs are engine Memory verbs. Conservative invalidation is safe; changed-line placement wording is informational. No downstream V2 or billing completion is claimed. |
| Grok unfinished child candidates (supersede staleness, peer IDs, edge direction, wiring) | Original roots consumed these questions. Hidden superseded citations intentionally redact rather than leak state; actor stamps, canonical edges and production observe/census tests were verified. No unfinished child verdict is called a pass. |
| Opus dependency-policy observation | Retained limitation: its offline cargo-deny attempt could not resolve cross-platform dependencies. Not claimed passed; normal CI dependency policy remains required. |

Canon remains read-only. ARCH-0067's AGENT_DEF-always rule is current, not stale.
The runtime budget documentation needed the trusted-host limitation stated
explicitly. The discovery comment incorrectly described an always-host-free
server; it now describes actual per-server registration. Neither change invents
a new contract for the downstream V2 consumer or the owner-held billing step.

The actual new repair results and remaining capacity wait are recorded below.
Earlier green receipts cover unchanged code only; they are not proof of new bytes.


### Completed Codex collection (review `5257670619`)

Codex finished at 2026-09-19T21:05:33Z on `4248014b31`; status comment
`5745188068` is now Completed. Before its fixes, re-fetched every REST page and
all GraphQL threads and checked each nested comment page: all eight inline
roots are accounted for, with no remaining page. This completes, rather than
replaces, the original Qodo/Codex/CodeRabbit collection.

| Source | Disposition |
| --- | --- |
| `4054722373` | Fixed. Code-run candidate writes reject the incoming keyed predicate and existing keyed targets inside the write transaction. Canonical and on-record session traps share the guard. The fixture-write siblings use a transaction-bound target guard too; code-run supersession refuses either keyed endpoint. Erased IDs cannot conceal a former keyed target. Owned `key_value_put`/delete and sync replay doors are unchanged. A dispatcher regression exercises both storage routes, both candidate verbs, both supersession endpoints, ordinary writes, unchanged stored bytes/get/replay, and positive owned replacement/delete. |
| `4054722378` | Not applied: step 29 (ONE-2486) explicitly requires one generated tool per exported memory verb, including ask, with census/no-curation parity. `RuntimeDeferred` rows are reserved contract entries with a typed `RuntimeUnavailable` response, not a claim that they run on the separate `execute_code` host. Filtering only the server would break that named acceptance. Existing `memory_tool_census_keeps_native_names_schemas_and_reserved_refusals` and real-route `mcp_memory_reads_use_native_clamps_and_keep_runtime_refusals_typed` pin this behavior. |
| `4054722381` | Fixed. Plain response and board-cursor staged observations include `l2_base.evidence_ids()` only after final response projection/budgeting. The existing route fixture proves an L2-only served claim produces a later retraction rider; budget-shed and unobserved sessions stay empty. Plain requests currently have no companion subject input, but their shared response observation now handles an optional prefix too. |
| `4054722384` | Fixed. Redirect keeps `claimed_at`; fencing and placement still change. A rendered-event regression compares the entire first Claimed event before redirect, while requeued, and after the new worker claims the same try. |
| `4054722387` | Deduplicated with Qodo `4054676457`: not applied for ARCH-0067 §10's explicit AGENT_DEF-always discovery contract. Dispatch remains separately gated. |

Other external source IDs: `5745187297` is the owner's review request;
`5745187778` reports Greptile disabled; CodeRabbit `5745187829` and
`5745189899` report the 359-file/150-file limit (unavailable, not pending or
clean); Cursor `5745188699` is a risk/summary, not a defect; Qodo
`5745204712` is a PR summary and optional future split suggestion, not a
correctness finding. The initial Qodo busy comment `5745188773` was removed
by the bot and superseded by final review `5257617184` and summary
`5745211801`; its ID is retained here rather than silently dropped.

The first repair commit is `8ee6f361`. The initial native test run started
before the later Codex repairs. Its source scope is kept distinct from the
follow-on Codex regression run; no in-flight test is called a pass.


Latest complete refresh after thread replies still has eight finding roots and
no new defects. Qodo follow-ups `4054767148` (parent `4054676463`) and
`4054767202` (parent `4054676457`) both say “Dismissed — intentional.” Their
empty review wrappers `5257725816` and `5257725878` add no findings. These
acknowledgments confirm disposition; they are not substitute validation.


### Final bot-repair validation

All required scoped behavior checks have executed. No validation remains live.
The native wrapper used the authorized MacBook exclusion and its normal Mini
reserve / Arch two-slot, three-job limits. Capacity waits were productive
pending work, not permission blockers or passes. No guard was bypassed.

| Scope / receipt under `target/w7-validation/bot-review/` | Actual result |
| --- | --- |
| `focused-result.json`: eight L2 cases, unbound discovery, real QuickJS wire resume; core/server with sync,test-hooks and Wasmtime | 10 passed, 0 failed, 7,884 skipped; exit 0. This run began before the later Codex edits and is not their regression proof. |
| `featureless-result.json`: L2 cases with `--no-default-features` | 8 passed, 0 failed, 6,576 skipped; exit 0. |
| `production-clippy-result.json`: core/server with Wasmtime, `-- -D warnings` | Exit 0. Production Rust bytes are unchanged since this check; subsequent edits are test-only. Existing vendored heed lifetime warnings were emitted, not suppressed. |
| `codex/codex-focused-result.json`: four Codex regressions | 3 passed, 1 failed, 7,892 skipped; exit 100. Redirect history, ordinary code-run writes and L2-only session observation passed. The keyed test failed during fixture setup, before its behavior checks. |
| `codex/retry1/keyed-focused-result.json`: corrected keyed dispatcher matrix, same feature selection | 1 passed, 0 failed, 7,895 skipped; exit 0. |
| `codex/retry1/codex-featureless-result.json`: corrected keyed matrix, featureless | 1 passed, 0 failed, 6,583 skipped; exit 0. The selector named two cases but selected only one; the missing case is accounted for below, not counted as passed here. |
| `codex/redirect-featureless/redirect-featureless-result.json`: relocated redirect test, featureless | 1 passed, 0 failed, 6,584 skipped; exit 0. Finished 2026-09-19T21:46:20Z. |
| Changed-file rustfmt, `git diff --check`, code-map pin | Passed; 2,577 files / 16 current map artifacts. |

The initial keyed fixture installed an Agent-only manifest and then tried to
seed a different Human owner under it. Production correctly returned
`gate.pending.actor_ceiling`. Test-only commit `10350b12` admits the owner
Agent for setup/replacement and the separate code-run Agent for the overwrite
checks, using the fixture's explicitly trusted Generated source. Every refusal,
stored-byte, get, replay, replacement and delete assertion remains. No production
policy was changed. The three passing cases and production Clippy were reused.

The redirect test initially lived in the existing sync-only `tests/observe`
module. Its law is base-mode, so `13614149` moved the same test unchanged to
ungated `tests/claim_transitions`. The final receipt proves it actually ran
featureless. No test was disabled and no unselected test was reported as green.
The failed setup receipt and each sequential successor remain intact.

Each run has exact command, exit, timestamp, log and source-hash artifacts.
The final source manifest is `codex/redirect-featureless/source-sha.json`.
No broad unchanged-byte suite or full workspace gate was repeated. Retained
9,118-test factory and 6,577-test featureless results, Linux N-API and real Node
production-addon receipts cover their original unchanged scopes only.

The final complete REST reviews/issue comments/inline comments and GraphQL
threads were paginated and compared for NEW and EDITED bot bodies. There were
no new findings; labels were empty (no review-in-progress label). Qodo/Codex
are completed; CodeRabbit is unavailable due to its file limit, not pending or
clean. All eight inline roots have explicit dispositions, with replies checked
back from GitHub. Exactly one PR summary is maintained in place:
https://github.com/oneiron-dev/oneiron/pull/942#issuecomment-5745425103.
The launcher owns pushing these commits. This seat did not push, merge or close.
External V2 consumer integration and owner-held billing remain separate handoffs.
