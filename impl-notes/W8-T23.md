# W8-T23 implementation notes

- Reviewed all 13 `deny.toml` ignores against the 2026-09-25 `Cargo.lock` and the local cargo-deny advisory DB. Every advisory package still has its stated locked version; each RustSec record has `patched = []`. No dependency or lockfile change was warranted. The rsa advisory explicitly states that 0.9.10 remains affected; the signing-only exposure rationale remains.
- Refreshed all reasons to identify the exact `package@version`, current locked reverse-dependency chain, and a new `review by 2026-11-07` date. Corrected the bitmaps chain: `im` depends on both `bitmaps` and `sized-chunks`; `sized-chunks` also depends on `bitmaps`.
- The offline check reads advisory identity and package from cargo-deny's local `db-path`, including its hash-suffixed checkout, and requires the first reason token to name that exact locked `package@version`. Missing or ambiguous advisory records fail closed. The existing CI unittest discovery runs the two acceptance tests; no workflow changes or new tools are needed.
- The docs mirror has no binding advisory-policy canon page or ONE-2587 registry row. It should state that advisory ignores are reviewed regularly, name a currently locked crate version and dependency chain, and must not persist after the locked exposure disappears. The worktree's `deny.toml` and CI workflow are the current policy source; no docs-repo edit was made.
- Validation: `python3 -m unittest discover -s scripts/ci -p 'test_*.py' -v` passed (30 tests); existing `~/.cargo/bin/cargo-deny` 0.19.4 passed `--locked --offline check` with all four categories OK (existing duplicate/source warnings only). CI's `~/ci/tools/bin/cargo-deny` is absent on this host, and installing tools is prohibited by this ticket. The default Cargo wrapper tried to route metadata to a host without this worktree; the passing run put the installed local Cargo first on PATH. `Cargo.lock` was not modified; Rust build/nextest gates are not triggered. No Rust crate was touched.
