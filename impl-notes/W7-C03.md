# W7-C03 implementation notes

Base: `4689a7f1`. Worktree: `w7/W7-C03`. This records the engine implementation and the owner-approved host handoffs. Engine implementation and scoped validation are complete. The owner-approved external mail/host work is recorded explicitly below and in `W7-C03-host-handoff.md`. Earlier failed/interrupted invocations remain historical evidence; this is not a fresh full-workspace gate claim.

## Decisions

- The engine remains consumer-neutral. Cloud control-plane changes cannot be placed in this Rust engine as product-specific Convex files.
- OF-445 explicitly says POST-ENGINE and not in this wave. Step 23 remains outside implementation scope by its own contract.
- MAIL-11 DKIM crypto is blocked on a restricted Track-B annex. No replacement cryptography is invented.
- ONE-2343 is reachable on `w7/W7-TRIAL`, but is not an ancestor of this branch. No other branch is changed. C9 is reused for step-only waits. The outcome substrate is now imported from fixed dependency revision `2aee0b59834faeb421dd0f0925e9a17b96d7f512`, with the task adapter using its records and projector.
- New APIs live under their owning modules. No new crate-root export is required for the work so far.

## Per-step ledger

| Step | Work | Remaining / evidence |
|---|---|---|
| 1 | Shared owner index derived from replicated authority facts; maintained on write/replay/delete; lazy backfill; inbox and saved-query `task_owner` operator | Exact-membership, cursor, backfill and saved-query parity test PASS |
| 2 | Reserved typed esign claims, replayed state machine, unified document/template model, multi-PDF items, three recipient axes, fields and signature rows; VOIDED/EXPIRED are unsealed | State-machine and anti-bypass tests pass in scoped run |
| 3 | send/remind/void use existing outbound manifest, dispatch gate, intent ledger and receipts; content-bound idempotency and recipient-count recheck | Outbound tests pass. Delivery attempts are queued; real mail fulfillment requires the cloud host |
| 4 | Capability-only ceremony with turn/access/viewed/terminal gate order, immediate field writes, server DATE, required fields, sequential promotion and default-closed automation; browser page and public POST endpoints | Engine ceremony tests pass. Public HTTP/generated MCP run passed 69 tests; full server regression results and repairs are recorded below |
| 5 | Queue-backed native PAdES seal job, verification before atomic immutable artifact/status/audit swap, pristine-source reseal with standing owner grant, repair sweep | Real native seal integration test PASS, including verification refusal, immutable original, retry and owner-granted reseal. Production signing backend/clock/cron wiring remains host-owned |
| 6 | Typed append-only audit mirror survives document deletion; SHA-256 chain; certificate and optional audit appendix plus QR before native seal; artifact digest verification | All ten parsed-PDF tests PASS after correcting the ObjStm fixture. Audit/state and native-seal tests also PASS |
| 7 | Owner-editable plural signing principals and DEC-0006 rung/D6 policy, separate access and action gates | Default-closed automated signing and owner policy tests pass. Agent dispatch now checks `on_behalf_of` against the autonomous principal envelope inside the effect transaction; send-with-approval requires Human dispatch through the normal rail. The guard and independent signing-action dial tests PASS |
| 8 | Native HTML templates plus data-only geography packs; exact-byte upload path; bounded general PDF normalization; shared FieldMark drawing program for editor, ceremony and export | Parsed-PDF, HTML, shared-layout and 16 MiB HTTP geometry fixtures PASS in the 119-pass completion run. See `W7-C03-render.md` for supported inputs and typed refusals; no universal font/browser claim |
| 9 | Native mail CID-3 adapter and explicit SelfRun/CloudRun modes | Adapter fixture passes; Stalwart/cloud pack remains outside this checkout |
| 10 | Provider key, modes, deterministic mailbox address, provision/parse seam bound to ExternalEffect intent | Engine fixture passes. `control-plane/convex/mail` is not in this repository |
| 11 | Existing engine inbound event storage reused, canonical managed-domain identity parsing | First-party mailbox store/threading/reply extraction remain cloud work |
| 12 | Host-verified raw webhook bytes enter existing quarantine/gate surface-event admission; forged ingress writes no attempt | Fixture passes; actual Svix verifier and Convex HTTP route remain cloud work |
| 13 | Provision uses host fulfillment seam; e-sign delivery health distinguishes queueing from delivery | Embedded Stalwart send worker and delivery receipts remain cloud work |
| 14 | Host interface states Stalwart >=0.16/JMAP and external key custody | No embedded MTA/JMAP driver/DNS publisher is implemented here; MAIL-11 annex remains unavailable |
| 15 | Engine adapter constrains managed-domain identities | Platform side-domain pool, burn isolation and Cloudflare reconcile remain cloud work |
| 16 | Boundary documented | BYO delegated subzone/paste-record onboarding remains cloud work |
| 17 | Platform handles resolve to addressed-agent attributes; addressed companion wins the turn claim and other companions cannot speak | Mention, single-claimer and silent-unaddressed acceptance test PASS |
| 18 | Member-bounded rooms.list/messages/claim/speak; witness-time claim-before-speaking and receipts; joining never widens memory scope | Scope, speech and thread HEAD test PASS; generated MCP projection PASS. HTTP/Rust/N-API/PyO3/TypeScript/Python projections share the agent verb table; HTTP/Rust runtime and real Node-host checks PASS, PyO3 compilation and Python wrapper fixtures PASS |
| 19 | Vault WaveTaskPort lands TASK rows and typed blocked_by edges atomically, with persisted plan/local-key idempotency and live terminal/blocker reads | Real-store apply/reapply and non-TASK endpoint refusal tests PASS |
| 20 | Vault LinearTaskStore provides snapshots, OCC field writes, bidirectional links and atomic link CAS | Stale revision, moved link and reverse lookup tests PASS |
| 21 | Lease-fenced planner attempt application, queue ready-set dispatch, transactional TASK push outbox and persistent tracker pull cursor | Planner/dispatch and push/poll tests PASS. Hosts must configure and call the provided planner and synchronization doors; cloud credentials and scheduling are not supplied here |
| 22 | Typed manifest-bound per-tool slate, model-output validation, draft floor, header and legacy defaults, authenticated owner CAS overrides | Fixture, invalid draft, floor and owner override test PASS |
| 23 | Explicit POST-ENGINE exclusion retained | Not a current-wave implementation |
| 24 | Owner-adjustable 90-day completed-TASK and completed-attempt retention on cleanup cron; zero/disabled arm; immutable payload, reversible marker, proposal and auto posture retained | TASK/attempt archive, disabled arm and restore tests PASS |
| 25 | Owner-only archive preview with record byte counts; confirmation uses ARCH-0038 with per-row receipts and in-transaction snapshot checks | Preview, owner refusal, changed snapshot and erase receipt test PASS. Private attempt payloads are not entities and are not bulk-erased by this door |
| 26 | Resolver restores selected archived canonical row and its archived attempts inside the scoring transaction | Same-id, cleared-marker, byte equality and no-twin test PASS |
| 27 | Persisted task symbol declarations, holder/expiry, renew/release and stale-hold expiry | Overlap, release, expiry and holder tests PASS; deleted-task holds clear at the common deindex door |
| 28 | Generic and kind-specific queue claims acquire symbol holds atomically and exclude live overlap | Overlap/disjoint scheduler tests PASS |
| 29 | PROJECT uses a free persisted compiled-pack slot; root seed and parent/scope/session/task/branch/skill-fork/goal/budget/roster/ask refs | Root/child round-trip, cycle refusal, CRM coexistence and reopen tests PASS |
| 30 | Four embedded versioned bootstrap SKILL rows enter ordinary hub import/Active admission atomically; reopen marker preserves owner lifecycle changes | Fresh-open and no-write reopen fixtures PASS; all reported bootstrap-related library regressions have green follow-up evidence |
| 31 | Four hand-written top-level SKILL.md files define the improver, judge rubric, goal intake and wake recipe | Conformance parsing and unchanged ordinary hub import are covered by the bootstrap fixtures, which PASS |
| 32 | Project writes atomically derive named-MessagePack channel rooms, reciprocal links, roster membership and ChangeLog; root gets its house room | Membership propagation, forged-room refusal, public wire projection and thread-excluded HEAD tests PASS |
| 33 | Task creation changed to count-only admission, including Wave, consult fanout and counter minting; authority ceilings remain enforced | Count-only tests PASS (including authority proposal regressions). ask/first-answer/C9 step-only wait implemented; both orderings and unchanged run pass. External SDK step binding and generated MCP tasks/rooms projection PASS in the scoped runs. Typed shared ONE-2343 questions/answers/outcome joins and all non-MCP SDK projections are implemented. All five outcome regressions PASS with sync/test-hooks and again featureless. The real Node-host ask/wait/answer/outcomes test and final five-crate all-target lint also PASS |

## Canon corrections

- `backend/oneiron-arch-0006a-conversations-api-core.md`: the task/room table projects the new verbs across all SDK transports. Step-only C9 waits return control to the caller; they do not suspend the run.

- `agents/oneiron-arch-0073-vault-auto-cleanup-v1.md`: the resolver waterfall now restores its selected archived row. Earlier engine comments saying this hook is unbuilt are stale. Archive is a visibility marker, not payload erasure.
- `agents/oneiron-arch-0053-skills-agent-self-knowledge-v1.md` §7, §10a defines the embedded four. The shipped files are generic engine configuration, not consumer personas. The same HubPackage files enter the ordinary import gate unchanged.
- `agents/oneiron-arch-0063-agent-inbox-email-identity-v1.md` §6 / INB-11 defines the shared owner index; TASK authority facts, not caller-provided display fields, are its source.

## Validation

Early evidence used the worktree target. Current builds use the factory Cargo wrapper and its ticket-owned remote cache; explicit `-j 2` and test threads 2. No shared target/config or host changes.
- `cargo test -p oneiron --lib skill_hub::bootstrap -- --test-threads=2`: PASS, 2 tests.
- Initial `cargo test -p oneiron --lib vault_cleanup -- --test-threads=2`: 48 passed, 1 fixture failure (random agent had Proposed ceiling). Fixture changed to a Human actor; retry is green in the 177-test scoped run below.

## Integration decisions and review

- PROJECT is registered through the existing compiled-pack registration door with prefix `pj` / pack `oneiron.project`. The allocator selects a free compiled-product slot (107 on a fresh vault, not a universal type byte). Validators and projections resolve the persisted pack binding. Existing CRM slots are never hijacked. The static canon byte snapshot is not hand-edited. Root identity is minted once per vault; the initial leader is its existing `sys.team_lead` definition. The owner can replace the root roster and leader without reseeding.
- Home-room ids are derived from project ids. Project writes, including raw batch writes, project membership into existing CONVERSATION rows. Room membership grants no additional claims scope. ChangeLog rows are local derived projection evidence, not a second replicated authority source.
- Scope references and member references round-trip as canonical ids. Project ancestry must resolve and be acyclic. Optional goal/budget/member rows can be filled in after the root is seeded.
- Task-review finding: deleted TASK symbol holds now clear at the common deindex door. The suggestion to recreate a deleted wave slot was rejected: retry must not undo an owner's deletion by minting a replacement. A deleted planned task makes that plan fail closed.
- Task-review quota finding was resolved against the newer OF-520 contract, not by adding a rate refusal. Every new task is counted; plan retries do not recount reused tasks.
- Linear inbound fields update the mirror projection and title, never the engine's execution spec, terminal result, or assignee routing. Scheduler readiness uses authoritative execution state.

Additional validation:
- `cargo test -p oneiron --lib -- task_verb:: vault_cleanup:: connector_key::slate workspace_roster::project --test-threads=2`: PASS, 177 tests, 1 pre-existing ignored, 6376 filtered. This precedes room and count-only edits.
- Broad library run compiled and passed initial suites, but was cancelled for changed-source scoped validation. It is not a full-crate pass.

- `cargo test -p oneiron --lib workspace_roster::rooms -- --test-threads=2`: PASS, 1 acceptance test.
- `cargo test -p oneiron --lib -- task_verb:: workspace_roster::rooms --test-threads=2`: PASS, 126 tests, 1 pre-existing ignored. Includes count-only task creation and all integration fixtures.
- `scripts/ratchet/root-surface-check.sh`: PASS (701 names).

## Earlier integration evidence and limitations (superseded below)

- An earlier scoped run compiled core and ran `blob_artifact::esign`, `task_verb::`, `llm::step::`, native mail and outbound tests: **286 passed, 1 failed, 1 ignored**. The failure was PDF object-stream admission: lopdf filters both the stream and its xref entry. Admission now compares the parsed objects to the *original* xref set. Superseded by the green e-sign tests in the focused 77-pass run.
- Public signing capabilities are 256-bit values stored only as hashes. The browser consumes a fragment token and clears the fragment; JSON POST carries it, with no session/cookies, no-store and no-referrer responses. Download uses an immutable pinned artifact version, not a mutable head. Host-supplied forwarded IP headers are not trusted as audit identity.
- Native PDF preparation deliberately supports a bounded static profile. See `impl-notes/W7-C03-render.md`; it is not evidence that arbitrary upload, HTML templates or all fonts work.
- Task first-answer settlement is node-local serialized vault authority. Step-only C9 traps never park or resume the queue attempt. An external SDK step can use a private, actor-bound detached C9 context, without inventing an engine run.
- Engine SDK verb constants now generate the MCP tasks/rooms tools. Existing non-MCP HTTP/NAPI/TypeScript surfaces are hand-written; the canon's all-wire generation rule is not implemented repository-wide by this change.
- Canon correction: ARCH-0064 needs to state the production PDF rendering profile or supply the general renderer; ARCH-0063/CID-3 must distinguish engine admission from cloud fulfillment. ARCH-0006a's single generated SDK table remains an architectural target rather than current repository fact.

- Public HTTP/generated MCP validation: `cargo test -p oneiron-server --lib -- mcp::tests api::esign::tests --test-threads=2`: **69 passed**, using the factory MacBook target and worktree-relative real TMPDIR.
- The follow-up renderer refusal was traced to the fixture writer silently discarding ObjStm objects; the fixture now patches a same-length placeholder to carry a real object stream without changing xref offsets.
- Seal output and signature uploads now use a vault-local MACHINE actor (created transactionally on first use). Artifact ids are not provenance actors. This adds no class-wide policy grant; generated versions keep their existing Proposed floor.
- C9 step-only transitions clamp their time to the authenticated predecessor's time. A stale step context cannot create an inverted supersession range after a newer signal.
- Public PDF-first access carries the real peer IP and UA into Viewed. Missing peer transport fails closed. The managed listener must supply peer information before hosting signing pages. Forwarding headers alone are never legal audit evidence.
- Signature images have a 64 MiB aggregate stored-byte budget per document and seal preparation has a separate 64 MiB decoded raster budget. Exact image retries do not spend twice.
- Certificate QR links accept the shipped page's 64-hex fragment bearer without leaking it in an HTTP path or query. Other fragments remain rejected.

- Full crate testing exposed numeric-slot hijacking: the old PROJECT check interpreted every dynamic byte 107 as a project, even on an unseeded CRM vault. This is fixed at the common put validator and projector using the live pack binding; a CRM-before-project reopen fixture covers allocation and preserved type identities. Campaign registration fixtures now select free *live* slots and compare the registry before/after a refused operation instead of assuming an empty registry.


### Regression repair after full-crate invocation

- `cargo test -p oneiron -p oneiron-server --no-fail-fast` compiled both crates and completed the core library. It found 65 core regressions. All 17 e-sign/ask/renderer fixtures in that snapshot passed. It also passed integration binaries of 6, 36 and 13 tests, then ran 472 integration tests with one stale rate-oracle failure (now corrected). The run stopped during sync integration; no completed full-crate verdict is claimed.
- Most library failures were fixed-count assumptions about a fresh vault. Production bootstrap rows remain present. Read/write refusal tests now compare against their initial state; witness tests exclude only the known house room. Narrow embedding worker and migration fixtures use the existing unseeded test door because their law requires an isolated, explicitly supplied job set; fresh-vault bootstrap tests still use the production opener.
- The PPR corruption fixture must stamp the current graph version. Version zero became stale after bootstrap; that exercised stale-cache bypass rather than corrupt-cache rejection. The year-granularity discovery test now has room for the initial seed population rather than accidentally testing top-10 ranking.
- Malformed policy manifests remain readable for owner repair. Bootstrap skill imports are deferred while the manifest diagnostic forces fail-closed admission; the malformed bytes are not replaced and their gate is not bypassed.
- A focused rerun covers every reported library failure plus current e-sign, bootstrap and project acceptance tests. Results are pending; these edits are not yet a pass.

- Focused regression rerun: **77 passed, 9 failed** (86 run). Current e-sign, malformed-policy open, dynamic PROJECT/CRM binding, bootstrap, embedding, PPR corrupt-cache and temporal-discovery fixtures all passed. The remaining failures are seed baselines in witness edge counts, one community node count, and two follow-up count assertions. These assertions are corrected; a final narrow rerun remains.

### Validation transport and final scope boundary

- The factory's continued coding seat is client-owned. Its long-running command handles have not survived seat resumes; incomplete log tails are not test passes. `rlm.create_session` was attempted for an independent validation owner and refused with `requires a daemon-backed depth-0 session`. No daemon session was created. Commands continue through the ordinary factory wrapper; no target directory or host routing was overridden.
- The sync probe localized its failure to the oracle's full-range `temporal_learned` comparison, not replayed entity bytes: each vault has ten different initial claim/project/room ids. The corrected oracle snapshots initial rows, checks unrelated seeds byte-for-byte, and compares all exchanged and newly written rows. Production seeding is not disabled.
- Latest formatter completed successfully after the orphan test attribute and sole unused import were removed. Ratchet, 701-name root surface, JavaScript syntax and diff whitespace checks passed. Code maps were regenerated.
- This branch does not complete cloud mail (steps 9–16), the general PDF/HTML-template/shared-editor floor (step 8), or repository-wide SDK generation and ONE-2343 outcome integration (step 33). It also does not supply a host signing identity, mail fulfillment worker or external cron deployment. These are explicit remaining deliverables, not implied by the engine fixture passes.

- Final library follow-up: **18 passed, 0 failed**. Together with the 77-pass focused run, this covers all 65 failures from the completed broad library invocation. These are reused library results, not a claim that an entire new workspace run passed.
- The updated sync suite could not compile on the Mac mini: `No space left on device (os error 28)` at `/Users/olety/w7-build/wt/W7-C03/target/debug/deps/rmetadPN951`. This is a build-host failure, not a sync test result. No shared cache was deleted and no host was forced.

### Server regression follow-up and stored room format

- Full server invocation completed: library **738 passed, 10 failed, 5 ignored**; main integration binary **80 passed, 11 failed**; remaining binaries **36, 3 and 4 passed**. Failures were stale tool/seed censuses, occupied CRM fixture slots, and embedding fixtures that assumed no bootstrap claims. The corrected expectations retain production seeds and all generated rooms tools. Paging tests still check continuation, independent cursors, and terminal markers.
- The success snapshot exposed a production representation defect: PROJECT/home-room JSON was decoded as a MessagePack scalar by the generic entity projection. These unshipped records now use named MessagePack, like the core entity door. The wire snapshot checks the home room's project, roster and scope, then normalizes only its per-vault ids. No invalid scalar body is blessed and no legacy decoder is added.
- Featureless library and test-target compilation passed: `cargo check -j 2 -p oneiron --tests --no-default-features`. This precedes the storage encoder correction above; the encoder uses an existing feature-independent dependency.
- A final focused cross-crate invocation covers project/room behavior, sync convergence and harness oracles, the corrected task-rate oracle, and server failures. It completed with **134 passed, 2 failed** across the exercised binaries: core library 46, task integration 15, sync integration 32, additional task integration 2, server library 17, server integration 22. The two failures were later assertions in the retained-cursor and campaign-membership fixtures. Both are corrected; the final rerun passed **2 tests, 0 failures** (`cargo test -p oneiron-server --features oneiron/test-hooks --no-fail-fast -- <the two exact tests> --test-threads=2`). Formatter and code-map check are green for this source.

### Earlier handoff (superseded by the continuation below)

- All 74 observed failures from the earlier core invocation (65 library, one task oracle and eight interrupted sync cases) and all 21 server regressions now have green follow-up evidence. The corrected room producer and public projection are validated together. Unchanged green cases are reused; there is no claim of a new full-workspace gate.
- Formatter, code-map pin, whitespace check, structural ratchet and 701-name root-surface pin pass. No dependency installation, shared-host change or other-branch mutation was made.
- Remaining independent engine work: general PDF/HTML content-template rendering with the shared editor/ceremony/export layer, and generated non-MCP SDK wires plus the ONE-2343 outcome binding. Current bounded rendering and MCP/C9 behavior are not substitutes for those acceptances.
- Hard boundary: the first-party mail control plane, Stalwart/JMAP/domain operations and BYO onboarding belong to unavailable cloud sources, outside the authorized engine worktree. MAIL-11's restricted security annex is not available. Production signing identity and fulfillment/cron host configuration must also be supplied outside these fixtures. OF-445 remains excluded by its explicit POST-ENGINE contract.


## ONE-2343 ask-local outcome integration

- Imported the shared versioned question/outcome DTOs, canonical entity codec, storage keys and arrival projector from fixed dependency revision `2aee0b59834faeb421dd0f0925e9a17b96d7f512`. The adapter lives in `llm/decision/questions/task_ask.rs`; it does not create a second question registry. No dependency `llm/step`, ladder, model execution, provider implementation, standing evaluator, human-review lane, or owner dials were imported.
- `TaskAskSpec.outcome_binding` is now `Option<OutcomeBinding>`, not opaque text. Validation uses the shared type and retains a 1,024-byte serialized binding cap. A bound ask's JSON question also fits the shared 16,384-byte typed question-text limit.
- The first winning `tasks_answer` transaction writes one immutable `QuestionRecord` version (question id = task id), one `AnswerRecord` and private `judgment.answer` claim, the TASK completion, ask winner and all C9 signals. `TaskAskAnswer.answer_ref` and `.question_version` join Ready/AlreadyResumed payloads to that shared row. `AnswerRecord.unit` is the winning `result_ref`.
- The typed answer is the actual holder's result selection, `DecisionAnswer::Choice(result_ref)`. Probability 1 describes a definite submitted selection, not a model prediction. Provider receipts remain empty; no ladder answer or model confidence is invented. The existing request/answer doors provide entity references, not a ladder probability.
- `Memory::tasks_ask_outcomes` is owner-only. Shared `calibration_pairs` reads one snapshot and reuses the arrival eligibility checks. Both paths recheck principal scope, result-unit visibility, current answer receipt, fact lifecycle, predicate, subject/link, edge endpoint, observed value and event-time horizon. Approved answer claims retain their private-reader restriction. Labels remain idempotent `(answer,fact)` observations.
- Scope additions to claim/gate/write-envelope/edge/batch are only the dependency's needed privacy and relation helpers, facet access, and one shared final-state arrival hook. Existing C03 step-only code stays unchanged.
- Tests cover both wait orders with native/external waits, first-answer winner and concurrent CAS, shared question/answer/task/C9 joins, automatic labels, normal and replicated replay, rollback, horizon boundaries, mutated fact value/subject/predicate/time, private/rejected/retracted facts, private answer reads, linked-unit revocation, claim/event/edge/stage sources, failed unreadable results, idempotency changes, and vault reopen.
- The imported source is integrated and compiled. Runtime evidence below supersedes the earlier source-only inspection; no separate question registry or outcome implementation remains to build.
- Canon wording is not changed in the docs repository. Clarify in the contract that task ask outcomes bind the selected result entity; a human-versus-model comparison requires an actual model proposal/receipt supplied by its owning decision lane, not an invented one.


### Owner-ordered completion: integration history

- The 2026-09-19 recovery ruling treats mail/cloud implementation as an external
  handoff, not an engine blocker. `W7-C03-host-handoff.md` records the exact
  Convex, Stalwart/JMAP, domain pool, BYO and unavailable MAIL-11 boundaries,
  plus production signing identity, fulfillment and cron duties.
- The old static-only renderer and non-MCP/outcome limitations above are
  historical. The current renderer and SDK changes supersede them. Other
  unchanged-byte focused passes remain valid evidence for their stated scope.
- `scripts/sdk/agent-verbs.json` now generates eight agent verbs across engine,
  MCP, HTTP, Rust, N-API, PyO3, TypeScript and Python. CI pins generated bytes.
  Existing legacy memory-facade calls are not misrepresented as newly generated.
- Native validation is owned by a durable per-ticket user service calling the
  normal Cargo wrapper. The first check failed on an unclosed HTML table
  function; that brace is repaired. The interrupted formatter was not a pass.
- Python projection fixture and Bun projection fixture passed. All TypeScript
  package sources passed the MacBook's existing `tsc` with the project options
  and minimal declarations for Node's `createRequire` and `ImportMeta.url`;
  no compiler, dependency, or host package was installed. This scoped source
  check is not a native addon loading test.


### Completion compile evidence

- Cross-crate `cargo check -j 2 -p oneiron -p oneiron-server -p
  oneiron-remote -p oneiron-napi -p oneiron-py --features
  oneiron/test-hooks --tests`: **PASS on MacBook**, current renderer, outcome
  and SDK source. Python disables Rust test targets, so this is not its proof.
- Separate `cargo check -j 2 -p oneiron-py --lib`: **PASS on MacBook**. This
  checks the generated PyO3 methods and their base-mode engine dependency.
- Python wrapper validation now has **2 passing fixtures**, including the public
  namespace seam and native-error translation. Changed Python sources also pass
  `py_compile`. Bun projection remains **1 passing fixture, 4 assertions**.
- A combined runtime invocation was routed to Linux by the normal wrapper and
  failed linking the N-API harness on `napi_reference_unref`,
  `napi_delete_reference`, and `napi_call_threadsafe_function` (documented
  ONE-1997). It is **not** a runtime pass. The portable core/server/Rust-client
  test invocation is now separate. No linker workaround or host change was made.
- Source is committed in `5f21b4b7`, `081e51f9`, `4fcda446`, and `316cacff`.
  Committing source does not change the pending runtime-validation boundary.

### Completion runtime evidence

- Portable core/server/Rust-client invocation: **119 passed, 5 failed**. The
  passing cases include native PAdES sealing, all selected PDF/HTML/shared-field
  fixtures, public HTTP geometry, generated HTTP/MCP transport, and Rust SDK
  dispatch. The five failures were confined to the new outcome fixtures.
- Four outcome regressions subsequently **PASS** on MacBook: first-answer CAS,
  both wait orders and unchanged run, privacy/horizon/mutation/reopen, and
  invalid binding/unreadable result atomicity. The remaining event/edge/stage
  case reached an undeclared `crm.stage` fixture predicate. Its narrow policy
  rule is corrected and the single-case rerun is pending. Production policy
  was not weakened. A deleted test edge is recreated before provenance is
  attached, and rollback now asserts the deliberate abort variant.
- `cargo build -j 2 -p oneiron-napi`: **PASS** on Arch selected by the normal
  wrapper. `node scripts/tests/test_native_agent_verbs.mjs
  target/debug/liboneiron_napi.so`: **PASS** with that real library. Both wait
  orders, exactly-once resume, first-answer replay, bound outcome labels and
  room listing run through the actual N-API methods, not a fake client.
- Repeated standalone N-API test attempts reached the same documented Linux
  linker limitation. No runtime pass is claimed for that Rust harness. The
  real Node-host test supplies the proper native host instead; no linker
  stubs, host installs or routing overrides were used.
- Five-crate formatting **PASS**. The lint recheck cleared core, Rust client,
  N-API and PyO3 and found six server comment/item-order warnings. Those are
  corrected. Final formatted lint and featureless outcome tests are pending.
- The lint cleanup preserves archive preview sorting/deduplication and moves
  lexical-hint validation and final batch projection into private functions,
  without changing validation or transaction order. Follow-up dequeue
  extraction keeps the batch function below its size limit after formatting.

## Final engine acceptance evidence

The pending statuses in the chronological sections above are superseded by these
terminal results. All engine work in the per-step ledger is implemented. Steps
9–16 retain the owner's explicit cloud-repository handoff; step 23 retains its
contract's POST-ENGINE exclusion. Deployment credentials, signing identity and
host scheduling are not claimed as engine fixtures.

- **124 focused Rust cases have green evidence**: 119 passes in the portable
  core/server/Rust-client completion invocation, plus all five repaired outcome
  cases. The final event/edge/stage case passes with `sync,test-hooks`, including
  replicated replay, link revocation, provenance arrival and CRM stage arrival.
  All reported failures from this invocation are resolved; unchanged passing
  cases are reused, not rerun ceremonially.
- `cargo test -j 2 -p oneiron --no-default-features --lib task_verb::ask --
  --test-threads=2`: **PASS, 5 tests**. The command also compiled the complete
  featureless library test target. The same five cases have green sync/test-hooks
  evidence. They were not cfg-disabled to obtain a featureless build.
- `cargo clippy -j 2 -p oneiron -p oneiron-server -p oneiron-remote -p
  oneiron-napi -p oneiron-py --all-targets --features oneiron/test-hooks --
  -D warnings`: **PASS**. Normal wrapper host selection was used. Existing
  vendored heed lifetime warnings and the workspace duplicate bench-target
  warning remain external to the changed-source lint gate; vendor code was not
  modified or silenced.
- Cross-crate test-target check and separate PyO3 library check **PASS** as
  recorded above. Python's Rust test harness is disabled by its crate manifest;
  its generated methods are compiled, and its two public-wrapper fixtures pass.
- The native N-API shared library builds and the real Node-host regression
  **PASS**. Standalone Linux N-API libtest remains the documented ONE-1997 host
  limitation, not a test pass; the actual host-bound behavior is exercised in
  Node without mocked native methods or unresolved-symbol stubs.
- Five-crate formatting **PASS**; the last three changed test files separately
  pass the pinned formatter check. Generated SDK bytes, Python/Bun projection
  fixtures, scoped TypeScript checking, code-map pin, structural ratchet, the
  701-name root-surface pin and whitespace checking **PASS**.
- Earlier broad core/server runs and every reported regression's focused repair
  evidence are retained above. No new full-workspace VERDICT is claimed.

No host package was installed, no shared target/cache was deleted, and no other
branch, worktree or docs repository was changed. The exact cloud mail, MAIL-11,
production seal and fulfillment/cron handoffs are in `W7-C03-host-handoff.md`.


## Acceptance test repair (current validation)

The rejected factory invocation linked `oneiron-napi` as a standalone Linux
libtest binary and stopped on missing Node-host `napi_*` symbols. That command
ran no tests and is not reused as passing evidence. Native bindings are now
validated in their actual Node/Python hosts, separate from the portable Rust
crate suites. No linker stubs or production feature changes were added.

New acceptance checks:
- SGN-03: `signing_field_save_survives_reopen_without_rotating_the_bearer`
  verifies the server-computed DATE, persisted per-field value, same bearer,
  idempotent Viewed audit, and later-signer turn gate after reopening.
- ONE-2098/2100: `linear_vault_occ_cas_reverse_lookup_and_production_push_poll`
  now checks successful outbox acknowledgment, persisted pull cursor after
  reopen, and a completed TASK push with no repeated push on the next poll.
- ONE-2095/2096: `rooms_http_routes_only_the_addressed_companion_and_requires_a_claim`
  drives all four HTTP room verbs, receipt-bearing claims and speech, claim
  replay, and silence of the unaddressed companion.
- SGN-03 HTTP: `forwarded_headers_cannot_replace_the_signing_transport_peer`
  checks that action, PDF, and preview refuse forged forwarding headers when
  the authenticated socket peer is absent.
- ONE-2564: `crates/oneiron-py/tests/test_agent_verbs.py` exercises the actual
  PyO3 extension through the public Python SDK. Both answer/wait orders,
  step-local exactly-once delivery, work while pending, outcome joins,
  count-only bursts, and typed room refusal are covered.

Fresh runtime evidence (ticket-owned Arch target selected by the normal wrapper;
Cargo jobs 2, Rust test threads 2; no shared cache/config changes):
- `cargo build -j 2 -p oneiron-napi -p oneiron-py`: PASS.
- Real Python extension copied into an isolated package staging directory;
  `python -m pytest crates/oneiron-py/tests -q`: **144 passed**. The isolated
  test venv is `/tmp/W7-C03-fix-tests/venv`; `PYTHONPATH` names only the staged
  current wrapper and freshly built `_native.abi3.so`. One existing fork
  deprecation warning is reported by CPython.
- `node scripts/tests/test_native_agent_verbs.mjs target/debug/liboneiron_napi.so`:
  PASS against the freshly rebuilt library.
- `bun test /tmp/W7-C03-fix-tests/js-package/test`: **183 passed**, 737 assertions.
  The current package source/tests are staged unchanged; its test loader loads
  the real rebuilt N-API library, not a mock native client.
- Generated SDK byte check, two Python projection fixtures, code-map pin and
  diff whitespace check: PASS.
- The broad core/server/Rust-client invocation compiled every test target and
  ran 431 passing core cases with no runtime failures before it was stopped
  during unchanged authority cryptographic property tests. This is not a
  full-crate pass. The coding-seat rule requires reusing unchanged green
  evidence, not repeating the whole repository gate for test-only additions.
- The replacement invocation runs the complete named acceptance modules in
  core plus HTTP/MCP and Rust SDK tests. Featureless execution checks the new
  core fixtures separately. Logs, exact filters and durable unit identities
  are in `/tmp/W7-C03-fix-tests/run.json`.
- Named acceptance run: **335 passed, 0 failed, 1 pre-existing ignored**:
  core 259, server 75, Rust SDK 1. All new Rust fixtures ran and passed.
  Command: `cargo test -j 2 -p oneiron -p oneiron-server -p oneiron-remote
  --features oneiron/test-hooks --no-fail-fast -- <filters> --test-threads=2`.
  Filters (libtest ORs them): `task_verb::`, `blob_artifact::esign::`,
  `workspace_roster::project::`, `workspace_roster::rooms::`,
  `connector_key::slate::`, `vault_cleanup::`, `skill_hub::bootstrap::`,
  `channel_identity_provider::native_mail::`, `llm::step::`,
  `api::facade::tests::`, `api::esign::`, `mcp::tests::`, and
  `generated_sdk_ask_answer_and_wait_round_trip`.
- Featureless core acceptance: **3 passed, 0 failed**. Command:
  `cargo test -j 2 -p oneiron --no-default-features --lib --
  blob_artifact::esign::tests::signing_field_save_survives_reopen_without_rotating_the_bearer
  task_verb::production_ports_tests:: --test-threads=2`. This compiled the
  complete featureless library test target and ran both real-store planner /
  mirror tests and the new signing-reopen test; no tests were cfg-disabled.
- These are changed-crate acceptance results, not an unfiltered workspace or
  full-crate verdict. All code changes in this repair are tests; prior green
  unchanged-byte evidence remains listed above. The known Linux standalone
  N-API libtest link restriction remains; actual Node-host tests replace that
  invalid host invocation, and Python's manifest intentionally uses Python
  tests rather than a Rust libtest harness.

Final command exit statuses are zero. Winning logs and a SHA-256 manifest are
retained at `/home/lexi/w7-build/tickets/W7-C03/artifacts/fix-tests-8d9052aa/`.
Pinned formatting, code-map check and whitespace check pass on the final source.


## Owner-authorized factory fixture repair (after tests:2)

The full standard factory run at 2026-09-19T10:55:49Z ran 8,772 tests and
exited 101. Its failures were eleven secret lease/snapshot fixture cases and
eight `vault_read_parity` cases. This is separate from the earlier passing
scoped acceptance evidence and is not the C08 floating-point parity issue.
The original writer record, fix-tests transcript, and appended factory logs
remain untouched. The full rejected log is preserved under
`recovery/rejected-continuation-20260919/c03-test-recovery/ticket-before/logs/`.

- Secret T2 fixtures now create their temporary directory under
  `std::env::temp_dir().canonicalize()`. On macOS `/var` is a symlink to
  `/private/var`; it is an accidental unsafe ancestor in the old fixture.
  Canonicalizing the existing root before fixture construction removes only
  that host alias. Explicit malicious target/parent symlinks are still planted
  afterward and must still fail. Production `check_secret_file_policy`,
  `O_NOFOLLOW`, occupant refusal, permissions, and teardown are unchanged.
  This matches the read-only C08 fixture precedent; no other worktree changed.
- The parity fixture's `probe_short_ref` searched only `cl1` through `cl8`.
  Production first-open now imports four bootstrap skills and their eight
  claims. `batch/short_id.rs::plan_short_id_update` correctly assigns the next
  per-type counters to the two appended fixture claims, so the test never
  searched their actual refs. The probe now uses the live CLAIM count as its
  finite bound. This fixture is fresh and append-only, with no deletions, so
  that count bounds the allocator. It still uses the public hydrator to prove
  the actual identity and hash. No short-ID alias, index rewrite, policy grant,
  unseeded opener, relaxed score normalization, or fallback-to-hex was added.
  All scope-denial/absence indistinguishability and exact parity assertions
  remain unchanged.
- Commits: `cee5bfea` (canonical temp roots), `1223303d` (seed-aware probe).
  Source changes are only in the three failing test files.

### Review receipt boundary

Before edits, collected PR 933's latest top-level comments, reviews and all
inline threads, plus the supplied internal root/subreview packet. At head
`ff98e02bb9d0a10e2e4669e19a7d5e30403cd9c3`, PR 933 is **W7-C08**, not C03.
There is no PR for `w7/W7-C03` at collection time. Its 24 deduplicated bot
findings and two internal findings are recorded with IDs and explicit
cross-ticket dispositions in
`tickets/W7-C03/artifacts/factory-test-repair-review/ledger.md`. C03 does not
resolve or edit C08's findings. Raw current receipts and hashes are retained
beside that ledger. Codex's latest summary reported Running for C08, not a
quota error; this seat starts no new review and does not wait on that unrelated
request. Existing incomplete internal review lanes are not marked clean.

Validation: the complete secret_lease and secret_snapshot unit modules plus
all nine vault_read_parity tests are queued through the normal Cargo wrapper
with two build jobs and two test threads. The ticket-owned service is
`w7-c03-factory-fixtures-20260919`; exact argv, host selection and full output
are under `tickets/W7-C03/artifacts/factory-fixture-repair-104dcf40/`.
The standard factory submit must still rerun its normal full test stage; no
factory test receipt is replaced by this focused validation.

A native Node-host allocation probe against the previously validated, unchanged
production addon confirms the registration diagnosis: a fresh seeded vault
returns `cl9` for entity `22222222222222222222222222222222` and `cl10` for the
next appended claim. This is diagnostic evidence only, not the repaired parity
suite result. Script and output are retained beside `fixtures.log`.

The post-fix GitHub explanation is comment
[5741434829](https://github.com/oneiron-dev/oneiron/pull/933#issuecomment-5741434829).
It explicitly identifies PR 933 as C08, lists C03's repairs, preserves the
cross-ticket findings rather than dismissing them, and states that Rust
validation remains pending. Its raw response is retained with the review
receipts. No PR/thread resolution or fresh review was requested.

At the earlier capacity-wait checkpoint the validation service was alive but had not obtained a
build slot. The MacBook and Arch slots were occupied; the mini had about
13,108,616 KiB free, below the wrapper's 30 GiB reserve. Do not launch another
C03 validation or bypass shared-host limits. Resume the existing service's
log/result, then replace this pending status with the actual test outcome.
A source-format check, the code-map check and `git diff --check` pass.


### Final fixture repair validation

The existing owned service completed successfully (`Result=success`,
`ExecMainStatus=0`). It was consumed, not restarted. The normal wrapper chose
Arch slot 2/2 after the capacity wait. Source commit `1223303d` was unchanged
by the intervening documentation commit.

- `cargo test -j 2 -p oneiron --features sync,test-hooks --lib
  --test vault_read_parity --no-fail-fast -- secret_lease::tests::
  secret_snapshot::tests:: structured_success_parity
  scope_denial_is_indistinguishable_from_absence validation_error_parity
  engine_error_parity runtime_unavailable_parity_across_all_three
  cloud_structured_read_contract in_process_is_not_privileged
  serialization_round_trip golden_wire_shapes --test-threads=2`:
  **42 secret module tests + all 9 parity tests passed; zero failures/ignored**.
- Reused the freshly compiled unit-test binary with `TMPDIR` pointing to a
  deliberately created symlink to a real temporary directory. Both complete
  secret modules passed again: **42 passed, zero failures/ignored**. This
  exercises the aliased-temp-root cause even though the wrapper selected Arch.
  The planted hostile-parent and occupied-target security tests still pass.
  The alias and its owned directory were removed after the test process exited.
- `rustfmt --edition 2024 --check` on the three changed Rust test files,
  `python3 scripts/codemap/codemap.py --check`, and `git diff --check`: pass.
- Exact commands, logs, binary hash, service result and final SHA-256 manifest:
  `tickets/W7-C03/artifacts/factory-fixture-repair-104dcf40/`.

These results resolve this repair's nineteen prior failing cases. They are
focused validation, not a new full-suite pass. The standard factory test stage
must still rerun normally; factory state and genuine writer receipts remain
untouched. The earlier 335/3/144/183/native-addon scoped evidence remains valid
with its original scope and is not relabeled as full-suite evidence.


## Standard factory continuation: Linux N-API libtest link

The new standard factory attempt at 2026-09-19T11:43:24Z exited 101 and ran
zero tests. It reached Arch and failed to link `oneiron-napi`'s standalone Rust
test binary: `napi_reference_unref`, `napi_delete_reference`, and
`napi_call_threadsafe_function` normally resolve from the importing Node host.
The secret/parity repair is not implicated.

Commit `104ba7b4` adds napi-rs's supported `dyn-symbols` feature as a **Linux
only dev-dependency**. Workspace resolver 2 keeps this test feature out of
normal production builds. The production dependency still disables defaults
and selects only `napi8` and `serde-json`. No Rust test is removed or ignored;
there is no no-op implementation or ABI mock in Oneiron. napi-rs's hostless
fallback symbols are not evidence of JS correctness: these Rust tests cover
converters and actual engine calls; real Node-host tests remain the JS ABI
proof. Their previous green unchanged-production-byte evidence is preserved.

Before this edit, refreshed all PR933 REST comments/reviews and GraphQL
threads. Current counts: 12 top-level comments, 4 reviews, 34 inline threads,
with pagination exhausted. Codex completed review 5255585686 with six new
findings on C08 at ff98e02; it is neither pending nor a quota failure. Cursor
reported its own explicit usage limit. All six additional findings are in
`artifacts/factory-napi-test-repair-review/ledger.md` with IDs, current head,
raw bodies and cross-ticket dispositions. C03 does not edit C08 or resolve its
review threads. The existing internal reviewer/subreview packet and prior
24 bot/two internal dispositions remain preserved. No fresh review launched.

One unfiltered rerun of the exact standard five-crate Cargo command is owned
by `w7-c03-full-after-fixtures.service`, with source commit, full output and
terminal-result writer in `artifacts/factory-full-after-fixtures/`. It includes
oneiron, oneiron-napi, oneiron-py, oneiron-remote and oneiron-server. No filter,
ignored-test override or crate exclusion was added. The original factory
logs/state/writer/session remain untouched; this owned command does not write
a fake factory receipt. Validation is in progress, not yet a pass.

GitHub explanation 5741434829 was updated after this change with the new
link cause, Linux test-only dependency, six fresh cross-ticket findings, and
the exact pending unfiltered validation scope. Raw update response is retained
in `artifacts/factory-napi-test-repair-review/github-post-pending.json`.


### Unfiltered standard-command result

The exact standard five-crate command completed with exit 0 on the MacBook
(`olety@100.124.216.116`, wrapper slot 2/3): **8,772 passed, zero failed,
21 pre-existing ignored, zero filtered out**, across 30 libtest result blocks.
This includes all 9 `vault_read_parity` tests and all 22 N-API Rust tests.
The core library result is 7,077 passed / zero failed / 4 pre-existing ignored;
remaining integration/server/Rust SDK/doctest results are in the full log.
Python has no Rust libtest target; its prior 144 real-extension tests remain
separate evidence, not part of this Rust count. No `#[ignore]`, test filter,
crate exclusion, or fake factory state was introduced.

Exact argv:
`cargo test --no-fail-fast -p oneiron -p oneiron-napi -p oneiron-py
-p oneiron-remote -p oneiron-server`.
Receipt: `artifacts/factory-full-after-fixtures/{command,result,summary}.json`;
complete output: `artifacts/factory-full-after-fixtures/cargo-test.log`.
Source commit is `104ba7b4`; later changes only document the test-host contract.
This is the standard factory changed-crate command, not a claim that all nine
`scripts/verify.sh` workspace stages ran.

Because macOS's default test build does not enable a Linux-only dev-dependency,
a separate narrow validation explicitly exercises `napi/dyn-symbols` with
`cargo test -p oneiron-napi --lib --features oneiron/sync,napi/dyn-symbols`.
It is the single owned job `w7-c03-napi-dyn-symbols.service`; source receipt
and completion writer are in `artifacts/napi-dynamic-symbol-validation/`.
The completed full run is not repeated. This final feature-path check is
pending; no production feature or source changes are made for the check.


### Final Linux feature-path result and handoff

The narrow check completed with exit 0 on **Arch**, selected by the normal
wrapper (slot 1/2). `oneiron-napi` linked its standalone Linux test binary and
ran **22 tests: all passed, zero failed/ignored/filtered**. The exact argv,
source commit, host output and exit receipt are retained under
`artifacts/napi-dynamic-symbol-validation/`. This closes the previously
unvalidated dynamic-symbol feature path without repeating the full suite.

Final evidence for this continuation:
- Unfiltered standard five-crate Cargo command on MacBook: 8,772 passed,
  zero failed, 21 pre-existing ignored, zero filtered, exit 0.
- N-API dynamic-symbol feature path on Linux: 22 passed, zero failed,
  zero ignored, zero filtered, exit 0.
- Prior fixture repair on Arch: 51 passed; the complete 42 secret tests also
  passed with a deliberately symlinked TMPDIR. The full MacBook command now
  additionally proves the fixture repair on its original failing platform.
- Prior 144 real Python extension tests, 183 JS tests/737 assertions, native
  Node addon, three featureless acceptance tests, and remaining scoped
  acceptance receipts retain their exact original scope. Production bytes
  covered by those tests did not change in this continuation.
- No production security policy, parity assertion, test ignore, gate exclusion,
  shared Cargo configuration, factory state or genuine writer record was changed.

AGENTS.md/WORKFLOW.md now describe the test-only Linux loader and preserve the
separate real-Node ABI requirement; they no longer advise that this repaired
Rust test link must fail. The existing verify.sh exclusions remain unchanged.
The result is a passing standard changed-crate test command, not a claim that
the entire nine-stage workspace verification script ran. All logs, terminal
receipts and source pins have SHA-256 manifests in their artifact directories.
GitHub explanation 5741434829 is updated with these final results and preserves
the explicit C03/C08 review boundary and six newly collected Codex findings.


## Review repair: audit survival after document deletion (S6 / SGN-05)

The Opus finding at `cdda5112` was valid: the previous audit durability test
only reopened a live document. Added one regression in
`blob_artifact/esign/tests.rs`: draft, send and view a document, capture its
three audit rows, delete the artifact through `Vault::delete_entity`, prove
the artifact is absent, and require `esign_audit` to return exactly the same
rows both immediately and after reopening. The public audit reader validates
the sequence/hash chain. No production code or deletion policy changed.
The canon's survival requirement was correct; its test coverage was missing.

Before editing, refreshed all-state C03 PR discovery (no PR exists) and the
previously named PR933's complete review record: 15 top-level comments,
6 reviews and 40 complete inline threads at C08 head
`8504e48978737cc41eae473f85d88ef3087131e4`. Preserved and deduplicated the prior
Qodo/Codex/CodeRabbit and internal findings, plus six newer Codex findings.
None of those inline paths overlaps C03's branch changes. They remain with
C08; no other ticket's code or review threads were changed. Codex completed;
Cursor reported explicit usage-limit errors. The supplied Opus report is the
only actionable finding for this repair. Raw receipts and the combined ledger:
`tickets/W7-C03/artifacts/esign-audit-delete-review/`.

Validation completed successfully on the MacBook (`olety@100.124.216.116`,
standard wrapper slot 2/3) at source commit `6ee0f0ab`:
`cargo test -j 2 -p oneiron --lib blob_artifact::esign::tests:: -- --test-threads=2`.
**9 passed, 0 failed, 0 ignored, 6570 filtered out.** The featureless core
library test target compiled, and the new delete-and-reopen regression passed.
The run used the existing ticket-owned target with two compiler jobs and two
test threads. The persistent `w7-c03-esign-audit-delete-review.service` completed
with `Result=success`, `ExecMainStatus=0`; its log and terminal receipt are in
the artifact directory above. The earlier session-bound capacity waiter ran
no tests and is retained only as a non-evidence diagnostic log.

Changed-file rustfmt, the code-map check (2580 files) and `git diff --check`
passed. Prior green full changed-crate evidence remains scoped to its original
head; this test-only repair does not repeat unchanged workspace/binding suites
or claim a new full gate. GitHub explanation 5741434829 was updated with this
repair, the cross-ticket skips and these exact final results. No PR933 thread
was resolved; that PR remains C08's, not C03's.


## Tests-after-review repair: deterministic deletion race fixtures

The standard five-crate run at 2026-09-19T13:51:19Z exited 101 with one
failure: `tests::deletion::headerless_delete_raced_to_nothing_emits_no_receipt_sweep_or_pt`.
Its bare barrier did not order the read probe before the eraser commit, so all
three attempts could enter the fully-missing path instead of the intended race.
This is a test scheduling defect, not an observed deletion-policy failure.

The headerless test now parks the deleter at the existing vault-local,
entity-keyed `AfterTombstonePublish` seam, commits the competing vector erase,
then releases the purge. This seam is reached in both feature modes. The
headerful sibling now uses the existing post-header-read rendezvous. Removed
the unused bare-barrier helper and retry branches; every run must construct
the race. All audit, sweep, pending-marker, propagation and reason-byte
assertions remain. No production source or process-global test state changed.
The canon's deletion law remains correct; only fixture scheduling was wrong.

Before edits, refreshed all PR933 comments/reviews/inline threads and consumed
C03 internal findings. No C03 PR exists; PR933 is C08 at `8504e489`. Intake
contains 16 top-level comments, 20 reviews, 94 inline comments in 40 complete
threads, including new C08 fix replies and CodeRabbit's still-disputed F18.
Cross-ticket findings stay with C08, not dismissed or resolved by C03. Codex
completed review 5255660020; Cursor's explicit limit is not a Codex pending
state. Opus's audit-deletion finding is already fixed by `6ee0f0ab`; Grok
reported LANDABLE. Full raw receipts and one grouped ledger are retained in
`tickets/W7-C03/artifacts/deletion-race-test-repair/review/`.

### Final validation of `2051005e`

Both serialized services completed successfully on Arch, selected by the
normal wrapper (slot 1/2), using the existing ticket-owned target:

- `cargo test --no-fail-fast -p oneiron -p oneiron-napi -p oneiron-py
  -p oneiron-remote -p oneiron-server`: **8,798 passed, 0 failed,
  21 pre-existing ignored, 0 filtered**, across 30 result blocks, exit 0.
  The core library contributed 7,096 passes. Both repaired race tests,
  all 13 deletion tests, and the e-sign delete-and-reopen regression passed.
  This is the unfiltered standard five-crate command, with no new test
  exclusions or ignores. The wrapper enforced three compiler jobs on Arch;
  `RUST_TEST_THREADS=2`. The earlier environment-only request for two compiler
  jobs was overridden by that standard wrapper ceiling.
- `cargo test -j 2 -p oneiron --lib --no-default-features tests::deletion::
  -- --test-threads=2`: **12 passed, 0 failed, 0 ignored, 6,585 filtered**,
  exit 0. The complete featureless library test target compiled, and every
  deletion-module case available without sync ran, including both repaired
  races and the partial-residue guard. This command explicitly kept two
  compiler jobs. It started only after the standard command succeeded.
- Changed-file rustfmt, regenerated code-map pin and `git diff --check` passed.
  Final Rust and map bytes are unchanged from the validated source commit.

Logs, exact commands, host/budget/source pins, result summaries and terminal
receipts are under `tickets/W7-C03/artifacts/deletion-race-test-repair/`.
The factory's genuine logs/state/sessions were not overwritten. These are
standard changed-crate tests plus featureless regression evidence, not a claim
that all nine workspace verification stages ran. Prior real Node/Python ABI
results retain their original scope; no binding production code changed.
GitHub explanation 5741434829 records the repair, explicit C08 skips, and
these final results. No C08 review thread was changed or resolved.
