# W7-C03 implementation notes

Base: `4689a7f1`. Worktree: `w7/W7-C03`. This is an in-progress record, not a full-cluster acceptance claim.

## Decisions

- The engine remains consumer-neutral. Cloud control-plane changes cannot be placed in this Rust engine as product-specific Convex files.
- OF-445 explicitly says POST-ENGINE and not in this wave. Step 23 remains outside implementation scope by its own contract.
- MAIL-11 DKIM crypto is blocked on a restricted Track-B annex. No replacement cryptography is invented.
- ONE-2343 is not present in base git objects. Existing C9 signal/wait doors are the available substrate; dependency integration remains to be checked.
- New APIs live under their owning modules. No new crate-root export is required for the work so far.

## Per-step ledger

| Step | Work | Remaining / evidence |
|---|---|---|
| 1 | Shared owner index derived from replicated authority facts, maintained on TASK writes including replay and on delete; lazy backfill; inbox task query and saved-query `task_owner` operator | Test pending |
| 2–8 | E-sign context packet inspected | Implementation and acceptance remain |
| 9–16 | CID-3 and cloud boundary context gathered | Engine adapter and external cloud implementation remain; restricted DKIM annex unavailable |
| 17–18 | Room and addressing seams identified | Implementation and acceptance remain |
| 19–21 | Vault Wave/Linear storage seams identified | Implementation and acceptance remain |
| 22 | Grant slate context gathered | Implementation and acceptance remain |
| 23 | Explicit POST-ENGINE exclusion retained | Not a current-wave implementation |
| 24 | Owner-adjustable 90-day completed-TASK retention on existing cleanup cron; zero/disabled arm; archive retains bytes | TASK test pending; attempt retention remains |
| 25 | Owner-only explicit archive preview with record byte counts; confirmation uses existing gated ARCH-0038 path with per-row receipts; snapshot checked inside linearizing transaction | Scoped test passes; full crate pending |
| 26 | Resolver restores selected archived canonical row in the same scoring transaction | Regression test pending |
| 27–28 | Lease and queue seams identified | Implementation and acceptance remain |
| 29,32 | Project/home-room/DAG seams identified | Implementation and acceptance remain |
| 30–31 | Four embedded, versioned bootstrap SKILL.md files; first-open hub import and Active admission atomic; marker makes reopen a no-op, preserves owner lifecycle changes | Two bootstrap tests pass; full crate pending |
| 33 | C9/task-verb seams identified | ask/wait and no-rate-refusal implementation remain |

## Canon corrections

- `agents/oneiron-arch-0073-vault-auto-cleanup-v1.md`: the resolver waterfall now restores its selected archived row. Earlier engine comments saying this hook is unbuilt are stale. Archive is a visibility marker, not payload erasure.
- `agents/oneiron-arch-0053-skills-agent-self-knowledge-v1.md` §7, §10a defines the embedded four. The shipped files are generic engine configuration, not consumer personas. The same HubPackage files enter the ordinary import gate unchanged.
- `agents/oneiron-arch-0063-agent-inbox-email-identity-v1.md` §6 / INB-11 defines the shared owner index; TASK authority facts, not caller-provided display fields, are its source.

## Validation

Linux, worktree-owned `target/`, `CARGO_BUILD_JOBS=2`, `RUST_TEST_THREADS=2`. No shared targets or host changes.
- `cargo test -p oneiron --lib skill_hub::bootstrap -- --test-threads=2`: PASS, 2 tests.
- Initial `cargo test -p oneiron --lib vault_cleanup -- --test-threads=2`: 48 passed, 1 fixture failure (random agent had Proposed ceiling). Fixture changed to a Human actor; retry pending.
