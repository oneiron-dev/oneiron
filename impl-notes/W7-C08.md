# W7-C08 implementation notes

## Canon and decisions

Binding pages (docs mirror is read-only):
- `site/src/pages/oneiron/runtime/index.astro`: ContentPart, failure contract, raw-call seam, streaming, and `mint:S4` schema policy. The ticket's L1/L2/L4/L6/L7 labels are stale; use the named anchors.
- `site/src/pages/oneiron/agents/oneiron-arch-0026-dreamer-v2.astro`: wire adapters and catalog data.
- `site/src/pages/oneiron/extraction/oneiron-arch-0036-model-families-v1.astro`: roles, tier defaults, tagger and surprise gate.
- ARCH-0040 `dashboard-v2:D5` and the OF-137 decision ledger: propose-only BYO runners. The ticket's surfaces §3 reference has no BYO runner text.
- ARCH-0027: provider ingest/read codecs and secret-null serialization.
- ARCH-0061: owner voice reference bank, render targets and normalized ASR.
- ARCH-0076 §5: priced model rows and external score evidence. OF-328 uses its registry/ruling text.

New APIs live under existing public modules, not the flat crate root. The root remains at 701 names. No image SDK, process-global state, or lint allowance was added.

Raw adapters do not retry, allocate budgets, or write deltas. The step layer owns retries, named deterministic fallbacks, schema correction and terminal memoization. Fallback finish reasons record runner/failure provenance; corrective spend survives fatal fallback and memo replay. JSON schema remote fetching is disabled. Agent-loop calls do not pass through this one-shot shim.

Manifest route overrides can only narrow locality. VERDICT consults retain the existing owner/confirmation exclusions and can only hold a rule-granted write. Preflight and apply share one LMDB write transaction, so a preflight receipt cannot cross a manifest revision. The enforced-hold fixture retains an existing Proposed claim; shadow mode permits the ordinary write and records the hold reason.

The host injects the tagger/checkpoint and calibrated checker. Shadow tagging has no write door. Surprise receipts name the input refs and are included with the delta in the canonical request, preventing memo reuse across different inputs. Named Dreamer runners pass argv and credential handles through the existing CLI sandbox. User-login dispatch is refused on Hosted nodes both at admission and execution replay.

The vendor seed is a public OpenRouter snapshot fetched from `https://openrouter.ai/api/v1/models` on 2026-09-19. Its 54 real provider rows carry source prices converted to per-million without floating-point rounding. `~` routing pseudo-providers are excluded. The engine identity normalizes `:free` to `.free`, while `wire_model` preserves the exact upstream name. Revisions are explicitly catalog-snapshot revisions, not invented immutable provider revisions. Hosts can replace discovery bindings with production pins. Registry reseeding never overwrites prices or scores. Unchanged score observations advance the freshness watermark but do not add diff rows; score updates never route an answerer.

Owner voice register clips remain private per-vault data. Clone reads return owned copies for each render target. Vendor-origin identity writes are rejected. Provider read formats sanitize before truncation as well as during serialization.

## Acceptance ledger

All implementation acceptance fixtures below have passed, including the later fix-tests additions in the completed eight-crate run. Earlier owning-crate Clippy and workspace type-check evidence is retained below with its scope; those checks were not rerun during this handoff. This is not a full workspace runtime VERDICT.

| Step | Implementation and passing acceptance | Implementation left |
|---|---|---|
| 1 ONE-2302 | OpenAI reasoning/tool triads, id correlation, end-only JSON validation, reconstructed executable call; trailing usage preserved. OpenAI suite: 9 passed, including header-only tool refusal and parallel call correlation. | none |
| 2 ONE-2303 | Anthropic indexed thinking/text/tool blocks, accumulated JSON and signature fragments, executable Done content. Anthropic suite: 10 passed, including terminal-only malformed input refusal. | none |
| 3 ONE-2305 | Named non-LLM runner registry; fatal Durable execution, typed unknown/failed/empty outcomes and memo replay. `llm::step::tests`. | none |
| 4 ONE-2306 | Extraction failure-rule fallback completes a partition; conflict fallback explicitly escalates through the same step seam. `fatal_extraction_executes_declared_fallback_and_completes_partition`. | none |
| 5 ONE-2307 | Registry-backed Gemini builder/parser/typed stream/transport/status and capability admission. Two adapter tests cover generate, stream, lease settlement, classifications and vendor-data swap. | none |
| 6 ONE-2308 | Own-server adapter over oneiron-remote authenticated HTTP transport; bounded JSON/NDJSON and terminal validation. Scripted transport test and three HTTP/NDJSON tests cover both verbs, lease headers, usage, typed status failures and truncated streams. | none |
| 7 ONE-2309 | Manifest v2 file loader, 13 role keys, persisted slot routes and call-path bindings. All-role fixture and unknown-key refusal passed. | none |
| 8 ONE-2310 | VERDICT slot, band floor and shadow/enforce mode; real Proposed hold and shadow receipt, malformed band refusal. Manifest and gate fixtures passed. | none |
| 9 ONE-2311 | Central seven-purpose tier/locality defaults; production callers use the constructor. Seven-purpose precedence fixture passed. | none |
| 10 ONE-2313 | One producer, independent subscribers, Done-only sink, partial cancellation fanout. A fake LlmStream drives three subscribers in the fixture. | none |
| 11 ONE-2314 | Ephemeral clock-injected progress and punctuation/six-word/150ms voice chunking; raw deltas unchanged. Streaming subscriber fixtures passed. | none |
| 12 ONE-2315 | Propose-only provider trait and duplicate/unknown-refusing registry; output reaches exhaust landing. Registry fixture passed. | none |
| 13 ONE-2316 | Claude Code, Codex and OpenCode argv-only sandbox runners; each fixture lands exactly one exhaust artifact. | none |
| 14 ONE-2317 | Typed cloud-login refusal before dispatch plus execution-time recheck; identical local login succeeds. Dispatch fixture passed. | none |
| 15 ONE-2318 | Host-injected mention/PPR/affect/coreference tags, validated shadow compare over a fixture vault, no writes. NER checkpoint fixture passed. | none |
| 16 ONE-2319 | Deterministic surprise gate: zero calls for boring input, one for surprising input, named reads and delta-bound memo receipt. Step fixture passed. | none |
| 17 ONE-2320 | Generation-owned reservation settles actual usage or aborts on barge-in; reservation released. Headless voice session fixture passed. | none |
| 18 ONE-2321 | Versioned 54-provider priced/capability seed and validating insert-only loader. Seed parity/admission fixture passed. | none |
| 19 ONE-2322 | OpenAI, Anthropic and Gemini IngestSources normalize matching conversations at Imported/Proposed trust. Registry parity fixture passed. | none |
| 20 ONE-2323 | Three provider read formats, recursive secret nulling before truncation and valid conversation envelopes. Serialize/ingest fixture passed. | none |
| 21 ONE-2324 | ImageBackend generate/reference-edit, intent types, data catalog dispatch and byte validation. Fake adapter fixture passed. | none |
| 22 ONE-2332 | Private owner register bank and clone-into-target reads; two targets and vendor-origin refusal tested. | none |
| 23 ONE-2335 | Soniox AsrStream client normalizes partial/final/endpoint records from queued audio transport. Audio fixture passed. | none |
| 24 ONE-2337 | Step schema embedding, up to three corrective responses, typed terminal validation error, native bypass and actual accumulated spend. Schema fixtures passed. | none |
| 25 ONE-2347 | Persistent provider/model/revision rows with wire format and input/output prices; production adapters load registry rows. Restart and catalog fixtures passed. | none |
| 26 ONE-2348 | Config-driven fetch seam, schedule and schema mapping; atomic score diffs, stale-replay fence and nominate-only API. Scraped snapshot fixtures passed. | none |

## Validation evidence

Saved command logs and exit receipts are under ignored `target/w7-validation/`.

### Later factory rerun: macOS fixture and parity repairs

The factory's **07:41:37 `tests:2 rc=0 ran=7289`** receipt is preserved. The
following review failed to spawn because its prompt exceeded the argv size
limit (`E2BIG`), not because of a product or test failure. The recovery installed
stdin prompt transport and deliberately reran the normal factory test stage.

That later **07:57:50** run passed the adapter, remote and driver suites, but
reported two failing core targets:
- Eleven secret lease/snapshot tests inherited macOS `/var` from the temp root.
  T2 correctly rejects this symlink ancestor. Their fixtures now create temp
  directories under the canonical temp root. Production path validation is
  unchanged, including the tests for forbidden symlink targets and parents.
  The `AGENTS.md` real-`TMPDIR` caveat is no longer needed by these fixtures;
  they resolve their own test root without changing the engine path policy.
- `in_process_is_not_privileged` sampled two scoring seconds. Access decay
  changed a result by one f32 ULP. It now uses the existing zero-age fixture
  (`learned_at = u64::MAX`), like `structured_success_parity`. Exact response
  and score comparison remains intact, with explicit admitted-ID and neutral
  score assertions.

These are fixture-only fixes for the observed failures. No earlier green
receipt is erased or treated as evidence for the new source. Focused validation
uses the normal Cargo wrapper and does not repeat the eight-crate writer suite.

Focused follow-up on the repaired source passed through the wrapper's Arch
fallback (`factory-rerun-focused.json`, **exit 0**):

```text
cargo nextest run -p oneiron --all-features --lib --test vault_read_parity --test-threads 3 --no-fail-fast -E 'test(secret_) | binary(vault_read_parity)'
```

**148 passed, 0 failed, 6,940 skipped**, including all twelve failures above.
The two changed test targets compiled with all features. The receipt records
matching source hashes and the individual pass lines for those twelve cases.
The existing symlink-refusal tests and exact adapter score comparisons passed.

Final checks also passed:
- `cargo fmt -p oneiron --check`: **exit 0**, `factory-rerun-fmt-2.json`.
- `scripts/codemap/check.sh`: **exit 0**, all 18 generated artifacts current.

The first formatter check requested only one assertion line wrap. Its exact
output was applied. The focused pass is reused for that formatting-only change;
the receipts retain both the tested hashes and the final formatter hashes.

### Completed fix-tests handoff, 2026-09-19

The six repair commits through `e326aff8` are preserved:
- `1ef132e6`: stronger OpenAI/Anthropic triad and executable-tool checks; OpenAI now rejects a terminal tool header with missing input instead of silently losing the call.
- `f6f4e33a`: adapter transport, failure classification, lease settlement and Done-only durability coverage, including real loopback HTTP/NDJSON.
- `61653c68`: registry-backed local runtime capability intersection and driver binding tests.
- `28165899`: deterministic empty-output refusal, fatal conflict fallback, schema embedding and corrective-feedback assertions.
- `9f430c08`: terminal subscriber/progress behavior, tag validation and provider wire-shape coverage.
- `e326aff8`: regenerated code maps for the added acceptance tests.

`repair-touched-tests-3.json` records **exit_code 0**. Its corresponding log confirms formatting followed by this completed command on the MacBook, using a real temp path:

```text
cargo test -p oneiron -p oneiron-driver -p oneiron-remote -p oneiron-llm-openai -p oneiron-llm-anthropic -p oneiron-llm-local -p oneiron-llm-gemini -p oneiron-llm-own-server
```

Results on the repaired source:
- Core library: **6,552 passed, 0 failed, 4 ignored**.
- Core `it` binary: **470 passed, 0 failed, 11 ignored**. Other core integration binaries and the compile-fail suite also passed.
- Driver: **86 passed**; remote: **7 unit tests** plus its integration suites passed.
- OpenAI **9**, Anthropic **10**, local **6**, Gemini **2**, own-server **1** unit tests passed; own-server HTTP/NDJSON integration: **3 passed**.
- Core doctests: **7 passed**; the remote doctest target completed successfully.

This is the exact-source green evidence consumed for closure, not a queued or stale PID marker. The pending supervisor test import wrap was produced by formatting before this run and is committed with this handoff; it changes no behavior. No duplicate test run was started.

The earlier automatic factory failure was `No space left on device` while compiling on the Mac mini, before tests ran. It was not a product test failure. The subsequent MacBook queue completed successfully. Nothing remains queued or blocked for this handoff.

### Earlier writer evidence (preserved, not rerun for the repairs)

- Core library run: 6,536 passed, 12 failed, 4 ignored. One failure exposed invalid upstream seed aliases; the other 11 were macOS `/var` symlink refusals.
- After correcting the seed and using a real temp path, `core-regressions-3`: **146 passed**, including every prior failure and the changed LLM/gate cases. The 6,536 unchanged green results are reused.
- `adapter-tests-2`: **passed** for driver, remote and all five LLM adapters, including their integration tests/doctests. Unit counts: driver 85; remote 7; OpenAI 7; Anthropic 9; local 5; Gemini 2; own-server 1.
- `core-integration-2`: **477 passed, 11 ignored**, plus the compile-fail suite passed.
- `rustdoc`: **passed**, workspace/all-features/no-deps with warnings denied and inherited encoded flags cleared.
- Root-surface and structural ratchets passed unchanged. Later repair code-map regeneration is preserved in `e326aff8`.
- `recovery-clippy`: **passed**, all targets and all features for all eight owning crates, warnings denied.
- `recovery-featureless`: **passed**, core library and test targets without default features, warnings denied.
- `recovery-format`: **passed**. Final code-map check reports all 18 artifacts current; structural ratchets and root-surface check passed.
- `recovery-workspace`: **passed**, `cargo check --workspace --all-targets --all-features`, including the macOS app, bindings and voice-feature test targets.
- No implementation or scoped acceptance work remains. The interrupted factory test is not included in this evidence.

Cargo runs through the factory's normal host/slot wrapper. It does not forward TMPDIR. Cargo accepts dotted env overrides; Nextest requires the complete environment table in one config file. The ignored `.w7/real-tmp.toml` sets a host-relative, real `target` temp directory without changing shared Cargo configuration.

The recovery operator stopped a later factory test after the old completion parser falsely accepted an empty writer final. That interrupted test is not counted as a pass. The independent completed results above were preserved and inspected after recovery.

## Supplied bot-comment triage for PR #933

Reviewed all seven supplied comments against published head `0c458e0a`.
All are `issue_comment` entries; there are no supplied inline review threads
to reply to. None reports a concrete defect. Pending, disabled, or quota-limited
review notices are not treated as approvals or completed reviews.

| Comment ID | Bot | Disposition |
|---|---|---|
| 5740792745 | Greptile | No code action: automatic reviews are disabled; this is a service-status notice, not a defect finding. |
| 5740792880 | CodeRabbit | No code action: review-in-progress status, file inventory, and optional generation controls; no concrete defect or requested correction. |
| 5740793021 | Qodo | No code action: review-in-progress notice only; not completed review evidence. |
| 5740793289 | Codex | No code action: running-review status only; not completed review evidence. |
| 5740793353 | Cursor | No code action: PR overview and High Risk classification identify sensitive areas, but no faulty behavior, violated invariant, or requested correction. |
| 5740793506 | Cursor | No code action: usage-limit failure is a reviewer-service limitation, not a repository defect; no passing review is inferred. |
| 5740794449 | CodeRabbit | No code action: acknowledgement that a review was triggered; no finding and no completed review evidence. |

Only this implementation note changed. No crate, source, dependency, or test
changed, so no new Cargo test or compile run is required for this triage. Reuse
the completed eight-crate test evidence and focused 148-test repair evidence
above; the saved successful final-format source hashes still match. Validate
this note with `git diff --check`. No new canon discrepancy was identified,
and no docs-repo edit is needed. This disposition covers only the supplied
comment snapshot, not any later review results or an overall merge approval.


## Required-review recovery (PR #933)

The earlier seven-status-comment disposition above was not a disposition of the later
inline reviews. It is superseded by `impl-notes/W7-C08-review-ledger.md`, which retains
all 24 deduplicated groups from 28 inline comments plus both internal root verdicts.
The pre-fix and pre-publication REST refreshes each contain 10 issue comments, 3 reviews,
and 28 inline comments. The GraphQL snapshot contains all 28 threads with no missing
nested pages. Raw IDs, original head and bodies are retained outside the repository in
`tickets/W7-C08/fix-review-receipts/` and the original read-only consolidation packet.

Repairs:
- Preserve legacy checker Hold/Unavailable and host reasons under verdict floors.
- Persist a stream terminal before subscriber publication. A failed sink leaves retry
  possible; producer drop closes subscribers without inventing a successful Done.
- Validate deterministic fallback output against the requested JSON schema before
  memo/persistence, with actual prior spend settled on failure.
- Validate previous retrieval tags before surprise gating can spend model budget.
- Normalize provider-export credential aliases and PEM/private/SSH keys before
  truncation; reject malformed provider text blocks and empty Anthropic system text.
- Accept Gemini success statuses and signature-only metadata, retain Anthropic raw
  input-side usage, and preserve Soniox string error codes.
- Bound scraper IDs early and reject same-timestamp conflicting score observations.
- Delete owner-indexed voice reference packs atomically with biometric withdrawal and
  retention deletion. The trusted in-process owner-capture boundary remains unchanged.
- Replace blocking remote stream reads with cancellable async body reads in a
  transport-owned worker. Streaming uses an idle deadline, not a total-response deadline.
- Add actual `/v1/llm/generate` and `/v1/llm/stream` server routes. These require owner-grade
  authentication and explicit host backend/budget injection. A remote lease ID is not
  authority: the server always acquires its own local lease. No backend is enabled by
  default. Stream errors remain typed NDJSON errors, never successful terminal events.

Canon clarification: the own-server adapter contract needs an explicit server half,
including endpoint paths, owner authentication, local server admission, disabled-host
behavior, typed error frames, and cancellation. The earlier scripted adapter acceptance
alone did not establish server interoperability. The new real HTTP test uses the shipped
remote client and server router together. No docs-repository edit was made.

Validation is tracked in the recovery ledger. The independent user service
`w7-c08-review-validation.service` owns sequential factory-wrapper commands, so a factory
seat handoff cannot erase an in-flight result. This service does not bypass ticket/host
locks, change host order, impose a timeout, or create another build for the ticket.


### Final review refresh (`ff98e02b`)

Six additional Codex findings were valid and are recorded as F25–F30 in the review ledger.
Server inference now derives admission locality from the host-owned model catalog, requiring
registered models. A started request without terminal usage conservatively charges its own
reserved estimate; a successful terminal settles its actual usage exactly once. This closes
both cancellation/error spend leakage and caller-claimed local-continuation bypasses. The
upstream provider codec door recognizes additional opaque credential aliases and rejects
unknown declared block types. Equal-time scoreboard conflict checks use pre-snapshot
watermarks, so a legitimate newer snapshot can update multiple benchmarks atomically.
Deterministic fallback terminal content must be nonempty and structurally valid before it
can be memoized. Regression and production compile evidence is in the review ledger.

Canon correction: an own-server transport envelope is not the authority for inference
locality or budget; the injected server backend's host-maintained catalog is. Interrupted
inference has a conservative reservation charge when no actual terminal usage is available.

### Latest bot recovery (`8504e489`)

The full refreshed review record adds six Codex findings after F01–F30. The
recovery ledger now includes F31–F36: native schema validation, cancellable
non-streaming HTTP, seed watermark refusal, failure-versus-cancellation bus
terminals, full own-server terminal shape validation, and provider-specific
ingest roles. Earlier fixes and explicit dismissals remain intact. Both original
review roots and their final continuation/recheck results were collected; no
new review fanout or concurrent editor was created. Exact new validation and
GitHub response receipts are recorded with the final ledger below.

Final Rust validation remains pending in the correctly routed durable service
`w7-c08-bot-routed-validation.service`. Its current capacity wait and the two
excluded interrupted starts are recorded in the recovery ledger. The source
repair is committed as `8b439482`; no incomplete run is counted as a pass.
