# W7-C08 implementation notes

## Canon and decisions

The runtime L1/L2/L4/L6/L7 labels in the ticket are stale. Binding text is in
`site/src/pages/oneiron/runtime/index.astro` (`ONE-LLMB`, `mint:S4`) and
`site/src/pages/oneiron/agents/oneiron-arch-0026-dreamer-v2.astro`.
`surfaces §3` has no BYO runner text; its binding is the OF-137 decision ledger
and ARCH-0040 `dashboard-v2:D5`. OF-328 has only registry/ruling text.
No docs-mirror files are edited.

New seam types are exported through existing public modules, not the flat crate root.
The schema shim uses jsonschema with remote reference fetching disabled. It carries
schema/correction data rather than hard-coded natural-language prompt policy.
Fallback outcomes use the existing terminal response and finish reason, so the
memoized step preserves fallback name/failure provenance without a second ledger.

## Acceptance ledger

| Step | Implementation / evidence | Left |
|---|---|---|
| 1 ONE-2302 | Shared typed assembly; OpenAI reasoning/tool deltas; 6 adapter tests pass on MacBook | final integrated checks |
| 2 ONE-2303 | Indexed Anthropic thinking/text/tool blocks, signatures and usage; 9 adapter tests pass on MacBook | final integrated checks |
| 3 ONE-2305 | Named deterministic runners; fatal durable steps run them and memoize result; typed unknown/failed/empty | integrated retest after remaining steps |
| 4 ONE-2306 | Extraction declares version-1 failure rules; conflict fallback explicitly escalates | partition acceptance |
| 5 ONE-2307 | Registry-driven Gemini request/response, capability/locality gate, typed stream/status/transport | conformance tests |
| 6 ONE-2308 | Own-server raw adapter over oneiron-remote HTTP stack, bounded JSON/NDJSON, typed failures | conformance tests |
| 7 ONE-2309 | Manifest v2 file loader, 13 typed roles, persisted per-slot narrow-only vault routes; extraction/merge bind on call path | tests |
| 8 ONE-2310 | VERDICT model slot/floor band/mode; gate consult fails enforce closed, shadow receipt; confidence/band validation | real-write tests |
| 9 ONE-2311 | Central seven-purpose tier/locality policy and constructor; production call sites use it | integrated retest after remaining steps |
| 10 ONE-2313 | One-owner bus; full independent subscriptions; only terminal DTO reaches sink; cancellation fanout | tests |
| 11 ONE-2314 | Clock-injected progress and punctuation/word/tick voice chunker | tests |
| 12 ONE-2315 | Propose-only connector trait and duplicate/unknown-refusing registry | tests |
| 13 ONE-2316 | Claude Code, Codex, OpenCode argv-only sandbox runners, handle credentials, one exhaust | tests |
| 14 ONE-2317 | Host-stamped user-login flag refused on Hosted posture at admission and execution replay | tests |
| 15 ONE-2318 | Injected retrieval tagger, validated mention/PPR/affect/coreference output and write-free turn comparison | fixture test |
| 16 ONE-2319 | Deterministic tag delta surprise gate; boring avoids call and receipt names inputs | tests |
| 17 ONE-2320 | Generation owns issuing guard/reservation; usage feeds RAII settle/abort on barge-in and completion | headless tests |
| 18 ONE-2321 | 61-vendor source-priced data seed, validating atomic insert-only loader, capability gate | tests |
| 19 ONE-2322 | Three provider IngestSources at Imported/Proposed with parity fixtures | tests |
| 20 ONE-2323 | Three provider read formats; secret fields nulled before truncation, independent of options | tests |
| 21 ONE-2324 | ImageBackend generate/reference-edit, data catalog dispatch, byte validation; no image SDK | fake conformance test |
| 22 ONE-2332 | Private per-vault owner register audio refs; clone reads for any target; vendor-born origin rejected | tests |
| 23 ONE-2335 | Soniox queued-audio AsrStream client; partial hypotheses, endpoint-correlated final, normalized metadata | tests |
| 24 ONE-2337 | Step-owned schema validation, up to three corrective attempts, typed terminal error and honest accumulated spend; native bypass | integrated retest after remaining steps |
| 25 ONE-2347 | Persistent priced registry/wire rows; existing adapters resolve production catalogs from vault registry | restart and adapter tests |
| 26 ONE-2348 | Config-driven fetch seam/schedule/shape mapping; atomic score diff, bounded history, nominate-only surface | fixture tests |

## Build evidence

Cargo uses the factory shim and the MacBook per-ticket target at
`/Volumes/Cinema/w7-build/wt/W7-C08/target`. Build jobs = 2, test threads = 2.
The first cold adapter run passed (15 tests, 2m03s). Scoped passes are not a full gate.

`cargo test -p oneiron --lib llm::`: 101 passed on MacBook (before bus/image additions). Covers named fallback, unknown/failed runner, rule rows, all seven defaults, schema correction/exhaustion/native bypass.

Catalog seed is a dated public OpenRouter catalog snapshot, fetched from
`https://openrouter.ai/api/v1/models` on 2026-09-19. It contains 61 distinct vendor
names with source prices converted from per-token to per-million without floating-point
rounding. Revisions are explicitly `catalog_snapshot`, not invented immutable
provider revisions. `wire_model` keeps the upstream provider/model spelling.
Hosts replace these discovery bindings with pinned vendor revisions for production.

Build scheduling changed during the seat: the factory now owns fixed slots in
MacBook → mini → Arch order. The current full core-lib test waits/runs through
that wrapper; it selected the mini at `/Users/olety/w7-build/wt/W7-C08/target`
with 2 Cargo jobs and 2 test threads. No duplicate build or shared target bypass.
Clippy found three style issues in the first tranche (two collapsible ifs and a
redundant conversion); all three were fixed. The next compile found a missing
`async move` in the image fixture and a newly exhaustive checker match; both fixed.
These changes still require final integrated validation; pending does not mean passed.

## Integrated validation checkpoint

Implementation is committed in `7ad34dcc` and `fa65560c`; acceptance is still in progress.
The owning-eight-crate formatter passed through the factory wrapper. Validation logs and
exit receipts live under ignored `target/w7-validation/` so a writer-session change
cannot erase their results. The current touched-crate test run selected the Mac mini.
The root surface remains 701 names and all four structural ratchets pass unchanged.
Batch preflight and apply use one LMDB write transaction; the verdict consult cannot
be reused across a manifest revision. The enforce fixture starts with a Proposed claim
and checks that a below-floor promotion leaves that claim Proposed.
