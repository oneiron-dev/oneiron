# W7-C05 implementation notes

Canon is read-only at `/home/lexi/code/oneiron-docs/generated/`.
This file tracks implementation and remaining acceptance work, not a completed verdict.

## Decisions and canon corrections

- OF-464: the typed scope API stays `Option<EntityId>`. The wire always writes
  `world: base` / `rel: all` for `None`, or a binary entity ID. Absent tags fail
  decode, including replay. No legacy decoder: REVIEW.md says pre-release.
  The context pack's compatibility warning was stale. Relationship IDs now
  pass the same off-record reference fence as world IDs.
- Rule A: one receipt rides each durable Loro Change message. Its document and
  exact operation span come from the log, so no self-referential frontier is
  stored inside the document. The row fold excludes any unreceipted commit.
  The existing peer-to-actor authority remains separate from derivation data.
- Cold attach: a marker lands atomically with the first model identity. The
  serve worker consumes it with the backfill scan in one transaction. Retries
  do not double-fill, hotter jobs retain urgency, and no migration epoch moves.
- OF-360 corpus: 500 explicitly synthetic, owner-authorized memory points in
  50 varied scenarios. This is not real owner data. Grounded QA is separate
  from extraction matching. Missing answers stay in the denominator.
- Edit path: the oracle copies only fixture source units into a clean crate.
  Candidate manifests, build scripts and tests cannot replace its checks.
  Success requires functional AND contract tests. Regressions stay separate.

## Per-step implementation and remaining work

| Step | Work / remaining acceptance |
|---|---|
| 1 | ICS polling and connectors now admit time kinds, wall time, timezone, RRULE/master, attendees and meeting links through the existing screen/gate. Removed properties retract. 119 calendar tests passed. Later selector/recurrence read changes, UID-safe detached ICS exceptions and absence/revival are implemented but unvalidated. Connector multi-event recurrence resources now share admission; renderer, full-resource hash and deletion coverage are under validation. Floating/all-day outbound intervals refuse rather than invent an unstored duration. |
| 2 | Mandatory world/rel wire tags; candidate relationship scope and default encoding; write-door negative matrix. Claim-filtered featureless suite: 570 passed. |
| 3 | Durable Loro commit receipts and complete-history fold. Finalization now refuses missing/untrusted receipts; peer actor binding and prompt/trigger existence are checked. Hardened two-commit, birth, forged/unregistered-peer and finalization tests passed in the 314-test focused core run. |
| 4 | One generated-text birth door records prompt, trigger and pinned process for every artifact shape. Added a generated interview entry point. Birth and current caller tests passed in the focused core run. |
| 5 | Post-blend stated/concluded/all filter before truncation, unchanged rank factors, rerank accessor, explicit hydrated made_by field and fork hash. Early membership test passed; latest hydration change needs regression. |
| 6 | Durable KEEP-only one-shot sealed referee promotion, spent-before-measurement marker, receipted win-only default strategy, and reward-schema firewall test implemented. Wake driver now consumes only the exact receipted winning strategy and matching loaded candidate metadata. Promotion and firewall tests passed in the focused core run. |
| 7 | Added host-injected local ExtractionEncoder beside ordinary witness. Shadow returns out-of-store traces and swallows serving failures; golden fixture and zero-delta test added. No real served checkpoint exists at this base. Featureless production code compiled after seam fixes; the next test build found two include paths, now fixed. All three shadow/persistence tests passed in the focused core run. |
| 8 | Added model-pinned parity receipt and atomic persistence through existing mention, VAD, consolidation and proposed identity-topology doors. Factored VAD transaction seams to avoid partial commits. Golden parity, successful consolidation, missing-row rollback and late distinct-pair gate rollback tests passed. |
| 9 | Idempotent cold-attach marker/priority-3 scan and serve worker call. Rung-0 core fixture updated; BEAM transition fixture passed. Full core acceptance remains. |
| 10 | Owner-server remote rung, lease window and actual persisted vector locality wired. Remote/local-fallback server tests passed in the earlier scoped run. |
| 11 | Explicit per-entity allow/deny/no-verdict predicate and third-party startup refusal wired. Scoped server tests passed. |
| 12 | Deterministic BEAM reader covers attach, local fill, owner-server fill and remote failure fallback. Included in 98 passing BEAM tests. |
| 13 | Interactive/noninteractive local|endpoint|none init, atomic serve-config write, pinned local preparation, environment-only keys and onboarding instructions implemented. Init-to-semantic-hit and all 70 scoped embedder tests passed. Full server tests remain. Network endpoints cannot claim on-device locality; cloud endpoints require explicit remote-rung configuration. |
| 14 | Production model scorer validates prompt card before calls, reserves and executes exactly three votes, and reports separate judge overhead. Tests passed in BEAM 98-test run. |
| 15 | Pinned GPT-4.1-mini 2025-04-14 and GPT-4o 2024-08-06 Eval configurations, identity/parameter validation and usage receipts implemented. Pin/refusal tests passed. |
| 16 | Official int-cast and fixed float-clamped columns, per-ability rows and scorer convention implemented. Half-nugget fixture passed. |
| 17 | Seven required comparability axes, missing-axis refusal and main/appendix/dropped partition implemented. Axis/disposition tests passed. |
| 18 | Per-number disposition vocabulary and tracked citation corpus integrated. Unverifiable published comparisons remain walled or dropped; no missing comparator numbers are invented. |
| 19 | LongMemEval-S and BEAM 500K/1M/10M JSONL manifests plus Unix-second timestamp validation. Adapter tests passed. Shipped files are clearly marked small conformance samples, not native-scale proof corpora. |
| 20 | Previous-tier no-regression gate and not-yet-run (10M-gated) report cells implemented; tests passed. |
| 21 | Dual nugget averages, abilities and four wedge buckets; score-nuggets command writes a commit-hash proof directory. Scorer snapshot passed. Final result artifact still to generate after code commit. |
| 22 | Independent Chroma v2 HTTP collection/add/query/delete arm, outside ContextPackBuilder, and mock-service purity fixture implemented. Test passed; no installed live Chroma service was available or installed. |
| 23 | Carded cost-framing-only infra report and v1 vector-DB rows added. Unknown matched vendor measurements remain null with provenance/caveats. Fixture test passed. |
| 24 | Actual LlmBackend ProviderUsage input/output/cache pricing, pinned model table, call receipts and nonzero/zero cost fixtures implemented. Tests passed. |
| 25 | Shared measured run records real ingest/index elapsed and tokenizer input counts with fixed question denominator. No offline model call means zero provider dollars, not fabricated extraction costs. Later legacy-loader measurement correction needs regression. |
| 26 | Measured shared backbone-solo arm alongside memory arms, same answerer pin, real calls/usage/elapsed/cost. Fixture passed. Retrieval-only legacy run still uses explicit NotReady for model arms. |
| 27 | Effort Pareto frontier implemented and tested. Added context-budget-off and neutral OF-095 access-factor ablation legs with separate triples; these later changes need regression. Canon did not name the two ablation implementations; this choice is disclosed. |
| 28 | Identical answerer pin and parameters enforced for deterministic/agentic/solo/Chroma, separately pinned cheap chat. Mismatch and measured-row tests passed. |
| 29 | Agency lift accepts only matched deterministic/agentic points; chat remains separate. Refusal test passed. |
| 30 | Updating and QA lanes plus 500-point authorized synthetic corpus. Two unit tests passed. All six selected integration tests and OF-360 units passed; full crate run remains. |
| 31 | Known-span Q&A, held-out slice, tokens-per-answer and generator-free retrieval fixture protocol implemented. Included in 98 passing BEAM tests. |
| 32 | Five edit tasks, sandbox pack CLI and independent tests-plus-contract oracle. Acceptance test passed. |
| 33 | Same task pack/oracle; correct edits pass, inconsistent refactor fails, passing-but-regressing cleanup is not success; separate per-arm counters. Acceptance test passed. |

## Evidence

MacBook, per-ticket `target/`, `CARGO_BUILD_JOBS=2`, `RUST_TEST_THREADS=2`.
Logs are in the worktree-local ignored `.w7/` directory.

- `cargo test -p oneiron --lib --no-default-features claim`: 570 passed,
  `.w7/claim-test.log` (scope-tag snapshot, before subsequent additions).
- `cargo test -p oneiron --features sync,test-hooks --lib made_by`: 2 passed,
  `.w7/made-by-test.log` (receipts and provenance filter snapshot).
- `cargo test -p oneiron-bench edit_path_oracle`: 1 passed,
  `.w7/edit-path-test.log` (five correct tasks and two refused edits).
- `cargo test -p oneiron-bench beam`: 98 passed, `.w7/beam-test.log`.
  This predates the late ablation, legacy offline-cost and hydration edits.
- `cargo test -p oneiron --features sync,test-hooks --lib calendar`: 119 passed,
  `.w7/calendar-test.log`. This includes property admission/removal and floating/
  all-day handling, but predates selector and recurrence read changes.
- Combined touched-crate test reached compilation and found a test-module path
  error. Fixed. No full test pass yet.
- Encoder featureless test attempt failed before engine compilation on the Mac
  mini with `No space left on device`. Normal-wrapper retry is waiting for a
  usable host; repeated mini rsync failures are not source failures.
- No full-gate claim.

## Canon paths

- `oneiron/core/oneiron-arch-0035-projector-runtime-v1.md` (Rule A/birth).
- `oneiron/retrieval/oneiron-arch-0004-retrieval-graph.md` (made_by predicate).
- `oneiron/retrieval.md`, `oneiron/eval-beam.md` (rung-0/cold attach).
- `oneiron/eval/oneiron-arch-0042-beam-evaluation-harness-v1.md` (BEAM/edit path).
- `oneiron/code-in-oneiron/oneiron-arch-0068-repo-custody-v1.md` (fixture protocol).
- `docs/of360-extraction-eval.md` (local evaluator contract; seed-only status is
  now stale for the new full-corpus loader, retained seed loader stays explicit).

## Current build constraint

The Mac mini exhausted disk during two builds. No shared cache or host setting
was changed. The next combined test uses the wrapper's per-command host list to
wait on the preferred MacBook, retaining its slot budget. This is not a green
receipt for the late changes.

## Final audit decisions (pending validation)

- Keep incomplete seven-axis citations in the appendix. A canon `cite` label
  does not supply missing runtime judge/backbone/k pins. Weakening this gate
  would violate steps 17–18. Non-native score scales are walled too.
- Refuse self-judging, pin all answerer parameters in agency lift, require
  matched Chroma k, and allow cleartext Chroma only on loopback.
- Pin judge-instruction hash and the fixed execution contract in reports.
  Split ablation spend from main-table spend; the grand total includes both.
- The corpus adapter optionally accepts `metadata.stated_claim_predicate`.
  Its value is the verbatim corpus text, subject/evidence is that corpus row,
  and the existing claim write door admits it as an observed statement. It
  never reads gold. This exercises real OF-095 decay in the ablation fixture.
  TURN-only corpora report `no_claim_rows`, not a vacuous measured ablation.
- Record learned time from corpus order and freeze measured retrieval with an
  explicit `temporal_now`. No ingest wall clock becomes dataset truth.
- `ProposalTextArtifact::open` now takes the vault and binds its peer before
  the birth commit. Only human-stated births use it. Generated births use
  `open_generated(vault, ..., prompt, trigger, process)`.
- Connector imports share poll admission for complete master/exception
  resources. A resource missing its master refuses before admission; it is
  not treated as evidence that the master disappeared.

## Latest scoped result

`.w7/scoped-final.log`: 314 core library tests, six core integration tests and
70 server embedder tests passed. BEAM passed 94 and rejected four fixtures whose
carded retrieval limit differed from their actual limit. Fixed the fixture
cards, including the low-confidence zero-limit and PPR-VAD 15-limit cases.
This run predates the full-resource calendar outbound repair. The next run is
`cargo test -p oneiron -p oneiron-bench -p oneiron-server` with sync/test-hooks,
no failure short-circuit, and per-command real-path temporary storage.
