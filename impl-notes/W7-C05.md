# W7-C05 implementation notes

Canon is read-only at `/home/lexi/code/oneiron-docs/generated/`.
Implementation is committed through `1f750931`. Cluster runtime acceptance,
featureless coverage, lint, and strict rustdoc passed. The per-binary coverage
audit found no missing cases. No cluster implementation or acceptance work
remains. These are scoped and explicitly reused receipts, not a fresh
full-workspace gate verdict.

## Decisions and canon corrections

- OF-464: the typed scope API stays `Option<EntityId>`. The wire always writes
  `world: base` / `rel: all` for `None`, or a binary entity ID. Absent tags fail
  decode, including replay. No legacy decoder: REVIEW.md says pre-release.
  The context pack's compatibility warning was stale. Relationship IDs now
  pass the same off-record reference fence as world IDs.
- Rule A: one receipt rides each durable Loro Change message. Its document and
  exact operation span come from the log, so no self-referential frontier is
  stored inside the document. The row fold excludes any unreceipted commit.
  The existing peer-to-actor authority remains separate from derivation data.
- Cold attach: a marker lands atomically with the first model identity. The
  serve worker consumes it with the backfill scan in one transaction. Retries
  do not double-fill, hotter jobs retain urgency, and no migration epoch moves.
- OF-360 corpus: 500 explicitly synthetic, owner-authorized memory points in
  50 varied scenarios. This is not real owner data. Grounded QA is separate
  from extraction matching. Missing answers stay in the denominator.
- Edit path: the oracle copies only fixture source units into a clean crate.
  Candidate manifests, build scripts and tests cannot replace its checks.
  Success requires functional AND contract tests. Regressions stay separate.

## Per-step implementation and acceptance

| Step | Implementation / acceptance |
|---|---|
| 1 | ICS polling and connectors now admit time kinds, wall time, timezone, RRULE/master, attendees and meeting links through the existing screen/gate. Removed properties retract. Calendar selector/recurrence reads, UID-safe detached exceptions, absence/revival, shared multi-event admission, whole-resource rendering/hashes and deletion tests passed in the 7,038-pass core library coverage. Floating/all-day outbound intervals refuse rather than invent an unstored duration. Explicit-delete basis and provider-hash repairs passed the 20-test calendar integration retry. The marker-title round-trip regression also passed. |
| 2 | Mandatory world/rel wire tags; candidate relationship scope and default encoding; write-door negative matrix. Claim-filtered featureless suite: 570 passed; full core library coverage also passed. Stale replay/server scope fixtures are fixed in `aacebd26`; quarantine and server retries passed. |
| 3 | Durable Loro commit receipts and complete-history fold. Finalization now refuses missing/untrusted receipts; peer actor binding and prompt/trigger existence are checked. Hardened two-commit, birth, forged/unregistered-peer and finalization tests passed in the 314-test focused core run. |
| 4 | One generated-text birth door records prompt, trigger and pinned process for every artifact shape. Added a generated interview entry point. Birth and current caller tests passed in the focused core run. |
| 5 | Post-blend stated/concluded/all filter before truncation, unchanged rank factors, rerank accessor, explicit hydrated made_by field and fork hash. Membership, hydration and fork-hash tests passed in the current core library coverage. |
| 6 | Durable KEEP-only one-shot sealed referee promotion, spent-before-measurement marker, receipted win-only default strategy, and reward-schema firewall test implemented. Wake driver now consumes only the exact receipted winning strategy and matching loaded candidate metadata. Promotion and firewall tests passed in the focused core run. |
| 7 | Added host-injected local ExtractionEncoder beside ordinary witness. Shadow returns out-of-store traces and swallows serving failures; golden fixture and zero-delta test added. The host supplies the served checkpoint; no trained checkpoint is bundled or fabricated. Shadow/persistence tests passed in the current core library coverage. |
| 8 | Added model-pinned parity receipt and atomic persistence through existing mention, VAD, consolidation and proposed identity-topology doors. Factored VAD transaction seams to avoid partial commits. Golden parity, successful consolidation, missing-row rollback and late distinct-pair gate rollback tests passed. |
| 9 | Idempotent cold-attach marker/priority-3 scan and serve worker call. Rung-0 core fixture passed in the current core library coverage; BEAM transition fixture passed in the earlier scoped run. |
| 10 | Owner-server remote rung, lease window and actual persisted vector locality wired. Remote/local-fallback server tests passed in the earlier scoped run. |
| 11 | Explicit per-entity allow/deny/no-verdict predicate and third-party startup refusal wired. Scoped server tests passed. |
| 12 | Deterministic BEAM reader covers attach, local fill, owner-server fill and remote failure fallback. Included in 98 passing BEAM tests. |
| 13 | Interactive/noninteractive `local \| endpoint \| none` init, atomic serve-config write, pinned local preparation, environment-only keys and onboarding instructions implemented. Init-to-semantic-hit and all 70 scoped embedder tests passed. Combined server evidence: 751 unit tests and 134 integration tests passed, with five intentional ignores; all stale scope-fixture failures are repaired and tested. Network endpoints cannot claim on-device locality; cloud endpoints require explicit remote-rung configuration. |
| 14 | Production model scorer validates prompt card before calls, reserves and executes exactly three votes, and reports separate judge overhead. Tests passed in BEAM 98-test run. |
| 15 | Pinned GPT-4.1-mini 2025-04-14 and GPT-4o 2024-08-06 Eval configurations, identity/parameter validation and usage receipts implemented. Pin/refusal tests passed. |
| 16 | Official int-cast and fixed float-clamped columns, per-ability rows and scorer convention implemented. Half-nugget fixture passed. |
| 17 | Seven required comparability axes, missing-axis refusal and main/appendix/dropped partition implemented. Axis/disposition tests passed. |
| 18 | Per-number disposition vocabulary and tracked citation corpus integrated. Unverifiable published comparisons remain walled or dropped; no missing comparator numbers are invented. |
| 19 | LongMemEval-S and BEAM 500K/1M/10M JSONL manifests plus Unix-second timestamp validation. Adapter tests passed. Shipped files are clearly marked small conformance samples, not native-scale proof corpora. |
| 20 | Previous-tier no-regression gate and not-yet-run (10M-gated) report cells implemented; tests passed. |
| 21 | Dual nugget averages, abilities and four wedge buckets; score-nuggets command writes a commit-hash proof directory. Scorer snapshot passed. Committed scorer-conformance artifact under eval-results/273d7cdde9b09a4dc89667dcb12a01d59ffe83b9/w7-c05-nugget-conformance; explicitly not native-scale model accuracy. |
| 22 | Independent Chroma v2 HTTP collection/add/query/delete arm, outside ContextPackBuilder, and mock-service purity fixture implemented. Test passed; no installed live Chroma service was available or installed. |
| 23 | Carded cost-framing-only infra report and v1 vector-DB rows added. Unknown matched vendor measurements remain null with provenance/caveats. Fixture test passed. |
| 24 | Actual LlmBackend ProviderUsage input/output/cache pricing, pinned model table, call receipts and nonzero/zero cost fixtures implemented. Tests passed. |
| 25 | Shared measured run records real ingest/index elapsed and tokenizer input counts with fixed question denominator. No offline model call means zero provider dollars, not fabricated extraction costs. The legacy-loader measurement correction passed in the recovery 98-test BEAM run. |
| 26 | Measured shared backbone-solo arm alongside memory arms, same answerer pin, real calls/usage/elapsed/cost. Fixture passed. Retrieval-only legacy run still uses explicit NotReady for model arms. |
| 27 | Effort Pareto frontier implemented and tested. Context-budget-off and neutral OF-095 access-factor ablation legs with separate triples passed in the recovery BEAM run. Canon did not name the two ablation implementations; this choice is disclosed. |
| 28 | Identical answerer pin and parameters enforced for deterministic/agentic/solo/Chroma, separately pinned cheap chat. Mismatch and measured-row tests passed. |
| 29 | Agency lift accepts only matched deterministic/agentic points; chat remains separate. Refusal test passed. |
| 30 | Updating and QA lanes plus 500-point authorized synthetic corpus. Two unit tests passed. OF-360 unit and full-corpus integration tests passed in the full run. |
| 31 | Known-span Q&A, held-out slice, tokens-per-answer and generator-free retrieval fixture protocol implemented. Included in 98 passing BEAM tests. |
| 32 | Five edit tasks, sandbox pack CLI and independent tests-plus-contract oracle. Acceptance test passed. |
| 33 | Same task pack/oracle; correct edits pass, inconsistent refactor fails, passing-but-regressing cleanup is not success; separate per-arm counters. Acceptance test passed. |

## Validation evidence

The normal factory wrapper owns host admission and target isolation. No shared
host setting or cache was changed. Three test threads were used for recovery.
The per-binary inventory audit found no uncovered cases or unintended skips.
Combined evidence covers **9,011 passes and 20 intentional ignores**, with the
source and platform boundaries below. This is not a fresh full-workspace gate.

- Core library: **7,038 passed, four ignored** on the Mac inventory. This combines
  2,722 named successes from the interrupted source-`273d7cdd` run with 4,316
  successes from its remainder. The inventory equals their disjoint union plus
  the ignored tests. Eleven affected unit tests passed after the calendar repair.
- Core integrations: **747 passed, 11 ignored** after the targeted repairs.
  The retry passed 20 calendar and 19 quarantine cases, including all five
  previous failures. No production scope validator was relaxed.
- Linux-only coverage: **18 core unit tests and seven integration tests passed**
  on the native binaries built by the wrapper for source `264a666d`. These are
  additional platform cases, not duplicate Mac cases or a separate local build.
- Bench: **299 unit tests and ten integration tests passed** across the platform
  receipts. All 98 BEAM cases passed on source `273d7cdd`, including provider
  usage, three-vote judging, Chroma isolation, ablations, cards and dual scores.
  Seven unchanged eval tests have the documented macOS `UnsupportedPlatform`
  failure and passed on the existing factory-built Linux executable. That
  narrow reuse is not a fresh full-tree Linux claim.
- Server: **751 unit tests and 134 integration tests passed; five ignored**.
  The 22-test fixture retry includes all five repaired scope failures.
- Core doctests: **seven passed**.
- The marker-title repair in `35742874` passed its updated existing test on the
  rebuilt Mac binary. The fixture's new marker literal was confirmed present
  in that executable. No extra Cargo build was started for this test.
- Formatting passed, including the changed resource file after the marker fix.
  Code-map generation is current: **2,545 files**. Structural ratchet passed:
  giant files **2**, allow attributes **96**, print macros **91**, process-global
  statics **33**. Root surface: **701 names**. Later edits do not change those
  structural surfaces.
- A mini attempt failed on disk reserve before engine compilation. The retry
  used the owner's current wrapper policy; no host setting or cache was changed.
- Core and benchmark lint diagnostics were corrected in `d6c55f0c` and
  `d3f2fdbc` without changing runtime behavior. Formatting and code-map generation
  passed. Scoped Clippy passed for all targets of all three touched crates.
  Featureless all-targets Clippy and production-server Clippy also passed.
- Featureless library coverage: **6,552 passed, four ignored**. The full base
  run passed 6,551 tests and exposed the Git discovery defect. All 36 Git tests
  passed after its repair, including the single failed case. All 6,556 inventory
  names have outcomes; the passing base results were not rerun.
- Featureless all-targets Clippy passed again after the Git boundary repair.
- Strict rustdoc passed for core, bench, and server with `RUSTDOCFLAGS="-D warnings"`
  and inherited encoded rustdoc flags cleared. The final serialized job returned
  **exit 0** on source `1f750931`. Green evidence was reused rather than rerunning
  unchanged test cases in the same feature lane.

Raw evidence is worktree-local: `.w7/remaining-tests.log`,
`.w7/final-coverage-audit.json`, `.w7/core-combined-evidence.json`,
`.w7/beam-current-evidence.json`, `.w7/linux-eval-evidence.json`,
`target/w7-validation/featureless-evidence.json`, and the stage logs in
`target/w7-validation/`. The completed serialized job receipt is
`.w7/git-boundary.receipt.json` (Git tests, repair Clippy, and rustdoc all exit 0).
Logs live under `target/` so source-sync cannot overwrite them. The audit removes only the exact unrelated
SSH shell-startup cache warning and retains the raw inventory output.

## Canon paths

- `/oneiron/agents/oneiron-arch-0060-event-ics-meetings-v1/` (calendar epic).
- ARCH-0024, three heads / one encoder; ARCH-0036, engine serving and presets
  (encoder seams and first-run embedding selection).
- `oneiron/core/oneiron-arch-0035-projector-runtime-v1.md` (Rule A/birth).
- `oneiron/retrieval/oneiron-arch-0004-retrieval-graph.md` (made_by predicate).
- `oneiron/retrieval.md`, `oneiron/eval-beam.md` (rung-0/cold attach).
- `oneiron/eval/oneiron-arch-0042-beam-evaluation-harness-v1.md` (BEAM/edit path).
- `oneiron/code-in-oneiron/oneiron-arch-0068-repo-custody-v1.md` (fixture protocol).
- `docs/of360-extraction-eval.md` (local evaluator contract; seed-only status is
  now stale for the new full-corpus loader, retained seed loader stays explicit).

## Final audit decisions

- Keep incomplete seven-axis citations in the appendix. A canon `cite` label
  does not supply missing runtime judge/backbone/k pins. Weakening this gate
  would violate steps 17–18. Non-native score scales are walled too.
- Refuse self-judging, pin all answerer parameters in agency lift, require
  matched Chroma k, and allow cleartext Chroma only on loopback.
- Pin judge-instruction hash and the fixed execution contract in reports.
  Split ablation spend from main-table spend; the grand total includes both.
- The corpus adapter optionally accepts `metadata.stated_claim_predicate`.
  Its value is the verbatim corpus text, subject/evidence is that corpus row,
  and the existing claim write door admits it as an observed statement. It
  never reads gold. This exercises real OF-095 decay in the ablation fixture.
  TURN-only corpora report `no_claim_rows`, not a vacuous measured ablation.
- Record learned time from corpus order and freeze measured retrieval with an
  explicit `temporal_now`. No ingest wall clock becomes dataset truth.
- `ProposalTextArtifact::open` now takes the vault and binds its peer before
  the birth commit. Only human-stated births use it. Generated births use
  `open_generated(vault, ..., prompt, trigger, process)`.
- Connector imports share poll admission for complete master/exception
  resources. A resource missing its master refuses before admission; it is
  not treated as evidence that the master disappeared.

## Scorer artifact and repair decisions

`eval-results/273d7cdde9b09a4dc89667dcb12a01d59ffe83b9/w7-c05-nugget-conformance/`
contains the native `score-nuggets` CLI result from a clean source commit.
Four half-credit nuggets give official **0.0** and fixed-float **0.5**, with all
four wedge buckets. This is synthetic scorer conformance, not model accuracy or
native-scale performance.

The integration repair preserves the evidence distinction: an explicit remote
tombstone uses `imported_cancel`; a complete feed omission uses
`imported_absence`. Both retain the multi-source no-bounce law. A single-event
outbound passport commits the provider's receipt hash, including canonicalized
representations. Multi-event resources retain per-component hashes beside the
whole-resource receipt. The now-unused single-passport argument and its lint
suppression were removed.

Independent replay/server ClaimSeed serializers now carry the mandatory scope
tags. This restores their valid baseline, so malformed provenance reaches the
intended provenance gate. No production validator was relaxed.

The resource renderer now matches CRLF-delimited VEVENT boundary lines. Marker
text inside a SUMMARY is ordinary data, not a component terminator. The existing
master/exception round-trip fixture includes `END:VEVENT` in its title and
asserts the exact title survives. The updated runtime check passed.

The Linux featureless run exposed an existing Git boundary gap: after a nested
repository's `.git` was deleted, discovery could enter the containing checkout
and report a missing ref instead of a repository failure. Git children now
canonicalize the requested root and set a per-child search ceiling at its
containing directory. Unrepresentable ceiling paths fail closed. The existing
fatal-vs-absence test now always creates a containing repository, so the case
is reproducible on every test host. The focused retry passed all 36 Git tests.

## Test-platform recovery (2026-09-19)

The factory rerun at `3ea17fa2` failed in 18 tests, not in the managed-server
output shown at the end of the log. Eleven secret lease/snapshot fixtures
constructed absolute paths below macOS's symlinked `/var` temp root. Canonicalize
only the owned fixture root before constructing declared targets. Intentional
occupant, target-symlink and parent-symlink refusal checks remain unchanged.
The production no-symlink policy is unchanged.

Seven pre-existing benchmark eval success cases require the Linux-only
`Vault::open_existing` descriptor-bound door (ONE-1996). Mark those exact cases
ignored only off Linux instead of requiring every test runner to repeat a name
filter. They still compile everywhere and run on Linux. Add a non-Linux test
that both eval commands return the typed `UnsupportedPlatform` refusal without
changing either LMDB file or creating entries. Do not replace `open_existing`
with the create-capable door or invent unsafe macOS descriptor support.

C05 has no published PR: both GitHub branch and title lookups returned empty.
The supplied PR933 packet is for W7-C08, not C05; none of its bot findings can be
applied as C05 findings. Original C05 Opus/Grok review sessions are resumed in
place for read-only completion, with no fresh fanout. Raw queries, failure-log
hash and review paths are retained in `.w7/fix-tests/`.

## Recovered internal review fixes

Both original review roots completed with `VERDICT: DEFECTS`; no unavailable
status or clean result was fabricated. Their preserved reports and raw exit
receipts are under `.w7/fix-tests/`. Opus cross-checked the eight unfinished
original subreviews itself; no new reviewers or parallel editors were started.
The combined ledger has five distinct accepted findings:

- Promotion now pins evaluated authoring behavior on every split. Joins and
  comparison construction validate that pin; promotion rejects a foreign pin
  before consuming the one-shot marker or calling the referee. Dataset/budget
  relabeling cannot grant the same behavior another sealed shot. This is a
  structural host-evidence binding, not a cryptographic attestation of execution.
- Init can now write the existing remote-rung configuration. Network URLs
  default to third-party; owner-server is explicit. Same-model on-device
  fallback/query endpoint and explicit entity-list or all-row egress consent
  are required. Both provider configurations validate before vault creation.
  HTTPS/locality/egress gates are unchanged. The onboarding skill uses these
  CLI flags rather than implementing config writes itself.
- The shipped measured plan uses its own synthetic knowledge-update fixture
  with a supported wedge label. The old arbitrary-label transport fixture is
  unchanged. The test executes the shipped plan with a fixture model backend,
  not by rewriting gold labels.
- Shared-scaffold Chroma coverage now verifies both retrieval arms, retained
  retrieval-card IDs, citations, and exact corpus-only context. Retrieval cards
  remain distinct from measured answerer/judge model pins. The existing Chroma
  wire mock is shared instead of duplicated.
- Infra cost rows have an explicit accuracy-isolation test with contrasting
  measurements and a typed nugget-input refusal. This closes an acceptance-test
  gap; it does not imply that the prior scorer consumed infra measurements.

All five findings are accepted; no applicable finding was dismissed. C05 still
has no GitHub PR, confirmed again before these edits. The supplied PR933 raw
packet belongs to C08. Do not post a C05 explanation on that unrelated PR.
Canon corrections belong in these notes: network onboarding must describe
same-model local fallback and affirmative egress consent; campaign promotion
must bind the scored behavior, not an independent caller-supplied pin.

The first narrow Opus recheck caught an R1 regression: arm declarations validate
by identity, so their order cannot be part of the behavior fingerprint. Hashing
now sorts the three declared arm identities first. The one-shot regression
reorders a valid configuration after measurement, rebuilds the comparison, and
asserts both the same pin and refusal before a second referee call.

The final structural check caught `oneiron-server/src/cli.rs` crossing 800 lines
after the new flags. Its unchanged inline test module now lives at
`cli/tests.rs`; an exact dedented-body comparison proves the move did not drop
or change tests. The production CLI module stays under the existing ratchet.
No baseline was raised. The code map was regenerated and the ratchet passed.

The first final-source Mac test pass compiled but stopped at a new fixture:
`max_rounds_k` is pinned to the landed round cap, so the alternate candidate now
varies legal fan-out instead. Strategy hashing also normalizes signed-zero
uncertainty thresholds; `0.0` and `-0.0` have the same admission behavior and
must not mint distinct shots. Existing policy gates are not relaxed.
The Grok follow-up found the root onboarding artifact still had the old flag
recipe. Its onboarding section now matches the crate's shipped pack, including
fallback/locality and explicit egress consent. No docs repository was edited.

## Test-recovery validation (2026-09-19)

The original factory failure was reproduced as platform-specific fixture
handling, not a managed-server failure. Both original internal review roots
were resumed rather than replaced. Their final closeouts begin `VERDICT: OK`
at `25d13a5a`, including all five deduplicated findings and the arm-order,
signed-zero, root-onboarding, and move-only CLI follow-ups. The subsequent
`2f5c10df` edit only applies Clippy's collapsed-condition suggestion.

Completed evidence:

- Initial recovery `cargo nextest run -p oneiron -p oneiron-bench
  --all-features --profile full --test-threads 3`: **8,121 passed**, 15 skipped,
  on wrapper-selected Arch. This predates the review fixes; it is unchanged-code
  coverage, not a claim about the later candidate.
- At `25d13a5a`, all `oneiron-bench` and `oneiron-server` tests plus core
  `autoreason_campaign`, `secret_lease`, and `secret_snapshot` scopes:
  **1,284 passed**, 7,748 skipped under the scoped selection, no retries, three test
  threads on wrapper-selected Arch. This includes the new promotion, remote
  onboarding, Chroma/card/citation/gold-isolation, and infra-isolation tests.
- After the mechanical lint fix, all three measured-scaffold tests passed on
  the MacBook at `2f5c10df`, no retries, three test threads.
- MacBook native binaries, one test thread: **31 secret-lease tests passed**,
  **11 secret-snapshot tests passed**, and the new non-Linux eval
  **typed-refusal/no-write test passed**. These exercise the original failure
  platform. The test sources' hashes match the Mac compile tree; no second
  build or production permission bypass was used.
- Formatting for all changed Rust files, `git diff --check`, the code-map pin
  (2,547 mapped files), and the structural ratchet passed. The ratchet remains
  2 giant files / 96 allows / 91 print macros / 33 process-global statics.

The original 9,011-test evidence is preserved. Raw logs, command plans, head
bindings, original-root review reports and receipts are retained under
`/home/lexi/w7-build/recovery/c05-fix-tests-20260919/` outside Cargo's source-sync
path. This is not a full-workspace gate claim. The final serial validation
receipt `.w7/fix-tests/final-checks-3.receipt.json` is now `finished`, exit 0,
with all five stages exit 0. No queued or running stage remains.

Final code-bearing commit: `2f5c10df`; later commits only update these notes.
The completion receipt ended at `2026-09-19T12:34:08Z` and is archived with its
command plan and every stage log. Host selection came from the standard wrapper;
Cargo jobs were limited to three. Completed final selections:

| Command | Host | Result |
| --- | --- | --- |
| `cargo clippy -p oneiron -p oneiron-bench -p oneiron-server --all-targets --all-features -- -D warnings` | MacBook | exit 0 |
| `cargo test -p oneiron --lib --no-default-features autoreason_campaign:: -- --test-threads=3` | Arch | 43 passed, 6,514 filtered out |
| `cargo clippy -p oneiron --all-targets --no-default-features -- -D warnings` | Arch | exit 0 |
| `cargo clippy -p oneiron-server --all-features -- -D warnings` | Arch | exit 0 |

The fifth receipt stage is the three-test scaffold rerun reported above. This
reuses the 1,284-test pass and Mac platform proof rather than repeating broad
unchanged tests. The standard factory submission and full gate remain separate;
this repair closeout does not claim they have run again.

The earlier terminal `BLOCKED` messages during build-slot waits were incorrect:
the same durable validation driver was alive and retained the work. The driver
was preserved, admitted normally, and produced the real terminal receipt above.
No validation result was fabricated or shared host lock overridden.

### Cloud-review disposition and publication

The final branch and title PR queries both return no C05 PR (exit 0, empty
arrays). The provided
PR933 consolidation packet belongs to W7-C08 and is inapplicable; posting C05
changes there would misattribute the work. No Qodo, Codex, or CodeRabbit result
is being claimed or silently skipped, and no failed-provider request is being
waited on. The internal findings above are fixed, not waived. This explanation
and the final validation receipts are ready for the factory's eventual C05 PR;
no GitHub comment could be posted to a nonexistent C05 PR.

## PR #937 bot repair (2026-09-19)

This section supersedes the historical statements above that C05 had no PR.
The reviewed GitHub head is `29886ce62e75086b1ff4893934ddd811620db22f`.
Before these repairs, fetched all pages of issue comments, reviews, inline
comments, and GraphQL review threads (including each thread's comment page
flags). Read the original internal roots and their retained findings/closeouts;
the eight original child transcripts contain no separate final findings. No
new review fanout replaced them. Raw, head-bound receipts and the combined
assessment are retained at
`/home/lexi/w7-build/recovery/c05-pr937-bots-20260919/`.

### Source-linked dispositions

All Qodo items below also map to top-level comment **5742124936** and review
**5255829653**. They are overlapping presentations of the same nine findings,
not eighteen separate defects.

| Finding / inline source | Disposition and reason |
| --- | --- |
| Q1 / 4053270720 | Fixed: init parses the process environment once and passes it through the same staged-file resolver as serve. Preparation follows the effective provider after overrides, not the original CLI choice. An injected-environment regression checks invalid dimensions cause no vault/config writes and that a valid overridden provider opens with serve's model/dimensions. No test mutates the process environment. |
| Q2 / 4053270730 | Invalid: the entire `PendingEmbeddingReconciler` impl, including the alleged unguarded call, is `cfg(sync)` at `embed.rs:221`. The helper has the same guard. Existing featureless Clippy and library tests already passed. |
| Q3 / 4053270732 | Fixed: cap distinct meeting links at 32 per VEVENT across URL, CONFERENCE, LOCATION and DESCRIPTION. Duplicates do not consume the cap. Overflow returns typed `CalendarError::IcsParse` for the complete feed, never a truncated set that can imply removals. Individual URL length remains capped. |
| Q4 / 4053270735 | Inapplicable trust-boundary claim: this is a trusted in-process host callback with Vault access, not an untrusted HTTP/sync receipt verifier. R1 already binds the supplied strategy to the KEEP winner. The contract is host-evidence binding, not cryptographic execution attestation; no unspecified signing protocol was invented. |
| Q5 / 4053270712 | Invalid counterexample: `render_component` propagates `get_claim` errors. The claim decoder validates calendar timezone, time-kind and status shapes before returning a row. Malformed stored/synced values therefore never reach the UTC fallback. |
| Q6 / 4053270716 | Fixed: move judge instructions from the Rust constant into a required exact-content/SHA-256 pin in runtime `JudgeConfig`. Refuse empty/mismatched pins before calls, send the exact configured bytes, and record the configured hash in measured reports. Retrieval-only reports use null because no judge ran. The example plan carries the old instruction explicitly; no legacy/default prompt is fabricated. |
| Q7 / 4053270718 | Invalid equivalence premise: the mock is the external `LlmBackend` boundary. Exactly three independent provider calls, no short-circuit on failures, and zero calls on card refusal are explicit acceptance requirements. Its fixture-local counter observes those paid-call effects, not a private engine implementation detail. |
| Q8 / 4053270723 | Invalid platform premise: pinned Rust `std::fs::rename` replaces existing destination files on Windows (`MoveFileExW` / `SetFileInformationByHandle`). The directory-specific difference does not apply to sibling regular config files. No Windows runtime result is claimed. |
| Q9 / 4053270726 | Fixed: generated birth and imported-history fold share trigger type validation. `Task` requires TASK; `Ask` requires the initiating TURN. Unrelated/missing rows fail admission and imported history produces no provenance. The writer also checks prompt-row existence before committing. |

### Retained internal findings

All were fixed before GitHub publication and remain accepted. No new internal
approval was requested or fabricated; both original roots ended `LANDABLE`.

| Internal source | Disposition |
| --- | --- |
| Opus R1, `review-opus.md` | Fixed in `4737fd63`: evaluated-strategy binding through split, comparison and promotion before referee/marker. |
| Opus R1 order follow-up, `review-opus-final.md` | Fixed in `5816f9b7`: canonical arm ordering; order-only changes cannot mint another sealed shot. |
| Signed-zero / legal-candidate follow-up, `review-opus-closeout.md` | Fixed in `25d13a5a`: normalize zero; test varies legal fan-out, not forbidden round cap. |
| Opus R2, `review-opus.md` | Fixed in `ff785b34`: network onboarding writes the authorized remote rung, same-model local fallback and explicit egress. Q1 above adds effective-environment parity; it does not relax egress/locality gates. |
| Grok onboarding follow-up, `review-grok-final.md` | Fixed in `25d13a5a`: root and crate onboarding skill sections agree. |
| Opus R3, `review-opus.md` | Fixed in `898eed5b`: shipped measured plan uses the compatible knowledge-update corpus and is executed unchanged. |
| Opus R4, `review-opus.md` | Fixed in `898eed5b`: shared Chroma/deterministic fixture checks card IDs, citations, corpus context and gold isolation. |
| Grok R5, `review-grok.md` | Fixed in `898eed5b`: infra accuracy-isolation test. This was a required-test gap, not existing scorer coupling. |
| CLI size follow-up, `review-opus-closeout.md` | Fixed in `d704ba79`: exact move of tests into `cli/tests.rs`, no changed production behavior. |
| Recovery T1/T2, original `ledger.md` | Fixed in `9c266eaa`/`e74755bc`: owned temp-root canonicalization and explicit non-Linux eval refusal tests. Production security policies stay unchanged. |

### Canon clarifications

- An ask trigger is represented by its TURN row; Task/Ask labels cannot reclassify
  unrelated entity IDs. The Rule A canon names task/ask refs but did not spell out
  that runtime representation. Its prompt remains a caller-supplied input row.
- The calendar adapter limit is 32 distinct links per VEVENT, with fail-closed
  whole-feed parsing. The canon did not give an aggregate meeting-link cap.
- Judge instruction content belongs to the supplied run plan, with an exact hash.
  A retrieval-only result must not claim a model-judge instruction was executed.
- Init validates the effective file-plus-environment configuration used by serve,
  including provider changes from the environment.

Validation and final cloud-status receipts are recorded below when complete.


### Completed Codex review (5255851436)

Codex status comment **5742102504** changed from pending to completed on the
reviewed `29886ce6` head. The completed review has eleven inline findings.
The refresh includes all 20 review threads; outer and nested pagination flags
are false. Each full finding was assessed before the second repair batch.

| Inline source | Disposition |
| --- | --- |
| 4053293245 | Fixed: a successful Cargo exit also requires libtest's completed-success report with the expected nonzero test count pinned in the sealed task pack. An early `exit(0)` candidate is rejected for both task and contract suites. This local oracle is not an OS sandbox for arbitrary hostile native programs. |
| 4053293248 | Fixed: scoped recurrence withholding is keyed by master and UID, not a vault-wide boolean. A hidden exception suppresses its own series and its hidden exception row, without removing unrelated visible series from search/freebusy. |
| 4053293250 | Duplicate of Q6 / 4053270716: the runtime instruction content/hash fix in `3022863f` covers both sources. |
| 4053293252 | Fixed: replace the product-specific referee prefix with a generic caller-configured `identity@sha256:<64 lowercase hex>` pin; validate it at measurement admission and when loading a stored default. This is a real consumer-boundary/content-pin defect, distinct from Q4's inapplicable cryptographic-attestation premise. |
| 4053293253 | Fixed: the writer and fold share process-shape validation. Empty or oversized identity/version/parameter strings fail before generated document creation; the 256-byte accepted boundary remains foldable. |
| 4053293255 | Fixed: owned property claims carry a stable per-source provenance prefix plus their original blob/UID reference. Reconciliation compares/retracts only rows from that source. Two feeds' identical links remain independently supported; removing one feed's link/attendee does not remove the other's. |
| 4053293257 | Fixed: preflight the selected JSONL record dataset identities against the judge benchmark before any model call. Recheck the loaded record before answering. Both BEAM-on-LME and LME-on-BEAM mismatches have a no-model-call regression. |
| 4053293258 | Fixed: remove the unchecked `default_admission` helper. Its only production caller constructs tournament admission after checking the loaded candidate against the exact promoted pin. |
| 4053293259 | Fixed: the documented `beam rung-fixture` command takes no dummy file argument; the CLI route is exercised directly. |
| 4053293260 | Fixed: featureless cold attach preserves the durable pending marker because that build cannot populate the sync queue. A feature-independent reopen test checks that sync consumes the pass and featureless leaves it available. |
| 4053293262 | Duplicate of Q9 / 4053270726: trigger-kind validation in `3022863f` covers both sources. |

Additional canon corrections: calendar replacement ownership is per source,
not per resolved EVENT; hidden recurrence masks apply only to the referenced
series. Referee identities are generic and content-pinned. Featureless cold
attach cannot acknowledge work that only a sync serving build can enqueue.
The documented no-argument rung fixture CLI is now executable.


The final source recheck caught a repair regression before the test job acquired
a build slot: imported evidence is nested under WriteEnvelope's
`candidate_evidence`, not at the top level. Property reconciliation now reads
that existing structure and verifies both the import kind and ICS source ID.
The queued test job was cancelled before compilation; it produced no runtime
result. Its successful formatting/code-map receipts are retained.

The follow-up source-only formatting used the pinned MacBook Rust 1.96
`rustfmt` with this repository's `edition = "2024"` config. It exited 0.
Only `property_claims.rs` changed after the earlier full touched-crate format
pass. The added scaffold test precondition uses its existing indentation.
Code-map regeneration/check and `git diff --check` passed. This operation
opened no Cargo target; all compilation/test/lint commands retain standard
factory host admission. The queued Cargo-format step was cancelled before
admission, not reported as a test result.


### PR explanation and remaining validation

All 20 source inline threads have replies. The single PR explanation is
https://github.com/oneiron-dev/oneiron/pull/937#issuecomment-5742411670. It maps every Qodo/Codex source and the retained internal
findings, explains the explicit dismissals and CodeRabbit unavailability,
and states that runtime validation is incomplete. Update that same comment
with terminal results; do not post a second summary.

Runtime validation retains custody in `w7-c05-bot-checks-1789825768` with receipt
`/home/lexi/w7-build/recovery/c05-pr937-bots-20260919/checks-4/receipt.json`. No runtime test or Clippy pass is claimed for these repair
commits yet. The earlier green evidence is historical only. No push, merge,
or PR close was performed.


The first actual bot-repair runtime pass compiled all touched test targets on
wrapper-selected Arch. Of 53 selected tests, 38 passed, one cold-attach fixture
failed, and fail-fast left 14 unrun (exit 100). The failure was
`gate.pending.criticality_floor`: the fixture tried a raw approved claim.
The fixture now uses the established human/UserStated `WriteEnvelope` admission
with Auto approval. No production gate or cold-attach code changed. The pinned
Mac formatter and code-map regeneration passed for that test-only correction.
The continuation reuses the 38 passes and runs only the failed/unrun selection,
then the featureless selection and lint stages. No failed test is called green.


The failed/unrun continuation passed all 15 tests on wrapper-selected Arch.
Together with the 38 retained passes, all 53 distinct focused all-features
tests now pass. The featureless selection passed all 27 selected core tests
on the MacBook, including the deferred cold-attach reopen test.

Touched-crate all-target Clippy then rejected two redundant clones in the
new server-init test (exit 101). Removing those clones changes only ownership
of values that are never used again, not assertions or runtime behavior.
The 53/27 green test evidence is retained; the remaining serial job reruns
lint/typecheck only. No production code was changed for this lint repair.


All three lint stages in `checks-6` completed with exit 0. The final source
recheck then found another part of Q1: `ONEIRON_CONFIG` selected serve's file
but did not select init's output file. Init now uses the same precedence:
explicit `--config`, then `ONEIRON_CONFIG`, then the default path. The existing
environment-parity regression now uses the environment-selected path and an
injected decoy default. It checks that no decoy file is written and avoids any
possibility of writing the user's global config during regression testing.
Core/benchmark source and green evidence stay unchanged. Only the strengthened
server test and server lint/typecheck need rerunning for this follow-up.


## Final PR #937 repair validation

This section supersedes every earlier pending-validation status above.
Final code-bearing commit: `669d1548`; the final notes commit changes no Rust.
The PR remains open at reviewed head `29886ce6`; fixes are local for the
launcher to push. No push, merge, or close was performed.

| Validation | Host | Actual result |
| --- | --- | --- |
| Focused nextest, all features, core/bench/server | Wrapper-selected Arch | 53 distinct tests passed: 38 retained passes plus 15 failed/unrun continuation passes. |
| Focused core library nextest, no default features | MacBook | 27 passed, 0 failed; 6,516 outside-scope tests skipped. |
| Strengthened init environment/config-path regression | MacBook | 1 passed, 0 failed; 893 outside-scope server tests skipped. |
| `cargo clippy -p oneiron -p oneiron-bench -p oneiron-server --all-targets --all-features -- -D warnings` | MacBook | Exit 0 (`checks-6`). |
| `cargo clippy -p oneiron --all-targets --no-default-features -- -D warnings` | MacBook | Exit 0 (`checks-6`). |
| `cargo clippy -p oneiron-server --all-targets --all-features -- -D warnings` after the final server-only follow-up | MacBook | Exit 0 (`checks-7`). |
| `cargo clippy -p oneiron-server -- -D warnings` after the final server-only follow-up | MacBook | Exit 0 (`checks-7`; also passed before the follow-up). |
| Touched-crate formatting plus pinned Rust 1.96 native formatting of follow-up files | MacBook / standard wrapper | Exit 0; two redundant-clone deletions retained existing formatting. |
| Final code-map check and `git diff --check` | Coordinator | Exit 0; 2,547 files mapped and all 16 artifacts current. |

The original cold-attach test setup failure (criticality gate) and the two
redundant-clone lint findings were repaired, not suppressed. The fixture-only
clone cleanup changed no runtime assertions or behavior. Core and bench bytes
did not change during the final server-only path follow-up, so their green
results were reused. The strengthened init test replaces its earlier pass in
the 53 distinct all-features tests; it is not counted as a new distinct case.
The logs retain the pre-existing vendored heed lifetime warnings; owned-crate
Clippy commands still exited 0 with warnings denied. No vendor source changed.

Focused selection used for the retained and continuation runs:

```text
test(calendar::ics::parse::) | test(calendar::ingest::admission::) | test(calendar::query::) | test(edit_distance::proposal_text::) | test(autoreason_campaign::tests::sealed_beam) | test(embed::cold_attach::) | test(commands::init::) | test(beam::llm_judge::) | test(beam::model_scaffold::) | test(beam::edit_path::) | test(beam::rung_fixture::)
```

The all-features command selected the three touched crates, used `--retries 0`
and three test threads. The continuation excluded the 38 exact green names and
used `--no-fail-fast`; it passed the remaining 15. The featureless command used
`-p oneiron --lib --no-default-features` with the same scoped expression. The
final server follow-up selected only
`commands::init::tests::init_uses_serve_environment_and_refuses_invalid_overrides_before_writes`.
Full argv, raw logs, exact names, host lines and terminal receipts are retained
at `/home/lexi/w7-build/recovery/c05-pr937-bots-20260919/`, including
`final-validation.json`. This is scoped repair validation, not a fresh full
workspace VERDICT. Earlier full factory evidence stays historical.

### Final review coverage

The post-validation snapshot fetched every page of comments, reviews, inline
comments and GraphQL review threads. It contains 20 root findings/threads and
no incomplete nested comment pages. All 20 have our source-linked replies;
Q1 additionally has reply `4053493980` for the config-path follow-up. The final
refresh adds only that reply and its empty review container `5256061385`, not
a new bot finding. Qodo and Codex completed their original-head reviews.
CodeRabbit remains unavailable for the documented plan/file-limit reasons,
not a clean approval. The five explicit dismissals and all internal findings
remain mapped above. No original reviewer result was replaced or discarded.

Exactly one PR summary exists, comment `5742411670`. It is updated in place
with these final results and the full source disposition map.


## PR #937 merge conflict repair (2026-09-19)

Merged `origin/main` at `debda07582a03718ae84485363d85ca9d5dab1ac`
into C05 at `e98b869efc5f926f8e81576375bbb164e5e4542c`. The only
conflicts were generated summary counts in `docs/CODEMAP.md` and
`docs/codemap/codemap.json`. Regenerated the maps from the combined source;
all 2,557 mapped files and 16 generated artifacts pass the read-only check.
No Rust conflict required a hand-written resolution.

Checked both-parent diffs for the four automatically merged Rust files:

- `batch/base_apply.rs` retains C15's narrow-only custody replay validation
  and C05's model-present condition for pending embedding queue admission.
- `handler/tests.rs` retains C05's mandatory claim scope tags alongside
  C15's protocol, document sync, and broadcast-payload tests.
- Secret lease/snapshot fixtures retain both branches' macOS-safe temporary
  path handling. Production no-symlink checks are unchanged.

No canon correction or docs-repository edit was needed. This is a local
merge repair only; no push or GitHub merge is authorized or performed.


### Validation recovery

The first all-features run compiled successfully on the MacBook, then reported
603 passes and 970 failures with OS error 28. A separate native resource probe
proved file writes succeed but a second simultaneous POSIX named semaphore
allocation fails with `ENOSPC`; LMDB needs those semaphores. The failure was
shared-host semaphore exhaustion, not a source regression or a full filesystem.
A temporary-directory relocation probe also failed, as expected from that cause.
No failed test was counted as green, no shared semaphore limit changed, and no
other job's resources were removed. The retry uses the wrapper's action-scoped
MacBook exclusion and normal fallback admission, retaining the 603 exact passes.


Merge commit: `a7676a994dc9639fab3b20faa3ee163cfa97926f`.
`git show --remerge-diff` confirms that only the two generated map files needed
manual conflict resolution. The committed tree matches the staged validation
tree `5808592161d82211778bc819630186c74349fad5` exactly.

### Completed merge validation

All checks below passed on the merged Rust source. Later commits change only
these notes. No source or test repair was needed after map regeneration.

| Check | Host | Result |
| --- | --- | --- |
| Core/server selected nextest, all features, full profile, no retries | MacBook + wrapper-admitted Arch | 1,573 distinct passes: 603 retained Mac passes plus all 970 recovered failures on Arch. The exact-name audit has no missing cases or duplicated passes. |
| Core selected library nextest, no default features, full profile, no retries | Wrapper-admitted Arch | 225 passed, 0 failed; 6,337 outside-scope tests skipped. |
| `cargo fmt -p oneiron -p oneiron-server --check` | MacBook | Exit 0. |
| `cargo clippy -p oneiron -p oneiron-server --all-targets --all-features -- -D warnings` | MacBook | Exit 0. |
| `cargo clippy -p oneiron-server -- -D warnings` | MacBook | Exit 0. |
| `cargo clippy -p oneiron --all-targets --no-default-features -- -D warnings` | MacBook | Exit 0. |

The all-features selection covers all server tests plus core batch, cold attach,
secret custody/lease/snapshot, sweep, sync, and sync-bridge scopes. The featureless
selection covers batch, cold attach, secret custody/lease/snapshot, and sweep.
Both runtime commands use three test threads; the wrapper caps compilation at
three jobs in the ticket's isolated target. No build lock or host reserve was
bypassed. Unchanged benchmark and other C05 evidence above is retained, not rerun.

Raw argv, host lines, logs, completed step receipts, and the exact-name audit are
under `/home/lexi/w7-build/recovery/c05-fix-merge-20260919/`, summarized by
`final-validation.json`. The first four `checks-8` steps and both `checks-9` steps
completed with exit 0. The queued featureless step in `checks-8` was replaced
before admission, not counted as a pass. Failed environment probes remain in the
record. This is scoped merge validation, not a fresh full-workspace gate verdict.
No push or GitHub merge was performed.


## Post-merge factory test recovery (2026-09-19)

The factory's subsequent plain-feature `cargo test --no-fail-fast -p oneiron
-p oneiron-bench -p oneiron-server` ran on the MacBook and failed again with
OS error 28. A fresh native probe confirmed that file writes succeed, but a
second simultaneous POSIX named semaphore allocation fails with `ENOSPC`.
The host had 31 GiB free internally and 758 GiB free on Cinema. This is shared
semaphore exhaustion, not evidence that the vault data volume is full.
All resources created by the probe were closed and unlinked.

Recovery uses the wrapper's existing action-scoped
`W7_CARGO_EXCLUDE_MACBOOK=1`, without forcing a local build or changing shared
settings. The mini was below its 30 GiB reserve, so the wrapper admitted Arch
at three Cargo jobs with this worktree's isolated default target. Tests use
three threads. No engine behavior, storage checks, test assertions, or feature
gates are weakened to hide the resource error. The factory must also use a
healthy runtime host until MacBook semaphore capacity is restored; the local
recovery override does not change the factory's environment.

The initial client-owned server retry was interrupted during compilation on
seat restart and produced no test outcome. Its replacement uses the existing
durable validation runner. Full touched-crate runtime results are pending at
`/home/lexi/w7-build/recovery/c05-tests-after-merge-fix/receipt.json`.
The failed factory output, native probe, exact command, and host admission log
are preserved in that directory. No canon or docs-repository change is needed.
