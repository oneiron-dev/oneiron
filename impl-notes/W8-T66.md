# W8-T66 implementation notes

- Decision: exercise the existing retrieval default-kind scope through an owner PERSON and an actor with an explicit read permit. Each recall uses a fresh vault and immediately precedes the request with a pairing SlipMint, an authority fold, a policy-manifest rewrite, and a scoped MESSAGE AccessGrant. Test both Medium and Light, including explicitly named POLICY_MANIFEST and ACCESS_GRANT reach.
- Canon: `generated/oneiron/retrieval.md` (Control rows, line 90 in the read-only docs mirror) says control kinds never reach recall for any caller. This agrees with the unnamed `Memory::recall` contract, including the owner. Its unqualified wording would be stale if applied to explicitly filtered `context_pack()`: this ticket retains named-kind reach. It should say that ordinary unfiltered recall never returns control kinds; an explicitly named control kind may be retrieved only through an authorized filtered query. `generated/oneiron/core/oneiron-arch-0002-structural-entities.md` (§ storage ABI) explains that maintenance records are not prompt-facing entities; `generated/oneiron/decisions/oneiron-dec-0005-policy-residence.md` (§ ScopedRead) requires explicit scope without silent narrowing or widening.
- The first named-kind check used a default now anchor and found no POLICY_MANIFEST candidate: its stock row retains occurred time 0 through the test policy rewrite. The named-kind context-pack checks now explicitly anchor on that stored timestamp (and on the fresh ACCESS_GRANT timestamp). The unfiltered facade recalls still have no time hint. No default-scope change was needed after the owner recall passed.
- No docs repository edits. The scoped all-features acceptance command passed (223/223 tests); `cargo test -p oneiron -j 6 -- --test-threads=4` also passed (7,430 lib tests, integration tests and doctests). The code-map check passed.
