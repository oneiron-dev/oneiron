# W7-C04 implementation notes

Status: all 32 steps are implemented. Native acceptance now passes with the repaired Linux guest and the reviewed real QuickJS component. Per-step evidence is below; earlier failed/provisioning attempts remain historical records. This is a ticket-scoped result, not a full-workspace VERDICT.

Canon: `/oneiron/code-in-oneiron/`, `/oneiron/code-in-oneiron/oneiron-arch-0050-repo-as-world/`, and `/oneiron/code-in-oneiron/oneiron-arch-0068-repo-custody-v1/`.

## Decisions

- Code file documents use Loro in base mode, not only the optional sync transport.
  The transport's `sync` gate stays separate. This makes the file plane available
  to featureless engine callers as required by the contract.
- REAPI keys are SHA-256 digests of canonical protobuf Action messages. Existing
  blake3 snapshot identities map into a Directory descriptor. Hosts with a real
  REAPI CAS can supply the exact Directory digest. Cache rows use schema 2 and a
  new digest-key prefix; no deployed v1 rows exist.
- Account sharing is explicit. A host binds each member and an account artifact
  vault to the same immutable account identity. Both cache rows and their bytes
  live in that account vault. Artifact IDs are not reinterpreted in a member's
  entity namespace. `BuildCache::new` remains a local/trusted cache handle.
- Cache-keyed build/verify legs reserve an action before invoking the host. An
  uncertain execution is not automatically sent again. A completed cache row
  always wins. Returned provenance names the original producer.
- Working views use content-addressed codebase blobs and read-only hardlinks in
  a private host-owned directory. These are not a hostile-code sandbox. Foreign
  guests need the separate read-only microVM mount boundary. Writers submit
  operations rather than mutating these links. One view-set owns one LSP process.
- Consent wording and visible-file policy are consumer configuration in examples,
  authored by an agent asked in chat. Production code only renders/executes the
  supplied recipe. Host blast-radius numbers cannot be replaced by recipe input.
- Object-only GitWire writes pin objects but never create a served-ref intent.
  The origin protocol, not generic GitWire recovery, owns exported ref publication.

## Per-step record

Steps 2 and 3 now have real native acceptance. Other per-step records retain their prior validation evidence. No step remains deferred. Validation evidence follows the table.

| Step | Ticket | Implementation and remaining acceptance |
|---|---|---|
| 1 | ONE-2127 | Review requests carry host blast-radius counts, max depth and a configured risk-summary slot. The agent-authored v1 recipe executes; free-lane admission stays direct. |
| 2 | ONE-2128 | Real Wasmtime Component Model runtime, canonical C13 typed run-step ABI, tier-specific imports and gated-write traps. Six canonical typed-runtime fixtures pass; prior string-ABI fixtures are superseded. The reviewed real C13 QuickJS interpreter passes the gated first-party write fixture, including an actual JavaScript expression and durable Generated/Proposed claim receipt. Nothing remains. |
| 3 | ONE-2129 | Configured jailer, image pins, budget, vsock credential proxy and proposals-only file edits. Added oneiron-guest PID-1 source, typed conformance generator, pinned kernel/rootfs recipes and source/start plumbing. Guest crate tests pass on Linux and MacBook (13 each); the repaired Linux guest passes all 13 again. Real Firecracker/jailer boot passes with the pinned kernel and repaired rootfs: host-only credential resolution, proposals-only edits, untouched source, consume-once collection. Missing/unconfigured VMM refusal fixtures remain green. Nothing remains; the conformance guest is not JavaScript evidence. |
| 4 | ONE-2130 | Symbol/chunk operation, actor, turn, activity and intent chains reach both blame readers. Old manifests without operation references still decode. |
| 5 | ONE-2131 | Content-addressed semantic units own text rendering and changed-chunk selection. Literal interior spaces are preserved; inter-item line shifts do not create symbol changes. |
| 6 | ONE-2132 | Snapshot-first Proposed operation journal; unapproved changes leave HEAD unchanged and approved changes apply. |
| 7 | ONE-2133 | Immutable multi-critic verdicts, distinct critic identities, split-review refusal, hard veto and unanimous approval. Verdicts remain queryable. |
| 8 | ONE-2134 | Typed session claims and round-tripping commit trailers. Promotion checks applied receipts for every changed file, exact frontiers, producing provenance and current session head. Positive and missing-review runtime fixtures pass. |
| 9 | ONE-2135 | One read-only mount path for live head, fork hash and snapshot, with identical bytes and no write door. |
| 10 | ONE-2136 | Stock Git writes enter the repository lock and document operation plane with pusher attribution. Files in each ref apply atomically; concurrent base changes refuse, and receipt replay never reapplies edits. |
| 11 | ONE-2137 | Blake3 manifest snapshots and lookup, with SHA256 legacy capture/prepared recovery and restore support. |
| 12 | ONE-2138 | Canonical REAPI Action/Command protobuf digests, explicit CAS input digest, schema-2 digest-keyed rows and first-writer semantics. Standard digest vectors and public cache tests pass. |
| 13 | ONE-2139 | Explicit immutable account binding shares index and artifact bytes. The public integration fixture now verifies a cross-member hit, original producer, readable shared bytes and cross-account refusal. |
| 14 | ONE-2140 | Declared-idempotent build/verify cache runner reserves before execution, stores a miss and skips execution on a repeat hit. View builds use it. |
| 15 | ONE-1911 | Typed, hash-keyed change-index claims, source proofs and door-minted inbound change IDs. |
| 16 | ONE-1912 | Origin residence, authority epochs and host leases protect publication, inbound Git writes, cutover and recovery. Mirrors and stale hosts refuse; epoch fixtures pass. |
| 17 | ONE-1913 | Servable .jjconflict-shape projection; stock Git clones and renders the conflicted tree. This is engine v1, not jj private-format compatibility. |
| 18 | ONE-2141 | Base-mode Loro file sessions persist one current document per scoped path; concurrent sessions merge and converge. Exact-head writes avoid redundant history import, while real concurrent histories still merge. |
| 19 | ONE-2142 | Validated Unicode span edits with sequenced, durable, idempotent receipts. Guest and Git whole-file inputs lower against their captured base instead of bypassing the document plane. |
| 20 | ONE-2143 | Per-file operation folds join revision folds; verification authenticates the operation log and rejects tampered snapshots or operation metadata. |
| 21 | ONE-2144 | Persistent queued/testing/landed/failed batching, receipts and crash census over the repository mutation coordinator. |
| 22 | ONE-2145 | Speculative worktrees and tested inclusion paths, sealed landing permits, out-of-order agreement and red quarantine. The production adapter lands reviewed stacks and recovers authorized prefixes. |
| 23 | ONE-2146 | Red-batch bisection, last-green advancement only on green, and slow-red automatic rollback. |
| 24 | ONE-2147 | Recorded crate-level Rust public-name baselines detect removals/renames and allow private additions. |
| 25 | ONE-2148 | Cargo metadata reverse-dependency graph selects affected downstream Rust test crates and excludes unrelated crates. |
| 26 | ONE-2149 | Recorded JSON data-shape and command output/exit contracts report typed drift verdicts; unchanged fixtures pass. |
| 27 | ONE-2150 | Content-addressed blob-linked views, per-view receipts, and an agent-authored visible-file selection recipe. Repeated views reuse stored bytes. |
| 28 | ONE-2151 | One bounded stdio language-server process serves per-view completion/diagnostic requests; restarting a view leaves it alive. A real process fixture exercises routing. |
| 29 | ONE-2152 | View builds share the account cache and materialize admitted outputs on hits. A Rust compiler fixture verifies the second view skips execution and receives the linkable output. |
| 30 | ONE-2153 | Canonical tree/parent/message/author projection through GitWire object writes and origin CAS publication. Determinism, conflict refusal and stock-Git successive clone/fetch fixtures pass. |
| 31 | ONE-2154 | Conflict recording atomically opens one reconciliation task linked to open claim, branch subject, repository and conflicted paths. |
| 32 | ONE-2155 | Stale settles and divergent code revisions persist proposals against the current head, without applying or automatically rebasing stranded operations. |

## Canon clarifications

The current spine says guests still propose whole files. The step-19 contract is
not permission to expose whole-byte canonical writes. Whole-file guest/git inputs
must be lowered at the door to spans against the guest's base document.

The old context pack called Loro sync-only and snapshots sha256. Those were code
observations, not the target architecture. Per-file documents are a base-mode
engine concern. New repository snapshots use blake3 and retain legacy recovery.

## Repository journal ownership

`repo.proposed_diff` and `repo.session` are engine-owned predicates. They record
review and execution facts, not arbitrary model memory. The review door writes
these records through the reserved claim owner path and retains the actor in
standard evidence and the session in subject/value. They intentionally do not
set `sess`, which routes envelope-bound session-bundle candidate writes. Public
claim APIs cannot rewrite these journal records. The normal queue still requires
immutable independent critic approval before any file effect.

## Prior validation (before the typed ABI/guest-agent continuation)

Evidence is composite and reuses unchanged passing cases. It is not a claim of
one all-green full-crate command, a full workspace gate, or native guest acceptance.
At that checkpoint only `oneiron` Rust source had changed. The continuation also
adds the `oneiron-guest` crate. No host tool was installed and no docs-mirror file
was edited. Prior runtime evidence is reused only for unchanged behavior.

- The broad `cargo test -p oneiron --features sandbox-wasmtime,microvm-firecracker`
  library run passed **6,577 tests**, ignored 4, and failed one newly added epoch
  fixture because the mirrored checkout has no `.git`. The fixture now invokes
  Git in its own repository. Its corrected runtime test passed with the origin
  scope and the final affected-scope run. The broad run was not repeated.
- Final affected library nextest: **192/192 passed** across `code_document::`,
  `code_revision::`, `code_symbol::`, `origin::`, and `repo_mutation::`.
  This includes exact-ingress rollback/replay and literal-whitespace regressions.
- Core integration coverage: public build cache **6/6**; code memory **36/36**;
  stock-Git HTTP **13/13** (the large-push case plus the other twelve); lens
  regeneration **17/17**; compile-fail driver **1/1**; read parity **9/9**;
  voice segment sync **3/3**.
- The `it` target initially passed 468 tests and ignored 11. Its two failures
  expected the now-forbidden automatic dev-backend fallback. The always-run
  routing test now requires Firecracker or a typed unavailable error; the
  duplicate obsolete dev-routing test was removed. All **11 microVM contract
  tests passed in each of sandbox-feature and featureless builds**. The 468
  unaffected passing cases were reused, not rerun.
- Core doctests: **7/7 passed** (two runnable examples and five compile-fail cases).
- Strict all-target core Clippy passed with
  `sync,test-hooks,sandbox-wasmtime,microvm-firecracker` and with
  `--no-default-features`, both using `-- -D warnings`. After the test-only routing
  correction, strict `--test it` Clippy passed in both feature selections too.
- Formatting and generated code-map checks pass. Structural ratchets remain
  **2 / 98 / 91 / 33**, and the root-surface check remains **701 names**.
- Earlier unchanged runtime evidence covers consent, operation blame, REAPI cache,
  shared views/LSP and real Rust builds, Wasmtime ABI/traps, proposal retention,
  merge queue/bisection, and all three contract oracles. The broad library run
  includes these cases. WAT and dev/socketpair fixtures are not QuickJS or VMM
  boot proof.

Local receipts are under `target/w7-validation/`: `cargo-test-durable.log`,
`performance-results.json`, `routing-results.json`, their named stage logs, and
`native-artifact-availability.json`. They are ignored build output, not PR fixtures.
The per-command test config used `[env]` with
`TMPDIR = { value = "target", relative = true, force = true }` in
`.w7/cargo-test-portable.toml`. It avoids both macOS symlink temp roots and
macOS-only paths on Linux. The final commands used the official factory wrapper;
a full Mac mini disk delayed routing but the remaining stages completed on the
MacBook.


## Validation-driven repairs

- Epoch receive-pack fixtures run Git from their own temporary repository, not the mirrored build checkout, which intentionally has no `.git`.
- Exact ingress verifies text and the captured frontier inside the writer transaction. A later-file refusal aborts every file edit for that pushed ref. Replay returns receipts without hiding newer edits.
- A stack sample of the existing 3 MiB streamed-push fixture identified redundant Loro history import and rich-text diff tracking. Equal-head/new-document writes now use their already-validated candidate directly. Existing stored snapshots are still verified, and genuine concurrent histories still merge. The same public fixture passed on the MacBook in 3.70 seconds; this is not a host-matched benchmark against the earlier Arch sample.
- Semantic comparison skips whitespace-only root syntax gaps instead of collapsing all whitespace. The regression preserves spaces inside `include!` string literals and ignores inter-definition blank lines.
- Cargo calls use the factory wrapper without a local-host override. The wrapper selected both MacBook and Arch slots during validation. Temporary test directories are worktree-relative (`TMPDIR={value="target",relative=true,force=true}` in a per-command config), so both hosts use a real existing path. No shared host configuration changed.
- Updated legacy integration expectations: automatic routing must select Firecracker or return `MicroVmBackendUnavailable`, even in debug builds. The development backend is available only by explicit construction. Routing fixtures pass in both sandbox-feature and featureless builds.

## Native acceptance still required

- Step 2: C13 ONE-2464 owns the real QuickJS interpreter. Its actual component
  must be built, pinned and run against the canonical typed ABI with a gated
  first-party write. The in-repo WAT conformance component is not that interpreter.
- Step 3: the guest **source and build recipe are now in this change**, not an
  external unowned implementation dependency. The Linux x86-64 guest passes
  13 tests, and its rootfs/conformance component are built. A pinned compatible
  kernel and a provisioned native boot host are still required. Building a kernel
  here also needs the source archive/digest and missing `bc`; the recipe installs
  nothing.
- Real boot additionally requires authorized privileged Linux access, compatible
  Firecracker/jailer binaries, KVM, dedicated non-root uid/gid, delegated cgroup
  v2 cpu/memory/pids and private chroot/scratch roots. This seat is unprivileged.
  Firecracker/jailer are not on PATH, and the four native test configuration
  variables are absent. `/dev/kvm` exists but is not a complete provisioned stack.
- Run the ignored native fixture explicitly with that configuration. It must
  prove host-only credential resolution and proposal-only output. LOCALTEST and
  ext4 packaging checks do not prove boot. No full-cluster DONE is claimed.

## Owner-ordered guest-stack continuation

- Read C13 **only**. Canonical WIT/SDK/generator snapshot is from boundary commit
  `681957d045b59b4a3d5ae1c09875025f48fcba98`; WIT SHA256 is
  `01472b9aa9ae50942e42ab4c1f0d8f73bed8857bea32be0acaeb7760b0995386`.
  The first inspected snapshot had no interpreter source. A later read-only
  inspection at C13 `fb2db678` found `components/code-run-quickjs`, its pinned C
  interpreter build, runtime factory and server provider. The WIT hash is unchanged.
  No built manifest/component exists in its documented artifact directories.
  Its recipe requires wasi-sdk 27, wit-bindgen 0.46.0 and wasm-tools 1.239.0.
  ONE-2464 remains owned there; this ticket does not edit or duplicate that guest.
- Removed the incompatible string `run` WIT. Both the in-process adapter and
  microVM agent use typed `run-step` and the same record/variant definitions.
  Retained the engine adapter's artifact pin, budgets, per-op gate bridge and
  replay clock/RNG. `code-sandbox-wasmtime` replaces the unshipped `sandbox-wasmtime` feature
  spelling. No alternate compatibility ABI/feature is retained.
- The first-party result-json envelope and JSON subject convention are now
  explicit in `code_sandbox/firecracker/guest-protocol.md`. First-party returned
  proposals refuse; they cannot bypass typed write imports. The canonical WIT
  does not carry the engine's budget JSON extension in typed return records;
  enforcement/recording remain host-side. No alternate WIT was invented.
- Added `oneiron-guest`: bounded strict framing, sequential artifact/snapshot
  receipt, descriptor-relative workspace access, exactly four read-only typed
  imports, bool-only credential receipts, proposal validation and enumeration.
  Linux PID 1 creates bounded tmpfs/OverlayFS, cgroup pids limits, drops all child
  IDs/groups and sets no-new-privileges before compilation. It reaps before
  finishing and stays alive for the host's shutdown. Claims/deletions refuse
  rather than disappearing. LOCALTEST is explicit and is not a dev fallback.
- `scripts/microvm/` builds a required-SHA256-pinned Linux kernel source archive,
  validates built-in kernel drivers, stages an explicitly supplied Linux ELF and
  loader/library closure into ext4 without root, and records artifact identities.
  The recipe installs/downloads nothing. Its conformance component copies source
  bytes and makes the prescribed credential call; it does not interpret JS.
- The older protocol page saying that no in-repo guest agent existed was stale.
  It now points at the implementation and recipe and distinguishes ABI, packaging,
  native boot and real JavaScript evidence.

### Continuation validation

- Artifact recipe: 8 Python tests pass. Canonical generator check, 3 Python SDK
  tests and Node SDK adaptation pass. CI runs these drift/conformance checks.
- Initial new guest crate test routed to the Mac mini and failed with ENOSPC
  during dependency compilation, before any new project code compiled. This is
  not a Rust test result. The replacement core check passed on the MacBook with all test targets and
  canonical sandbox features. The sandbox runtime tests are in progress; no
  duplicate build or host installation was started.
- A real unprivileged ext4 recipe probe using `/usr/bin/true` as a packaging
  fixture passed. debugfs verified root-owned mode-0755 init bytes and the pinned
  epoch. This proves image assembly only, not a guest-agent or VM boot.

- Fresh typed guest crate tests pass on the MacBook: 11 library tests plus two
  native CLI integration tests. The CLI tests generate and type-validate a real
  conformance component, check its BLAKE3 digest, refuse overwriting it, and
  refuse production startup outside Linux PID 1. This remains conformance, not
  JavaScript or microVM-boot evidence.
- Five fresh engine typed-runtime tests pass: gated writes and confidence
  preservation, tier import restrictions, artifact/fuel refusal, invalid-argument
  host-call accounting, and NaN confidence refusal before dispatch.
- The first Linux guest compile identified an ambiguous setup-closure error type;
  it now explicitly returns the guest Result type. Subsequent MacBook compilation
  does not validate Linux-only PID-1 code. The initial typed conformance fixture
  incorrectly exported its world record types; it now imports canonical named
  types, as the WIT requires. All affected guest fixtures pass.
- Corrected an actual image recipe defect: the ELF interpreter had mode0644,
  which the kernel's PT_INTERP exec path refuses. Every staged ELF now has0755.
  The 8-test recipe suite passes; debugfs confirms the real ext4 loader inode
  has mode0755, uid/gid0 and the pinned timestamps. This is not boot proof.
- Current receipt set: typed3-results.json and typed3-*.log. Formatting passes.
  Strict all-target Clippy for both crates with the sandbox features passes on
  Arch, including the Linux-only PID-1 and host protocol paths. Featureless core
  Clippy passes on the MacBook. Code-map generation passes. These checks do not
  replace the missing privileged VM boot or real QuickJS artifact acceptance.


## Shared-engine deadline regression and native acceptance commands

C13's runtime factory clones compiled Engine/Component handles. Each Store has
its own authority and limits, but the Engine epoch counter is shared. An
unguarded `set_epoch_deadline(1)` therefore allowed a completed run to interrupt
an unrelated live sibling. A rendezvous fixture reproduced that failure on
Arch before the fix. The same fixture passes with a per-store callback that
checks that Store's own deadline. All six typed-runtime fixtures pass on Arch.
`fresh()` shares compiled code, never an attached adapter or a Store. Adapter
ownership now satisfies the C13 factory's Send boundary; the borrowed engine
host remains on its caller thread. No process-global state or default fuel
increase was added. Final formatting and strict all-target core Clippy pass; the code map was
regenerated. Receipts are `epoch-results.json` and `epoch-final-results.json`.
The earlier negative-control exit101 is the expected reproducer, not a failing
final test. The guarded six-test set passes.

The real-JavaScript acceptance is separate and explicitly ignored until the
reviewed C13 artifacts are supplied. It validates the component header, reviewed
SHA-256, WIT hash and interpreter identity, executes an array/closure expression,
and checks the resulting Generated/Proposed claim plus its real gate receipt.
Use the C13 reviewed artifact directory on the selected build host, or place the
reviewed manifest/components at `components/code-run-quickjs/artifacts` before
source sync. With the non-default directory configured for that host:

```text
cargo test -p oneiron --features code-sandbox-wasmtime native_quickjs_component_write_lands_through_gate -- --ignored
```

The non-default setting is `ONEIRON_QUICKJS_ARTIFACT_DIR`; use Cargo's `[env]`
configuration when crossing the build wrapper. The test's 100M fuel budget is
explicit for the C13 interpreter probe. Other callers retain the 10M default.
No real-JavaScript result is claimed without those reviewed bytes.

The separate ignored Firecracker native boot fixture still requires the full
provisioned Linux stack described above. Its image/proxy/proposal assertions
cannot be substituted by the passing localtest or image-assembly fixtures.


### Tested Linux guest artifacts

The normal wrapper selected Arch for `cargo test -p oneiron-guest --target
x86_64-unknown-linux-gnu`: all 11 library and 2 CLI tests pass. From that exact
native executable, the recipe produced a 256 MiB ext4 image with an explicit
loader/libc/libgcc closure and a type-validated conformance component. No mount,
root privilege, package installation or other worktree edit was used. debugfs
confirms root-owned mode0755 agent and interpreter inodes. Local artifact
identities and inputs are in `target/w7-validation/native-guest-artifacts/`:

- agent: SHA256 `cd4a2bede4ddde9da6f2e9d4050560b7139cecfff3dcc08e4e02241853ca845a`, BLAKE3 `c7de9d4eb2428b767a1111a4f84248739fdac85bb6439c5c09c804e2f2dba6e2` (115859232 bytes).
- rootfs: SHA256 `dbd8e3bab21cb736ed069352f3c1c5f3587bea8aa30309bd3e1c92d00c43cfdc`, BLAKE3 `7322e75bb1c0fa0c30f6fc6b890ca0988bb26c929612f6f39ecc3972beaac477` (268435456 bytes).
- component: SHA256 `5b7a91c89907a6ba8dfa9ede5a8a318597b23410790cb169610ed0ec8ee416cf`, BLAKE3 `0627fb91ebd79261eae64411c32707fb5279fc704685d157f112536cbee19cab` (1476 bytes).

This is executable/test and image-assembly evidence, not a VM boot. The remaining
native provisioning is a pinned compatible Linux kernel, compatible Firecracker
and jailer binaries, an authorized privileged launcher, and the host profile
(KVM, non-root jail uid/gid, delegated cgroup v2 cpu/memory/pids and private
roots). `bc`, Firecracker and jailer are absent on the inspected Linux host.
Kernel source/archive pin is also not supplied. The in-repo recipe requires no
installation; this seat does not install the missing prerequisites.

Final read-only C13 check at `0d21ac7d`: no manifest exists in either documented artifact location. No QuickJS or Firecracker boot result is claimed.


## Owner-authorized native acceptance continuation (2026-09-19)

This section supersedes the earlier missing-provisioning/artifact status, not
its preserved test evidence. The owner supplied Firecracker/jailer 1.17.0,
a signed-source Linux 6.18.52 kernel, the exact tested rootfs/conformance bytes,
uid963/gid962, delegated cgroups and private roots. Scoped privileged execution
is authorized on Arch only. Host Linux7.0 remains outside the upstream tested
matrix; compatibility is not assumed. Storage batch13 completed with stable
artifact links before they were used.

C13 committed real QuickJS artifacts at
`7f4eb1103428ba86d9cc879f3ed57da606d58e03`. Read-only intake verified its
manifest, canonical WIT hash, and component SHA256 values. C04 copied those
bytes into its ignored acceptance area; it did not build or edit C13:

- first-party: `3ddb36f6ca801c9b19dc59f17960ad139539a6a05a3f74d05de4220dc295f1f6`;
- foreign: `4ae3c57ba74758d5669b6568c2508b76914ce036fde9389788b3704381366a07`.

The first real native attempt reused the previously compiled, unchanged
Firecracker fixture. It failed before boot with `vsock listener bind failed`:
the provisioned private jail path exceeds Linux sockaddr_un's pathname limit.
The repair binds through a held directory descriptor under `/proc/self/fd`.
It does not change process cwd, create a public alias, shorten the private
host profile, or relax ownership. A long-path socket regression covers the
transport and its permissions. Native/QuickJS reruns are pending; receipts and
durable job custody are in `target/w7-validation/native-acceptance/`.

Review intake refreshed all PR933 top-level comments, reviews and inline
threads: 10/3/28, with 28 complete threads. PR933 is C08, not C04; none of its
24 grouped findings cites a C04-changed file. Its internal Opus findings are
also C08-specific. They are not dismissed globally or fixed in this worktree.
Codex completed its review; Cursor reported an explicit usage-limit error,
not pending work. C04 has no published PR or reviewer-stage output yet. The
full raw intake and per-finding scope ledger are preserved in
`.w7/review-native-continuation/`. No unrelated PR thread was resolved.


### First provisioned-run findings

The long-path regression passes, and the repaired core test binary compiles.
The first subsequent native fixture timed out. A diagnostic-only root-owned
jailer wrapper captured the otherwise discarded serial output while retaining
the provisioned kernel, rootfs, component, jail identity and budgets. It proves
Firecracker1.17.0 started the VM and Linux6.18.52 reached `/sbin/oneiron-guest`
on this host. PID1 then returned EBUSY and panicked; that is not completed guest
acceptance. The guest now remounts/hardens an existing kernel-provided devtmpfs
instead of attempting a second mount. Other filesystems at `/dev` refuse.
The original artifact/config receipts are preserved; the fixed guest must be
rebuilt and its replacement rootfs pinned before another native acceptance.

The real QuickJS fixture also ran and returned a guest error. Its JavaScript
input incorrectly supplied raw-WIT JSON strings to the generated JavaScript
SDK, which already serializes those fields. The fixture now supplies plain
subject/value data, retaining the actual JS expression and gate assertions.
No production SDK or C13 bytes were changed. A fresh native-core binary and
Linux guest crate test are in one serialized build service. No pass is claimed
for either corrected native fixture until those exact runs complete.


## Final native acceptance

Source commit `d88b353a` passed both explicitly ignored acceptance fixtures.
The Linux guest crate passed all 11 library and 2 CLI tests again after the
PID1 repair, and the core sandbox-feature test targets compile. The prior
long-path regression passed; final formatting and navigation are current.
Native custody used the existing Arch slot2 and ticket locks with three Cargo
jobs and one native-test thread. The FIFO admission replaced only a not-started
try-lock job; no other ticket or shared wrapper was changed.

- `firecracker_real_boot_returns_only_proposals`: **1 passed**, privileged Linux
  invocation, actual Firecracker1.17.0 and jailer, pinned Linux6.18.52 kernel,
  uid963/gid962 and the original delegated cgroup/private roots. Only the new
  rootfs digest changes in the copied host profile; original provisioned files
  remain intact. The exact test checks the host credential resolver and read
  transport, nonempty FileEdit proposals, unchanged input/no output in the host
  source tree, and consume-once collection. No VMM or child cgroup remains.
- `native_quickjs_component_write_lands_through_gate`: **1 passed**, unprivileged
  in-process Wasmtime, actual reviewed C13 QuickJS first-party bytes. JavaScript
  array/reduce computes `blend-6`; the claim is persisted as Generated/Proposed
  with its actor-bound gate receipt. This is separate from the VM's conformance
  component, which does not execute JavaScript.
- `repair-build-results.json`, `repair-native-results.json`, the exact command
  logs and `repaired-artifact-manifest.json` are in
  `target/w7-validation/native-acceptance/`. Both durable services exited0.

Artifact pins for that native pass:

- component: SHA256 `5b7a91c89907a6ba8dfa9ede5a8a318597b23410790cb169610ed0ec8ee416cf`, BLAKE3 `0627fb91ebd79261eae64411c32707fb5279fc704685d157f112536cbee19cab` (1476 bytes).
- kernel: SHA256 `35859c1ac0a122cc43ae38c670a7bd081ba08f9b944b6d3677610320659ab316`, BLAKE3 `302e365040f8b7ce793b8e8e1ec601a583491c62507aeac8f7790aeec7f6907a` (49435640 bytes).
- rootfs: SHA256 `9db81c1d7cd8c0eae4f6cb3e1096b89ade194fd5d30d97976561795e5feab05d`, BLAKE3 `660634b6b8b15684dd7202ea12f38e79fbdd1c4c38a6e12fc59272638595d004` (268435456 bytes).
- repaired agent: SHA256 `d73b2bd3037b4fb66e6fd086d6c7e4c4a2ad49d9f3828840a3d6fbb3b1b122b9`.


### Final check scope and review handoff

The exact repaired Rust files pass pinned rustfmt; the final code-map check
reports 2567 files and 17 current artifacts, and `git diff --check` passes.
Fresh Linux guest tests and core sandbox-feature test-target compilation cover
the changed native code. The prior unchanged-step test/strict-lint evidence is
retained, not rerun. A duplicate Cargo-formatting job was stopped while still
waiting for a slot, before any formatting or subsequent Clippy ran. No new
final Clippy or full-workspace VERDICT is claimed; the factory/coordinator gate
remains separate from this ticket's acceptance record.

C04 has no published PR at this writer terminal. The supplied PR933 review
packet and refreshed GitHub threads belong to C08. No C08 finding was silently
dismissed as globally invalid, no other ticket was edited, and no unrelated
thread was resolved or given a misleading acceptance comment. C04's publication
should carry this native fix/skip rationale and exact validation; any new C04
review findings must be collected in full before review fixes and followed by
a GitHub disposition on that actual PR.


## Internal review repair (2026-09-19)

The full intake precedes edits at `df38767d2f29625af419ed1fd4d185135c902c8b`.
C04 has no published PR (all-state branch query). The supplied PR933 is C08,
head `8504e48978737cc41eae473f85d88ef3087131e4`, not this change. A fresh
paginated collection captured 14 top-level comments, 6 reviews and 40 inline
comments in 40 complete threads. No inline finding names a C04-changed file.
`.w7/review-fix-intake/` retains raw IDs/bodies/head receipts, the prior 24
deduplicated groups, 12 newer findings and the internal reviewer dispositions.
Codex completed; it is not quota-blocked or pending. Cursor explicitly reports
a usage-limit error. CodeRabbit's latest automatic review was skipped; its
earlier substantive review is retained. No unrelated review thread is resolved.
The original C04 Grok verdict is LANDABLE; the eight Opus findings were assessed
against the full files, not the review's truncated diff:

| Finding | Disposition |
|---|---|
| Boot reach | Made the module private and re-exported only `run_production` at the library root. The function definition remains `pub`, as required for a public re-export and the separate binary crate. |
| Conformance reach | Made the module private and re-exported `conformance_component`; WAT is `pub(super)` for the existing sibling unit fixtures, not an external API. CLI and unit callers use the root function. |
| Filesystem helper reach | No change: `protocol::Session` calls `virtual_relative`, and runtime calls `validate_files`. Crate visibility is required; the private module exposes neither externally. `pub(super)` has the same scope here because the parent is the crate root. |
| AccountMismatch text | Informational, not a correctness defect. `bind_account` and `for_account` enforce account identity on both vaults and refuse rebinding. The existing message already names account; OF-428 prescribes cross-vault hit/provenance behavior, not error wording. |
| Build leg type reach | Rejected the requested `pub(crate)` declarations: Rust cannot widen these through public re-exports (E0365). `leg` is already private and `build_cache.rs` supplies the public seam. |
| Truncated REAPI diff | Full 131-line file is complete, including `reapi_digest` and its digest-vector test. `build_cache.rs` privately declares account/leg/reapi and exports the required types. There is no `build_cache/mod.rs`. Existing core compilation/tests pass. |
| macOS test support | README now explicitly distinguishes portable protocol/CLI tests from Linux-only PID1 setup and Firecracker/KVM boot. Non-Linux production refusal is intentional, not contradictory coverage. |
| Missing CI fixtures | Both fixture files are tracked. Exact CI generator check and discovery commands pass: 3 WIT tests and 8 artifact tests. The third command is generation, not another discovery command. |

No docs-mirror change is needed. The in-repo guest README was too terse about
test scope; it now names the portable and Linux-only boundaries. No storage ABI,
cache behavior, guest protocol, privilege guard, component bytes, write-import
set or secret-handling behavior changed. The native Firecracker and real QuickJS
receipts above remain evidence for their tested artifacts, not a claim that this
API-only patch rebuilt the pinned image. The factory's 7,174-test pass at the
pre-fix head is retained for unchanged core code.


### Review repair validation complete

Repair commit: `586af438`; fresh guest validation ran at `e72db4ed` (same Rust
bytes). `cargo test -j3 -p oneiron-guest --config .w7/cargo-test-portable.toml
-- --test-threads=2` compiled the changed crate and passed **11 library + 2 CLI
tests** on the MacBook. No failures or ignored tests; Cargo exited0. This checks
the root conformance API and ordinary-process production refusal on macOS, not
Linux PID1 boot. The earlier Linux and native acceptance evidence above is
retained for the unchanged boot implementation and tested pinned artifacts.

The changed guest files pass pinned rustfmt; code-map check and `git diff
--check` pass. Exact CI generator and Python discovery commands pass (3 WIT +
8 artifact tests). No core source changed in this repair; the factory's prior
7,174-test pass remains the unchanged-core evidence. No full-workspace verdict
is added by this scoped repair.

The unstarted retry-admission service was replaced by a FIFO wait on the
existing MacBook slot2 with the same C04 ticket lock and target, three Cargo
jobs, and two test threads. No other job or shared wrapper changed. Service
`w7-c04-review-fix-fifo-20260919.service` exited0 and released both locks.
`target/w7-validation/review-fix/fifo-results.json` and `fifo-guest-test.log`
retain exact commands, timestamps and output. This supersedes the temporary
capacity blocker and the old queued-service receipt.

The required GitHub disposition is posted at
https://github.com/oneiron-dev/oneiron/pull/933#issuecomment-5741774270.
It explicitly distinguishes the supplied C08 PR from this unpublished C04
repair, records all fixes/skips and exact validation, and resolves no C08
thread. The same comment is updated with the fresh 13-test pass.
