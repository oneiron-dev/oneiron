# W7-C04 implementation notes

Status: implementation integrated; final crate validation in progress. Steps 2
and 3 lack native guest artifacts, so full-cluster acceptance is not claimed.

Canon: `/oneiron/code-in-oneiron/`,
`/oneiron/code-in-oneiron/oneiron-arch-0050-repo-as-world/`, and
`/oneiron/code-in-oneiron/oneiron-arch-0068-repo-custody-v1/`.

## Decisions

- Code file documents use Loro in base mode, not only the optional sync transport.
  The transport's `sync` gate stays separate. This makes the file plane available
  to featureless engine callers as required by the contract.
- REAPI keys are SHA-256 digests of canonical protobuf Action messages. Existing
  blake3 snapshot identities map into a Directory descriptor. Hosts with a real
  REAPI CAS can supply the exact Directory digest. Cache rows use schema 2 and a
  new digest-key prefix; no deployed v1 rows exist.
- Account sharing is explicit. A host binds each member and an account artifact
  vault to the same immutable account identity. Both cache rows and their bytes
  live in that account vault. Artifact IDs are not reinterpreted in a member's
  entity namespace. `BuildCache::new` remains a local/trusted cache handle.
- Cache-keyed build/verify legs reserve an action before invoking the host. An
  uncertain execution is not automatically sent again. A completed cache row
  always wins. Returned provenance names the original producer.
- Working views use content-addressed codebase blobs and read-only hardlinks in
  a private host-owned directory. These are not a hostile-code sandbox. Foreign
  guests need the separate read-only microVM mount boundary. Writers submit
  operations rather than mutating these links. One view-set owns one LSP process.
- Consent wording and visible-file policy are consumer configuration in examples,
  authored by an agent asked in chat. Production code only renders/executes the
  supplied recipe. Host blast-radius numbers cannot be replaced by recipe input.
- Object-only GitWire writes pin objects but never create a served-ref intent.
  The origin protocol, not generic GitWire recovery, owns exported ref publication.

## Per-step record

| Step | Ticket | Implementation / current remaining work |
|---|---|---|
| 1 | ONE-2127 | Review requests carry host walk facts and optional risk summary; configured v1 recipe and test added. Focused Linux tests passed (66-test scope). |
| 2 | ONE-2128 | Real Wasmtime Component Model runtime integrated; featureful Linux cargo check passed. WAT host-trap tests added. Real QuickJS guest artifact acceptance remains unavailable. |
| 3 | ONE-2129 | Firecracker jailer/config/pins/vsock credential proxy integrated; featureful Linux cargo check passed. Boot test is explicitly ignored until host supplies VMM, kernel/rootfs and guest agent. No boot acceptance claimed. |
| 4 | ONE-2130 | Producing operation/actor/turn/activity/intent chains in symbols, chunks, and both blame readers; optional legacy decode test added. Focused Linux tests passed (66-test scope). |
| 5 | ONE-2131 | Semantic symbol/content changes own text rendering and chunk/embedding selection; removed old line-window differ. Focused Linux tests passed (66-test scope). |
| 6 | ONE-2132 | Snapshot-first Proposed and immutable review tests pass on Linux. |
| 7 | ONE-2133 | Split, hard-veto, and unanimous independent-critic tests pass on Linux. |
| 8 | ONE-2134 | Session claims/trailers integrated. Promotion requires applied review receipts for every changed file, exact document frontiers, matching provenance and current session head. Positive and missing-review tests added; runtime proof pending. |
| 9 | ONE-2135 | Live-head, fork and snapshot read-only mounts pass on Linux. |
| 10 | ONE-2136 | Stock Git push/fetch, actor-attributed operation receipts and concurrent-push serialization pass on Linux. |
| 11 | ONE-2137 | Blake3 capture and SHA256 legacy prepared/restore recovery pass on Linux. |
| 12 | ONE-2138 | REAPI Action digest and Command protobuf encoders; explicit input CAS digest; schema/key change and standard fixture added. Focused Linux tests passed (66-test scope). |
| 13 | ONE-2139 | Explicit account-bound shared index/artifact vault and cross-member provenance test added. Focused Linux tests passed (66-test scope). |
| 14 | ONE-2140 | Cached build/verify runner plus repeat-hit test added. View builds use it. Focused Linux tests passed (66-test scope). |
| 15 | ONE-1911 | Typed change-index and host source-proof publication tests pass on Linux. |
| 16 | ONE-1912 | Six epoch/cutover/recovery tests pass on Linux, including mirror and stale-host refusal. |
| 17 | ONE-1913 | Stock Git conflict-tree clone and render fixture passes on Linux; format is engine v1, not jj private compatibility. |
| 18 | ONE-2141 | Concurrent-session Loro convergence, cursor and rename fixtures pass on Linux. |
| 19 | ONE-2142 | Span edits, sequenced receipts and durable retry deduplication pass on Linux. Firecracker lowering uses the exact boot base. |
| 20 | ONE-2143 | Document operation folds and tamper/refusal fixtures pass on Linux. |
| 21 | ONE-2144 | Persisted batching and crash-census fixtures pass on Linux. |
| 22 | ONE-2145 | All seven production reviewed-stack tests pass on Linux, including authorized-prefix recovery and stale-document refusal. |
| 23 | ONE-2146 | Bisection, last-green and slow-red rollback fixtures pass on Linux. |
| 24 | ONE-2147 | Public Rust name removal/private-addition oracle fixtures pass on Linux. |
| 25 | ONE-2148 | Cargo metadata affected-test graph fixtures pass on Linux. |
| 26 | ONE-2149 | JSON shape and command output/exit drift fixtures pass on Linux. |
| 27 | ONE-2150 | Blob-linked views, durable per-view receipts, configured visible-file policy test added. Focused Linux tests passed (66-test scope). |
| 28 | ONE-2151 | One bounded stdio LSP process with per-view routing and restart; real process fixture added. Focused Linux tests passed (66-test scope). |
| 29 | ONE-2152 | Views use the account cache runner and materialize admitted outputs on hits; Rust compiler fixture added. Focused Linux tests passed (66-test scope). |
| 30 | ONE-2153 | Deterministic canonical metadata projection, CAS refusal and stock Git successive clone/fetch fixture pass on Linux. |
| 31 | ONE-2154 | Atomic conflict claim/reconciliation task and reviewed conflict recovery tests pass on Linux. |
| 32 | ONE-2155 | Stale settle and divergent revision proposal fixtures pass on Linux; neither advances/rebases the canonical head. |

## Canon clarifications

The current spine says guests still propose whole files. The step-19 contract is
not permission to expose whole-byte canonical writes. Whole-file guest/git inputs
must be lowered at the door to spans against the guest's base document.

The old context pack called Loro sync-only and snapshots sha256. Those were code
observations, not the target architecture. Per-file documents are a base-mode
engine concern. New repository snapshots use blake3 and retain legacy recovery.

## Validation log

- Budget for local commands: Linux, worktree `target/`, `CARGO_BUILD_JOBS=2`,
  `CARGO_INCREMENTAL=0`; runtime test threads 2.
- The host cargo shim unexpectedly routed early checks to a MacBook. A later
  `cargo check -p oneiron --tests` passed there, but is not the Linux gate.
  That old local override was removed at the owner restart. Future Cargo commands
  use the factory MacBook wrapper. No host tool installation is used.
- Focused Linux nextest passed: 66 tests, 0 failures. Filter: `code_symbol::`, `build_cache::`, `code_run::consent::`, `code_view::`. This is not the full touched-crate pass.

- Integrated worker snapshot passed Linux `cargo check -p oneiron --tests --features sandbox-wasmtime,microvm-firecracker` (`.w7/check5.log`). Later ingress additions are not covered by that check.
- First featureful whole-crate `cargo test` compiled runtime dependencies but failed at integration parsing/exhaustive match errors, fixed next. No runtime green claimed from that attempt.
- Whole-crate test retry is running at `.w7/cargo-test-current.log`; root owns the only Cargo target writer.

- Direct native binary `code_document::` passed 7/7 (same unchanged document bytes as whole-crate build), `.w7/doc-runtime.log`.
- Sandbox scope: 31 passed, 3 WAT-fixture parse failures, 1 provisioned-host boot test ignored. Fixed the new WAT parser's strict nested core-memory/function reference syntax. Standalone native probe using the project's already-built Wasmtime and Oneiron libraries now compiles and executes that exact clock-import component successfully. The three crate tests still need a rebuilt run.

- Whole-crate attempt completed its library run: 6,574 passed, 8 failed, 5 ignored. The eight failures were the three WAT syntax fixtures, two unbound-session proposal writes, a null-commit receive-pack probe, an exact sync-marker expectation missing the new typed change claim, and per-ref LFS behavior changed by early whole-push document ingress. Fixes are applied; focused integrated rerun is active. Integration binaries/doctests did not run after the library failure.
- Epoch and reviewed merge-stack workers are fully integrated. Their runtime acceptance remains pending. Legacy snapshot recovery now recognizes both digests for prepared pre/post comparisons as well as explicit restore.

- Integrated focused run: 134 passed before one origin LMDB `BadRslot` stopped it. Document/revision folds, stale proposals, all non-provisioned sandbox tests (including real Wasmtime gate writes), oracle fixtures and merge-queue fixtures passed. Cause was a nested same-thread read transaction in the new epoch validator; it now reuses the caller transaction. Origin/repository retry runs with fail-fast disabled.

## Repository journal ownership

`repo.proposed_diff` and `repo.session` are engine-owned predicates. They record
review and execution facts, not arbitrary model memory. The review door writes
these records through the reserved claim owner path and retains the actor in
standard evidence and the session in subject/value. They intentionally do not
set `sess`, which routes envelope-bound session-bundle candidate writes. Public
claim APIs cannot rewrite these journal records. The normal queue still requires
immutable independent critic approval before any file effect.

The latest origin/repository lane passed 106 tests, including every origin test;
ten review/promotion tests stopped at the old generic journal admission gate.
The ownership correction above is being tested independently.

- Journal ownership correction passed 9/10 focused tests, including both review
  verdict cases and all seven production reviewed-stack recovery tests. The last
  test exposed the same generic-gate mismatch in the new typed run-provenance
  recorder; that recorder now uses its existing engine-owner claim transaction.
  It does not grant code promotion. Full current-source Clippy is running.

## Current final validation

- MacBook strict Clippy passed for all core targets with
  `sandbox-wasmtime,microvm-firecracker` and `-D warnings`.
  Log: `target/w7-validation/clippy-final.log` (excluded from host sync).
- Core crate test command is running on the MacBook with both sandbox features.
  Cargo's per-command `env.TMPDIR` is forced to `/private/tmp` for the documented
  macOS secret-lease path rule. No shared Cargo or host configuration changed.
- Source is committed through `260ac0fe`; code map and structural/root-surface
  ratchets pass. Full native QuickJS and provisioned Linux microVM acceptance
  remain unproven. These are not replaced by WAT or socketpair fixtures.
