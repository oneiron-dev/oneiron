# W8-T21 implementation notes

- `withheld_claim_carriers` now borrows the export caller’s `RoTxn`; selector authorization opens its snapshot before the withheld scan, and the window scrub releases its read before any history-free write.
- The new local-claims test puts a stale ordinary window body under a fresh ID, opens an older read, then appends an esign claim at that ID. The later write is visible to a fresh transaction but not the older snapshot supplied to the helper. It must append, not overwrite: esign events are append-only.
- Canon: `generated/oneiron/sync/oneiron-arch-0023b-crdt-sync-implementation-v1.md` (ARCH-0023b, “One admission rule, two planes”). The page does not specify single-snapshot export consistency or LMDB's one-reader-slot constraint. It should state that the selector's local-only claim exclusion and all subsequent admission checks use the same export read snapshot. The in-tree export comment already states the operational rule.
- The context pack notes that the owner-calls page citation and ONE-2586 registry row were not found in the read-only docs mirror; no docs mirror change was made.
