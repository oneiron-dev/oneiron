# W8-T24 implementation notes

- DAG test policies now authenticate the embedded owner, revalidate that owner and the live actor/class in the same write transaction, then write a non-default policy manifest through the local maintenance-band apply door. The actor-specific manifest ID is stable so an `auto` → `proposed` → `auto` toggle replaces its own row rather than leaving an earlier clamp in the fold.
- The shared custom-policy fixture also requires a live, class-matching actor, refuses the default ID, and refuses to reuse a non-manifest ID. Its server caller passes the persisted embedded owner.
- The fixture uses the current policy schema 1.2 rather than the stale 1.1 schema. No canon change is needed: `generated/oneiron/decisions/oneiron-dec-0005-policy-residence.md` line 26 already says the default is seeded only on vault creation. If the canon page documents test fixtures later, it should say test manifests live at non-default IDs and the owner is authenticated and revalidated before writing. The context pack's old fixture path describes the bug, not intended behavior.

- Preserving the real default exposed two pre-existing fold restrictions: the default classifies `conversation.append_record` as critical, and its single `generated` row is bound to the commitment projector. A second actor-bound source permit previously collided and denied both actors. To keep existing DAG assertions without replacing the default, the default now names the ordinary conversation-append predicate as normal; distinct trusted source-trust actor bindings fold independently, while overlapping/unbound rows remain restrict-only and untrusted rows cannot create permits. The resolved frontier includes the new bindings. DEC-0005's most-restrictive-wins rule is interpreted per actor scope; the canon page should clarify this disjoint-binding case and the default conversation-append classification.
