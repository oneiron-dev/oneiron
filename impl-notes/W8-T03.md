# W8-T03 implementation notes

- `ConsultResultKind::Answer` now carries a Rust-only optional `TaskAskOptionId`. The consult settlement passes it to `record_answer` without changing the stored terminal summary or `tasks.answer`.
- `validate_word` remains the single check for option membership. Its option refusal uses a distinct `InvalidTaskBody` context so the facade can return `BAD_REQUEST` with actionable option-id advice. The transaction aborts before the TASK terminal write.
- Canon cited by the packet: `ARCH-0067:230` (`what` is the question; options carry stable ids), `int/packets/P5.task.md:20` (predicates bind to option ids). These cited pages/lines are absent from the read-only docs mirror at this checkout; mirror should include the corresponding stable-id rule. In-repo equivalent: `crates/oneiron/src/task_verb/ask_types.rs:38` and `ask_record.rs::validate_word`. No contradictory canon found.
