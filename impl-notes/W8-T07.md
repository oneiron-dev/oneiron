# W8-T07 — Remote client errors say who caused them

- Canon: `generated/oneiron/agents/oneiron-arch-0028-mcp-external-agent-surface-v1.md:209,212` says the SDK and REST project one verb table and errors stay wire-visible. Generate the facade and typed wrappers from `scripts/sdk/generate.py`, never edit projections by hand.
- Typed remote calls now deserialize at the HTTP boundary. This preserves the actual 2xx status and serde error alongside the verb name; the transport's `INTERNAL_SERVER_ERROR` constructor supplies health and URL suggestions. Typed input encoding stays `BAD_REQUEST`, and remote input validation runs before sending. Embedded calls still preserve engine errors without rewriting the payload.
- The HTTP `invoke` path decodes and calls the verb's admission checks, so remove its redundant `validate_input`. Keep the typed `tasks.check` exception because it uses `facade_input`, not `invoke`. Keep request/response limits, envelope handling, and typed checks before JSON encoding.
- `sdk.rs` shape errors retain `BAD_REQUEST`. `serde_json::from_value` discards the failing field path for a malformed nested value, so use the already locked `serde_path_to_error` crate to include the field and serde type detail.
- Canon is not wrong on error semantics. The context pack reports that the docs registry omits ONE-2572; add that ticket row to the registry. The ticket's “16 MB cap” wording is not the transport limit; `MAX_REMOTE_REQUEST_BYTES` and `MAX_REMOTE_RESPONSE_BYTES` are 64 MiB, and witness message content has a separate entity payload cap. Clarify those separate limits on the canon page if it discusses a single cap.
