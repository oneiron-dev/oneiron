# W8-T41 implementation notes

- Added the existing zero-retry, all-test featureless nextest profile beside the shared-process featureless `cargo test` step in both CI test jobs. The macOS step copies its neighbour's `if:`; the Linux job keeps its current push/dispatch-only gate. The full profile, macOS bench carve-out and doctests are unchanged.
- Added a structural workflow test for both jobs, the exact commands, adjacency and matching conditions. The repo's `AGENTS.md` featureless-build acceptance rule and this ticket govern the change; the external canon has no page for this CI policy.
- `docs/ops/build-performance.md` still calls this profile inner-loop-only and says CI is unchanged. That operational page should say the featureless nextest profile now runs in CI beside, not instead of, the shared-process `cargo test` step. The separate docs mirror is unchanged.
- Validation: the CI structural test and all five cases in `test_wire_workflow.py` passed by direct stdlib invocation (a minimal in-memory decorator shim for pytest); the workflow also parsed as YAML. The exact `python3 -m pytest -q scripts/tests/test_ci_workflow.py scripts/tests/test_wire_workflow.py` command could not start because host `/usr/bin/python3` has no `pytest`, and tool installation is forbidden by the ticket. `cargo nextest run -p oneiron --lib --no-default-features --profile featureless --no-fail-fast` passed: 7,409 passed, 4 skipped, zero failures; controller wall time 1,727.406 seconds (28m47.4s), nextest suite time 1,448.799 seconds. Host was the factory Cargo proxy targeting aarch64-apple-darwin; `CARGO_BUILD_JOBS=2`, default worktree target, and no explicit nextest test-thread limit.
