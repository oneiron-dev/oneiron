# W8-T25 — feedback HTTP transport and space posting

- Reject `Some(space_posting)` in `HttpFeedbackTransport::deliver` with `RouteMismatch` before building an HTTP request. The deployment-selected HTTP endpoint cannot present a frozen channel identity. Leave the route match, headers, payload encodings and definite-failure mapping unchanged.
- The named loopback test proves the posting path produces a definite route failure without opening a connection. The existing no-posting delivery test remains unchanged.
- Canon: read-only docs mirror `registry/oneiron.json`, OF-420 (engine feedback channel). It requires preview approval and deployment-dependent transport, but is silent on space postings for feedback. Add an explicit note there that an HTTP feedback transport refuses a frozen space posting unless it can honor that presentation; do not silently drop it.
