# W7-C09 implementation

Canon (read-only docs mirror):
- `generated/oneiron/agents/oneiron-arch-0066-self-healing-v1.md`
- `generated/oneiron/backend/oneiron-arch-0011-billing-payments-v2.md`
- `generated/cross/hosting/cross-arch-0019-hosting-deployment-architecture-v1.md`
- `generated/oneiron/sync/oneiron-arch-0043-federation-membership-v1.md`
- Registry OF-296, OF-301 and OF-311.

All 24 steps are implemented and their scoped acceptance is complete. No separate
implementation piece remains. Validation scope and host limits are explicit below.

## Per-step delivery and remaining work

| Step | Implementation and acceptance | Remaining |
|---|---|---|
| 1 | `recovery/checkpoint` snapshots canonical rows from one read transaction; restores rebuild text, phonetic, type, temporal, embedding work and auxiliary indexes; runtime leases and budgets reset. Restore/wake/migrate share the entry point. Core restore tests pass. Meter-rollup restore also passes. | None. |
| 2 | Every restore appends a local RESTORE epoch with source checkpoint digest, timestamp and reason. All three restore reasons are tested. | None. |
| 3 | RetrievalMiss and ConsolidationError pure detectors project native retrieval telemetry and gate/consolidation receipts. Vault entry-point tests cover class, stable identity and malformed silence. | None. |
| 4 | Consent queue depth and per-actor predicate/rate drift use fixed windows and manifest bounds. Bounds participate in decode, fold and frontier identity. Native gate receipts carry a bounded predicate-hash witness, erased with receipt reasons. Boundary and below-bound tests pass. | None. |
| 5 | Closed-form Dreamer run and silent-conversation predicates; healthy/malformed fixtures stay silent. No model-graded T2/T3 judgment. | None. |
| 6 | Scheduled T1 runs sign canonical event bodies with local Ed25519 identity. Unsigned, foreign and tampered envelopes persist nothing. Local eligibility receipts do not replicate. Diagnostic events are excluded from peer ingest and egress. Sync diagnostic bridge, selector and rematerialization regressions pass. | None. |
| 7 | External healer propose submissions count per engine-stamped actor, remain Proposed without rate refusal, raise one typed burst check and keep atomic run/reversal receipts. Burst fixture passes. | None. |
| 8 | External registration reads diagnostic-band ScopedRead and submits DevPatch/SchemaPatch through existing repair consent evaluation. TestFailure corpus and HumanReview patch fixtures pass. | None. |
| 9 | Human owner approval releases a PR-shaped shipped-release record; denial and reversal are terminal. No code/schema apply API. | None. |
| 10 | Embedded dev registration refuses; daemon/self-host registration requires the held writer lease. Production rejects code, schema, skill and protected engine/Soul/CORE targets at admission. | None. |
| 11 | Regional single-writer GPU registry implements P2C over transport/app/inference-healthy pods, capacity, 300-second leases and 60-second renewal instructions. Distribution, expiry, renewal and health tests pass. Host supplies clock and entropy. A fixed-sample prefix test distinguishes P2C from always-first and global-minimum routing. | None. |
| 12 | API middleware and guarded sync transport require matching vault/device/key leases before reads or writes. API fixtures reject unleased, cross-vault and revoked bindings. Grant-backed WORLD replication remains the native sharing path. Two-vault WORLD export/import, local API read, cross-vault query and unleased sync denial pass. | None. |
| 13 | Fresh-key proof plus terminal old-key revocation and new active same-vault binding commit atomically. Tests cover forged proof, valid rotation and no old-key reuse. | None. |
| 14 | Linux probes fscrypt policy ioctl and a dedicated non-root UID through a root-owned unique UID/name/path allocation table. Other platforms fail closed. Real managed vaults receive a persisted nonzero lease scope; canaries keep the explicit waiver. Positive/negative managed-open evidence-seam tests pass. | None. |
| 15 | Immutable subscribing-owner binding scopes replay, content and embedding constructors; native pending-embedding tokens and retrieval replay hashes carry the owner. Two holding vaults with byte-identical foreign WORLD yield different keys. Managed startup requires the supervisor owner before serving and refuses an owner mismatch on reopen. | None. |
| 16 | GPU work lease captures the holding scope and inputs before dispatch. Completion verifies a live healthy lease and returns an owner-sealed output. Shared-pod two-owner test passes. | None. |
| 17 | Hosted user-derived cache keys and seal checks are account-bound. Public-artifact cache is a separate explicit operation. Cross-account miss and public sharing tests pass. | None. |
| 18 | Fixed-point Money stores provider currency and price-table snapshot; checked integer rates, codec, event ledger and currency-separated counters. USD/JPY round-trip and replay tests pass. Overflow is a client error. | None. |
| 19 | Allowance ladder removed. Cached host policy stores original limit, one-time converted limit and exact rate. BudgetGuard owns 50/80/95 and typed exhaustion. Meters are recorded independently of admission, so denial never loses usage facts. Exhaustion returns the typed denial. | None. |
| 20 | Engine top-up route, wallet keys/codec and ledger operation removed. Route-absence fixture passes. Discovery, OpenAPI and the served skill pack no longer advertise deleted wallet routes. | None. |
| 21 | Owner/vault-only rollups; no owner-wide aggregate. Kept key bytes were already `usage:event:` and `usage:rollup:vault:` and are unchanged, so their stored-key migration is identity. Deleted aggregate families are no longer read or written. Unshipped old bodies have no compatibility decoder. Restored rollups rebuild from stamped money without re-pricing. | None. |
| 22 | CI lanes use pinned sccache 0.15.0 with host/shared storage, version/toolchain/lockfile keys and cache-stat receipts. Full-tier nextest commands unchanged. Native cold-artifact proof records 537 hits and 111.4s uncached versus 46.9s cached. | None. |
| 23 | Pinned cargo-mutants 27.1.0, scoped engine sentinel corpus, fresh-run report handling and CI baseline gate. Surviving/empty audit fixtures fail closed. Exact two-file selection yields 17 mutants. Measured score: 17/17 caught. One deliberate survivor fails the CLI gate. | None. |
| 24 | Pinned cargo-llvm-cov 0.8.7 collects nextest, featureless libtest and doctest lanes. Counter-only clean preserves builds; reports discover doctest binaries. LCOV union keys canonical file+line, taking max hits to dedupe overlap. | None. Three instrumented lanes merged and verified. |

## Storage and canon decisions

- OF-296's runtime B5 pointer is stale: the referenced runtime page has no B5 section. The registry's tier and RESTORE laws define this implementation.
- Mixed databases require row-family classification. Canonical text/phonetic caller instructions are retained as rebuild witnesses; an older index without its witness is refused rather than restored as silently empty search.
- `commitment_due:v1:` primary rows encode optional scheduling enrollment, not merely a derived index. Stored-but-unindexed commitment series are legal. Primary obligations stay canonical; reverse/project sidecars rebuild. The skill-attribution processed cursor likewise cannot be guessed from the maximum evidence sequence, which may be unprocessed.
- Meter events are canonical stamped facts. Rollups are derived and omitted from checkpoints; the ledger rebuilds them under one write snapshot without re-pricing or double-counting. Cached limits are runtime state and disappear on restore.
- CROSS-ARCH-0019 should spell out the fscrypt and root-owned UID-allocation probe requirements. Probe UAPI layout was checked against Linux `fscrypt.h`; the ioctl request deliberately encodes a 9-byte flexible prefix, while the provided buffer accommodates the full policy.
- The GPU ticket's `runtime.rs` is now `runtime/`. The engine registry is the regional single-writer state machine; deployment, probes and lease-renewal scheduling belong to its host.

- Device `ls:` rows are canonical authority, not process/runtime leases. Their revoked-pubkey floor survives restore and still rejects the old key under any same-vault client id.
- The detector default is 100 writes, within its 1,024-observation window. Manifest configuration still sets the bound. Proposal pressure is separate at 100,000 submissions and never refuses a proposal. The fixture crosses the actual production threshold.
- Tripwire observation markers use a distinct namespace from checker outcome reasons. The checker assertions remain intact; only their interpretation of additional observation metadata changed.

## Managed-owner handoff

Managed startup requires `--derivation-owner`: the trusted supervisor's non-secret
32-byte account/organization ID as hexadecimal. It is not inferred from a vault
ID, display name, DEK or spawn token. The existing 64-byte credential frame is
unchanged. The existing crate decoder is reused with an ASCII-hex guard, including
a test that rejects plus-sign aliases. No dependency was added.

The vault binds its owner after isolation and DEK gates, before listeners start.
Reopen with another owner fails closed. Required/malformed CLI identity, owner
persistence and owner mismatch tests pass. CROSS-ARCH-0019 federation:S3 and
ARCH-0043 federation:S4 should name this supervisor input; the managed-launch
text omitted the account handoff. Native standalone hosts use the explicit owner
binding API before hosted derivation. Unmanaged startup behavior is unchanged.

## Reviewed write-boundary pin

The OF-060 F2 scanner and its negative extra-write test are unchanged. The obsolete
tenant-rollup and top-up exceptions are removed. Approved writes are only
host-owned metadata: fixed-key managed lease scope, stamped usage events, derived
per-vault rollups and cached converted limits. None admits a foreign/guest entity,
edge, vector or arbitrary storage key. This is a deliberate pin update, not a
blanket exception. All six fitness tests pass, including the negative control.

## Final validation evidence

These are scoped acceptance receipts, not a full Linux gate verdict. Successful
unchanged-source results were reused; interrupted jobs and partial output were
not counted. Old completion-parser jobs were stopped by the owner. HEAD
`10d24de4` and the later corrections are preserved. Fresh native runners record
source SHA-256 values and each command's exit code; the final Rust source hashes
match this worktree.

### Core and server

- Core library: 7,034 passed, four ignored. Core `it` had 472 passes and one stale
  write-pin failure; after the reviewed pin update, all six fitness tests passed.
- Other core targets: build cache six, code memory 36, Git smart HTTP 13, sync 184
  (one ignored), lens 17, compile-fail boundary harness one, vault-read parity
  nine, voice sync six. Seven core doctests passed.
- Focused final detector/healer, restore, lease and usage acceptance: 73 passed.
  Earlier unchanged-source focused suites additionally cover the native
  WORLD/API composition and diagnostic bridge/selector paths.
- Full `cargo test -p oneiron-server`: 734 library tests passed (five ignored),
  GPU routing one, integration 91, managed mode 37, managed privacy three and
  WebSocket tier four. This includes the final managed-owner parser and handoff.
- Formatting passed. Core all-target/all-feature and featureless Clippy passed.
  Server all-target/all-feature and production-only Clippy passed. Each Clippy
  command denies warnings. Strict core/server all-feature rustdoc passed with
  inherited encoded rustdoc flags removed.
- Five dependency-free audit fixtures passed, including incomplete/surviving
  mutation refusal, line dedupe, private real-path temporary directories and
  deep report paths. The generated code map is current.

### Compilation cache and mutation audit

`scripts/audit/cache-baseline.json` records the native pinned-tool comparison:
111.426s uncached, 123.880s cache population, 46.943s repeated cold artifacts,
537 Rust hits. CI compiler lanes are wired to the same pinned cache tool and
emit statistics. This is native repeat-build evidence, not a claim that a GitHub
Actions run occurred in this writing seat.

The mutation run completed at 2026-09-19T07:26:59Z with 17 caught, zero missed,
timed out or unviable. The measured score floor is 100%. The minimum count stays
one so legitimate touched-crate subsets can run. The production sentinel files
and tripwire test hashes match the measured source.
`scripts/audit/mutation-evidence.json` retains per-mutant results and source
hashes. The saved-report CLI passes. Changing one outcome to a survivor fails
with exit 1 (16/17 below the floor), the same gate used by CI.

### Coverage collection

The full-tier instrumented lane ran 9,312 tests: 9,297 passed, including one
allowed full-profile retry, and 15 failed. One failure was the repaired write
pin; 14 managed fixtures exceeded macOS Unix-socket path limits. No failing test
was excluded. The collector now creates a short, private, canonical temporary
directory independent of report output; `--tmp-dir` selects a short parent.

The continuation checked original source hashes, retained the full run's raw
profiles, and reran all 42 fitness/managed tests successfully. Lane 0 was then
reported. Featureless library tests passed (6,534; four ignored), followed by lane 1
reporting. All seven workspace doctests and lane 2 reporting passed. The final
merge was checked against the exact per-file/per-line union of all three reports:

- 1,609 files, 289,309 unique executable lines, 260,278 covered lines.
- 578,561 overlapping line occurrences removed; each merged line appears once.
- Every merged hit count equals the maximum across its input lanes.
- `scripts/audit/coverage-evidence.json` records per-lane counts and checksums.
  The merged SHA-256 is
  `3a12ca654d3da59a32d7ec80282e6ce1fe2aa81072b247cf6644e3abe0b34d29`.
  Reports are retained locally in `.w7/coverage-reports/` and in the native
  snapshot's `target/coverage/`; `scripts/audit/coverage.py` reproduces collection
  and merging. This is line coverage, not a branch-coverage claim.

Coverage uses an immutable pre-managed-owner-correction source snapshot, plus
the repaired test pin and collector path fix. It proves the collection/merge
mechanism, not coverage of later server source. Those later server changes have
the exact-source full server, Clippy and rustdoc receipts above. The snapshot's
production core sources remain unchanged. The collector uses pinned Rust 1.96
and preinstalled matching-major LLVM tools; `RUSTC_BOOTSTRAP=1` is restricted to
the doctest lane/report. Counter-only cleaning preserves compiled artifacts.

## Host and scope limits

- No tools or services were installed, restarted or upgraded. No docs-mirror,
  other-ticket source, remote configuration or shared Cargo configuration changed.
- Managed fscrypt admission was tested through the same typed evidence seam,
  with positive and negative fixtures. No privileged Linux fscrypt provisioning
  was performed on the MacBook. Non-Linux real-tenant admission remains closed.
- GPU deployment, external health probing and lease-renewal scheduling belong to
  the host. The engine supplies the per-region state machine and enforces all
  three health tiers and expiry on its routing/renewal/completion doors.
- No autonomous code/schema repair executor was added. Human release produces a
  PR-shaped release record; the engine does not apply patches or run a product's
  external healer.
