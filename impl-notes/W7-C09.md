# W7-C09 implementation

Canon (read-only docs mirror):
- `generated/oneiron/agents/oneiron-arch-0066-self-healing-v1.md`
- `generated/oneiron/backend/oneiron-arch-0011-billing-payments-v2.md`
- `generated/cross/hosting/cross-arch-0019-hosting-deployment-architecture-v1.md`
- `generated/oneiron/sync/oneiron-arch-0043-federation-membership-v1.md`
- Registry OF-296, OF-301 and OF-311.

## Per-step delivery and remaining work

| Step | Implementation and acceptance | Remaining |
|---|---|---|
| 1 | `recovery/checkpoint` snapshots canonical rows from one read transaction; restores rebuild text, phonetic, type, temporal, embedding work and auxiliary indexes; runtime leases and budgets reset. Restore/wake/migrate share the entry point. Core restore tests pass. | Meter-rollup restore test passed in the server library run. |
| 2 | Every restore appends a local RESTORE epoch with source checkpoint digest, timestamp and reason. All three restore reasons are tested. | None in focused scope. |
| 3 | RetrievalMiss and ConsolidationError pure detectors project native retrieval telemetry and gate/consolidation receipts. Vault entry-point tests cover class, stable identity and malformed silence. | None in focused scope. |
| 4 | Consent queue depth and per-actor predicate/rate drift use fixed windows and manifest bounds. Bounds participate in decode, fold and frontier identity. Native gate receipts carry a bounded predicate-hash witness, erased with receipt reasons. Boundary and below-bound tests pass. | None in focused scope. |
| 5 | Closed-form Dreamer run and silent-conversation predicates; healthy/malformed fixtures stay silent. No model-graded T2/T3 judgment. | None in focused scope. |
| 6 | Scheduled T1 runs sign canonical event bodies with local Ed25519 identity. Unsigned, foreign and tampered envelopes persist nothing. Local eligibility receipts do not replicate. Diagnostic events are excluded from peer ingest and egress. | Sync diagnostic bridge, selector and rematerialization regressions pass. No autonomous repair executor is introduced. |
| 7 | External healer propose submissions count per engine-stamped actor, remain Proposed without rate refusal, raise one typed burst check and keep atomic run/reversal receipts. Burst fixture passes. | Scope is the healer propose lane and runaway-healer run in ARCH-0066 §8; no new generic claim rate rejection. |
| 8 | External registration reads diagnostic-band ScopedRead and submits DevPatch/SchemaPatch through existing repair consent evaluation. TestFailure corpus and HumanReview patch fixtures pass. | None in focused scope. |
| 9 | Human owner approval releases a PR-shaped shipped-release record; denial and reversal are terminal. No code/schema apply API. | None in focused scope. |
| 10 | Embedded dev registration refuses; daemon/self-host registration requires the held writer lease. Production rejects code, schema, skill and protected engine/Soul/CORE targets at admission. | None in focused scope. |
| 11 | Regional single-writer GPU registry implements P2C over transport/app/inference-healthy pods, capacity, 300-second leases and 60-second renewal instructions. Distribution, expiry, renewal and health tests pass. Host supplies clock and entropy. | No Cloudflare deployment is part of the engine crate. |
| 12 | API middleware and guarded sync transport require matching vault/device/key leases before reads or writes. API fixtures reject unleased, cross-vault and revoked bindings. Grant-backed WORLD replication remains the native sharing path. | Two-vault grant-backed WORLD export/import, local API read, cross-vault denial and unleased sync denial pass. Managed nonzero-scope and owner-recovery tests pass. |
| 13 | Fresh-key proof plus terminal old-key revocation and new active same-vault binding commit atomically. Tests cover forged proof, valid rotation and no old-key reuse. | None in focused scope. |
| 14 | Linux probes fscrypt policy ioctl and a dedicated non-root UID through a root-owned unique UID/name/path allocation table. Other platforms fail closed. Real managed vaults receive a persisted nonzero lease scope; canaries keep the explicit waiver. | Open-path positive/negative evidence-seam fixture passes. No privileged hardware fscrypt provisioning was performed. |
| 15 | Immutable subscribing-owner binding scopes replay, content and embedding constructors; native pending-embedding tokens and retrieval replay hashes carry the owner. Two holding vaults with byte-identical foreign WORLD yield different keys. | Host must bind its trusted account/organization identity before hosted derivation; the engine does not invent an account directory. |
| 16 | GPU work lease captures the holding scope and inputs before dispatch. Completion verifies a live healthy lease and returns an owner-sealed output. Shared-pod two-owner test passes. | None in focused scope. |
| 17 | Hosted user-derived cache keys and seal checks are account-bound. Public-artifact cache is a separate explicit operation. Cross-account miss and public sharing tests pass. | None in focused scope. |
| 18 | Fixed-point Money stores provider currency and price-table snapshot; checked integer rates, codec, event ledger and currency-separated counters. USD/JPY round-trip and replay tests pass. | API overflow-as-client-error test passes. |
| 19 | Allowance ladder removed. Cached host policy stores original limit, one-time converted limit and exact rate. BudgetGuard owns 50/80/95 and typed exhaustion. Meters are recorded independently of admission, so denial never loses usage facts. | Exhaustion-variant fixture passes. |
| 20 | Engine top-up route, wallet keys/codec and ledger operation removed. Route-absence fixture passes. Discovery, OpenAPI and the served skill pack no longer advertise deleted wallet routes. | Full server test pass pending. |
| 21 | Owner/vault-only rollups; no owner-wide aggregate. Kept key bytes were already `usage:event:` and `usage:rollup:vault:` and are unchanged, so their stored-key migration is identity. Deleted aggregate families are no longer read or written. Unshipped old bodies have no compatibility decoder. | Derived rollup rebuild from stamped canonical money passes. |
| 22 | CI lanes use pinned sccache 0.15.0 with host/shared storage, version/toolchain/lockfile keys and cache-stat receipts. Full-tier nextest commands unchanged. | MacBook proof: 537 repeat Rust hits; cold artifacts 111.4s uncached versus 46.9s cached. Machine-readable evidence is scripts/audit/cache-baseline.json. |
| 23 | Pinned cargo-mutants 27.1.0, scoped engine sentinel corpus, fresh-run report handling and CI baseline gate. Surviving/empty audit fixtures fail closed. Exact two-file selection yields 17 mutants. | Actual engine kill-score baseline not measured yet. |
| 24 | Pinned cargo-llvm-cov 0.8.7 collects nextest, featureless libtest and doctest lanes. Counter-only clean preserves builds; reports discover doctest binaries. LCOV union keys canonical file+line, taking max hits to dedupe overlap. | Real merged instrumented report pending. |

## Storage and canon decisions

- OF-296's runtime B5 pointer is stale: the referenced runtime page has no B5 section. The registry's tier and RESTORE laws define this implementation.
- Mixed databases require row-family classification. Canonical text/phonetic caller instructions are retained as rebuild witnesses; an older index without its witness is refused rather than restored as silently empty search.
- `commitment_due:v1:` primary rows encode optional scheduling enrollment, not merely a derived index. Stored-but-unindexed commitment series are legal. Primary obligations stay canonical; reverse/project sidecars rebuild. The skill-attribution processed cursor likewise cannot be guessed from the maximum evidence sequence, which may be unprocessed.
- Meter events are canonical stamped facts. Rollups are derived and omitted from checkpoints; the ledger rebuilds them under one write snapshot without re-pricing or double-counting. Cached limits are runtime state and disappear on restore.
- CROSS-ARCH-0019 should spell out the fscrypt and root-owned UID-allocation probe requirements. Probe UAPI layout was checked against Linux `fscrypt.h`; the ioctl request deliberately encodes a 9-byte flexible prefix, while the provided buffer accommodates the full policy.
- The GPU ticket's `runtime.rs` is now `runtime/`. The engine registry is the regional single-writer state machine; deployment, probes and lease-renewal scheduling belong to its host.

## Validation receipts (not a full-gate verdict)

- Core/server `cargo check --tests` passed before the latest ledger and managed-open integration.
- First `cargo test -p oneiron -p oneiron-server`: core 7,032 passed, 17 failed, 4 ignored; server was not reached. Fixed nested replay read transactions, restore pre-open type-index rebuild, a healer token and policy fixtures.
- Focused replay/pipeline/code-run/federation run: 238 passed; one restore fixture nested-read error and 67 not run. Restore fixture fixed.
- Follow-up no-fail-fast acceptance run: 70 passed, three stale billing assertions failed. Assertions fixed; full server test is running against the subsequent integration.
- An attempted server build overlapped source changes on the Arch fallback and saw a mixed revision. It failed with a missing newly added method. Subsequent builds keep Rust sources fixed until completion.
- Python audit fixtures: 2/2 pass. Audit scripts compile. No measured mutation or merged coverage claim yet; the later cache measurement is recorded below.
- Builds use the factory shim's MacBook → mini → Arch slots and worktree-owned targets, with bounded Cargo jobs and two test threads. No host tools were installed, restarted or upgraded. No docs-mirror edits.

### Latest test evidence and host restriction

`server-test8.log`: 733 server library tests passed, 5 ignored. All public-booking
regressions pass after isolating the closed public-capability router from the
tenant-device lease layer. The integration binary passed 90 tests; its only
failure was the root/served skills-pack mirror, now synchronized and verified
by the focused skills-pack tests.

`focused4.log`: 485 tests passed, including checkpoint permissions and rebuilds,
BM25/phonetic/pending embedding, native gate paths, diagnostic suppression and
federation selectors. Four checker assertions needed to separate checker
outcomes from new tripwire observation markers. The new WORLD composition
fixture initially selected the entire publisher vault, including its authority
log; it now requests the granted WORLD and CORE band. `focused6.log` passes all
15 checker/composition tests after those fixture repairs.

The Mac mini ran out of space (118 MiB available). Only this ticket's failed
9.5 GiB mini target was removed. This session now selects the MacBook or Arch
without changing shared host configuration. No other ticket's source or build
artifacts were touched. The first native mutation run completed its baseline test but was interrupted by a session restart before any mutant result. It is not a measured kill score. Final lint,
crate-test completion, merged coverage remain pending. Cache measurement passed: 537 Rust hits, 111.4s uncached versus 46.9s cached.


### Durable native validation

The attached audit attempts were interrupted by session resets, not test verdicts.
The current worktree-local native runners on the MacBook retain PID and lane exit
records under `.w7/native-*.json`. Formatting passed. The check runner executes
Clippy, featureless and production feature checks, then both touched crate test
suites. The mutation runner has its own copied source and target. No score from
an interrupted report is accepted. These runners install no tools or services.
