# W8-T14 — cyclic received conversation migration

- A received Parent cycle is an invalid conversation DAG, so migration returns `InvalidConversationDag` with a cycle reason. Maintenance already skips that error per conversation and continues in separate transactions; lazy callers still propagate it.
- The maintenance report carries skipped conversation `EntityId`s instead of an anonymous count. Removing `Copy` from `MaintenanceReport` follows from owning a `Vec<EntityId>`.
- Canon: read-only `generated/oneiron/backend/oneiron-arch-0006a-conversations-api-core.md` lines 62, 66 and 191 define Parent-edge acyclicity and the HEAD rule. It is silent about maintenance skip reporting. The page uses MESSAGE/Record vocabulary while current engine code calls these TURN records; a future canon edit can clarify that equivalence and name the maintenance skip/report behavior.
