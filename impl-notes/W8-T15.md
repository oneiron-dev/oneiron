# W8-T15 / ONE-2580

- `create_conversation_with_text` now accepts an id only when `live_entity_row_in_txn` reports `Absent`. Existing live rows and deleted shells return the same `ConversationState("conversation already exists")` error. Membership appends and the text-index write remain unchanged.
- The regression test soft-deletes a created room, then checks that creating it again fails with the existing error kind and text. The unchanged `codec_defaults_validation_and_membership_are_atomic` test covers the original membership path.
- Canon: `generated/oneiron/backend/oneiron-arch-0006a-conversations-api-core.md` says one `Conversation` record carries the room's membership ledger, but does not explicitly ban id reuse after soft deletion. The page should say that an id that has ever held a conversation row, including a deleted shell, cannot be used to create a new conversation: its existing join/leave history still belongs to that id.
