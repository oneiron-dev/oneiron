{
  "_comment": "Wave-5.5 furniture ratchet baseline. Regenerated by scripts/ratchet/check.sh's own recompute logic \u2014 see that script for the exact find/rg commands, and scripts/ratchet/process_globals.py for the fourth metric, which needs a scanner rather than a pattern. Do not hand-edit the counts without rerunning them; hand-edit only the metadata fields (computed_at_commit, computed_date, bump_reason) when intentionally moving the baseline.",
  "computed_at_commit": "hyg/state-ownership-2 after the test-seam move; measured by check.sh on that tree. The other three counts are re-measured unchanged from the PR-1 merge at 9f4b8866.",
  "computed_date": "2026-09-11",
  "bump_reason": "LOWERED process_global_statics 38 -> 33. Nothing is raised and no other count moves: giant_files 2, allow_attrs 98 and print_macros 91 are re-measured unchanged on this tree and their _attribution blocks still hold. The move is worth 8 and the scanner fix costs 3, netting 5. Eight counted statics left the list: two because the state they held is now a field on the vault's StoreCore (the delete rendezvous and the force-sync counter), two because the lock they held is per vault and moved to StoreCore as a production field, and four because they were dead - written or armed by nobody, read by nobody. A ninth, the staged-import pre-content hook, also moved onto StoreCore but was never in the count: the old scanner could not see through its alias. The scanner now resolves same-file type aliases and named wrapper structs, so the three real globals that were hiding behind a name (known_gap_4_statics, minus the one this PR deleted) are counted instead of merely stated. Lowering a ratchet is free; raising one is a stated decision in the PR.",
  "metrics": {
    "giant_files": {
      "description": "Non-test .rs files with >= 800 lines. Non-test = path has no 'tests/' directory component, filename is not 'tests.rs', does not end '_tests.rs' and does not start 'tests_'. vendor/ and target/ excluded.",
      "count": 2,
      "_attribution": {
        "summary": "188 -> 2. 106 over-bar production modules split into directory modules across eleven move-only batches, plus three single-function files streamlined semantically under the bar (api/openapi/descriptions.rs, sync/window/forward.rs, pipeline/execution/channels.rs), plus error.rs, which left the list when ONE-2001 decomposed `Error` into twelve per-domain enums under crates/oneiron/src/error/.",
        "verification": "Every split checked move-only by scripts/refactor/tools/split_check.py against its base revision: no item added, removed, duplicated or altered in body. Zero exceptions across all eleven batches. Root surface held at 702 names throughout and the codemap was regenerated at every batch boundary.",
        "nothing_is_deferred_any_more": "Every file still over the bar is over it for a stated structural reason below, not for scheduling.",
        "remaining_2_and_why": {
          "voice_cascade/tts_spikes.rs (856)": "Reasoned indivisible (reader G25): the only clean seam is the cfg(test) mod tests, which drives private Adapter and Phase directly, so extracting it forces pub(crate) churn for zero runtime gain.",
          "claim/lifecycle.rs (800)": "Reasoned indivisible (reader G26): one cohesive impl Vault of 18 chain-walk and gating methods, 733 lines, 91% of the file, shared across the supersede, reserved-supersede, retract and demotion paths. Exactly on the bar."
        },
        "error_rs_left_2026_09_11": "error.rs was the third file and is gone from the list. It was capped by one item, the 1436-line `pub enum Error`, and the attribution said clearing it meant decomposing `Error` into per-domain types - a semantic redesign, owner call. That call was made (ONE-2001, DESIGN-error-enum.md option B): the 21 cross-cutting variants stay flat on `Error`, the other 203 moved into twelve `#[error(transparent)]` domain enums, one file each under error/. error/mod.rs is 565 lines and the largest child, error/sync.rs, is 529. Variant names, `#[error(..)]` strings and `ErrorKind` are byte-identical; Debug gains one wrapper level for moved variants.",
        "correction_2026_09_09": "The earlier claim that four of the remaining files were ordinary splittable modules was itself over-counted by one: pipeline/execution/channels.rs holds a single associated item, not several, so it was never cuttable per item. Single-function files are accepted; semantic streamlining is a separate pass."
      }
    },
    "allow_attrs": {
      "description": "Occurrences of '#[allow(' under crates/*/src, excluding test files (same non-test definition as giant_files).",
      "count": 98,
      "_attribution": {
        "summary": "+11 against the 2026-08-26 baseline. The +19 that this lane inherited all arrived on main before it started; the lane added zero and has since retired 8.",
        "arithmetic": "87 baseline, 106 at main 7eed085f, 106 at 5d7de40c, 98 after the bare-suppression retirements below. 20 additions minus 1 removal = +19, then minus 8 real fixes = +11.",
        "cause": "The 239 commits between ce47e61e and 7eed085f, via ordinary feature work: ONE-1608 VaultReadClient, ONE-1887 failure ladder, ONE-1403 majority-vote judge, the deep-retrieval host, the MemoryReasonCompose API, the GitWire repo-lock refactor and provenance txn threading.",
        "the_one_removal": "distance.rs carried #[allow(clippy::type_complexity)] on a #[test] fn. Commit 1a7aca7d split distance.rs into a directory module and the test moved to distance/tests.rs, a filename this metric excludes. Nothing was fixed or deleted; the occurrence crossed the test-exclusion boundary. That is why the net is 19 and not 20.",
        "debt_13_justified": "13 of the 20 carry an inline reason or doc comment arguing the case (memory/caps.rs x4, llm/step/step_claim.rs x2, code_run/vault_read/contract.rs with an explicit reason=, repo_mutation/queue.rs, attempt_queue/cancel/verbs.rs, oneiron-bench/src/beam/judge.rs, oneiron-server/src/server.rs, api/memory_reason.rs, api/memory_reason/deep_admission.rs). Several say plainly there is no production caller yet - for those the honest lever is deleting the code, not the allow.",
        "bare_suppressions_retired_2026_09_09": "The 7 sites the 2026-09-09 audit found carrying a bare suppression with no stated reason are GONE, fixed rather than waived: 8 attributes in total, because the claim-write site covered two sibling doors. gate/doors/claim_write.rs:50 and :88 -> both pre-check doors take a ClaimGateWrite pack from the caller (new ClaimGateWrite::plain). store/retrieval_telemetry/run_store.rs:79 and :443 -> the four context-pack finalize seams thread one RetrievalRunFinalize value. oneiron-bench/src/interface_bench/eval_run.rs:21 -> ExpectedRowIdentity. provenance/writes.rs:361 -> EdgeProvenanceWrite. origin/smart_http/serve.rs:265 -> ExchangeSetup separates the exchange's fixed setup from its live handles. failure_ladder/lineage.rs:207 (clippy::large_enum_variant) -> the Healer arm carried the same AttemptRecord twice and the duplicate is deleted; the residual size gap (arms of 664, 3456 and 2040 bytes) is closed by boxing each arm's payload, taking the enum from 3464 bytes to 32, with the trade-off stated on the type. No #[allow] or #[expect] was added anywhere; the root-surface pin held at 702."
      }
    },
    "print_macros": {
      "description": "Combined occurrences of println!, eprintln!, and dbg! under crates/*/src, excluding test files (same non-test definition as giant_files). Counted with word-boundary-anchored patterns so 'eprintln!' lines are not double-counted by a bare 'println!' substring match.",
      "count": 91,
      "_attribution": {
        "summary": "+3 against the 2026-08-26 baseline. All three predate the hygiene lane; the lane added zero.",
        "arithmetic": "88 baseline, 91 at main 7eed085f, 91 at 5d7de40c. Every other file is unchanged.",
        "cause": "One new oneiron-bench CLI subcommand, \"beam community\", absent at baseline: oneiron-bench/src/beam/runner.rs:29 (its line of print_help), oneiron-bench/src/beam/arms.rs:160 (the success-path report) and arms.rs:164 (the failure-path eprintln).",
        "assessment": "Not debug residue. All three are structurally identical to the println!/eprintln! lines the pre-existing smoke and run arms of the same CLI already print. The metric counts CLI output in a benchmark binary the same as a stray probe in library code; that is a limitation of the metric, not a regression in the tree."
      }
    },
    "process_global_statics": {
      "description": "`static` items (including `static mut`) under crates/*/src whose DECLARED TYPE contains one of Mutex, RwLock, Atomic, OnceLock, OnceCell, LazyLock, 'Lazy<', 'Cell<', RefCell - or that REACHES one of them through a `type` alias or a named struct declared in the SAME file, so a global cannot duck the metric by being given a name. A static inside a `thread_local! { ... }` block is EXCLUDED (per-thread by definition). Test-gated (#[cfg(test)]) statics COUNT: under `cargo test --lib` sibling tests share one process, so a test-only global is exactly the seam that flakes. Non-test file = the same definition as giant_files (no 'tests/' path component, filename not tests.rs / *_tests.rs / tests_*.rs); vendor/ and target/ excluded. Recomputed by scripts/ratchet/process_globals.py; `--list` prints every hit as `path:line NAME: type`, with ` [via NAME]` naming the local alias or struct a resolved hit went through.",
      "count": 33,
      "_attribution": {
        "summary": "38 -> 33 on 2026-09-11 (PR-2 of the per-vault ownership migration). Eight statics are gone and the scanner's alias gap is closed, which adds three that were always real. Every one of the 33 below is named with its bucket and its reason; there is no residue class left to work through.",
        "law": "Engine state belongs to a vault (a field on StoreCore, the Arc-shared inner handle every Store and Vault reaches) or to a thread (thread_local!, when writer and reader are the same thread by construction). The process owns only locks that mirror an OS or LMDB rule, write-once registries and plugin doors, and monotonic id allocators. Every remaining static below is named with its bucket reason; a new one is a stated decision in its PR (AGENTS.md ratchet law).",
        "why_it_exists": "Two flakes in one week came from process-global mutable state that tests read or arm. #922: the BM25 diagnostics counters are process-wide, so a sibling test in the same `cargo test --lib` binary moved them under a test that asserted on them. #923: the delete rendezvous slot is a single process-global Mutex, so two raced-delete tests clobbered each other's channel; the fix made that seam thread-local. PR-1 (#927) and PR-2 (#929) of the per-vault ownership migration moved or deleted every engine counter and test seam a vault could own; this counter now stops the class regrowing.",
        "moved_to_storecore_7_2026_09_11": {
          "bm25/diagnostics.rs BM25_DIAGNOSTIC_COUNTERS": "-> bm25::Bm25Diagnostics, a StoreCore.diagnostics field. This is the #922 array itself; its tests dropped BM25_DIAGNOSTICS_TEST_LOCK and went back to exact deltas.",
          "gate/decision.rs GATE_METRIC_COUNTERS": "-> gate::GateMetrics, same field. The per-thread GATE_METRIC_EMISSIONS counter that proves a decision is not double-counted stays thread-local.",
          "attempt_queue/telemetry.rs ATTEMPT_QUEUE_CLEANUP_RUNS / _STALE_REQUEUED / _RETRY_REASON_COUNTERS": "-> attempt_queue::AttemptQueueCleanupMetrics, same field (3 items).",
          "authority/first_seen_clock.rs LOCAL_CLOCKS": "-> StoreCore.authority_local_clock: Mutex<AuthorityLocalClock>. It was a BTreeMap keyed by clock domain that grew per opened store and never shrank; the clock now dies with the handle.",
          "store/open_gates/open_version_keys.rs NEXT_AUTHORITY_CLOCK_DOMAIN": "DELETED. It existed only to mint keys for LOCAL_CLOCKS. With the clock on the handle there is no key, so StoreCore::authority_clock_domain and StoreOwner's release-on-drop went with it."
        },
        "pr2_2026_09_11": {
          "moved_onto_the_vault_3": "deletion/rendezvous.rs DELETE_RENDEZVOUS (the #923 flake itself) and its AFTER_HEADER_READ thread-local, outbound_intent_ledger/store.rs FORCE_SYNC_CALLS, and batch/export/foreign_stage/export_foreign_stage.rs STAGED_IMPORT_PRE_CONTENT_HOOK -> fields on `#[cfg(test)] StoreCore.test_hooks`, reached as vault.test_hooks(). The scanner only ever counted two of the three: the pre-content hook was hidden behind the ArmedPreContentHook alias.",
          "locks_moved_onto_the_vault_2": "batch/export/foreign_stage/export_foreign_stage.rs STAGED_IMPORT_ADMISSION_LOCK and sync/client/federated.rs STAGED_IMPORT_LOCK -> StoreCore.staged_import_admission_lock and .staged_import_confirm_lock, both production fields under #[cfg(feature = \"sync\")]. The design left these undecided pending evidence. Every row inside either critical section is addressed through the vault that owns it - the receipt row, the staged content row and the selector admission all go through `vault.store.sync_state` - so neither guards a shared host path, and a process-wide lock was serializing unrelated vaults for nothing.",
          "deleted_as_dead_4": "identity_topology/shell_edge_reconcile.rs FULL_RECONCILIATIONS was incremented on every full reconcile and read by nobody; sync/client/federated.rs STOP_AFTER_STAGED_IMPORT and its two arming fns had no caller in any crate; export_foreign_stage.rs STAGED_IMPORT_ADMISSION_COUNT and STAGED_IMPORT_FIRST_STAGE_BARRIER likewise. A seam nothing reads is not state that needs an owner, and AGENTS.md deletes a helper with no remaining caller rather than keeping it.",
          "serial_lock_deleted": "memory/tests/support.rs DELETE_RENDEZVOUS_TESTS and its seven lock_delete_rendezvous() holders are gone: the lock existed only because the rendezvous was one process-global slot, and a per-vault seam needs none. It never counted toward this metric (it lives under a tests/ path), but it is the cost the shared seam was imposing - seven raced-delete tests that had to run one at a time.",
          "scanner_alias_gap_closed": "The metric read the DECLARED type, so an alias or a named struct hid a global from it. process_globals.py now resolves both when they are declared in the same file, and the three surviving known_gap_4_statics items are counted: store/test_hooks.rs BEFORE_LMDB_OPEN and AFTER_LMDB_OPEN (LmdbOpenHookSlot) and oneiron-remote/src/embedded.rs REGISTRY (a Registry struct holding an AtomicU32 and a LazyLock<Mutex<HashMap>>). The fourth, STAGED_IMPORT_PRE_CONTENT_HOOK, this PR deleted. Resolution is same-file and textual; a cross-file alias would still hide one, which is stated rather than guessed at."
        },
        "split_33": {
          "resource_locks_6": "Process-wide mutual exclusion over a process-wide resource. One LMDB env per path per process: store/open_gates/open_version_keys.rs:185 LMDB_DATABASE_OPEN_LOCK, :187 VAULT_ROOT_OPEN_LOCK and :189 OPEN_STORE_PATHS, all three of which fire before any Store exists, so no owner can hold them. Git repos on disk are process resources: git_wire/repo.rs:91 GIT_WIRE_REPO_LOCKS. One embedded vault per path per process, pid-guarded: oneiron-remote/src/embedded.rs:86 REGISTRY. One overflow probe at a time: oneiron-bench/src/perf/build_profile.rs:296 PROBE_HOOK.",
          "write_once_registries_and_plugin_doors_12": "Set once at init and immutable after, or a door a host installs into. Engine: agent_def/manifest.rs:287 MANIFEST, channel_identity_manifest.rs:164 MATRIX, edit_distance/routing/version.rs:20 MODEL_STACK_REGISTRY, git_wire/env.rs:29 GIT_WIRE_PROCESS_ENV, ingest/image.rs:91 RECOGNIZER and :92 CAPTION_RECOGNIZER, outbound/capability.rs:212 MANIFESTS, skill_optimize/gate/basis.rs:285 HELD_OUT_REPLAY_SCORER. Server: mcp/exec_host.rs:253 MCP_CODE_EXECUTION_HOST, mcp/surface.rs:406 PRIMARY and :407 TOOL_FIRST. Bench: perf/build_profile.rs:283 OBSERVED_OVERFLOW_CHECKS.",
          "id_allocators_7": "Monotonic counters whose VALUES nothing observes - uniqueness across the process is the point. Engine: repo_mutation/worktree.rs:18 REPO_MUTATION_WORKTREE_COUNTER, session_overlay/overlay/segment.rs:29 NEXT_OVERLAY_GENERATION. Server request/session ids: api/facade.rs:396 COUNTER, commands/api.rs:422 COUNTER, error.rs:555 REQUEST_ID_COUNTER, livequery/error.rs:24 COUNTER, server/lifecycle.rs:18 NEXT_LIFECYCLE_SESSION_ID.",
          "host_process_caches_and_live_registries_4": "State whose SUBJECT is the server process, not a vault: one server answers many requests and holds one set of live connections. oneiron-server/src/api/booking/page_token.rs:76 MEMO, api/context_board/cursor.rs:19 MEMORIES_CURSOR_STORE, livequery/connection.rs:30 HUBS, oauth_relay.rs:40 JWKS_CACHE.",
          "process_scoped_observation_1": "oneiron-remote/src/embedded.rs:96 STORE_OPEN_COUNT. It counts the Vault::open calls THIS PROCESS made, which is the fact the same-pid-reopen-shares contract test asserts; per-vault it would be meaningless.",
          "pre_store_test_hooks_2": "store/test_hooks.rs:161 BEFORE_LMDB_OPEN and :168 AFTER_LMDB_OPEN. They fire between the vault root being bound and the LMDB environment being opened - before any Store exists to own them - and are path-keyed, so a hook armed by one test cannot fire inside another test's open. Newly counted here because the scanner now resolves their LmdbOpenHookSlot alias.",
          "server_test_seam_1": "oneiron-server/src/oauth_relay.rs:125 TEST_TRANSPORTS, a path-keyed map of stub transports. The only cfg(test) seam left on the list. It is a server concern, not an engine one: no vault owns an OAuth relay transport, and the server's own per-test isolation is a separate question from this migration."
        },
        "expected_direction": "Flat. The engine class this migration existed for is closed: no #[cfg(test)] static remains under crates/oneiron/src that a vault could own. The seven survivors there are two pre-store hooks, three open-time resource locks, one repo-lock map and one process env, each named above. A new static is a stated decision in its PR (AGENTS.md ratchet law)."
      }
    }
  },
  "_metric_definition_gap_found_2026_09_09": "CLOSED 2026-09-09. The non-test exclusion matched 'tests.rs' and '*_tests.rs' but not 'tests_*.rs', a spelling the repo also uses (branch_store_oracle/tests_leak_sweep.rs, api/git_http/tests_auth.rs, oneiron-bench/beam/tests_*.rs and 15 more), so those 18 files counted as production. check.sh now excludes that spelling too, in both the ripgrep globs and the giant_files find pipeline. NO COUNT MOVED: those 18 files hold zero '#[allow(' and zero print macros and none reaches 800 lines. This closed a latent hole before it could report a test file as a production giant; it did not relax any number."
}
