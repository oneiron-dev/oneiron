# Packet A — consumer configuration and inbound

**Status:** all cases below are proposed adversarial probes, not verified bugs. Bound: two consumer cases and two inbound cases; stop after each has a normal and hostile result. Baseline `e17eeb2b`.

## Consumer configuration (July surface 1)

Canon: `CLAUDE.md` consumer boundary; generated `oneiron/retrieval.md` §Retrieval contract (scope narrows before assembly); generated `oneiron/core/oneiron-arch-0052-off-record-branch-store-v1.md` (host-owned marker text). Current doors: `crates/oneiron/src/lens/generated_ui/genui_envelope.rs`, `lens/mediation/mediation_frame_read.rs`, `lens/mediation/mediation_frame_write.rs`, `context_pack/builder/assembly.rs`, `context_pack/validation.rs`, `prompt.rs`; actual callers `agent_dispatch/context.rs`, `context_projection/resolution.rs` and `crates/oneiron-server/src/api/context_pack/resolve.rs`. Existing focused tests: `lens/tests/{genui_validation,instrument,mediation}.rs`, `context_pack/tests.rs`, `context_projection/tests/resolution.rs`, `tests/it/prompt_blocks.rs`, server `api/tests/context_pack_disclosure.rs`.

| Probe | Setup and attack | Observable expected result and stopping rule |
| --- | --- | --- |
| A1: delegated context | Create two unrelated worlds/rooms with unique sentinels; dispatch a child with a narrower `ContextSpec`/`AgentScope`, and request recent chat plus memory. Compare resolved sections (`context_projection/resolution.rs:147-165,186-259`) with the actual model-facing request (`agent_dispatch/context.rs:20-171`), not only returned IDs. Narrowed child must receive neither the out-of-scope sentinel bytes nor resolvable refs. Include an in-scope room as positive control. | If only a reference appears, trace its dereference before claiming exposure. Note that the memory scan applies `world_scope` while the recent-TURN scan currently takes only `last_n`; that is a **question**, not proof of a leak. Stop at the dispatch boundary. |
| A2: authored/generated pack | Put an in-scope row and an out-of-scope or quarantined neighbor behind the same query; assemble by the public server/core path and the generated-lens frame read. Try a stale handle, invalid wire envelope and a prompt include escaping its configured root. Check the serialized pack/model request and typed refusal, including any edge metadata. | No forbidden bytes or backing ref become a prompt or read handle, and invalid input fails closed. Use a normal in-root prompt include and in-scope neighbor as controls. Stop after the read/assembly and host-mediated write boundaries; do not equate a parser-level rejection with full prompt-package safety. |

Prior September audits already proved separate context-pack neighbor/control-row and scoped temporal top-N issues on an older revision (`/home/lexi/w8-opus/audit/{authority,retrieval}/REPORT.md`), while #984 landed separate remote/MCP repairs. Check their present dispositions; do not rediscover or relabel them as new ONE-1884 findings. Prompt package placement/product-specific defaults are consumer-boundary work, not license to ship product prompts in the engine.

## Inbound consent and identity (July surface 2)

Canon: generated `oneiron/agents/oneiron-arch-0063-agent-inbox-email-identity-v1.md` §§passport/lens/CC (notably R4: inbox lens is a projection, not new storage). Current reads: `agent_inbox_lens.rs:59-85,98-201` and `agent_inbox_lens/inbound.rs`; writers/callers: `surface_event/inbound.rs`, `surface_event/cc_intake.rs`, `channel_identity_provider/gmail.rs`, `agent_dispatch/resident.rs`. Existing tests: `agent_inbox_lens/tests.rs` and `agent_inbox_lens/tests/inbound.rs`, `surface_event/tests/cc_intake.rs`, `channel_identity_provider/tests.rs`. Avoid testing an invented inbox mailbox: the source explicitly says renderer-neutral projection only.

| Probe | Setup and attack | Observable expected result and stopping rule |
| --- | --- | --- |
| A3: receipt/passport provenance | Admit a valid inbound message through the provider and consent door. Then retry with a forged/mismatched mailbox, identity stamp, actor/facet or `payload_ref`; replay after identity rebind and after an inbound suppression write. Compare stored identity/passport, resident dispatch target and final inbox view. | Invalid provenance or suppressed contact cannot become a deliverable message or approval item; a valid message remains visible to its intended identity. Check writer authentication, not only the lens's string parsing. Stop at ingest → durable attempt/receipt → resident dispatch. |
| A4: inbox ordering and CC | Mix a held send, its later terminal receipt, two inbound attempts on distinct identities and a CC-intake event with colliding impact/time. Page by the public cursor; try a cross-identity thread header. | One current item per real intent/identity, stable no-duplicate paging, no cross-identity thread merge, and no pending approval after its terminal outcome. Use receipt/attempt ordering controls. `attempt_queue/engine/reads.rs` already fails on incomplete scans; do not report silent prefix truncation without disproving that guard. |

Existing provider mailbox/custody/scope checks (`channel_identity_provider/gmail.rs:560-659,740-773`) and bounded inbox scan tests (`agent_inbox_lens/tests.rs:255-286`) are controls, not a new security verdict. The ARCH-0063 visual inbox half may still be owner-sitting; investigate only implemented ingestion and projection doors here.
