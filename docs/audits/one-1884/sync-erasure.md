# Packet B — sync, erasure and topology

**Status:** proposed probes, no verified new defect. Bound: two sync cases, one active-sidecar erase case, one topology scratch probe. Baseline `e17eeb2b`. Use **disposable vaults**; never simulate HardErase on real data.

## Window and restore (July surface 3)

Canon: generated `oneiron/sync/oneiron-arch-0023b-crdt-sync-implementation-v1.md` §§stream classes/recovery and `oneiron/core/oneiron-arch-0038-deletion-redaction-canonical-snapshot-v1.md` §§HardErase/recovery. The former labels Pending outbound intent device-local, while terminal Delivered/Failed outcomes replicate as facts. Canon §recovery specifies pending-mirror replay → reverse rematerialization → forward rematerialization, with byte comparison and delete-wins. A restored checkpoint is not automatically a new device or a crash retry; identify the node/authority provenance before judging a repeated send.

| Probe | Setup and attack | Observable expected result and stopping rule |
| --- | --- | --- |
| B1: two-node rematerialization/delete | Write entity + edge on node A; export/import monthly window on B with a same-key divergent immutable row, a malformed edge, and a valid control. Then HardErase on A, replay old payload/reordered tombstone update on B and restart both. Compare exact stored bytes, active read/edge/shell state, tombstone reason and quarantine rows; repeat with a hard purge failure and retry. | Immutable conflict is quarantined; valid bytes survive; hard-delete scope cannot be silently downgraded or resurrected by old windows, and a failed purge cannot be marked complete. Trace `sync/window/forward/{entity_pass,edge_pass,tombstone_pass}.rs`, `sync/window/tombstones.rs`, `sync/bridge/{entities,edges}.rs` and `deletion/erase.rs`. An `x:` diagnostic expiry is not a quarantine *release*; trace `sync/quarantine/{remat_markers,reassert_drain}.rs` and test later valid repair separately. Stop at one window + one restart/retry. |
| B2: restored pending effect | With one idempotent outbound effect Pending, make a checkpoint before its Done write; allow the original node's provider effect to complete. Compare (i) crash/reopen of original with (ii) checkpoint restored to a different node identity and (iii) restore of a terminal intent. Observe local ledger rows, provider idempotency key/effect count, authority and terminal receipt across both nodes. | A crash retry and a restored clone may have different authority; a second dispatch invocation is **not** proof of a second external effect if provider dedupes. Distinguish permitted same-node retry from cross-node effect duplication or stale authority. Trace `recovery/checkpoint/{mod,tiers}.rs`, `outbound_intent_ledger/{store,dispatch}.rs`, and real dispatch callers. Default checkpoint tiers may copy the private ledger: treat this as a hypothesis until a real restore + provider control runs. Stop after those three cases. |

Known September DAG audit found a separate received `Parent`-edge replay defect in this area on older main (`/home/lexi/w8-opus/audit/dag/REPORT.md`), with an open fix PR #985 at baseline. Exclude that known case rather than inflate B1's new-finding count. Existing sync focused tests: `sync/window/tests.rs`, `sync/quarantine/tests.rs`, `tests/it_sync/sync_{quarantine,replay_reason,tombstone_v2}.rs`. The `sync` feature, unlike featureless base mode, is required for the two-node lane.

## Active erasure carriers (July surface 4)

Canon: generated ARCH-0038 active-purge and historical-carrier sections, including the distinct exterior-ledger/historical-restore rule. A receipt or tombstone may intentionally retain opaque accountability facts; a readable body/result ID or derived bytes are a separate question. `deletion/erase.rs` contains the synchronous purge and active-scope probe; `store/retrieval_telemetry/{types,run_store}.rs` persists retrieval run/output rows; `store/gate_decision/{ledger,sidecar}.rs` holds decision and pending-delete families. `recovery/checkpoint/tiers.rs` marks telemetry Runtime (not restored by default), but live-vault erasure still needs checking.

| Probe | Setup and attack | Observable expected result and stopping rule |
| --- | --- | --- |
| B3: sidecar census | Create a claim/entity with unique body and result ID, run retrieval, obtain a gate decision and a receipt referring to it, then hard-delete the source. Compare public retrieval/decision/receipt reads and the actual active LMDB row families before/after delete; reopen and sync to a peer. Include a retained nonpersonal receipt as control. | No forbidden erased bytes/result pointers remain readable or persist contrary to the specified carrier class. Classify `retr_run`/`retr_out`, gate-decision rows, pending-delete sidecar, and other receipt fields independently. Do not call intentional opaque audit evidence a leak. Do not re-report the receipt archive already explicitly retired by `deletion/erase.rs`. Stop after one entity, one dependent row per family and a restart. |

## Topology vs participant deletion (July surface 8)

Canon: generated `oneiron/core/oneiron-arch-0055-entity-merge-split-consent-ramp-v1.md:14,174-176` says the July 23 amendment makes **applied** topology events self-contained, author stamped at write, while participant deletion defaults tombstone and pending proposals become moot. [ONE-1862](https://linear.app/oneiron/issue/ONE-1862/) owns that amendment (ONE-1870 is a duplicate), so this is a disposition probe for an existing ticket, not a new proposal to redesign topology.

| Probe | Setup and attack | Observable expected result and stopping rule |
| --- | --- | --- |
| B4: delete an applied participant | In separate fresh vaults: merge multiple sources into one head, then delete one source; split one source into two heads, then delete a split head. Observe effective fold, `merged_into`/`split_into` edges, lifecycle state and undo eligibility before delete, after delete, after reopen, and on a sync receiver. Include pending proposal versus applied-event controls. | Applied event must follow its self-contained author/participant rule; a deleted head must not leave a readable shell exposing erased payload. Compare `identity_topology/{ledger_fold,store_entity_helpers,shell_edge_reconcile}.rs` with `deletion/erase.rs`. Source still checks live participants in fold; that is **not** an empirical verdict. If reproduced, route to ONE-1862 unless the failure is demonstrably distinct. Stop after the two shapes + one receiver. |
