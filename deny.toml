[graph]
all-features = true

[advisories]
db-path = "~/.cargo/advisory-db"
db-urls = ["https://github.com/rustsec/advisory-db"]
yanked = "deny"
ignore = [
    { id = "RUSTSEC-2023-0089", reason = "transitive via loro; tracked for follow-up dep bump" },
    { id = "RUSTSEC-2025-0141", reason = "transitive via loro/heed chain; tracked for follow-up" },
    { id = "RUSTSEC-2026-0097", reason = "rand 0.8.5 aliased mut ref; transitive via loro; tracked for bump to 0.8.6+" },
]

[licenses]
allow = [
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "MIT",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "BSL-1.0",
    "ISC",
    "MPL-2.0",
    "Unicode-3.0",
]
confidence-threshold = 0.9

[bans]
multiple-versions = "warn"
wildcards = "allow"

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
